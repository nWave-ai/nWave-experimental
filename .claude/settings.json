{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys,json; d=json.load(sys.stdin); f=d.get('tool_input',{}).get('file_path') or d.get('tool_response',{}).get('filePath',''); print(f)\" | { read -r f; case \"$f\" in *.py) ruff check --fix --quiet \"$f\" 2>&1 | head -5; ruff format --quiet \"$f\" 2>/dev/null ;; esac; }",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
