# Architectural Tech Debt — nWave OSS T1

Living ledger of architectural debt surfaced by periodic 3-architect audits (solution / system / domain lenses) against the canonical brief (`docs/product/architecture/brief.md`) and the ADR corpus (`docs/adrs/`, `docs/architecture/`).

**Companion of** [`docs/backlog.md`](docs/backlog.md) — this file is the *architectural* debt view; the backlog is the planned-work SSOT. Each entry below proposes a backlog ID (`F-…`) to be opened/linked there. **Append-only**: new audits add a dated section; prior findings are amended (status change) in place, never deleted.

> Conventions — **Type**: SSOT-violation · duplication · drift-vs-brief · drift-vs-ADR · boundary-violation · SPOF · concurrency-risk · aggregate-design · bounded-context-drift. **Convergence**: how many of the 3 lenses independently flagged it (3/3 strongest). **Evidence is verified file:line read from source at the audit HEAD.**

---

## 🎯 UNIFIED ToC PRIORITIZATION

> **run-8 delta (2026-06-04, HEAD `608d9d3d4`, autonomous 3-architect):** **AD-54 RESOLVED** — E refactor shipped (`1b1d8ec87` omission-classes JSON single-SSOT, `import yaml` removed from the bundled service) + **ADR-PLAT-007 authored** (`29d9adc0c`, declares `src/des` bundle stdlib-only, scopes PLAT-001 to installer, names `test_des_bundle_steps.py` as its gate). **R3 slice-01 keystone SHIPPED** (`e625b1e16`, Gate-Scope `cbff50d3…` verified, `SliceCommitted verified:true`) — the AD-54 binding blocker is GONE, the held keystone commit landed. R3 #1 now means **slice-02** (LOUD-on-absent-arch-scope floor) remains. New ids from the run-8 audit of the E+R3 code: **AD-58** — ADR-PLAT-007 is itself UNDER-ENFORCED (5-pkg blocklist lets bundled `_collect_scope_worker.py:48 import pytest` through; same class as AD-54, but pytest is a test-runner dep not a data-dep → lazy-import/carve-out, not just AST-allowlist) → R4; **AD-59** — the E refactor left ~210 LOC §5.3 core duplicated scripts/↔src/des, now CONSOLIDATABLE (scripts already imports `from des.*`) + the loader/attestation legs UNGUARDED by port-parity → R8-HIGH. R3+E behaviour verified CLEAN (git-free, fail-closed-loud, single pytest-argv seam). **Drift data-point (Ale Q 2026-06-04): the crafter RESPECTED the architecture it was passed — zero code→architecture drift in E+R3; the drift that accumulates is doc→code (brief line-cites stale after +139 R3 LOC, AD-43/44/45 class), fixed by brief-projection, not crafter deviation. Supports "drift must not increase" for the code→arch direction; needs a per-feature architecture-adherence GATE (#63) to make it mechanical, not audit-sampled.** No root re-rank. **Stale-self correction: the DoR 8-vs-9 question I'd left "open" was ALREADY resolved by AD-55 — canonical is 9; the 8-item skill is a stale missing-Item-9 copy; SF already unblocked.**
>
> **run-7 delta (2026-06-04, HEAD `2909782b3`, consolidation run-10):** Re-derived from source — `git log 1f368a65c..HEAD` = 6 commits (R2 slice-03 `4f019de7` + slice-04 coverage-map `aa17d106` + closeouts + run-7 audit panel `2909782b3`). **R2 re-marked honestly: "CLOSED-4/4 at mechanism, 1 genericità-residue OPEN".** slice-04's coverage-map leg relocated `import yaml` into `coverage_map_verify_service.py:37` — the ONLY external import in all `src/des/**` — which VIOLATES the CI-enforced stdlib-only DES-bundle invariant (`test_des_bundle_steps.py:141` `des_no_external_deps`). This is **AD-54** (run-7 audit), the immediate **binding blocker**: it reds the zero-failing tree → holds R3 slice-01's keystone commit uncommitted. So R2's mechanism is complete but its unqualified "CLOSED" is invalid until the yaml-removal residue lands (HALT-for-Ale, `docs/analysis/HALT-r2-yaml-bundle-violation-2026-06-04.md`). **ToC #1 stays R3 (gate non-vacuity)** — slice-01 keystone BUILT (feature-scoped gate excludes build-tier arch-tests, collect-AND-RUN per §6 ADDENDUM) but HELD behind AD-54. Run-7 also added AD-55 (DoR 8-vs-9 SSOT drift → R6), AD-56 (subprocess-harness DRY empirically 15 features / 24 files, 3× undercount vs the ledger's 5 → R8 high-leverage), AD-57 (`python_for` interpreter-boundary needs a permanent ADR → R4 doc). No root re-rank — R3 #1, AD-54 the binding blocker.
>
> **run-6 delta (2026-06-04, HEAD `cd7549485`, autonomous):** **R2 CLOSED 4/4** — slice-03 run (`4f019de7`) + slice-04 coverage-map leg (`aa17d106`) shipped; the feature-end cycle now mechanically emits all 6 done-gate records (run-real-verify, OQ-3=(i)). **R1+R2 mechanism both complete** → the spine's A→G + feature-end emission WORKS end-to-end. **New ToC #1 = R3 (gate non-vacuity)**: with the spine working, the binding constraint is making its gate VERDICTS trustworthy — a vacuous/narrower-than-contract green lets broken slices through. R3 cross-couples the test-debt audit's gate-honesty findings (TD-19 env-mask, TD-27 build-isolation, TD-26 truncated-record divergence-gap). R2-residue: a concrete real-feature all-6-emission needs an out-of-band human coverage-map signoff (not a blocker). Full ToC re-rank (all 8 roots, brief projection) still owed to the consolidation cron. **[SUPERSEDED by run-7: R2's "CLOSED 4/4" is now qualified — AD-54 yaml-bundle violation re-opens a genericità-residue.]**
>
> **Re-derived from source 2026-06-02 (run 5)** (HEAD `f0d1f78f6`) — NOT recalled. Inputs: `git log --oneline 354b361ba..HEAD` (17 commits since run-4) + current OPEN backlog frictions + the 3-architect audit panels below (panel-2 AD-36..42 `f0d1f78f6`) + the R1/R2 feature-deltas + the committed-scope ADR-CP-001/002 state. **Major delta vs run-4: R1 advanced 3/5 → 5/5 SHIPPED.** slice-04 gate-composition landed (`e5757e38b` — subagent.stop feature-end records routed through the atdd_pure.yaml flavor SSOT, the dead-config if-ladder collapsed + `b91d067fd` flip slice-04→shipped); slice-05 config-asset drift landed (`ceb4618b9` + `e4e9d8af1` "ALL 5 SLICES SHIPPED", AD-27 CLOSED); feature-end degrade-loud consolidation `5df0d8875`; whole-tree env-parity contract gate 0-red (gate-honesty masking removed `f57aee267`). **R1's structural multi-representation disease is CURED** — but its FORMAL feature done-gate (`des verify-integrity --feature-id des-spine-control-plane-ssot`) still fail-closes `FeatureEndCycleIncomplete` on 2 un-emitted feature-end records, which is precisely **R2's** scope. **R2 (feature-end emission) is therefore the new binding constraint** — slice-01 emit-CLI SHIPPED `f9cff135c` (IN-FLIGHT 1/3), slice-02/03 design-gated on Ale's hook-vs-CLI ratification (`30167b3ec`). **AD-04 DISPROVED** (PhaseEvent VOs load-bearing — row needs revision, not implementation).
>
> **Ranking axis (ToC)**: what blocks *throughput* — correct features flowing through the spine to a genuine `workflow.completed` → the SF Distribution-Gate. The constraint is the spine's CONTROL PLANE, not any single domain finding. The night refactor consolidated the DOMAIN (declared EXHAUSTED) but the *control plane* (which gate runs, in what mode, against what tree, from what deployed copy) is still hand-wired — the same multi-representation-without-SSOT disease one level up. Every per-feature DELIVER re-surfaces it (one slice-02 DELIVER surfaced 5 frictions, all one root), so it gates every subsequent feature.

| Rank | Root | Status | Collapses (open AD/F-ids) | Why this rank (constraint logic) |
|------|------|--------|---------------------------|----------------------------------|
| **R1** | **Spine CONTROL-PLANE SSOT** (friction #67 `F-SPINE-CONTROL-PLANE-SSOT`; subsumed `F-OSS-GATE-COMPOSITION-UNIFY`; R1-adjacent: #68 RCA-reprompt-nontermination + #69 `F-OSS-SPINE-WATCHDOG`) | **GREEN (substance) 5/5** — *formal done-gate chains through R2* | all 5 facets closed: freshness slice-01 `b2d86fa02` · digest-timing slice-02 `bdd629565` (AD-22/23) · mode-resolution slice-03 `3033d4f9a` (`resolve_workflow_mode()` SSOT, #65 dissolved) · **gate-composition slice-04 `e5757e38b`** (subagent.stop routed through flavor YAML, dead if-ladder collapsed) · **config-asset drift slice-05 `ceb4618b9`** (freshness covers lib/nWave/ config, AD-27 CLOSED) · residue AD-39 D/STALE telemetry-flatten (MINOR) · residue AD-50 `_LEGACY_ONLY_PHASES` dup domain+application (fold to tdd_schema, with AD-30) · **test-debt TD-5 (fire-2): skew Scenario Outline drives PRIVATE `_classify_hook_version_skew`, masking the real SessionStart emission contract — `F-TESTDEBT-SKEW-AT-DRIVING-PORT-GAP`, gated-APPLY)** · **fire-4 R1 test-debt: TD-10 `F-TESTDEBT-READINESS-PRESERVATION-ORACLE` (THEATER — vacuous unbounded-preservation oracle on the readiness gate) + TD-11 `F-TESTDEBT-DISPATCHER-HOOK-BOUNDARY-AT` (no AT through the real `handle_pre_tool_use` hook boundary)** | **THE structural constraint — now RESOLVED in substance.** The multi-representation-without-SSOT disease that re-surfaced 5 frictions per DELIVER is cured (control plane has an SSOT: which gate, what mode, what tree, what deployed copy). Whole-tree env-parity gate 0-red. The feature's FORMAL done-gate blocks only on the feature-end records R2 emits → R1 close chains through R2. #69 watchdog (collection-health) still OPEN, R1-adjacent. |
| **R2** | **Spine mechanical A→G + feature-end emission** (`F-ATDD-PURE-FEATURE-END-CYCLE-UNWIRED` P1; `F-ATDD-SPINE-LEDGER-ENFORCEMENT-GATE` P0; `F-OSS-HOOK-SIDE-PHASE-INJECTION-GAP`) | **MECHANISM-COMPLETE 4/4, 1 genericità-residue OPEN (run-7, 2026-06-04): all slices shipped — 01 emit `f9cff135c` · 02 sign `5c98dbdfa` · 03 run `4f019de7` · 04 coverage-map leg `aa17d106` (run-real-verify, OQ-3=(i)). The feature-end cycle emits all 6 done-gate records mechanically; R1's formal done-gate chains closed at mechanism level. BUT slice-04 relocated `import yaml` into `coverage_map_verify_service.py:37` → AD-54 stdlib-only DES-bundle VIOLATION (the only external `src/des` import; CI-enforced by `test_des_bundle_steps.py:141`) → reds the zero-failing tree, holds R3 slice-01's commit. So "CLOSED" is invalid until the yaml-removal residue lands (HALT-for-Ale, `docs/analysis/HALT-r2-yaml-bundle-violation-2026-06-04.md`). Residue: concrete all-6-emission for a real feature-end needs an out-of-band human coverage-map signoff (OQ-3=(i)). slice-04 amended committed slice-03 scenario-1/4 under Ale night-autonomy, reviewer-confirmed sentinel-cmap-dual-7f3a.** | slice-01 `des emit-feature-end` CLI SHIPPED `f9cff135c` (EBatchRefactorCompleted + FeatureEndReviewVerdict-with-signed-hash; anti-theater refusal wired) · **slice-02 deep-review signing SHIPPED `5c98dbdfa`** (`des feature-end sign` thin shim over a platform-agnostic signing use-case, reuses `at_review_signing` SSOT, NEVER mints — anti-theater genuineness mechanically verified) + slice-03 feature-end PASS (runs the 4 gate-records) PENDING, per **RATIFIED DDD-7** (hooks=thin cross-platform shims invoking ONE platform-agnostic feature-end-cycle use-case; CLI+hook both shim over it; CLI consolidated to single `des feature-end` entry — `30167b3ec`+ratification) · folds AD-36 (feature-end-records QUADRUPLICATED — SSOT collapse) + AD-40 (anti-theater invariant → push to `append_feature_end_event` writer) + **AD-51 (feature-end event-NAME constants re-hardcoded as raw literals in `verify_deliver_integrity.py:444-451` + `subagent_stop_handler.py:825-828` — silent done-gate false-negative landmine; fix = 2 imports)** · **fire-5 TD-13: feature-end gate lacks a driving port → slice-02 hand-wires the prod composition root; real SubagentStop feature-end hook untested live → `F-TESTDEBT-FEATURE-END-GATE-DRIVING-PORT`, CONVERGES with slice-03's DDD-7 `build_feature_end_gate` use-case** · **fire-11 TD-32: the common-audit-log caller-migration ATs (slice-02/02c-A) are STUB-THEATER masking the audit-log-SSOT spine surface — they drive `python -c` stubs not the real callers, which are verified UN-migrated (`carpaccio_intercept.py:285,380,618` + `reverify_slice_commit.py:190,443` still legacy per-feature); `F-TESTDEBT-COMMON-AUDIT-MIGRATION-STUB-THEATER`, R2-adjacent feature-debt for the unbuilt per-feature→singleton migration** | **THE binding constraint now.** Until the feature-end ledger triplet is mechanically emitted+signed, no feature reaches a genuine `workflow.completed` → no done-gate close (R1's own formal done-gate blocks HERE). Directly unblocks R1. DDD-7 ratified + **slice-02 SHIPPED** (signing leg done); slice-03 (the feature-end-cycle gate-pass) remains. |
| **R3** ⬅ **ToC #1** (2026-06-04, after R2 mechanism-complete) | **Gate non-vacuity / verification-completeness** (`F-CONTRACT-GATE-EXCLUDES-BUILD-TIER-ARCH-TESTS` (KEYSTONE — per-slice gate excludes tests/build/ arch-tests = false-positive); ~~F-CONTRACT-GATE-DIGEST-UNDERCOUNT~~ CLOSED 2026-05-30; `F-VERIFY-SLICE-COMMIT-E2-WRAPPER-DIVERGENCE`; `F-GCOMMIT-CONTRACT-SUITE-MULTI-RUN`; AD-25 collection-cost; AD-08 audit-writer flock; **immediate blocker AD-54 `F-DES-BUNDLE-STDLIB-ONLY-ADR` (yaml bundle violation reds the tree → holds slice-01)**; **test-debt cross-coupled: TD-19 env-mask freshness (fire-6, R2-keystone hooks) · TD-27 build-isolation network-flake (fire-9, hi-CI-reliability) · TD-26 done-gate truncated-record divergence-gap (fire-9)**) | **OPEN — THE binding constraint; slice-01 keystone BUILT but HELD uncommitted behind AD-54** | slice-01 keystone (feature-scoped gate excludes build-tier arch-tests, collect-AND-RUN per §6 ADDENDUM) is built + GREEN but cannot commit while AD-54's yaml import reds the zero-failing tree · digest fingerprints ~75% of suite · build-tier arch tests outside per-slice gate scope · E2 wrapper refuses a slice the gate passes standalone · ≥2 full-tree collections on the sync hook path | A gate that passes a vacuous/narrower-than-contract GREEN lets broken slices through — corrupts the throughput R1/R2 are trying to make trustworthy. The gate must be honest before its verdict is worth enforcing. **Unblock path: resolve AD-54 (HALT-for-Ale yaml-removal) → land slice-01 keystone.** |
| **R4** | **git-dependency removal / target-machine generality** (`F-GATE-GIT-DEPENDENCY-REMOVAL` epic — AD-21; AD-22 `F-COMMITTED-SCOPE-PORT-EXTRACT`; AD-23 digest-parity; AD-24 uniform-INDETERMINATE) | **PARTIAL** (AD-22 committed-scope-port extracted; **AD-23 committed-scope degrade-LOUD on git-absent SHIPPED `bdd629565`** as control-plane slice-02; runtime git-coupling SURFACE still broad — multiple `_git` call sites + AD-24 split-brain signalling open) | the 3 git seams emit 3 different signals on git-absence (AD-24) · gate logic still welded into application layer, not fully behind a swappable port · the real `Protocol` for D8 cross-language adapters not yet abstracted · **AD-37/38 `commit_slice.py` raw-subprocess git for diff/commit/amend bypasses its OWN `git_run` seam** (`:151`/`:178`/`:202` — highest-leverage mechanical fix, the seam already exists) | The genericità/agnosticismo mandate (CLAUDE.md STANDING) + D8 cross-language adapter goal both block on a real git port. Co-evolves with R1's digest-timing facet (AD-22/23 shipped under the control-plane feature; AD-24 uniform-INDETERMINATE remains). |
| **R5** | **Carpaccio / DISTILL handoff + per-slice authoring ergonomics** (`F-CARPACCIO-DISTILL-DELIVER-HANDOFF-FRICTION`; `F-CARPACCIO-SLICE-PLAN-PARSER-BRITTLE` → two-parser divergence; `F-CARPACCIO-FUTURE-SLICE-SCAFFOLD-BLOCKS-COMMIT`; `F-AT-E2E-REANALYSIS-POST-GATES`; `F-E2E-AT-MIGRATION-TO-GATE-FRAMING`) | OPEN | discovery-tag gap · file-level `@slice-NN` not inherited · entry-gate vs hook two-parser divergence · future-slice scaffolds block non-final commit · AT corpus not re-analysed vs new gate constraints | These are the friction that makes each slice expensive to author and land. Below R1–R4 because they slow throughput rather than block a correct terminal, but they are the recurring per-slice tax once the spine itself is sound. |
| **R6** | **Architecture/SSOT structural debt** (AD-01 boundary-enforcement gate; AD-03/AD-06/AD-07 version-SSOT; AD-20 ADR-SSOT fragmentation; AD-02 ledger-port DIP; AD-16 domain singletons; `F-ATDD-MANDATE-12-SSOT-DUPLICATION` residue) | OPEN (AD-02/AD-05/AD-16 partially advanced by the night refactor — boundary fixes shipped; the *enforcement gate* AD-01 still absent) | declared-but-absent `import-linter`/AST boundary gate · `nWave/VERSION` stale + 3 uncross-checked version mechanisms · permanent ADRs across 3 folders with ID collisions · **AD-53 hook-handler-thin-shim ADR-gap** (Accepted ADR-CA-001/OC-001 cover the cross-platform shim at the OS/protocol boundary, but the deeper "Python hook handler = thin adapter over an application use-case, no inline decision logic" DDD-7 principle has NO Accepted ADR — ADR-005 Proposed only decomposes into modules; `_handle_feature_end_gate:912` carries inline logic → `F-HOOK-THIN-SHIM-LOGIC-EXTRACTION`) | The root *enabler* of the boundary drift the night refactor keeps repairing by hand (AD-01 is the gate that would have blocked AD-02/05/16 ever forming). High-leverage-per-effort but not throughput-binding until R1–R3 are sound. |
| **R7** | **Upstream-wave gate-PAIR coverage + review architecture-adherence** (D11 `F-OSS-UPSTREAM-WAVE-GATE-PAIRS`; #63 `F-SLICE-REVIEW-MUST-CHECK-ARCHITECTURE-ADHERENCE`; AD-12/AD-13/AD-14/AD-15/AD-18 doc-SSOT realignment) | OPEN | per-pair DESIGN→DISTILL / DISCUSS→DISTILL / DEVOPS gate-PAIRs (drift-minimization) · review step asserts ADR/brief/feature-delta conformance · brief TDD-canon + domain-model + command-vocab realignment | Reduces upstream drift propagating into DELIVER. Cheap doc realignments (AD-12..15) stop *new* drift at near-zero cost; the gate-PAIR + architecture-adherence review are the durable mechanism. Subordinate to a sound spine. |
| **R8** | **D1 operability surface — UX / install / lean output** (`F-UX-FERRARI-AS-UTILITARIA`; `F-ALL-OUTPUTS-LEAN-BY-DEFAULT`; `F-DES-SELF-HOSTED-GATE-SYNC`; AD-09 hook SPOF; AD-10/AD-11 install/leak gates; D6 `F-ATDD-PURE-ACCELERATION-PILOT`; **test-debt: `F-TESTDEBT-SPINE-CONTROL-PLANE-HARNESS-DEDUP`** (fire-1 TD-1/2 — harness copytree+DRY, gated APPLY); **`F-TESTDEBT-SUBAGENT-STOP-HARNESS-DEDUP`** (fire-2 TD-6 — slice02/04 subprocess-harness ~40-line DRY, gated APPLY); **fire-3** `F-TESTDEBT-CLASSIC-GIT-WORKSPACE-DRY` (TD-8 ~60-80-line git-workspace DRY) + `F-TESTDEBT-CLASSIC-FLAKE-VECTORS` (TD-9 chmod-restore + **slice-14 live-repo-read = HALT-for-Ale release-contract-vs-flake**) + `F-TESTDEBT-CLASSIC-DEAD-SCAFFOLD` (TD-7); **fire-4** `F-TESTDEBT-DISPATCHER-TENSE-VARIANT-DRY` (TD-12 — 3 tense-variant @then bindings → 1); **fire-5** `F-TESTDEBT-WS-PRESERVATION-ORACLE-MISTARGET` (TD-14) + `F-TESTDEBT-WS-PIP-NETWORK-HERMETICITY` (TD-15 — pip-network flake gates the suite's only-green scenarios, CI-reliability); **run-7 AD-56 `F-TESTDEBT-CLI-SUBPROCESS-HARNESS-DEDUP` (HIGHEST non-blocked leverage — empirically 15 features / 24 composition files, ~600-900 LOC dup, supersedes/unifies TD-6/16/23/24/29; the ledger declared 5 = 3× undercount; extract `_harness/cli_subprocess.py` + session-template-repo fixture enforcing fresh-dict env-parity by construction → also closes TD-19); AD-55 `F-DOR-ITEMS-SSOT` (DoR 8-vs-9 SSOT drift → R6/methodology); catalog gap `F-CATALOG-DDD-ARCHITECT-SKILL-MISSING`** — see 🧪 TEST-DEBT LEDGER + run-7 panel) | OPEN | human-readable gate surfaces · lean-by-default outputs · self-hosted stale-code freshness (overlaps R1 deploy-copy) · hook import SPOF + install-probe parity + release leak gate · acceleration measured apples-to-apples · **cross-feature subprocess-harness dedup (AD-56, taxes the every-commit gate)** | The non-Lyra-operator polish + the empirical-acceleration proof. Lowest throughput-leverage now (the spine must *work* before its UX/measurement matters), but the publish-gate ultimately needs D1 + D6 to flip. |

**ToC #1 = R3 Gate non-vacuity / verification-completeness** (HEAD `2909782b3`, run-7) — **R1 closed in substance 5/5** (control-plane SSOT facets all shipped `b2d86fa02`/`bdd629565`/`3033d4f9a`/`e5757e38b`/`ceb4618b9`; whole-tree env-parity gate 0-red) and **R2 mechanism-complete 4/4** (slice-01 emit `f9cff135c` · 02 sign `5c98dbdfa` · 03 run `4f019de7` · 04 coverage-map `aa17d106`) — so the binding constraint moves to **R3**: making the gate's VERDICTS trustworthy (a vacuous/narrower-than-contract green lets broken slices through). R3 slice-01 keystone (feature-scoped gate excludes build-tier arch-tests, collect-AND-RUN) is **BUILT but HELD uncommitted behind AD-54**. **The immediate binding blocker is AD-54** — slice-04's coverage-map leg relocated `import yaml` into `coverage_map_verify_service.py:37`, the ONLY external import in `src/des/**`, violating the CI-enforced stdlib-only DES-bundle invariant (`test_des_bundle_steps.py:141`); it reds the zero-failing tree → holds R3 slice-01's commit. AD-54 is **HALT-for-Ale** (`docs/analysis/HALT-r2-yaml-bundle-violation-2026-06-04.md`); R2's "CLOSED" is therefore qualified "mechanism-complete, 1 genericità-residue open". Exploitation order: **R3** (gate honesty — unblock via AD-54 yaml-removal, then land slice-01 keystone; + AD-42 F21 AST-guard, TD-19/26/27 gate-honesty) → R4 (git port — **AD-37/38 commit_slice raw-git seam bypass = highest-leverage mechanical fix**; AD-24 uniform-INDETERMINATE; AD-54 `F-DES-BUNDLE-STDLIB-ONLY-ADR` + AD-57 `python_for` ADR) → R5 (per-slice authoring tax) → R6 (boundary-enforcement gate that stops the drift re-forming; + AD-55 DoR Item-9 SSOT) → R7 (upstream drift-min; + AD-41 verdict-vocab glossary) → R8 (operability + acceleration proof; **AD-56 cross-feature subprocess-harness DRY = highest non-blocked leverage**, 15 features / 24 files, taxes the every-commit gate). **8 roots: R1 GREEN-substance (done-gate→R2), R2 mechanism-complete (1 genericità-residue AD-54), R3 ToC #1 [slice-01 HELD behind AD-54], R4 PARTIAL, R6 +AD-53/AD-55, R8 +AD-56, rest OPEN.** D6 (acceleration, w.15) is STRUCTURALLY blocked — gated on a PO decision (instrument+reconstruct vs redefine the vs-classic criterion), NOT more same-kind measurement (`docs/analysis/d6-acceleration-pilot-2026-06-03.md`). *Cross-cutting GREEN (carried):* the contract-gate feedback loop (`F-SPINE-FEEDBACK-LOOP-TOO-SLOW`) was PARALLELISED `853202e33` (~30min→~5min via pytest-xdist) — removes the per-slice RUN latency tax across all roots. (Distinct from `F-GCOMMIT-CONTRACT-SUITE-MULTI-RUN`/AD-25, suite-recompute memoization, still OPEN.)

---

## Audit 2026-06-07 — feature-end frictions (at-mandate-mechanical-enforcement shipped, HEAD `b498a34f32`)

Three spine frictions surfaced shipping `at-mandate-mechanical-enforcement` (11 slices + feature-end, done-gate green) where a NON-STANDARD slice or the feature-end cycle does not fit the per-slice carpaccio `.feature`-AT model. Verified-from-source at HEAD; annotation-only (no fix here).

- **AD-63 `F-CARPACCIO-NONSTANDARD-SLICE-GATE-MISMATCH` (R5).** The carpaccio entry-gate + the E1 `verify-slice-commit` exit-gate both assume EVERY slice authors per-slice Gherkin `@slice-NN` `.feature` ATs. Two slice shapes break this: (a) a **removal-only consolidation** (slice-10 deleted the TD-41/45 ice-cream-cone subprocess ATs, authored none) → `verify-slice-commit --feature-id` returns exit 2 (`SliceCommitRefused`, no `SliceCommitVerified` written) because the commit carries no `@slice-10` `.feature`; EXPECTED, not a defect (the gate verifies a slice ADDS its ATs). (b) a **structural meta-gate** authored as plain parametrized pytest (slice-11 Tier-M, the natural shape for a filesystem-walk) → carpaccio entry-gate REJECTS `no-scenarios-for-slice` (exit 45), forcing a re-shape to pytest-bdd. Fix direction: the gate should recognise consolidation/meta/config slice CLASSES (no per-slice `.feature` required; E1 N/A by class) instead of failing closed. Workaround applied: commit consolidation with `Slice-Id`+`Gate-Scope` (E2 `--verify-gate-scope` + `verify-integrity` reconcile via the trailer regardless); re-shape meta-gates to pytest-bdd. Cross-ref R5 `F-CARPACCIO-FUTURE-SLICE-SCAFFOLD-BLOCKS-COMMIT` (same per-slice-authoring-model rigidity).

- **AD-64 `F-OSS-FEATURE-END-VERIFICATION-NO-SEQUENCER-ALIGNMENT` (R2, Ale-flagged 2026-06-07: "il sequencer NON esiste in OSS, la verifica va allineata").** The done-gate `verify-integrity` requires 6 feature-end ledger records (`EBatchRefactorCompleted`, `FeatureEndReviewVerdict`, `WalkingSkeletonGateRan`, `EnvironmentalE2eGateRan`, `CoverageMapVerifiedAtDistillExit`/`AtDeliverExit`); on absence it errors "the batch refactor + deep review never ran". **In OSS (hook-only, NO sequencer — there is no `des.cli.sequencer`/`des.application.zero_trust.main` orchestrator at runtime), these records are NOT auto-emitted by any engine** — the ORCHESTRATOR must explicitly call `des feature-end run` (which runs the WS + env-e2e gates, signs the deep-review verdict, emits the records, fail-closes anti-theater). The misalignment: the verification model + its error wording presume a sequencer emitted the records, but OSS has no sequencer → the OSS done-gate path must be aligned so its contract + diagnostics name the explicit CLI-driven emission (`des feature-end run`) as the OSS source of truth, not an absent sequencer. Also surfaced: feature-end requires a `docs/feature/<id>/walking-skeleton.json` manifest (declare `walking_skeleton_applicable:false` for internal/no-installable-artifact features; the WS gate verifies it against `git diff --diff-filter=A` build-system delta) — hand-orchestration creates none. Fix direction: align the OSS feature-end verification (contract + error messages + docs) to the hook+CLI reality; the canonical OSS driver is `/nw-deliver` looping `des feature-end run`, never an assumed sequencer. Cross-ref R2 `F-ATDD-PURE-FEATURE-END-CYCLE-UNWIRED`.

---

## Backlog epic 2026-06-07 — `F-LANGUAGE-ADAPTER-PLUGIN-INFRASTRUCTURE` multi-language adapters (PROD-READINESS, R4, Ale-flagged)

**The prod-readiness blocker for non-Python target codebases.** The genericità / target-machine-agnosticism mandate (CLAUDE.md STANDING + `[[feedback_language_adapter_plugin_architecture_2026_05_24]]`) requires nWave's gates/waves to run on any target machine with Python 3.10+ — but the AST/gate ports are currently **Python-only**. Only `src/des/testarch/adapters/python_ast.py` realizes the testarch AST capabilities, and the language-adapter port catalog (`nWave/data/language-adapter-ports.yaml`) declares `supported-languages: python, typescript, go` while every port is `classification: LANGUAGE_BOUND` with **Python-only witnesses**. The catalog + `src/des/cli/doctor.py` already SURFACE the per-target-language gap (by design); this epic CLOSES it.

**Scope — implement the TypeScript + Go (+ Rust future) adapters** realizing, per language:
1. The **testarch AST capability set** (`capabilities.py` — 9 capabilities today: `functions_with_decorator`, `imports_in_function`, `imports_in_module`, `calls_in_function`, `marker_decorators`, `spawn_shape_in_body`, `keyword_arg_names`, `assignments_constructing_type`, `layer_of_file`) **+ the inline-dup AST-normalization capability** (from `intra-implementation-duplication-gate`, D9) — each behind the `TestSuiteAstAdapter` port (per-language: tree-sitter for TS/Go/Rust per the genericità mandate; only the per-language adapter imports its parser).
2. The 3 `LANGUAGE_BOUND` catalog ports: `run_contract_gate` (per-language `TestRunnerPort` — jest/vitest, `go test`), `verify_environmental_e2e` (per-language build-toolchain — npm/tsc, `go build`), `check_robustness_density` (per-language `PropertyBasedTestingPort` — fast-check, gopter).
3. Each realization adds its `file:symbol` witness to `nWave/data/language-adapter-ports.yaml` (validated by `validate_language_adapter_catalog.py` exit 0) so `doctor.py`'s gap report shrinks toward full coverage.

**Done = prod-ready for that target language**: `doctor.py` reports zero gaps for the language; the testarch gates + the contract/env-e2e/PBT gates run on a target machine whose source is that language with nothing but the language toolchain + Python 3.10 assumed. **R4 (target-machine generality / D8 cross-language adapter goal)** — this IS the D8 realization. Sequence by demand: TypeScript first (largest target market per the runtime-pluggability urgency, `[[feedback_runtimes_must_be_pluggable_copilot_urgency_2026_06_06]]`), then Go, then Rust. The `intra-implementation-duplication-gate` feature records the normalization-capability port obligation (Python witness) so each language adapter inherits a complete, catalog-enumerated checklist — exactly the cross-language onboarding SSOT the capability-registry was built to be.

> **✅ SELF-ENFORCEMENT NUCLEUS SHIPPED (2026-06-07, HEAD `8328b9f0f`).** The keystone feature `language-adapter-registry-self-enforcement` (3 slices + feature-end, 12+ ATs green) delivered THE FIX described below: (a) catalog-as-projection cross-check + (b) the generalized per-plugin × capability conformance gate. Shipped surfaces — `src/des/testarch/rules/registry_conformance.py` (generalized 2-D detector, slice-01 `477cc1fd5`) + `detect_catalog_coverage_drift` 2-axis cross-check (`CATALOG_LANGUAGE_COVERAGE_DRIFT` / `PORT_OUTSIDE_CATALOG`, slice-02 `2662ba9a8`) + `src/des/testarch/discovery.py` live `nwave.lang.adapter` entry-point discovery + 3-lane (`0` / non-zero / INDETERMINATE-loud) conformance CLI (slice-03 `3f1b12df2`). **Scope note**: this is the ENFORCEMENT machinery + the Python dogfood witness — the multi-language REALIZATIONS (TS/Go/Rust adapters under the parent epic above) remain unbuilt. Credits **D8 0→18%** (see `docs/backlog.md` §D8). The "intent, not impl" caveat at the foot of item 3 below is now SUPERSEDED for the derivation + generalized-gate pieces.

**SELF-ENFORCEMENT — the keystone (Ale 2026-06-07: "finalmente raggiungiamo coerenza").** The epic is NOT just "implement the adapters" — it is "make the language-adapter registry SELF-ENFORCING so no one has to *remember* to maintain it." Three pieces exist sparse; the epic UNIFIES them into one coherent strategy-pattern-behind-a-port system:
1. **The strategy-pattern + self-registration ALREADY EXISTS** — `src/des/ports/language_adapter_plugin.py::LanguageAdapterPlugin` (M44 ABC, pure stdlib+abc): 4 mandatory abstract members (a subclass omitting any is non-instantiable = mechanical completeness), `target_language` (kebab-id), `register_adapters(registry)` (per-port wiring entry point), discovered via the **`nwave.lang.adapter` PyPI entry-point group (ADR-031)**. To support a language = implement the plugin + register in the group → auto-discovered. (Python slice-05a, TypeScript slice-07, Go/Rust/Java future all inherit this ABC.)
2. **THE GAP (the "remember to maintain it" drift)** — the catalog `nWave/data/language-adapter-ports.yaml` is still HAND-MAINTAINED ("Machine-readable distillation of docs/architecture/language-adapter-catalog.md"), NOT a projection of the registered plugins → the catalog and the actually-registered adapters can DRIFT (the exact cross-SSOT-drift class `intra-implementation-duplication-gate` + at-mandate slice-12 fight).
3. **THE FIX (a) catalog-as-projection** — derive `language-adapter-ports.yaml` (or cross-check it at gate-time) FROM the discovered `nwave.lang.adapter` plugins' actual capability coverage; hand-maintenance → drift = gate-RED. **(b) per-language conformance gate** — GENERALIZE the slice-12 `registry_conformance` gate (which already asserts "every registered Capability is realized on the real adapter") to "every registered language-adapter plugin realizes every registered capability." Then "remember to update the registry" DISSOLVES: the registry IS the set of registered plugins, the catalog DERIVES, the conformance gate ENFORCES completeness mechanically. The architect designs the derivation + the generalized gate in the epic's DESIGN wave — this note captures the self-enforcement INTENT, not the impl. This is the coherence: strategy-pattern (exists) + capability-registry SSOT (exists) + conformance gate (slice-12, shipped) wired into one self-enforcing whole.

---

## Audit 2026-05-31 — 3-architect panel (HEAD `b7d651feb`)

Lenses: `nw-solution-architect` (application/boundaries), `nw-system-designer` (system/infra), `nw-ddd-architect` (domain/SSOT). Model: Opus.

### ToC synthesis — the architectural constraint

**THE CONSTRAINT (elevate first): mechanical boundary-enforcement is declared but does not exist.** The brief (`brief.md:207,267`) and ADR-006 (`:70`) cite `import-linter` as the CI gate enforcing hexagonal purity, but **no `import-linter` config exists anywhere** (verified: no `[importlinter]`, `.importlinter`, or `lint-imports` in any toml/cfg/ini/workflow). Every domain→adapter boundary violation below (AD-02) is a *symptom* a working gate would have blocked. Two independent lenses (solution F-2, domain F-2) converged on this as the root enabler.

**Exploitation/subordination order** (highest leverage-per-effort first): elevate the gate (AD-01) → introduce the missing port that closes the live breaches (AD-02) → unify the fractured version SSOT (AD-03) → type the central aggregate at its parse seam (AD-04) → then the cheap doc realignments (AD-09..AD-12) which stop *new* drift propagating to downstream waves at near-zero cost.

### Priority 1 — the constraint + its highest-leverage structural fixes

| ID | Finding | Type | Severity | Conv. | Evidence | Proposed backlog |
|----|---------|------|----------|-------|----------|------------------|
| **AD-01** | `import-linter` boundary gate declared in brief + ADR-006 but **no config exists** — hexagonal purity is an unenforced convention that is actively eroding | drift-vs-brief / drift-vs-ADR | **critical** | 2/3 (sol F-2, ddd F-2) | `brief.md:207,267`; `docs/adrs/ADR-006-domain-layer-purity.md:70`; exhaustive grep → zero `[importlinter]`/`lint-imports` in any config or `.github/workflows/*`. (import-linter can't assert method-presence → an AST test `tests/des/.../test_hexagonal_boundaries.py` is the viable substitute) | `F-ARCH-BOUNDARY-ENFORCEMENT-GATE` |
| **AD-02** | Domain → Adapter DIP breach: 2 domain modules import the concrete `AtCompletionLedger` adapter; no port exists behind it (root cause = missing `AtCompletionLedgerPort`) | boundary-violation | **high** | 2/3 (sol F-3/F-4/F-7, ddd F-2) | `src/des/domain/conversion_planner.py:32,476` (stateful instantiate in impure `execute`); `src/des/domain/environmental_e2e/done_gate.py:24-27` (event-name constants — invert to domain SSOT) | `F-ARCH-LEDGER-PORT-DIP` |
| **AD-04** | Central aggregate `PhaseEvent` is stringly-typed (`phase_name/status/outcome: str`) while the matching VOs already exist **unused**; ADR-006 Fix 3 required conversion at the parse seam | SSOT-violation / drift-vs-ADR | **high** | 1/3 (ddd F-1) | `src/des/domain/phase_event.py:37-43` vs `src/des/domain/value_objects.py:50-129` (`PhaseName/PhaseStatus/PhaseOutcome`); `PhaseEventParser` does zero enum conversion (`phase_event.py:63-176`); 128 enum uses across 14 files | `F-ARCH-PHASEEVENT-TYPED-SEAM` |
| **AD-05** ✅RESOLVED (verified-from-source 2026-06-06) | CLI layer used as a library by application + adapter layers (layering inversion): verification logic (`extract_slice_ids`, `_feature_tag_files`) lives in CLI entrypoints, imported downward | bounded-context-drift / SSOT-violation | **high** | 1/3 (ddd F-3) | RESOLVED: the functions were PROMOTED OUT of `des.cli` into the application layer (`slice_at_completeness.py`, `feature_at_files.py`, `workflow_mode.py` — the residual `des.cli` mentions there are docstrings describing the promotion + subprocess-dispatch module-path *strings*, the correct adapter→CLI direction, NOT downward imports). Grep `from des.cli` in `src/des/application/` + `src/des/adapters/` = zero real imports. The "WORSENED" drift-note (row ~131, `slice_at_completeness.py:32 from des.cli...`) is also stale — that line is now a docstring. | `F-ARCH-CLI-LIBRARY-INVERSION` (closed) |

### Priority 2 — version SSOT unification (fractured across ≥4 derivation points)

| ID | Finding | Type | Severity | Conv. | Evidence | Proposed backlog |
|----|---------|------|----------|-------|----------|------------------|
| **AD-03** | `nWave/VERSION` stale by 2 minor (`3.13.0`) vs SSOT `pyproject.toml`/`catalog` (`3.15.1`); CLAUDE.md claims it is "synced" — it is not. Release bump path stamps only pyproject+catalog | SSOT-violation | **high** | 2/3 (sol F-1, sys F-1) | `nWave/VERSION:1`=3.13.0; `pyproject.toml:30`=3.15.1; `framework-catalog.yaml:2`=3.15.1; `release-prod.yml:382-385` bumps only `--pyproject --catalog`; yet `pyproject.toml:373-375` lists VERSION in commitizen `version_files` (config vs CI disagree) | `F-VERSION-SSOT-SINGLE-BUMP` |
| **AD-06** | `nwave_ai/__init__.py` version stamped out-of-band via raw `sed` at publish; absent from commitizen `version_files`; public floor is a 3rd source — 3 uncross-checked version mechanisms | SSOT-violation | **high** | 1/3 (sys F-2) | `nwave_ai/__init__.py:3`=`0.0.0-dev`; `release-prod.yml:562` (sed); floor `pyproject.toml:161`; recompute `release-prod.yml:710-712` | folds into `F-VERSION-SSOT-SINGLE-BUMP` |
| **AD-07** | `nwave-target` version read falls back to `0.0.0` and the published version is never used as a floor in the `max()` → non-monotonic public-version risk (PyPI is immutable) | SSOT-violation | low | 1/3 (sys F-9) | `release-prod.yml:698-703,712` | folds into `F-VERSION-SSOT-SINGLE-BUMP` |

### Priority 3 — robustness / failure-mode (substrate honesty, SPOF, leak gates)

| ID | Finding | Type | Severity | Conv. | Evidence | Proposed backlog |
|----|---------|------|----------|-------|----------|------------------|
| **AD-08** | JSONL audit writer has **no flock, no fsync** despite brief claiming "crash-safe append-only"; sibling `AtCompletionLedger` documents the opposite for the same substrate (flock+seq+hash). Two writers, opposite trust assumptions | concurrency-risk | medium | 1/3 (sys F-3) | `jsonl_audit_log_writer.py:95-96` (bare `open(...,"a").write`) vs `at_completion_ledger.py:23-34,52,293-296` | `F-AUDIT-WRITER-FLOCK-PARITY` |
| **AD-09** | Hook entry-point is a real import-chain SPOF (brief-acknowledged, unmitigated): any import error blocks **all** hooks fail-closed, no degraded mode | SPOF | medium | 1/3 (sys F-5) | `claude_code_hook_adapter.py:39`; brief `:489`; ADR-PLAT-005 limits per-handler radius only | `F-HOOK-SPINE-IMPORT-FALLBACK` |
| **AD-10** | `des_plugin.verify()` import-probe omits `import yaml` that the runtime adapter requires → false-green if PyYAML missing | drift-vs-ADR (probe) | medium | 1/3 (sys F-7) | `des_plugin.py:1586` (probes yaml) vs doc'd shape `nwave-plugin-system-architecture.md:805-806` (no yaml) | `F-INSTALL-VERIFY-PROBE-PARITY` |
| **AD-11** | Release-prod public sync is destructive (`rm -rf docs/` + `rsync --delete` + `git push --force` tag) with **no post-strip pre-push content-leak assertion** — single mis-edited exclude/`public:` flag leaks with no 2nd gate | SPOF / leak-risk | medium | 1/3 (sys F-8) | `release-prod.yml:719,722,782,924,947-948` | `F-RELEASE-PREPUSH-LEAK-GATE` |

### Priority 4 — documentation SSOT realignment (cheap, stops downstream drift)

| ID | Finding | Type | Severity | Conv. | Evidence | Proposed backlog |
|----|---------|------|----------|-------|----------|------------------|
| **AD-12** | Brief documents legacy 5-phase TDD cycle as **current** in 2 non-historical spots; ADR-025 canon is 3-phase RED/GREEN/COMMIT. Brief is the SSOT acceptance-designer + crafter read | drift-vs-ADR | medium | 2/3 (sol F-6, sys F-4) | `brief.md:104,268` vs `docs/architecture/ADR-025-3-phase-tdd-canon.md` (Accepted); code is correct (`tdd_schema.py` CANONICAL_PHASES) | `F-BRIEF-TDD-CANON-REALIGN` |
| **AD-13** | The 8-bounded-context model is **invisible in the SSOT brief**: brief has zero "bounded context"/"domain model"/"ubiquitous language"; the BC map lives only in a `PROPOSAL`-status analysis doc | drift-vs-brief | medium | 1/3 (ddd F-6) | `brief.md` (grep empty); `docs/architecture/des-bounded-context-reorganization-plan-2026-05-29.md` (PROPOSAL); brief `:42-50` is layer-organized not context-organized | `F-BRIEF-DOMAIN-MODEL-SECTION` |
| **AD-14** | `CommandName.VALID_COMMANDS` ubiquitous-language drift: hard-codes `/nw-develop` (does not exist; canon is `/nw-deliver`); `is_validation_command` gates on the phantom term | SSOT-violation | medium | 1/3 (ddd F-4) | `value_objects.py:227,240-242` vs `nWave/tasks/nw/*.md` (no `develop`) | `F-COMMANDNAME-VOCAB-SSOT` |
| **AD-15** | Plugin-system arch doc is internally inconsistent (`Status: IMPLEMENTED`/`PRODUCTION READY` header vs migration table "❌ NOT STARTED" for already-shipped base/registry/DES-plugin) and misnames DES ("Definition of Enforcement System") | drift-vs-brief | low | 1/3 (sys F-6) | `nwave-plugin-system-architecture.md:5,10,51,624,1661-1673` | `F-PLUGIN-DOC-ROT` |

### Priority 5 — lower-leverage residue

| ID | Finding | Type | Severity | Conv. | Evidence | Proposed backlog |
|----|---------|------|----------|-------|----------|------------------|
| **AD-16** | ADR-006 Fix 2 not applied: mutable global singletons (`_global_loader` + global getters) still in domain `tdd_schema.py`/`roadmap_schema.py`, still consumed by CLI. (ADR-006 is *Proposed*, so non-binding — but AD-02 shows purity eroding past it) | drift-vs-ADR | medium | 1/3 (sol F-5) | `tdd_schema.py:317,333,345,357`; `roadmap_schema.py:133-147`; `verify_deliver_integrity.py:42,505` | `F-DOMAIN-SINGLETON-REMOVAL` |
| **AD-17** | `parents[N]` positional path-coupling — 4 Class-B runtime landmines that fail silently on the customer machine; reorg-plan names this as the migration bottleneck, name-anchoring (Slice-0) not landed | aggregate-design | medium | 1/3 (ddd F-7) | `claude_code_hook_adapter.py:31`; `carpaccio_intercept.py:120`; `cli/doctor.py:48`; `repo_source_probe.py:34`; reorg-plan `:269-282` | `F-PATH-ANCHOR-PARENTS-N` |
| **AD-18** | Three "verdict" vocabularies (`ValidationStatus`/`GateVerdict`/`VerdictStatus` with `PASSED`/`PASS`/`GREEN`) model the same concept across 3 contexts — **NOT a merge target** (merging couples contexts); leverage is a glossary documenting them as deliberately distinct context-languages | duplication (doc-debt) | medium | 1/3 (ddd F-5) | `value_objects.py:124-129`; `gate_outcome.py:17-31`; `earned_verdict.py:40-45` | `F-VERDICT-VOCAB-GLOSSARY` |
| **AD-19** | Shared-kernel modules (`atdd_pure_phases.py`, `feature_classifier.py`) co-owned by 2 contexts with no joint-ownership contract → risk of duplication (not promotion) under the planned BC split | aggregate-design | low | 1/3 (ddd F-8) | reorg-plan `:76,92,96,120,424-426` | `F-SHARED-KERNEL-OWNERSHIP` |

### Verified-clean (explicit data — not debt)

- **ADR-PLAT-004 orchestrator decomposition** applied — `orchestrator.py:1-55` is a thin delegating facade (sol).
- **`ports/` layer pure** — no inverted `domain→`/`ports→adapters` imports (sol).
- **F-D-09 forbidden-import-roots** resolved — `ports/language_adapter_plugin.py` owns its ABC (sol).
- **ADR-006 Fix 1** (move `InvocationLimitsValidator` to application/) applied (sol/ddd).
- **`AtCompletionLedger`** is exemplary substrate-honesty (flock + monotonic seq + per-record hash) — the template AD-08 should adopt (sys).
- **Plugin dependency resolution** (Kahn topo-sort, circular fail-fast, deterministic tiebreak) correct (sys).
- **DES subprocess hygiene** — sampled `subprocess.run` all use `capture_output=True` + explicit `timeout=`, no pipe-deadlock class visible (sys). *(NB: contrast with the known `_run_contract_gate` capture_output divergence tracked separately as `F-VERIFY-SLICE-COMMIT-E2-WRAPPER-DIVERGENCE` — sampled DES paths are clean, the gate wrapper is the named exception.)*
- **Release concurrency control** — `concurrency` group + idempotent tag/release creation handles partial-rerun (sys).
- **`Result`/`Success`/`Failure`**, **VO identity types**, **`EarnedVerdict` aggregate**, **aggregate sizing** (no god-aggregate) — domain *design* is healthy; debt is cross-layer leaks + invisibility-in-SSOT, not modeling (ddd).

---

## Addendum 2026-05-31 (Ale-directed) — ADR-SSOT + python-only/git-free mandate

### Priority 1 — constraint-class

| ID | Finding | Type | Severity | Conv. | Evidence | Proposed backlog |
|----|---------|------|----------|-------|----------|------------------|
| **AD-20** | **ADR-SSOT fragmentation**: permanent ADRs scattered across 3 locations with number collisions + identical duplicates, despite `adr-ssot-document-model.md:36` declaring `docs/product/architecture/` canonical. Two distinct `ADR-001` (plugin-marketplace vs rigor-as-config) and `ADR-002` (des-hooks vs tdd-phase-enforcement). | SSOT-violation | high | n/a (Ale-flagged + verified) | Permanent ADRs in `docs/adrs/` (6), `docs/architecture/` + `adr/` + `adrs/` (9), `docs/product/architecture/` (14). Confirmed IDENTICAL dups: `docs/architecture/adr/ADR-PLAT-001/2/3` == product copies; `docs/adrs/ADR-004/005/006` == product `ADR-PLAT-004/005/006`. SSOT spec: `docs/architecture/adr-ssot-document-model.md:36` | `F-ADR-SSOT-CONSOLIDATION` (in progress 2026-05-31) — consolidate permanent ADRs into `docs/product/architecture/`, delete dups, renumber the 001/002/003 collision; feature-local (`docs/feature/{id}/design/adrs/`) + archive (`docs/archive/`) NOT moved |
| **AD-21** | **Gates/waves depend on `git` — violates the python-only/target-machine-agnostic mandate** (CLAUDE.md STANDING 2026-05-31). The exit-gate + contract-gate logic shells out to `git` as a hard requirement, so a target machine without git (or a non-git VCS) cannot run the gates. `git` is not a dependency per mandate. | drift-vs-constraint | high | n/a (Ale-mandated) | `src/des/cli/run_contract_gate.py:332-341` (`_git` hard `subprocess.run(["git", ...], check=True)`); `verify_slice_commit_completeness.py` (Slice-Id/Gate-Scope trailer reads via git); `src/des/adapters/driven/git/` (GitCommitVerifier, GitScopeChecker). Gate logic must move behind an optional driven-port that degrades LOUD (INDETERMINATE) when git absent, not a baked-in requirement. | `F-GATE-GIT-DEPENDENCY-REMOVAL` (extends ADR-PLAT-001 to runtime gate layer) |

> **Mandate recorded in `CLAUDE.md` → "Architectural Constraints (STANDING)"**: (1) depend ONLY on Python; git/external-CLI are NOT dependencies for gates/waves; (2) one canonical ADR folder `docs/product/architecture/` for permanent ADRs. AD-20/AD-21 are the debt against these.

---

## Audit 2026-05-31 (second panel — DELTA) — HEAD `6020dc76b`

Lenses: `nw-solution-architect`, `nw-system-designer`, `nw-ddd-architect` (Opus). Delta vs the prior panel (HEAD `b7d651feb`): the F-GCOMMIT-EXIT-GATE-SCOPING feature shipped slice-01 (`2e0e70c71`, committed-scope-digest mode) + slice-02 (`6020dc76b`, E2/trailer committed-scope wiring). slice-03 (E1 cross-feature scoping) was uncommitted-green in the working tree at audit time. The panel was scoped to DELTA only (status of AD-01..AD-21 + new findings from today's commits) — it did NOT re-derive the prior 21 findings.

### ToC synthesis — the NEW architectural constraint introduced today

**THE NEW CONSTRAINT (elevate first): AD-22 — `CommittedScopePort` was introduced as a concrete git-shelling class IN the application layer, named "Port" but with no ABC in `ports/` and no adapter in `adapters/driven/git/`.** Two independent lenses (solution NEW-D1, ddd NEW-1 → 2/3 convergence) flagged this as the structural root. It is why AD-21 is only *partially* addressed (degrade-LOUD achieved for the E2/committed path, but git is welded into application logic, not behind a swappable port), it blocks the D8 cross-language-adapter mandate (no `Protocol` for `nwave-lang-{ts,go,rust}` to implement), and the "Port" misnomer will mislead the AD-21 fixer into thinking the seam is already abstracted. **Exploitation order**: extract the real port (AD-22) → that unifies the scattered git seams, which is the precondition to fix AD-24 (uniform git-absence signalling) and AD-23 (producer/verifier digest parity) coherently → all three fold under the AD-21 `F-GATE-GIT-DEPENDENCY-REMOVAL` epic. Net AD-21 status: **degrade-LOUD advanced for one path, but the runtime git-coupling SURFACE GREW** (3 new call sites: `git ls-tree`/`rev-parse`/`log`/`cat-file`).

### Status deltas on prior findings (amended in place)

| AD-ID | Delta | Evidence at `6020dc76b` |
|----|----|----|
| **AD-05** (CLI-library inversion) | **WORSENED** | New bidirectional cycle: `run_contract_gate.py:48-52` (CLI) now imports `CommittedScopePort`/`CommittedFileSet` FROM `des.application.slice_at_completeness`, which itself imports BACK from CLI (`slice_at_completeness.py:32 from des.cli.carpaccio_slice_gate import _feature_tag_files`). New committed-scope machinery built ON the AD-05 substrate → larger fix blast-radius. |
| **AD-21** (git-dependency) | **PARTIAL + SURFACE-WORSENED** | Toward mandate: committed-scope path degrades LOUD via `CommittedScopePort`→`Indeterminate`→`health.gate.committed-scope.indeterminate` (`run_contract_gate.py:465-466,541-554`). Away: 3 new bare-`_git` call sites; structural port goal unmet (see AD-22). |
| AD-02/04/08/09/13/14/16/18/19 | **UNCHANGED** | Not touched by the delta. NB AD-08: new G_COMMIT ledger writes correctly use the flock+seq+hash `AtCompletionLedger` (`subagent_stop_handler.py:543,571`), NOT the bare JSONL writer → AD-08 stays scoped to the audit-log writer only. |

### NEW findings (deduped across the 3 lenses)

| ID | Finding | Type | Severity | Conv. | Evidence | Proposed backlog |
|----|---------|------|----------|-------|----------|------------------|
| **AD-22** | `CommittedScopePort` is an adapter masquerading as a port, embedded in the application layer: a concrete class shelling `git ls-tree` inline, no ABC in `ports/`, no adapter in `adapters/driven/git/`. Diverges from the established convention (`ports/driven_ports/scope_checker.py` ABC + `adapters/driven/validation/git_scope_checker.py`). Root cause of partial-AD-21 + blocks D8 cross-language. Subsumes `_git` triplication (solution NEW-D3). | boundary-violation / drift-vs-ADR (hexagonal) | **high** | 2/3 (sol NEW-D1, ddd NEW-1) | `src/des/application/slice_at_completeness.py:181-217` (concrete class, `subprocess.run(["git","ls-tree",...])` `:201-206`); canonical pattern `ports/driven_ports/scope_checker.py:38`; `_git` triplicated at `run_contract_gate.py:343-352`, `slice_at_completeness.py:42-51`, `reverify_slice_commit.py:65` | `F-COMMITTED-SCOPE-PORT-EXTRACT` (sub-task of `F-GATE-GIT-DEPENDENCY-REMOVAL`/AD-21) |
| **AD-23** | Producer-vs-verifier digest-definition asymmetry: `_mode_run_suite` (producer) stamps a WORKING-tree digest on git-absent fallback (silent, no LOUD marker), while `_mode_verify_gate_scope` ALWAYS uses committed-scope + refuses LOUD on git-absent → two non-equivalent digest definitions in one CLI selected by an is-git-work-tree predicate; a trailer seeded from a dirty/working tree is structurally un-verifiable. This is the C-reviewer's R1 residue, confirmed by 2 lenses as a PRODUCTION-mechanism property (not just test-fixture debt). | drift-vs-constraint / verification-narrower-than-contract (on the git-absent axis) | **medium** | 2/3 (sol NEW-D2, sys R1-assessment) | producer `run_contract_gate.py:640-647` (working-tree fallback `:647`, comment `:631-637`); verifier `:557-615`→`_committed_scope_digest_value:489-503` (LOUD refuse) | `F-GCOMMIT-PRODUCER-VERIFY-DIGEST-PARITY` |
| **AD-24** | Inconsistent git-absence signalling across the 3 git seams (split-brain substrate honesty): the SAME `git`-missing fault yields 3 different operator signals — committed-scope→LOUD `health.gate.committed-scope.indeterminate`; `verify_slice_commit` `_git`→generic `MalformedInput` (exit 2); hook `_resolve_head_sha`→misleading `AtddPureHookInternalError`. An operator on a git-less target (the AD-21 target env) cannot distinguish "substrate lacks git" from "gate has a bug". | SPOF / drift-vs-constraint (substrate-honesty) | medium | 1/3 (sys AD-22) | `run_contract_gate.py:465-466,541-554` (correct); `verify_slice_commit_completeness.py:333` + `slice_at_completeness.py:42-51,117-123` (generic/uncaught); `subagent_stop_handler.py:515-523,676` (internal-error label) | `F-GATE-GIT-ABSENCE-UNIFORM-INDETERMINATE` (under AD-21 epic) |
| **AD-25** | G_COMMIT exit-gate runs ≥2 full-tree pytest `--collect-only` child-interpreter spawns sequentially on the SYNCHRONOUS hook path (E1 then E2, each ≤120 s), each over ~4500 collected nodes, with no digest memoization across E1/E2 or the compute→verify window. As the suite grows, per-commit gate latency climbs toward the 120 s ceiling → timeout converts slow-but-correct collection into a false-negative `SliceCommitBlocked` under load. | concurrency-risk / scalability | low-medium | 1/3 (sys AD-23) | `subagent_stop_handler.py:626,641` (sequential subprocesses, `:457` 120 s); E2 collect `run_contract_gate.py:468-486`, worker `:167-179`; node count `:291-293`; producer/verifier each recompute independently (`:640`, `:569`) | `F-GCOMMIT-EXIT-GATE-COLLECTION-COST` (memoize per commit-SHA+file-set; collect once, share E1/E2) |

### Extends prior findings (no new F-ID)
- **AD-13 + AD-18** (ddd NEW-2): the committed-scope vocabulary (`gate-scope`/`committed-scope` digest, `_CommittedScopeDigest`/`_Indeterminate`/`_Refusal` tri-state, `VerifyOutcome` = `VERIFIED/UNVERIFIED/REFUSED/UNEXPECTED`, `health.gate.committed-scope.indeterminate`) is **invisible in the SSOT brief/ADRs** (AD-13 class) AND adds a 4th degrade-LOUD member to the AD-18 verdict-vocabulary family — a deliberately-distinct context-language (non-halting INDETERMINATE per `feedback_oss_acl_published_language_cross_tier_2026_05_31`) that is un-glossaried and growing. Fold into `F-VERDICT-VOCAB-GLOSSARY` (AD-18) + `F-BRIEF-DOMAIN-MODEL-SECTION` (AD-13).

### Verified-clean (explicit data — not debt; introduced by today's work)
- **Mandate-12 slice test-vocabulary is exemplary** — `domain_types_slice_02.py:31-35` / `domain_types_slice_03.py:39-41` re-export slice-01 nouns verbatim with explicit SSOT comments; each slice adds only its genuinely-new nouns. Zero duplication/drift across the slice `domain_types` modules (ddd point 3 clean).
- **The `_CommittedScopeDigest`/`_Indeterminate`/`_Refusal` tri-state is GOOD bounded-change design** — it makes the degrade-LOUD contract a type the compiler tracks (Principle-8: `Indeterminate` is structurally non-silenceable). AD-22 is about its MISLAYERING, not its modeling.
- **New G_COMMIT ledger writes use the substrate-honest `AtCompletionLedger`** (flock+seq+hash), not the bare JSONL writer — confirms AD-08 is scoped to the audit-log writer alone.

---

## Audit 2026-06-02 — 3-architect panel (HEAD `9c8d1b3af`)

Lenses: `nw-solution-architect` (application/boundaries), `nw-ddd-architect` (domain/SSOT), `nw-system-designer` (system/infra). Model: Opus. **Read-only** (architects returned structured findings; orchestrator did the single append). **Scope exclusion**: the in-flight R1 slice-04 gate-composition cluster (`subagent_stop_handler` frozenset + `flavor_dispatcher` + `subset_parser`), the shipped slice-03 mode-resolver, and slice-01 freshness were excluded to avoid double-counting on a half-modified working tree. Disprove-before-constraint applied (AD-04 PhaseEvent-VO false-positive was the cautionary case — none of the below re-raise it).

> **New findings map to EXISTING roots — no re-rank.** R1 remains ToC #1 (these reinforce it). New: one naming-SSOT root candidate (AD-29) of low throughput-leverage. The git-port finding (AD-28) is 2/3-convergent (solution + system lenses independently hit the same call-site).

### Priority 1 — R1 control-plane residue (verified STILL-OPEN after slices 01–03)

| ID | Finding | Type | Severity | Conv. | Evidence | Proposed backlog |
|----|---------|------|----------|-------|----------|------------------|
| **AD-26** | feature-end-required-records set is **TRIPLICATED**; copy #3 is a CLI consumer OUTSIDE the slice-04 dispatcher scope → will silently drift if a 7th record is added to the flavor YAML | duplication / SSOT-violation | **high** | 1/3 (sys F-3) | (1) `nWave/flavors/atdd_pure.yaml:78-84` (declared SSOT) · (2) `subagent_stop_handler.py:821` (slice-04 target, EXCLUDED) · (3) **`src/des/cli/verify_deliver_integrity.py:457-464`** hardcodes the same set, NOT routed through `_feature_end_required_records()` · **(4) NEW FACET (verified 2026-06-06, fix-feature-end-ws-gate-applicability slice-04 `90453b1cf`): `verify_deliver_integrity.py:474 _NA_MARKER_RECONCILES`** is a 2nd hardcoded representation of the required-records contract in the same file (maps each `*NotApplicable*` marker → its required `*Verified*` name); a 7th coverage/env record added to the flavor-YAML SSOT now silently drifts BOTH the hardcoded `required` set AND this reconcile-map → the F-SPINE-CONTROL-PLANE-SSOT fix must route the reconcile-map through the same YAML SSOT, not just copy #3 | `F-SPINE-CONTROL-PLANE-SSOT` slice-04-adjacent fast-follow (pull copy #3 AND the NA-reconcile-map into the YAML-read, or follow-on slice) — status=enqueued, skill=architect/crafter |
| **AD-27** | Config-asset (flavor YAML + catalog) is **OUTSIDE the freshness envelope** — SYS-4 still open after slice-01 (envelope hashes only `*.py`); a stale shipped `atdd_pure.yaml` diverges from repo SSOT silently | drift-vs-brief / SSOT-violation | **high** | 1/3 (sys F-1) | `src/des/runtime/tree_hash.py:61` (`rglob("*.py")` only); the gate-composition SSOT `nWave/flavors/atdd_pure.yaml` + `framework-catalog.yaml` not in hashed set; `brief.md:694-699` names it open | `F-SPINE-CONTROL-PLANE-SSOT` (SYS-4 config-asset envelope, deploy-copy facet) |
| **AD-30** | **Three parallel TDD/delivery-phase vocabularies**, none referencing each other: the canonical phase-set + legacy→canonical collapse rule is modelled 3× over overlapping-but-divergent vocabularies | SSOT-violation / duplication | **high** | 1/3 (ddd F-1) | `value_objects.py:50-121` (`PhaseName` enum + `to_canonical()`) · `tdd_schema.py:36-50` (`LEGACY/CANONICAL_PHASES` raw string tuples — NOT `PhaseName`) · `atdd_pure_phases.py:54-73,153-173` (`ATDDPurePhase` + own canon-collapse). All live (PhaseName 128 refs/14 files). Fix = fold `tdd_schema.CANONICAL_PHASES` onto `PhaseName` (consolidation, NOT deletion) | `F-ARCH-PHASE-VOCAB-SSOT` (R1 phase-identity; extends AD-04's disprove follow-on, distinct from field-typing) |
| **AD-32** | `PhaseEvent` carries raw `str` phase/status/outcome while typed VOs exist and are compared against it via accidental StrEnum equality — the **typed-accessor seam** AD-04's disprove already flagged (NOT AD-04 field-typing, which is correctly rejected) | drift-vs-ADR | medium | 1/3 (ddd F-3) | `phase_event.py:37-41` (raw str) vs `step_completion_validator.py:212-216,249,255` (`== PhaseStatus.EXECUTED` works only because `PhaseStatus(str,Enum)`). Fix = parse-once typed accessor at the validation seam, leave the lenient parser intact (AD-04 constraint) | folds into `F-ARCH-PHASE-VOCAB-SSOT` (accessor sub-facet) |
| **AD-34** | Upstream-wave reroute target modelled twice: `PhaseExit.REROUTE_{DISCUSS,DESIGN,DEVOPS}` enum members vs `Literal["DISCUSS","DESIGN","DEVOPS"]` union — a 2-representation of the same closed vocabulary | duplication | low | 1/3 (ddd F-5) | `atdd_pure_phases.py:76-90` (`PhaseExit.REROUTE_*`) vs `:475` (`SpecificationAmbiguityDetected.upstream_wave: Literal[...]`) | flag-only; folds into `F-ARCH-PHASE-VOCAB-SSOT` |

### Priority 2 — R4 git-port (2/3 convergence — solution + system lenses hit the same site)

| ID | Finding | Type | Severity | Conv. | Evidence | Proposed backlog |
|----|---------|------|----------|-------|----------|------------------|
| **AD-28** | Same-file git-access SSOT violation **with degrade-honesty divergence**: `slice_at_completeness.py` imports the AD-22 `git_text` seam at the top, then bypasses it three lines into a helper with a raw `subprocess.run(["git","cat-file","-e",...])` that **silently returns `False`** (→ "AT not tracked → report missing") instead of the loud INDETERMINATE the seam guarantees on git-absence | SSOT-violation / boundary-violation | **high** | **2/3** (sol MAJOR-1, sys F-2) | bypass at `src/des/application/slice_at_completeness.py:105-111` (imports `git_text as _git` at `:31`, uses it at `:73,92`, ignores it at `:105`); identical raw `cat-file -e` at `reverify_slice_commit.py:264-270`. Fix = add `git_path_exists(repo,rev,path)->bool` predicate to `git_subprocess.py`; collapse both call-sites onto it (restores degrade-LOUD) | `F-GATE-GIT-DEPENDENCY-REMOVAL` (AD-21; tightest mechanically-fixable instance) |
| **AD-28b** | The "consolidated" git seam `git_subprocess.git_text` is **a helper, not a port** — no ABC/`Protocol`, `check=True`-raises instead of returning typed INDETERMINATE; only `CommittedScopePort` wraps one seam. ~12 raw `["git",...]` call-sites remain in gate/wave CLIs (`commit_slice.py:152,179,203`; `verify_commit_trailers.py:135`; `verify_deliver_integrity.py:188`; `subagent_stop_handler.py:526`; `convert_to_atdd_pure.py:303`; …) | drift-vs-constraint | **high** | 1/3 (sys F-2) | `adapters/driven/git/git_subprocess.py:18-33` (helper, raises); STANDING "git is NOT a dependency" → a target machine without git raises (not INDETERMINATE) in E1-completeness, trailer-verify, G_COMMIT SHA-pin | `F-GATE-GIT-DEPENDENCY-REMOVAL` (the real swappable `Protocol`; AD-24 uniform-INDETERMINATE) |

### Priority 3 — boundary + naming (lower throughput-leverage)

| ID | Finding | Type | Severity | Conv. | Evidence | Proposed backlog |
|----|---------|------|----------|-------|----------|------------------|
| **AD-29** | **Two distinct `GateVerdict` enums under the identical class name** — a ubiquitous-language collision; both `str`-enums share `pass`/`fail` values so an accidental cross-import silently typechecks | SSOT-violation / bounded-context-drift | medium | 1/3 (ddd F-2) | `gate_outcome.py:17-31` (`PASS/FAIL/NOT_APPLICABLE/UNVERIFIED`, walking-skeleton gate) vs `environmental_e2e/stdout_token.py:19-27` (`PASS/FAIL/FLAKY/BROKEN/MISSCOPED/XPASS_STALE`, env-e2e gate); both live. Fix = rename to disambiguate (`WalkingSkeletonVerdict`/`EnvironmentalE2eVerdict`) or assess one closed superset | `F-ARCH-GATE-VERDICT-NAMING` (new root candidate; low ToC-leverage) |
| **AD-31** | Application services do direct `json`/`os`/`open` I/O instead of `FileSystemPort`/`ConfigPort` — brief names `StaleExecutionDetector` as an application component that "imports domain and ports only" | boundary-violation / drift-vs-brief | low | 1/3 (sol MINOR) | `stale_execution_detector.py:27-34,99-134`; `stale_resolver.py:28-32,74-97`; `boundary_rules_generator.py:8,116-120` vs `brief.md:45`. Pre-existing pattern (not a regression); testability debt | folds into `F-ARCH-BOUNDARY-ENFORCEMENT-GATE` (AD-01 import-linter contract: forbid `import json/os`/`open(` in `des.application.*`) |
| **AD-33** | `PhaseCReviewerVerdict` / `PhaseFReviewerVerdict` are a near-duplicate pair differing only in `verdict_hash` optionality | aggregate-design | low | 1/3 (ddd F-4) | `atdd_pure_phases.py:377-420` (C: hash optional `:389`; F: hash mandatory `:414`). Consider one `ReviewerVerdict` + phase discriminator + hash-presence invariant; conservatively flag-only (the optionality is a genuine C-may-halt-pre-verdict difference) | `F-ARCH-REVIEWER-VERDICT-SHAPE` (new root candidate; flag-only) |

### Cross-cutting verified-CLEAN (explicit negatives — inspected, NOT debt)

- **Domain-layer import purity holds** — zero `from des.adapters|application|cli` in `src/des/domain/` (sol). ADR-PLAT-006's `InvocationLimitsValidator` DIP concern is **resolved** (now in `application/` with `FileSystemPort` injection).
- **Mode-resolver (slice-03) genuinely consolidated** — `resolve_workflow_mode()` is the single SSOT; the surviving `init_log.resolve_dispatch_mode` delegates the absent-key default + adds only the explicit-classic advisory (NOT a twin resolver). No residue (sys).
- **Digest-timing producer fallback (AD-23) shipped** — `_warn_committed_scope_indeterminate` degrades LOUD, stamps no trailer; producer==verifier holds (sys).
- **Adjudicated CREATE_NEWs (NOT duplication)**: `decision_table_traceability_gate` vs `walking_skeleton_feature_end_gate`; `boundary_rules_generator` vs `_template`; `prompt_validator` vs `atdd_pure_prompt_validator` (divergent mandatory-section schemas); `GateTier` vs `TierCapability` (distinct axes, bridged) — all sound Reuse Analyses (sol, ddd).
- **No CLI-surface duplication** — `verify-slice-commit` / `reverify-slice-commit` (composes verify) / `check-slice-at-completeness` genuinely distinct; `des` dispatcher is registry-driven fan-out (sys).

> **Backlog linkage**: AD-26/27 → `F-SPINE-CONTROL-PLANE-SSOT` (R1). AD-30/32/34 → new `F-ARCH-PHASE-VOCAB-SSOT` (R1). AD-28/28b → `F-GATE-GIT-DEPENDENCY-REMOVAL` (R4). AD-29 → new `F-ARCH-GATE-VERDICT-NAMING`. AD-31 → `F-ARCH-BOUNDARY-ENFORCEMENT-GATE` (AD-01). AD-33 → new `F-ARCH-REVIEWER-VERDICT-SHAPE`. Open these in `docs/backlog.md` when scheduled; all are subordinate to R1 per the UNIFIED ToC (no re-rank — R1 remains the binding constraint).

### AD-35 — OSS wave-driving architecture (hook-driven, not sequencer-interpreted) is undocumented in ADR/brief

- **Type**: drift-vs-brief (ADR coverage gap) · **Severity**: low · **Surfaced**: 2026-06-02 (Ale Q + Lyra-OSS + sister Lyra-SF verify-from-source) · **ToC-root**: R7 (doc-SSOT realignment / DESIGN-owned back-propagation)
- **Finding**: OSS defines waves as `nWave/tasks/nw/*.md` slash-commands + `nw-{wave}` skills, DRIVEN by command-invocation + DES hooks (PreToolUse/SubagentStop TDD/phase enforcement) — **no sequencer, no YAML-workflow state-machine** (verified: no `src/des/data/workflows/*.yaml` in OSS). This is the OSS hook-only mandate (CLAUDE.md). The SF-tier `nwave-wave-customizer` plugin (`tier: enterprise`, `oss_floor: false`, extracted from nWave-software-factory per ADR-027 v2) owns the **Wave-lang YAML workflow catalogue** the SF Carrier/sequencer consumes — OSS does NOT use it; importing it would be an IP-blind SF-mental-model import (LYRA-DEV mandate forbids).
- **Gap**: this OSS-hook-vs-SF-sequencer wave-driving decision is a CLAUDE.md *mandate* but is NOT captured in any ADR nor the brief's wave section (the brief states WHERE the framework-definition lives — `nWave/` — but not the hook-vs-sequencer driving decision). The existing hook ADRs (ADR-002-des-hooks-in-plugin, ADR-005/PLAT-005-hook-adapter-decomposition, ADR-CA-001/CA-003) document hook *construction/install*, not the wave-driving architecture.
- **Fix direction (DESIGN-owned, NOT auto-written — flag only)**: author an ADR "OSS waves = hook-driven slash-commands + skills, not sequencer-interpreted YAML (IP separation vs the SF `nwave-wave-customizer` Carrier path)". `F-ADR-OSS-WAVE-DRIVING-ARCHITECTURE`.

---

## Audit 2026-06-02 (panel 2 — night-code) — 3-architect panel (HEAD `30167b3ec`, diff base `878c47db5`)

Lenses: `nw-solution-architect` (app), `nw-ddd-architect` (domain), `nw-system-designer` (system). Opus, READ-ONLY. Scoped to the NIGHT's NEW code (R1 slice-04/05, feature-end refactor, gate-honesty fixes, build_dist isolation, R2 emit CLI). Excluded already-catalogued AD-26..AD-35 + the `tree_hash` sort-key residue (disproved `5df0d8875`).

> **AD-27 CONFIRMED CLOSED** (system lens, verify-from-source): the SYS-4 config-asset freshness envelope is now produce-and-consume wired end-to-end (`tree_hash.canonical_config_assets_hash` → schema-v2 manifest `des_plugin.py:734` → `repo_source_probe._classify_config_assets` emitting `CONFIG_DRIFT`). slice-05 shipped it; AD-27 resolved.
> **Positive confirmations** (no debt): the feature-end `_degrade_loud`/`_persist_audit_record` consolidation (`5df0d8875`) is a CLEAN SSOT-win, not a near-dup (ddd); the gate catalog/registry 1:1 mirror (21==21) is clean (app+system); `emit-feature-end` is git-free + does not duplicate `commit-slice`/`verify-slice-commit` (all 3 lenses); hexagonal boundaries in the new CLIs hold (app); the `des_plugin` bootstrap-parity copy is acknowledged + parity-locked (system).

| ID | Finding | Type | Severity | Conv. | Evidence | ToC-root / backlog |
|----|---------|------|----------|-------|----------|--------------------|
| **AD-36** | Feature-end-required-records now **QUADRUPLICATED** with two representations inside ONE gate invocation: `_handle_feature_end_gate` runs copy #2 (YAML via `_feature_end_required_records()`) AND shells out to `verify_deliver_integrity` (copy #3 hardcoded) on the same return. slice-04 added the YAML-read seam but left copy #3 (`verify_deliver_integrity.py:444`) + copy #4 (the `_REQUIRED_FEATURE_END_RECORDS` frozenset `:821`, which `test_required_record_writer_registry.py` + `feature_end_seeding.py` pin to). Adding a 7th YAML record silently diverges the running gate. **Concretely worsens AD-26.** | SSOT-violation / duplication | **MAJOR** | 1/3 (sol) | `subagent_stop_handler.py:907`+`:985` (dual-run), `verify_deliver_integrity.py:444-451`, `:821` frozenset | **R1** — `F-SPINE-CONTROL-PLANE-SSOT` fast-follow (route copy #3+#4 through `_feature_end_required_records()`; frozenset = absent-flavor fallback INSIDE that fn only, not a pinned module SSOT) |
| **AD-37** | `commit-slice` **bypasses its own git single-seam** — raw `subprocess.run(["git", ...])` for `diff --cached`/`commit`/`commit --amend` while importing `git_run` only for `git add`. The module docstring CLAIMS git is confined to the `git_mutate`/`git_subprocess` seams; the grep-for-git invariant (AD-21) is already FALSE on the newest git call-site. | boundary-violation / drift-vs-ADR | **HIGH** | 1/3 (sys) | `commit_slice.py:151` (diff), `:178-185` (commit), `:202-209` (amend) vs `git_mutate.py:5-8` seam + `:61` import | **R4** — `F-GATE-GIT-DEPENDENCY-REMOVAL` (route the 3 mutations through `git_run`; the seam already exists — mechanical) |
| **AD-38** | `commit-slice` mixes the read-seam (`git_text`) with raw `subprocess` in the SAME lifecycle fn — `_amend_trailer` reads via `git_text` (`:197`) but mutates via raw subprocess (`:202`). A reader auditing the git-seam imports concludes commit/amend are seam-routed; they are not. | drift-vs-ADR | medium | 1/3 (sys) | `commit_slice.py:197`/`:277` (read seam) vs `:202` (raw mutate) | **R4** (folds into AD-37) |
| **AD-39** | State `D` (installed-tree drift, REFUSE-on-CLI) and `STALE` (repo-source drift, degrade-loud) **flatten to ONE telemetry name** on the hook path — both route through `_warn_stale` → `des.runtime.freshness.stale` + `HEALTH_GATE_INSTALL_FRESHNESS_STALE`. KPI-1 cannot distinguish "operator hand-edited the installed spine" from "repo moved on after install". Distinct truth-table states, non-distinguishable observable (mild AD-30-class). | SSOT-violation (observable vocab) | medium | 1/3 (ddd) | `freshness.py:246-252` (D+DEGRADED→_warn_stale), `:112`/`:123` callers; `repo_source_probe.py:143`(D)/`:152`(STALE) | **R1**-adjacent (freshness-state observable vocabulary) |
| **AD-40** | The **anti-theater invariant** (`FeatureEndReviewVerdict` REQUIRES a signed `--verdict-hash`; `EBatchRefactorCompleted` must NOT carry one) lives ONLY in the `emit-feature-end` CLI shell (`:114-138`), NOT in the writer `append_feature_end_event` (which accepts `verdict_hash=None` for either event). The **hook-side emitter** (`subagent_stop_handler.py`) writes the same records via the same writer and is NOT subject to the CLI check → the invariant is bypassable. | effect-isolation / contract-shape | minor | 1/3 (sol) | `emit_feature_end.py:114-138` (sole authority) vs `at_completion_ledger.py:318-341` (no pairing enforcement) | **R1**-adjacent — push the pairing invariant down to `append_feature_end_event` (hardening of the R2 emit CLI; both CLI + hook inherit) |
| **AD-41** | The 7-member freshness state vocabulary (`A/B/C/D/STALE/CONFIG_DRIFT/DEGRADED`) is **undocumented** in the brief Domain Model (which documents only gate-composition + mode-resolution contexts) and there is NO `docs/product/glossary.md` at all. Third verdict-language family with no canonical written home (alongside the two `GateVerdict` enums AD-29). | drift-vs-brief (vocab SSOT gap) | low | 1/3 (ddd) | `freshness_port.py:22-27` vs `brief.md` Domain Model §768+ | **R7** — fold into the proposed `F-VERDICT-VOCAB-GLOSSARY` (with AD-29) |
| **AD-42** | The F21/RCA-#68 **CLI-subprocess seed-marker gap is a forward-looking class with no mechanical guard** — `seed_dev_checkout_marker` is applied in only ~5 test files; ≥1 unmarked file (`fix_gcommit_exit_gate_scoping/.../composition.py:240` subprocess-invokes `des.cli.run_contract_gate`) passes ONLY under `.env NWAVE_FRESHNESS=skip` (the masked-green the env-parity contract forbids). The next such test will mask-green again (recurred THIS session on the R2 slice-01 composition). | test-infra gap | low | 1/3 (sys) | `tests/env_parity.py:43` (5 callers), `fix_gcommit_exit_gate_scoping/steps/composition.py:240-251` | **R3**-adjacent — `F-CLI-SUBPROCESS-ENVPARITY-AST-GUARD` (an AST guard over `tests/**/composition.py` subprocess-of-`des.cli` sites mandating seed-marker-or-explicit-NWAVE_FRESHNESS) |

> **Backlog linkage**: AD-36 → `F-SPINE-CONTROL-PLANE-SSOT` (R1, AD-26 worsening). AD-37/38 → `F-GATE-GIT-DEPENDENCY-REMOVAL` (R4, NEW seam regression in `commit_slice.py`). AD-39 → R1 freshness-vocab. AD-40 → R1 (R2 emit-CLI hardening — push invariant to writer). AD-41 → new `F-VERDICT-VOCAB-GLOSSARY` (R7, with AD-29). AD-42 → new `F-CLI-SUBPROCESS-ENVPARITY-AST-GUARD` (R3). **No re-rank** — all subordinate to R1/R2 (the binding constraints); AD-37/38 are the highest-leverage mechanical fixes (the seam exists). AD-27 CLOSED.

---

## Audit 2026-06-02 (panel 3 — brief/ADR/SSOT + dedup) — 3-architect panel (HEAD `c5c45b40b`)

Distinct scope from panels 1/2 (no new production code since panel-2 — last 6 commits doc-only): **brief↔code coherence · ADR-corpus SSOT integrity · cross-cutting duplication**. Read-only, verify-from-source, texas-sharpshooter guard (AD-01..AD-42 confirmed-and-skipped, not re-flagged). 3 spot-checks re-verified by the orchestrator from source (ADR-001 divergence, PLAT-004 byte-identity, glossary absence). New ids AD-43..AD-52.

**Brief ↔ shipped-code drift (R7 doc-SSOT, cheap near-zero-cost realignment):**
- **AD-43 [R7 HIGH]** `brief.md:606,618` marks **Auto-Update Infrastructure "Status: Design (option pending)"** but it is fully BUILT + wired: `src/des/application/pending_update_service.py` consumed at composition root `session_start_handler.py:64,79`, port+adapters present, **14 test files** import it (incl. acceptance walking-skeleton); ADR-001-package-manager Accepted. Stale-DESIGN misdirects DISTILL/DELIVER into re-authoring shipped work. Owner: DESIGN back-prop; bound: flip→DELIVERED, ~15min.
- **AD-44 [R7 MED]** `brief.md:637` marks **Codex Empirical E2E "Status: DESIGN (2026-05-13)"** but FM-1/FM-2/FM-3 shipped (`codex_des_plugin.py:87-111`) + real-boot e2e green (`tests/e2e/test_codex_real_boot.py:133`). Flip→DELIVERED.
- **AD-45 [R1-adj/R7 HIGH]** `brief.md:965` C4 documents `CommittedScopePort.probe()` ("probe() exercises git-absent lie", an Earned-Trust seam) that **does NOT exist**: port ABC `committed_scope_port.py:53-64` declares only `committed_contract_files`, adapter `committed_scope_adapter.py:71-79` shells raw `git ls-tree` and RAISES (not LOUD-Indeterminate). Brief promises a Principle-13 seam the code lacks → crafter/AD builds against a phantom `probe()`. Owner: DESIGN — drop the claim OR open `F-COMMITTED-SCOPE-PORT-PROBE`; ~20min.

**ADR-corpus SSOT integrity (R7, AD-20 sub-findings — Ale id-authority):**
- **AD-46 [R7 HIGH]** `ADR-001` and `ADR-002` are **DIVERGENT id-collisions** (verified): `docs/product/architecture/ADR-001-rigor-as-config-concern.md` vs `docs/adrs/ADR-001-plugin-marketplace-distribution.md` (zero overlap); `ADR-002-tdd-phase-enforcement` vs `ADR-002-des-hooks-in-plugin`. The join-key is genuinely BROKEN (two real decisions per id) — "see ADR-001" is unresolvable. **reconcile-needed, NEVER auto-merge** — needs id-reassignment (Ale authority per CLAUDE.md "ADR numbers globally unique"). Highest-leverage doc-SSOT fix.
- **AD-47 [R7 MED]** `ADR-PLAT-004/005/006` (canonical) are **byte-identical twins** of `docs/adrs/ADR-004/005/006` (PLAT-004 verified `diff -q` = ∅). safe-delete-candidate sub-class of AD-20: delete the `docs/adrs/` numeric twins AFTER Ale ratifies PLAT-* as survivor. Flag, do NOT delete.
- **AD-48 [R7 MED]** `docs/product/glossary.md` **ABSENT** (verified) — the vocabulary SSOT referenced by CLAUDE.md + injected by `load_project_context.py` does not exist. No project-wide ubiquitous-language SSOT to drift against; the finding IS the missing SSOT. Folds with AD-29/41. Owner: ddd/PO; bound: create the file OR retract the SSOT reference (decision).
- **AD-49 [R7 MED]** Status-drift: `ADR-PLAT-004:5` says `Proposed` but `prompt_rendering_service.py`+`step_execution_service.py` shipped; `ADR-PLAT-006:5` `Proposed` but partial-ship (Fix-2 singletons removed `2b92f5716`/AD-16; Fix-3 enums DISPROVED per AD-04 — single Status line can't express per-fix divergence). Flip survivors only (couples to AD-47 delete).

**Cross-cutting duplication (NEW multi-representation instances):**
- **AD-50 [R1 HIGH]** `_LEGACY_ONLY_PHASES = frozenset({"PREPARE","RED_ACCEPTANCE","RED_UNIT"})` byte-identical in `domain/step_completion_validator.py:71-73` AND `application/validator.py:292-294` + the legacy-detection prose duplicated. Distinct from AD-30 (raw-tuple-vs-enum) — the legacy-DETECTION subset in 2 NEW homes. Fold to `domain/tdd_schema.py` SSOT, both import. Ship with AD-30 (`F-ARCH-PHASE-VOCAB-SSOT`).
- **AD-51 [R2 HIGH — HIGHEST-LEVERAGE]** feature-end event-NAME constants re-hardcoded as raw literals despite the SSOT: `at_completion_ledger.py:79-89` defines `EBATCH_REFACTOR_COMPLETED`/`FEATURE_END_REVIEW_VERDICT`/… (re-exported via port `:38`); `emit_feature_end.py:53-54` correctly imports them, but `verify_deliver_integrity.py:444-451` AND `subagent_stop_handler.py:825-828` **re-type the same strings as bare literals**. Failure mode = worst class: rename a constant → the done-gate (`verify_deliver_integrity`) requires a record the ledger never writes → silent permanent `FeatureEndCycleIncomplete` on the very gate R1's formal close chains through. Sharpens AD-26/36: the fix is "consume the NAME constants" (2 imports), not only "route through `_feature_end_required_records()`". Sits on R2's hot path, mechanically available today.
- **AD-52 [R8 MED]** skip-reason vocabulary triplicated AND already-divergent: `domain/phase_state_validator.py:27-32` (`…AWAITING_CLARIFICATION`) vs `application/validator.py:87-91` (`…APPROVED_SKIP`) — share 3, each a unique 4th → a reason valid in one validator is unknown to the other. Reconcile 4th member with PO, single domain frozenset. Legacy-classic path → low leverage.

**Verified-CLEAN (explicit negatives — inspected, NOT debt):**
- `nWave/gates/_catalog.yaml` (21 gates) ↔ `cli/__main__.py:_REGISTRY` (21 rows) **1:1 mirror intact** (gate_id↔name, module↔module_path all match) — the AD-26 slice-04 lesson holds.
- `ADR-CP-001/002` ARE in the brief registry (`:223,224`) AND Accepted (`ADR-CP-001:5` "Accepted 2026-06-01") — no missing-registry defect there.
- `MANDATORY_SECTIONS` divergence (classic `validator.py:67-77` vs `atdd_pure_prompt_validator.py:36-44`) is the deliberate CREATE_NEW adjudicated verified-clean at panel-2 — NOT a merge target (the only residue is the stale 5-phase recovery string `validator.py:84`, an AD-12 live-code twin, trivial).
- No `Superseded`/`Deprecated` ADR presented as live in the canonical folder; all 16 canonical `ADR-*.md` carry a `## Status` line.

> **Backlog linkage (panel 3)**: AD-43/44/45 → `F-ADR-SSOT-COHERENCE` (R7, brief↔code marker+claim drift). AD-46/47/49 → `F-ADR-SSOT-COHERENCE` + AD-20 (R7, id-collision reconcile + twin safe-delete + status-flip — **Ale id-reassignment authority required**). AD-48 → `F-VERDICT-VOCAB-GLOSSARY` (R7, with AD-29/41 — the glossary SSOT is absent, not just undocumented). AD-50 → `F-ARCH-PHASE-VOCAB-SSOT` (R1, fold with AD-30). **AD-51 → `F-ATDD-PURE-FEATURE-END-CYCLE-UNWIRED` (R2, ToC #1)** — sharpens AD-26/36, the one panel-3 finding on the binding-constraint hot path. AD-52 → R8 legacy-classic. **No re-rank** — AD-51 reinforces R2 (already ToC #1); the rest are R7 cheap doc-SSOT realignments subordinate to R1/R2. **HIGHEST-LEVERAGE: AD-51** (2 imports remove a silent done-gate false-negative landmine on R2); **highest doc-SSOT: AD-46** (broken ADR join-key, Ale authority).

---

## 🧪 TEST-DEBT LEDGER

Incremental test-corpus quality audit (ATD-led), one `tests/des/` slice per fire, rotating by risk order. 8-dimension taxonomy, verify-from-source (file:line), conservative. Manifest: `docs/analysis/test-debt/coverage-manifest.md`. Read-only audits — APPLY of any consolidation is a separate gated fire.

### Fire-1 2026-06-02 — `acceptance/des_spine_control_plane_ssot` (R1 control-plane, 5 slices / 13 scenarios / 16 ATs)

Dual-auditor (nw-acceptance-designer dims 1/2/5/6/7 · nw-test-optimizer dims 3/4/8), HEAD `e37cbd77f`. **Verdict: high-quality suite — behaviour/coverage/domain-lang/documents/coupling ALL CLEAN; debt is test-harness DRY+speed only.**

**CLEAN (explicit negatives — inspected, NOT debt):**
- **dim1 BEHAVIOUR**: zero `@skip`/`@xfail`; every state-mutating Then routes through `assert_state_delta(before, after, universe, expected)` on port-exposed universes; exact-event / exact-set assertions (AT-03 slice-04 pins the 6-element `PRODUCTION_REQUIRED_RECORDS`, `domain_types_slice_04.py:75-84`) → divergence-pair flips RED on SUT break. No theater.
- **dim2 COVERAGE**: all 16 ATs drive a real composition-root driving port at Layer-3 subprocess (hook-import / `des run-contract-gate` / `des verify-integrity`+`init-log` / `subagent-stop` real stdin). No unit-shaped AT bypasses the entrypoint. `@real-io` honest.
- **dim5/6 DOMAIN+DOCS**: Gherkin is operator/maintainer ubiquitous language; structured event names appear as backtick contract tokens inside business sentences, HOW lives in composition docstrings.
- **dim7 COUPLING (Mandate-13)**: ZERO direct production-domain imports in any step/composition (only the subprocess module-path STRING); canonical hasher deliberately re-implemented in-harness to avoid a direct-domain import; no AST-shape/private-attr assertions.
- **dim8 FLAKY**: no `sleep`, no unseeded randomness, all tmp_path/function-scoped, glob results `sorted()` (FS-order safeguard), `installed_at_iso` hardcoded. Parallel-safe under `-n auto --dist worksteal`.
- **POSITIVE keystone**: slice-01/05 hook subprocesses build env from a **fresh dict** (`conftest.py:377`, `composition_slice_05.py:415`), NOT `dict(os.environ)` → repo `.env NWAVE_FRESHNESS=skip` does NOT mask the slice-under-test's own freshness gate. Env-parity-by-construction — directly defends the RCA-#68 trap. Preserve under any refactor.

**FINDINGS (test-harness scope, tests-only, coverage-preserving):**
- **TD-1 [SEV hi · dim3 speed]** `conftest.py:159-199` `_copytree_with_rewrite` does a full `copytree`+3×`re.sub` over the **entire 261-file / 4.2 MB `src/des` tree**, function-scoped, **once per scenario** → slice-01 5× + slice-05 3× = **8 full-tree rglob+regex passes**. Dominant wall-clock cost of the slice. The rewritten `des/` tree is read-only across scenarios (per-scenario variation is only the cheap manifest + source-tree + `.git/` adjacency). Fix: session-scoped golden-tree fixture built ONCE → 8→1.
- **TD-2 [SEV hi · dim4 DRY]** ~80 lines of harness logic duplicated verbatim between `conftest.py` and `composition_slice_05.py`: `_parse_structured_event_line` (`05:83-101`≡`cf:69-87`), `_read_audit_records` (`05:104-121`≡`cf:90-119`), `_copytree_with_rewrite` (`05:181-207`≡`cf:167-199`), `_canonical_py_hash` (`05:254-269`≡`cf:270-289`), `build_checkout` triplicated (`cf:328-339`/`05:373-384`). slice-05 is largely copy-paste of slice-01's freshness fixture. Fix: extract `steps/freshness_harness.py`. **NB coherence point**: this harness copy-paste is a faint echo of the very SSOT-multi-representation disease this feature exists to kill — test-scope, not a production regression.
- **TD-3 [SEV med · dim4]** duplicated step bodies + `VERDICT_UNIVERSE`/`_verdict_snapshot` between `steps_slice_05_config_drift.py` and `steps_slice_01_hook_freshness.py` (pytest-bdd per-module binding is intentional; the BODIES fold into the harness helper).
- **TD-4 [SEV lo · dim4]** `ConfigHookOutcome` (`domain_types_slice_05.py:103-118`) ≡ `HookInvocationOutcome` (`domain_types.py:130-145`) — identical dataclass, should be re-exported not re-declared.
- **NO parametrize-inflation** (texas-sharpshooter guard applied: 13 scenarios are distinct topologies/sinks — e.g. slice-01 AT-01/AT-05 share topology but assert different sinks stderr-event vs persisted-audit-record — NOT input-variants of one assertion).

**Highest-leverage consolidation (ONE, gated APPLY):** extract `steps/freshness_harness.py` housing the shared pure-functions + a **session-scoped golden-rewritten-`des/`-tree fixture** (TD-1 + TD-2 collapse into one move) → largest wall-clock win (8→1 full-tree copy) + ~80-line dedup, zero scenario-count change, zero behavior loss.

**Owned residue**: `F-TESTDEBT-SPINE-CONTROL-PLANE-HARNESS-DEDUP` — owner: a gated test-optimizer APPLY fire (NOT this read-only fire); bound: extract `freshness_harness.py` + session-scoped golden-tree fixture, coverage-preserving, `tests/` only, whole-tree gate stays 0-red. Linked from `docs/backlog.md`.

**ToC placement**: **R8** (AT-corpus discipline default — post-ship test-debt on a just-landed feature, masks no higher root). **Cross-coupling noted**: TD-1's per-scenario 4.2 MB copytree runs inside the whole-tree contract gate the spine fires **every commit** → it is a NEW contributor to the feedback-loop latency tax (`F-SPINE-FEEDBACK-LOOP-TOO-SLOW`, parallelised `853202e33` but per-suite copy cost is additive) that throttles every root's throughput. So R8-home, with an explicit D1/feedback-latency cross-note — not a higher-root mask.

### Fire-2 2026-06-03 — `acceptance/atdd_pure_spine_hardening` (R1 spine-hardening, 4 .feature / ~32 scenarios)

Dual-auditor (nw-acceptance-designer dims 1/2/5/6/7 · nw-test-optimizer dims 3/4/8), HEAD `3236920b3`, ground-truth `32 passed / 0 skipped / 0 xfailed`. **Verdict: high-quality suite — behaviour/domain-lang/documents/flakiness ALL CLEAN; 2 findings (one MASKS an R1 surface, one R8 harness-dedup).** fire-1 TD-1/TD-2 confirmed-and-skipped (this slice does NOT reuse the copytree/freshness conftest — local `sys.path.insert` runner, no copytree).

**CLEAN (explicit negatives):**
- **dim1 BEHAVIOUR**: every AT a real falsifiable oracle — ledger corruption injectors physically mutate the JSONL (`composition.py:98-113`: malformed/truncation/hash-tamper/seq-gap), slice-02 INCOMPLETE-shape is a true E1 defect-injection (No-Fixture-Theater respected), slice-01 WS drives real `handle_pre_tool_use` over stdin/stdout JSON. No skip/xfail (the RED-scaffold xfail was removed at GREEN, `conftest.py:6-8`).
- **dim5/6 DOMAIN+DOCS**: Gherkin in ubiquitous atdd_pure-spine language ("fails closed on corruption", "blocked dispatch never silent fall-through"); event names in Thens are the production contract vocabulary, not impl jargon.
- **dim8 FLAKY**: no sleep, no unseeded randomness, timestamps hardcoded literals (not `datetime.now()`), all real-git/subprocess rooted at `tmp_path`, ledger seq/hash content-derived, `frozenset` membership (order-irrelevant). Parallel-safe under `-n auto` (cwd used only for `src/` path resolution, no `chdir`/shared path).
- **PBT inflation**: ZERO — the skew/corruption/recognition Scenario Outlines are correctly-collapsed finite-domain decision tables (parametrize, not PBT), not N-tests-per-row inflation.

**FINDINGS:**
- **TD-5 [SEV med · dim2+dim7 · MASKS R1]** the 5-row D6 skew Scenario Outline (`feature-end-version-skew.feature:55-67`) drives the **private** `_classify_hook_version_skew(installed, checkout)` directly (`slice04_composition.py:269-273`) and asserts its bare return string — bypassing the real driving surface `handle_session_start → _emit_hook_version_skew_finding` (`session_start_handler.py:234-258`) which emits a `HookVersionSkew` `{"additionalContext":…}` JSON finding on stdout, atdd_pure-cwd-scoped, fail-open. The classification *table* is covered; the operator-observable *emission contract* (finding-shape + cwd-scoping gate + fail-open swallow) is NOT — a refactor of the finding envelope or cwd-scoping ships GREEN here. Dual defect: coverage-gap (dim2) + Mandate-13 private-import coupling (dim7), same root, one fix. Owner: gated DISTILL+crafter; bound: add 1 `@wiring_e2e` AT driving `handle_session_start` over JSON stdin asserting the emitted skew-finding shape + one classic-cwd no-emit row, then delete the private-import line. Residue `F-TESTDEBT-SKEW-AT-DRIVING-PORT-GAP`.
- **TD-6 [SEV med · dim3+dim4 · R8]** SubagentStop subprocess-harness duplicated byte-identical across two composition roots: `_git` (`slice02_composition.py:44-48` ≡ `slice04_composition.py:52-56`), `_subprocess_env` (`:260-265`≡`:277-282`), the `_interpret` stdout-block-parse loop (`:223-242`≡`:240-260`), and the `init_repo` 5-git-spawn bootstrap repeated across all 11 subprocess scenarios. ~40 lines dup + process-spawn wall-clock (git-setup dominates: ~10-12 spawns/scenario). Fix: extract a shared `steps/_subagent_stop_harness.py` (`git_repo_init`/`subprocess_env`/`run_subagent_stop`/`interpret`) + a session-scoped template-repo (rides on the same extraction → removes ~5 git spawns × 11 scenarios). Test-infra (§7) → owner gated platform-architect/crafter APPLY; bound: tests-only, coverage-preserving (NO scenario deletion — each pins a distinct fail-closed behavior E1/E2/M1/M6/M7/M8/M11/D6). Residue `F-TESTDEBT-SUBAGENT-STOP-HARNESS-DEDUP`.

**Highest-leverage:** TD-5 — one new `@wiring_e2e` AT closes the coverage-gap AND the Mandate-13 coupling at once, on an R1 spine session-start surface (the skew emission contract is currently unguarded).

**ToC placement**: **TD-5 → R1** (masks a spine SessionStart emission surface — ranks UP per the test-debt ToC rule, a green-but-bypassing AT that hides a spine surface is NOT R8-default). **TD-6 → R8** (fixture-cost/dedup, test-infra; same feedback-latency cross-note class as TD-1). Both gated-APPLY, tests-only, no production code.

### Fire-3 2026-06-03 — `acceptance/classic_spine_decommission` (R8 migration safety-net, 15 .feature / 46 scenarios — largest slice audited)

Dual-auditor (nw-acceptance-designer dims 1/2/5/6/7 · nw-test-optimizer dims 3/4/8), HEAD `95b744812`, ground-truth `46 passed / 0 skipped / 0 xfailed`. **Verdict: behaviourally HIGH-QUALITY (dims 1/2/5/6/7 CLEAN, divergence-pair EMPIRICALLY confirmed — disabling `_archive_roadmap` `conversion_planner.py:338` flips slice-05 RED) — but the suite's size surfaces real harness-DRY + 2 flakiness vectors.** fire-1/2 TD-1/2/5/6 confirmed-and-skipped (slice has its own slice-local compositions, no shared harness reuse).

**CLEAN (explicit negatives):**
- **dim1 BEHAVIOUR**: real-I/O conversion/rollback/reverify on real files; M2 SHA-reverify arms a REAL git commit graph (`composition.py:809-832`, GitHistoryProbe reads real reachability, never a fixtured verdict); F-13 slice-02 real `at_review_verdict` CLI + real ledger write the SAME file (M4 no fixture-uniform ledger). `_RED_SCAFFOLD_SLICES = frozenset()` — no @skip/@xfail theater.
- **dim2 COVERAGE**: correct L5/L4/L3 mix declared per-slice, every When drives a real subprocess/composition root. One LOW informational gap: slice-13/14 deprecation resolver driven via in-process composition root (valid Mandate-13), no `des`-subprocess-level AT for the mode-resolver-as-dispatched.
- **dim5/6 DOMAIN+DOCS**: migration ubiquitous language, `@contract-shape:` tags on every scenario, scenario names document user-valued outcomes.
- **dim7 COUPLING**: Mandate-13 compliant — zero direct-domain imports for behavioral coverage (ledger consumed as observable substrate; `resolve_dispatch_mode` IS the driving port), no AST-shape/private-attr.

**FINDINGS:**
- **TD-7 ✅ DRAINED (2026-06-07, test-consolidation loop)** — deleted dead RED-scaffold orphan `FeatureScanComposition.advance_feature_after_scan` (the `raise AssertionError("RED scaffold")` stub) + its never-bound step `given_feature_advances` (Gherkin phrase "gains a new commit after the scan" present in ZERO `.feature` files). HARD GATE verified-from-source: 46 passed before==after (zero scenario delta), src/des coverage byte-identical (1120/16870 covered), tests-only diff (11 del, 0 src/), ruff clean, order-independent. `F-TESTDEBT-CLASSIC-DEAD-SCAFFOLD` CLOSED.
- **TD-8 ✅ DRAINED (2026-06-05, test-consolidation loop)** — extracted `steps/_git_workspace.py` (`run_git`/`git_init_with_identity`/`subprocess_env` + module-level `_REPO_ROOT`); composition.py net −44 (66 del / 22 ins). HARD GATE verified-from-source: 46 passed before==after (no scenario deleted), coverage byte-identical (11653/10542/3122/34/8%), tests-only diff (zero src/), ruff clean, order-independent. `[SEV hi · dim3+dim4 · R8]` git-workspace harness duplicated across 3 composition roots (`_git()` `:263≡758≡1374`, git-init bootstrap ×3, `_repo_root`/`parents[5]` ×6, `_subprocess_env` ×7) now single-sourced. `F-TESTDEBT-CLASSIC-GIT-WORKSPACE-DRY` CLOSED.
- **TD-9 [SEV hi · dim8 · 2 flakiness vectors]** (a) `composition.py:1069-1074,1270-1279` slice-09 `chmod(0o555)` read-only feature dir with INLINE manual restore inside `run_convert` — if a scenario throws between make-read-only and restore, `tmp_path` teardown leaks a non-writable dir (order/timing flake). Fix: move restore to a `yield`-fixture finalizer. (b) **`composition.py:1533-1556` slice-14 reads the LIVE repo tree** (`parents[5]/docs/guides/classic-spine-migration.md` + repo_root artifacts OUTSIDE `tmp_path`) — shared mutable on-disk state under `-n auto --dist worksteal`, env/order-coupled (passes only in a checkout where the migration note shipped). **HALT-for-review (TEST-CHANGE rule)**: slice-14 may be a legit "is the customer migration note SHIPPED" release contract mis-placed in an acceptance AT (→ relocate to a build/docs contract test) rather than a flake (→ fixtured copy) — needs acceptance-designer/Ale ratification before touching, do NOT auto-collapse. Residue `F-TESTDEBT-CLASSIC-FLAKE-VECTORS` (a=gated fixture-finalizer fix; b=HALT-for-Ale).

**Highest-leverage:** TD-8 git-workspace DRY — ~60-80 LOC collapse in the suite's fattest file, zero behavior-count change, also trims subprocess setup. TD-9(b) is the one needing **Ale ratification** (release-contract-vs-flake fork).

**ToC placement**: all **R8** (classic→atdd_pure migration safety-net — downstream/independent of the active R1/R2 constraint; it consumes the mode-resolver but does not gate it). **Cross-coupling noted**: TD-9 flakiness in a suite that runs inside the every-commit whole-tree gate is a latent **R3 gate-determinism** risk (a flaky AT makes the gate verdict non-deterministic); and the slice's git-subprocess density (~25 git-using scenarios × 4-8 calls) couples its reliability to the `gc.auto=0` infra mitigation (earlyoom/.git-corruption under `-n auto`, MEMORY anchor) — infra-scope, already mitigated. All gated-APPLY/HALT, tests-only.

### Fire-4 2026-06-03 — `acceptance/d4_phase_3_flavor_dispatcher` (R1 gate-composition, 5 .feature / 16 scenarios)

Dual-auditor (nw-acceptance-designer dims 1/2/5/6/7 · nw-test-optimizer dims 3/4/8), HEAD `8930106ae`. **Verdict: mostly high-quality (domain/docs/flaky CLEAN, multi-gate-wire routing REAL — divergence holds) — but FIRST fire to find actual theater: one vacuous constant-oracle on an unbounded-preservation contract, plus a hook-boundary coverage gap. This slice IS the R1 gate-composition surface (slice-04/05 landed `F-OSS-GATE-COMPOSITION-UNIFY` here) — so its findings rank R1.** fires 1-3 TD-1/2/6/8 confirmed-and-skipped (slice reuses `tests/env_parity.py` seed-marker = TD-2 class, no copytree/git-workspace dup).

**CLEAN (explicit negatives):**
- **dim1 (mostly)**: slice-05 multi-gate-wire asserts the REAL dispatcher routes via real YAML composition + real `_gate_invoker_for` registry (`carpaccio_intercept.py:440-449`), NOT a mocked routing table — break the YAML order → invocation-log flips RED. slice-01/02/04 read real `CompositionResult`/`InterceptDecision`/on-disk JSONL. The `emit_raised()` hardcoded-`False` (conftest:769) is redundant-but-harmless (fail-OPEN pinned by real stderr read), not the oracle.
- **dim5/6 DOMAIN+DOCS**: business/operator ubiquitous language, no impl-jargon leak into `.feature`, scenario names document WHAT.
- **dim7 COUPLING**: zero `from des.{domain,application,adapters}` in any step module; conftest imports only public driving-port + contract types; no AST-shape/private-attr assertions. (LOW advisory: conftest:982-985 monkeypatches the private `_FLAVORS_DIR` constant — justified, no public seam; a public `flavors_dir` kwarg on `evaluate_atdd_pure_dispatch` would remove the coupling.)
- **dim8 FLAKY**: no sleep/PBT/unseeded-random, `tmp_path`-isolated, slice-05 monkeypatch function-scoped, `datetime.now` read into a never-asserted timestamp field. Parallel-safe.

**FINDINGS:**
- **TD-10 ✅ DRAINED (2026-06-06, test-consolidation loop)** — replaced the vacuous oracle with a real before/after filesystem-snapshot delta over the `repo_root` workspace the composition owns: `conftest.py` `_snapshot_workspace_tree(root)` (sorted path→fingerprint, files sha256 CONTENT-hashed → catches add/delete/rename/MODIFY) captured pre+post the gate `verify()` subprocess, surfaced via `verify_was_filesystem_preserving()`; `steps_readiness_gate.py:106` asserts it. HARD GATE verified-from-source: 16 passed before==after, coverage IDENTICAL (1540 lines / 130 branches), tests-only diff (2 files, zero `src/`), falsifiability empirically proven (write during `verify()` → RED), Mandate-14 closed-world (bounded `repo_root`), no swallowing-except. nw-acceptance-designer-reviewer CLEAN on all 6 points. `F-TESTDEBT-READINESS-PRESERVATION-ORACLE` CLOSED. [original finding ↓] `steps_readiness_gate.py:106-117` `then_filesystem_unchanged` was a VACUOUS constant-oracle: the `.feature` (`slice-03-d1-readiness-gate.feature:23`) declares `@contract-shape:unbounded-preservation` ("the system filesystem is unchanged"), but the step body merely re-asserts `last_verdict() is ReadinessVerdict.REFUSED` (already pinned by the preceding `then_verdict_refuses`) — it pins NOTHING about filesystem state. Its own docstring admits "structural placeholder ... any refused verdict implies no write" (FALSE — a gate can refuse AND write a log). Breaking the SUT to write during `verify()` would NOT flip this RED → theater on the slice's strongest contract claim (the readiness gate's read-only guarantee, friction #57). Owner: gated DISTILL/crafter (slice-03); bound: replace with a real before/after filesystem-snapshot delta over the `repo_root` universe the composition already owns. Residue `F-TESTDEBT-READINESS-PRESERVATION-ORACLE`.
- **TD-11 [SEV med · dim2 · R1]** coverage gap: NO AT drives the multi-gate wire through the real hook boundary `handle_pre_tool_use` (`carpaccio_intercept.py:632-648`, the actual Claude-Code hook entry that wraps `evaluate_atdd_pure_dispatch` in try/except → `AtddPureHookInternalError`). slice-02/05 drive `evaluate_atdd_pure_dispatch` directly (the layer BELOW the hook) — the hook wrapper could mis-marshal a block decision into the hook protocol (TBU-class, undetected). Owner: gated DISTILL; bound: add ≥1 `@walking_skeleton @driving_adapter` scenario invoking `handle_pre_tool_use` with a hook-JSON payload for the block path. Residue `F-TESTDEBT-DISPATCHER-HOOK-BOUNDARY-AT`.
- **TD-12 [SEV med · dim4 · R8 · cheapest mechanical] — RESOLVED `6d7188d10` (2026-06-06)** `steps_dispatcher.py:99-111` — 3 `@then` bindings (`then_completes_with_n_results`/`then_completed_with_n_results`/`then_recorded_n_results`) had BYTE-IDENTICAL bodies differing only in Gherkin tense. Collapsed to 1 via `parsers.re(...)` alternation (`completes with|completed with|recorded … gate results?`, capture `(?P<count_phrase>\w+)`) — the 3 `.feature` phrasings stay load-bearing prose. 16==16 green parity (zero scenario/coverage delta), test-only, reviewer-CLEAN 5/5. Residue `F-TESTDEBT-DISPATCHER-TENSE-VARIANT-DRY` CLOSED.

**Highest-leverage:** TD-10 — it is the ONLY finding where the SUT-break-flips-RED divergence-pair currently FAILS (a lying test on the strongest claim). Converting the placeholder to a real FS-snapshot oracle closes a genuine verification hole on the R1 readiness surface. TD-12 is the cheapest mechanical win; TD-11 the next-highest (missing surface, not a lying test).

**ToC placement**: **TD-10 → R1** (theater MASKS the readiness-gate preservation contract — a vacuous AT on the R1 control-plane surface ranks UP per the test-debt ToC rule, NOT R8). **TD-11 → R1** (missing hook-boundary surface on the same gate-composition control plane). **TD-12 → R8** (pure binding-DRY). slice-03's 3 `des -m` subprocess cold-starts (dim3) are a control-plane property (the `des` import-time freshness probe shared across all subprocess ATs), NOT a test-local defect — no in-slice collapse. All gated-APPLY, tests-only.

### Fire-5 2026-06-03 — `acceptance/walking_skeleton_production_like_gate` (R2/R1 done-gate + feature-end, 16 .feature / 6 live + 50 strict-xfail)

Dual-auditor (nw-acceptance-designer dims 1/2/5/6/7 · nw-test-optimizer dims 3/4/8), HEAD `8eb16cde4`, ground-truth `6 passed / 50 xfailed` (slices 03–16 are strict author-ahead RED scaffolds per `conftest.py:45-79` — **honest, ADR-025-correct, NOT debt**). **Verdict: strong living spec (dim5/6 CLEAN); the LIVE surface (slice-01/02) has 3 real findings, the most important architecturally significant + convergent with DDD-7/slice-03.** TD-1 confirmed present at `composition.py:149` (copytree of the synthetic feature root, not src/des — narrower than the catalogued TD-1, not re-flagged); no other catalogued fire here.

**CLEAN (explicit negatives):** dim5/6 — all 16 `.feature` titles+GWT are ubiquitous-language, no impl-jargon leak, strong As-a/I-want/So-that with real user-value; the 50 xfails are honest strict-xfail RED scaffolds (read genuine port observables once greened), NOT @skip-theater. dim8 — no PBT/sleep/unseeded-random; `capture_universe` sorts (FS-order safe); pip-cache race MITIGATED via per-scenario `uuid4` package name (`composition.py:175-177`). `common_steps.py` boundary-clean (zero `from des.*`, all production contact funnelled through composition methods, Mandate-12 step-bodies ≤2-stmt).

**FINDINGS:**
- **TD-13 [SEV hi · dim7+dim2 · R2 · convergent with slice-03]** the feature-end gate **application service lacks a driving port**: slice-02 `run_feature_end_gate` (`composition.py:344-373`) directly imports + hand-wires `des.domain.walking_skeleton_gate` (`FeatureUnderGate`/`WalkingSkeletonGate`) + `des.application.walking_skeleton_feature_end_gate.WalkingSkeletonFeatureEndGate`, duplicating the production composition root the CLI `walking_skeleton_gate.main()` already owns (`:106-124`). Consequences: a real feature-end composition wiring bug is invisible (harness wires its own graph), and constructor-shape renames red the test. The real **SubagentStop feature-end hook driving port is never exercised live** (slice-03, which would, is xfailed) — the spine-critical R2 surface untested. Fix: extract a composition-root factory `build_feature_end_gate(manifest)→service` invoked by BOTH the SubagentStop hook adapter AND slice-02 → closes the dim7 coupling AND the dim2 missing-hook-surface gap. **This IS the DDD-7 driving-port-fronted feature-end use-case slice-03 designs** — the audit independently surfaced the same architectural need. Residue `F-TESTDEBT-FEATURE-END-GATE-DRIVING-PORT` (R2, fold into slice-03 design).
- **TD-14 [SEV med · dim1 · R8, TD-10-class]** slice-01 scenario-3 `@unbounded-preservation` oracle is non-falsifiable: `then_no_source_tree_write` (`common_steps.py:599-610`) fingerprints the **developer checkout's** `src/`+`scripts/` (`composition.py:517-546`), which the gate subprocess — confined to `--feature-dir`/`--repo-root` under `tmp_path` — is structurally incapable of touching. A real gate bug writing into its own target would NOT flip it. Fix: re-point the preservation universe at the SUT's actual write-target (`prefix.tree` delta, already captured `:564-570` but unasserted in scenario-3). Residue `F-TESTDEBT-WS-PRESERVATION-ORACLE-MISTARGET`.
- **TD-15 [SEV hi · dim3+dim8 · R8 / CI-reliability]** slice-01's 3 live scenarios run real `pip wheel` + `pip install --target` via `des walking-skeleton-gate` subprocess (`composition.py:413-444`) — the suite's dominant wall-clock AND its only **network-flake** exposure (cold-cache `setuptools>=61` fetch hits PyPI → fails offline/firewall). Fix-a (reliability, platform-architect): pre-seed/pin setuptools or `--no-build-isolation` with a vendored backend. Fix-b (DRY+speed, gated): fold scenario `:48` (universe-unchanged) onto `:31` (PASS) → one pip build instead of two identical builds + merge the byte-identical `then_repo_unchanged`/`then_no_source_tree_write` step-defs. Residue `F-TESTDEBT-WS-PIP-NETWORK-HERMETICITY` (+ the :48→:31 fold).
- **Premature-DRY caveat (both auditors)**: the record-presence Then-triples + given-feature-built dups in `common_steps.py` serve slices 02–16, 15 of which are xfail RED scaffolds → their bodies REWRITE at GREEN. Collapsing them now races DELIVER's edits → DEFER until those slices green. Apply ONLY the slice-01-live collapses (TD-15 fix-b) now.

**Highest-leverage:** TD-13 — the feature-end-gate driving-port factory. It is R2-coupled, closes both a coupling and a coverage gap, and **converges with slice-03's DDD-7 use-case** (the same `build_feature_end_gate` use-case the CLI shim + hook shim both invoke). Folding this into slice-03's design means R2's remaining third both ships the feature AND fixes this test-debt. TD-15 fix-a (pip hermeticity) is the CI-reliability binding constraint (the only-green scenarios gate on PyPI availability).

**ToC placement**: **TD-13 → R2** (feature-end driving-port + missing live hook-contract surface — MASKS the R2 surface this suite is meant to protect; fold into slice-03 design). **TD-14 → R8** (TD-10-class non-falsifiable preservation oracle, safety-critical scenario). **TD-15 → R8** with a **CI-reliability cross-note**: the pip-network flake gates the suite's only-green scenarios → if "reliably-green" is required, the hermetic-build fix is binding (platform-architect scope, ahead of any test-count reduction). All gated-APPLY/HALT, tests-only. R1 done-gate behaviour (slice-05 RM-3 trust-inversion) is documented-but-unverified-live (xfailed) — residual R1 risk is the 50-xfail DELIVER backlog, not test debt in the live 6.

### Fire-6 2026-06-03 — `acceptance/oss-hook-side-phase-injection` (R2 keystone, D1 DISTILL/DELIVER-exit hook injection · 3 .feature / 8 scenarios + 1 type-invariant unit / 13 ATs)

Dual-auditor (nw-acceptance-designer dims 1/2/5/6/7 · nw-test-optimizer dims 3/4/8), HEAD `e26e103ee`, env-parity-honest. **Verdict: strong suite — coverage/domain-lang/documents CLEAN, driving-port boundary (Mandate-13) CLEAN; ONE env-parity masking finding (R3) + harness-DRY/speed/dead-code (R8).** Texas-sharpshooter: this slice never audited (fires 1-5 other slices); TD-6 is same-class but different feature (`atdd_pure_spine_hardening`) → extended, not re-flagged.

**CLEAN (explicit negatives):** dim2 all 13 ATs drive real composition-root Layer-3 subprocesses (`handle_subagent_stop`/`handle_pre_tool_use` JSON-stdin, `derive_review_trailer`+`verify_commit_trailers` CLI) — no entrypoint bypass; dim5/6 ubiquitous-language Gherkin, event-names only in step bodies via phrase-tables; dim7 Mandate-13 honored (only `des.*` imports are AtCompletionLedger read-back substrate + precondition seeds + the declared type-invariant guard — no gate logic called at the step boundary, no AST/private-attr asserts); dim8 no sleep/unseeded-random/now(), tmp_path-rooted, content-derived seq/hash.

**FINDINGS:**
- **TD-19 [SEV hi · dim8 · R3 ×R2-cross-note · env-parity masking]** all 3 composition roots build the hook/CLI subprocess env from **`dict(os.environ)`** (`composition.py:313`, `slice02_composition.py:78`, `slice03_composition.py:87`) → subprocesses inherit the repo `.env`'s `NWAVE_FRESHNESS=skip` → the hooks under test run their freshness probe (`freshness.py:45,170`) SHORT-CIRCUITED. A real freshness-gate regression in these R2-keystone spine hooks ships GREEN here. **The exact RCA-#68 trap** that fire-1 named a POSITIVE keystone *when avoided* (`conftest.py:377` fresh-dict env) — this slice does NOT avoid it. Masking (coverage-honesty), order-independent. **Residue `F-TESTDEBT-PHASE-INJECTION-ENV-PARITY`** — owner gated test-optimizer/platform-architect; bound: fresh-dict env (PYTHONPATH + slice03 signing-key only), mirror `conftest.py:377`, coverage-preserving. **HALT-CHECK (TEST-CHANGE rule): confirm with Ale/acceptance-designer that freshness SHOULD be active in these ATs (vs intentionally skipped for hermeticity) before flipping.**
- **TD-16 [dim4/7 · R8 · TD-6 EXTENSION]** `_git`/`_subprocess_env`/`init_repo`-bootstrap/stdout-block-parse byte-identical across the 3 composition roots (~50-60 LOC) — same class as catalogued TD-6, different feature. **Residue `F-TESTDEBT-PHASE-INJECTION-HARNESS-DEDUP`** — converges with `F-TESTDEBT-SUBAGENT-STOP-HARNESS-DEDUP`; bound: extract shared `steps/_hook_subprocess_harness.py` + session-scoped template-repo serving BOTH features, no scenario deletion.
- **TD-17 [dim3 · R8 · cross-note F-SPINE-FEEDBACK-LOOP-TOO-SLOW]** AT-3 "symmetric DELIVER completion record" = **2.92s** (`slice02_composition.py:263` `_gate_scope_digest` does an in-process pytest collection to mint a real Gate-Scope trailer); ~2.7s of the slice's ~3.6s. **Residue `F-TESTDEBT-PHASE-INJECTION-GATE-DIGEST-COST`** — bound: session-scope/cache the synthetic-repo digest or pin a known-good fixture.
- **TD-18 [dim4 · R8 · cheapest mechanical]** 3 dead phrase-tables defined, imported by ZERO step bodies (`GATE_OUTCOME_BY_PHRASE` `domain_types.py:85`; `ROUND_TRIP_VERDICT_BY_FAULT_PHRASE` `slice03_domain_types.py:120`; `EXIT_CODE_BY_VERDICT` `:144`) — delete ~15 LOC, zero coverage delta. **Residue `F-TESTDEBT-PHASE-INJECTION-DEAD-TABLES`**.
- **F1/TD-20 [dim1 · R8 · partial-theater]** slice-03 AT-3 `_then_verifier_refuses` (`test_trailer_projection.py:152-159`) asserts only `verdict != VERIFIES` (weak tautology, self-admitted narrative); the real exit-4-vs-6 refuse-taxonomy discrimination is carried by the SEPARATE sibling `_then_verifier_refusal_exit_code` step (real oracle vs `verify_commit_trailers.py:238/232`) → NOT fully vacuous but the Gherkin outcome column is decorative. **Residue `F-TESTDEBT-PHASE-INJECTION-AT3-OUTCOME-ORACLE`** — bound: use the already-defined-but-unused `ROUND_TRIP_VERDICT_BY_FAULT_PHRASE` typed table in the assertion.

**ToC placement:** **TD-19 → R3** gate-honesty (green-but-freshness-masked verdict on R2-keystone hooks → ranks UP per the test-debt-masks-higher-root rule, NOT R8-default; cross-note R2). **TD-16/17/18/TD-20 → R8** (harness-DRY + wall-clock + dead-code + partial-oracle; TD-16 folds into the cross-feature TD-6 harness-extraction APPLY). THE highest-leverage: **TD-19** (correctness — the only finding whose divergence-pair currently fails by construction; needs Ale ratification before APPLY) OR **TD-16** (throughput — largest LOC+wall-clock win, cross-feature). All gated-APPLY/HALT, tests-only, no production code touched.

### Fire-7 2026-06-03 — `acceptance/earned-verdict-gate` (R8 gate-honesty feature, 4 slices / 14 ATs)

Dual-auditor, HEAD `bd510c0a9`, 14/14 GREEN env-parity-honest. **Verdict: clean deterministic parallel-safe suite (dims 1/5/7/8 + speed CLEAN); ONE real coverage finding (capstone-unwired) + doc-debt.** Texas-sharpshooter: NOT in ledger (the TD-19/TD-16 file:lines belong to a DIFFERENT feature — confirmed do-not-exist at this HEAD, NOT re-flagged). **Env-mask cross-check: TD-19 class NEUTRALIZED here** — the SUTs (`earned_verdict_commit_gate_hook`/`run_tests`/`inject_seam`/`earned_verdict`) have ZERO freshness coupling (grep-count 0), so the inherited `NWAVE_FRESHNESS=skip` is inert — no probe to mask. (Lesson: TD-19 env-mask is per-SUT, evaluate freshness-coupling before flagging.)

**FINDINGS:**
- **TD-21 [SEV maj · dim2+dim6 · R8 → R3-on-promotion · TBU-class]** the slice-04 e2e capstone does NOT wire slices 02+03: `earned_verdict_commit_gate_hook.py:48-103` selects hard-coded `_EARNED_PERTURBED`/`_THEATER_PERTURBED` `TestRun` constants by SUBSTRING-matching the commit message, then feeds the REAL core (`compute_verdict`). `run_tests`/`inject_seam` are referenced ONLY in docstrings (`:19-20`), never invoked. So the {theater→deny}/{earned→allow}/{self→deny} decision-table is real + divergent, but the claimed "perturb each GREEN AT + re-run" integration is SIMULATED, not wired — a faint instance of the green-AT-bypasses-the-real-entrypoint (TBU) class on a feature that exists specifically to kill theater. R8 today (feature not promoted to guard real commits); would rank R3 (gate-determinism) on promotion. **Residue `F-TESTDEBT-EARNED-VERDICT-CAPSTONE-UNWIRED`** — owner solution-architect/acceptance-designer, **HALT-for-decision** (design fork: (a) wire the real `run_tests`+`inject_seam` ports → one true hook→ports→core chain AT [touches prod code, TEST-CHANGE rule] OR (b) ratify the simulated perturb-loop as the deliberate Layer-4 contract + correct the overclaiming docstrings/feature-prose).
- **TD-22 [dim6 · R8 · doc-debt]** stale RED-scaffold narrative: `conftest.py:1-17` + `slice-04…feature:20-26` + `composition_slice_04.py:18-25` describe an author-ahead `@skip @pending` pre-DELIVER state that is no longer true (zero skip tags remain; skip-hook inert). **Residue `F-TESTDEBT-EARNED-VERDICT-STALE-RED-NARRATIVE`** — gated crafter, tests-only, zero behavior change.
- **TD-23 [dim4 · R8 · low] — mkdtemp-leak leg ✅ DRAINED (2026-06-07, test-consolidation loop)**: `F-TESTDEBT-EARNED-VERDICT-MKDTEMP-LEAK` CLOSED — converted 3 per-scenario composition fixtures (common_steps.py `composition`, test_slice_02 `runner_composition`, test_slice_03 `injection_composition`) from `return X()` to `yield X()` + `shutil.rmtree(workspace, ignore_errors=True)` teardown; 49→0 stale /tmp residue, 14/14 green, tests-only (zero src/), coverage zero-drop by construction (subprocess suite, no in-process SUT lines), ruff clean. **OPEN legs**: `F-TESTDEBT-EARNED-VERDICT-HARNESS-DRY` (~35 LOC `_result_from_emission`+triad ×4 — folds into cross-feature AD-56 harness-extraction APPLY, owner-gated) + MINOR dim1 (slice-03 AT-3 redundant preservation limb). Same mkdtemp-leak fixture-teardown pattern cleanly drainable on TD-30/TD-34 (other features) in future rounds.

**ToC placement:** **TD-21 → R8** (cross-note R3-on-promotion — the only finding masking a control-plane surface IF the gate is promoted). TD-22/23 → R8 (doc-debt + harness-DRY). THE highest-leverage: **TD-21** — resolve the capstone fork (a gate-honesty feature must not simulate its own perturb-loop). All gated-APPLY/HALT; TD-21 needs architect/Ale ratification.

### Fire-8 2026-06-03 — `acceptance/fix_gcommit_exit_gate_scoping` (R8 G_COMMIT exit-gate scoping, 3 slices / 11-13 scenarios)

Dual-auditor, HEAD `a39dddebc`, 11/11 GREEN sequential + `-n auto --dist worksteal` (parallel-safe verified). **Verdict: HIGH-INTEGRITY suite — dims 1/2/5/6 + speed/flakiness ALL CLEAN; genuine divergence-pairs + anti-vacuity discriminators in source (slice-02 `:228` asserts `reason=="mismatch"` defeating the absent-trailer vacuous-pass).** Texas-sharpshooter: already in arch-debt as AD-42/AD-23/AD-25 (production-side); NOT a prior LEDGER fire; TD-19/TD-16 file:lines belong to other features (NOT re-flagged, EXTENDED-by-class).

**FINDINGS (all R8, low):**
- **TD-24 [dim4 · R8 · harness-DRY]** `_git`/git-bootstrap/`_committed_scope_digest`(near-byte-identical)/64-hex-scan/seed-contract duplicated ~55-70 LOC across the 3 composition roots (`composition.py`/`_slice_02`/`_slice_03`). **This is the 4th feature with the SAME subprocess-harness-DRY class (TD-6 subagent-stop, TD-16 phase-injection, TD-23 earned-verdict, TD-24 gcommit-gate)** → **Residue `F-TESTDEBT-GCOMMIT-GATE-HARNESS-DRY`**, owner gated test-optimizer; bound: extract ONE cross-feature `steps/_cli_subprocess_harness.py` (git_repo/git_commit/committed_scope_digest/scan_bare_digest/write_seed_contract) + session-scoped template-repo serving ALL FOUR features, coverage-preserving, no scenario deletion.
- **TD-25 [dim7 · R8 · low coupling]** slice-03 observes outcomes via the production `AtCompletionLedger` reader (`composition_slice_03.py:42,267`) rather than the hook's on-disk JSONL — observation-side (NOT a Mandate-13 driving violation), minor schema-coupling. **Residue `F-TESTDEBT-GCOMMIT-LEDGER-OBSERVATION-COUPLING`** — replace import-and-read with file-shape observation.
- **Env-parity (extends AD-42, NOT new)**: slice-03 `composition_slice_03.py:403` sets `NWAVE_FRESHNESS=skip` — but **DEFENSIBLE/INERT**: verified the SUTs (`run_contract_gate`, the G_COMMIT intercept `subagent_stop_handler.py:587/636/651`) have ZERO freshness coupling (grep-count 0) → no probe to mask, no masked-green possible. Confirms the **per-SUT TD-19 rule** (the same env-mask SHAPE is provably inert here, unlike the phase-injection TD-19 where it was a real masking finding). Add `composition_slice_03.py:403` to AD-42's `F-CLI-SUBPROCESS-ENVPARITY-AST-GUARD` caller inventory (→6).

**ToC placement:** all R8 (pure test-corpus quality — masks/blocks no higher root; the suite is a model of Mandate-13 subprocess-black-box discipline). THE highest-leverage: **TD-24 cross-feature harness extraction** — ONE APPLY converging TD-6/16/23/24 across 4 features (largest LOC + the git-bootstrap wall-clock amortization). No correctness lever (unlike fire-6 TD-19 / fire-7 TD-21). All gated-APPLY, tests-only, no production code touched.

### Fire-9 2026-06-03 — `acceptance/fix_oss_environmental_e2e_gate` (R2/R1 feature-end env-e2e floor, 4 slices / 23 scenarios)

Dual-auditor, HEAD `825a8efc1`, 23/23 GREEN sequential + `-n auto --dist worksteal` (xdist_group-pinned, parallel-SAFE not parallel-fast). **Verdict: strong suite (dims 5/6 CLEAN, Mandate-12 step-reuse EXEMPLARY, env-mask CLEAN per-SUT), but TWO real findings — a divergence-gap on the done-gate fail-closed floor + a hi-SEV build-isolation network flake.** Texas-sharpshooter: not a prior LEDGER fire; TD-15/TD-19/TD-6-24 belong to other features (EXTENDED-by-class, not re-flagged).

**FINDINGS:**
- **TD-26 [SEV med · dim1+dim2 · R8 ×R1/R3 cross-note · divergence-degenerate]** the slice-03 INTERRUPTED row (`slice-03-fail-mode-d-and-misscoped.feature:68-77`) does NOT exercise its headline C7b claim ("the done-gate must treat a truncated `EnvironmentalE2eVerified` record as ABSENT, not proof") end-to-end: the When returns a HARDCODED `GateResult(BROKEN, PARSE_IO)` (`composition.py:665-670`), and the done-gate Then reads `_staged_feature_end_records`=EMPTY frozenset, NOT the truncated ledger → `evaluate_done_gate` never sees a truncated record. A regression where the done-gate TRUSTS a truncated record would NOT flip this RED (lying on the feature's strongest fail-closed claim, on the every-commit-gate done-gate floor). **Residue `F-TESTDEBT-ENVE2E-INTERRUPTION-DIVERGENCE-GAP`** — owner gated DISTILL+crafter; bound: route the Then through the production done-gate over the ACTUALLY-truncated ledger (one reroute closes dim1+dim2+removes the hardcoded-GateResult theater).
- **TD-27 [SEV hi · dim8 · CI-reliability · TD-15 class on a new feature]** 7 run-mode scenarios fire a real PEP-517 ISOLATED `python -m build --wheel` (`verify_environmental_e2e.py:94`, no `--no-isolation`) with `hatchling` NOT installed → the build MUST fetch hatchling from PyPI → cold-cache/offline/firewalled CI reds 7-of-23 by-environment (the install step IS hermetic `--no-index`, the BUILD step is NOT). **Residue `F-TESTDEBT-ENV-E2E-BUILD-ISOLATION-HERMETICITY`** — owner **platform-architect**; **HALT-CHECK Ale**: real-build-isolation as intentional @real-io environmental realism vs an unhardened flake; bound: `--no-isolation` + vendored/pinned hatchling OR pre-seed the build backend.
- **TD-28 [dim7 · R8 · systemic M13 coupling, 4th fire]** `composition.py` direct-imports DOMAIN `evaluate_done_gate` (`:465`) + a PRIVATE ADAPTER fn `_missing_feature_end_cycle_records` (`:492`, driving surface `handle_subagent_stop():1321` exists) + slice-04 pure-fns. **4th fire finding M13 private/direct-domain coupling (fire-2 TD-5 / fire-4 TD-11 / here) → systemic atdd_pure-rollout pattern.** **Residue `F-TESTDEBT-ENVE2E-M13-DRIVING-PORT-COUPLING`** — needs a Mandate-9(layer-2-in-memory-exempt)-vs-Mandate-13(hard-direct-domain-ban) ADR ruling.
- **TD-29 [dim4 · R8 · low]** mkdtemp+ledger-bootstrap harness-DRY — **5th instance of the cross-feature subprocess-harness class** (TD-6/16/23/24+29). Folds into the ONE cross-feature `steps/_cli_subprocess_harness.py` extraction.

**ToC placement:** **TD-27 → hi CI-reliability** (the build floor reds offline — platform-architect, HALT-check). **TD-26 → R8 ×R1/R3** (divergence-gap on the done-gate fail-closed floor). TD-28/29 → R8 (M13-coupling ADR-call + the 5-feature harness extraction). THE highest-leverage: **TD-27** (correctness/reliability — the only finding that reds the suite by environment) then **TD-26** (the only lying-on-strongest-claim row). All gated-APPLY/HALT, tests-only, no production code touched.

### Fire-10 2026-06-04 — `acceptance/fix_robustness_pbt_density_gate` (R8 robustness-PBT-density gate, 5 slices / 16 tests)

Dual-auditor, HEAD `aa3687d6f`, **16/16 GREEN** (1.61s sequential / 2.08s `-n auto --dist worksteal`, parallel-SAFE + fast). **Verdict: HIGH-INTEGRITY — the cleanest slice audited. dims 1/2/5/6/7 ALL PASS (zero findings); dims 3/4/8 PASS with 2 low cosmetic residues.** The `check_robustness_density` CLI (the gate the `at_review_verdict` producer consults at DISTILL exit).

**Why clean (the genuine positives, source-confirmed):**
- **dim1 BEHAVIOUR**: every AT carries a real divergence-pair that flips (`zero_kills.json` killed:0 → exit 1 `RobustnessPBTNotFalsifiable` vs `valid_killing.json` killed:3 → exit 0; `common_steps.py:387-403`); all 8 production tokens emitted (`check_robustness_density.py:85-109`). **Meta-theater risk REFUTED** — the "genuineness layers" slices (03/04) run the gate as a black-box subprocess against REAL staged shallow-PBT bodies (`composition.py:237-307`), not tautology.
- **dim2 COVERAGE-SHAPE**: the producer→gate subprocess consultation IS covered (slice-05 AT1 drives the real `at_review_verdict` producer; asserts the producer's ledger-write decision + `RobustnessCoverageMiss` stderr; wired at `at_review_verdict.py:180-182`). Real hook block-path covered (slice-05 AT2, `subagent_stop_robustness_gate.py:73-96`). AT3 runs the gate against a real on-disk M-producer manifest.
- **dim7 IMPL-COUPLING**: Mandate-13 model — zero `from des.{domain,application,adapters}` imports in steps; SUT driven only via `python -m` subprocess. Stronger than several prior-audited features.
- **dim4 / AD-56 NEGATIVE FINDING (the key takeaway)**: this feature is the **AD-56 anti-pattern done RIGHT** — it has NO `_git`/`_subprocess_env`/git-init clone (grep-verified absent); it REUSES `tests/env_parity.py:seed_dev_checkout_marker` (`:881`), the exact shared helper the AD-56 extraction would generalize. **Do NOT increment the AD-56 count (stays 15 features / 24 files).** Cite as the reference exemplar in `F-TESTDEBT-CLI-SUBPROCESS-HARNESS-DEDUP`.
- **env-mask INERT per-SUT** (per fire-8/9 rule): 2 `dict(os.environ)` sites (`composition.py:886,:986`) inherit `.env NWAVE_FRESHNESS=skip` but mask NOTHING — `:886` honestly seeds the dev-checkout marker for the tmp-cwd nested call (AD-42 POSITIVE pattern), `:986` runs `cwd=_REPO_ROOT` (real `.git/` autoskips). Add both to **AD-42 `F-CLI-SUBPROCESS-ENVPARITY-AST-GUARD` caller inventory (→8) as seeded/defensible** (the guard should recognize seed-marker-present as compliant).

**FINDINGS (both low, gated-APPLY, tests-only, fold into existing cross-feature APPLYs):**
- **TD-30 [SEV low · dim8 · R8]** robustness-gate mkdtemp leak — 11× `tempfile.mkdtemp` (`composition.py:384,:406,:428,:448,:469,:495,:531,:556,:585,:706,:1067`), zero teardown (~16 tmp dirs/run, OS-reaped → cosmetic). **Folds into the TD-23 mkdtemp-leak class** (route stagers through `tmp_path`). Owner gated test-optimizer; bound tests-only coverage-preserving.
- **TD-31 [SEV low · dim6-doc · R8]** stale RED-scaffold narrative — `conftest.py:1-44` (`_RED_SCAFFOLD_SLICES = frozenset()` + author-ahead xfail-hook prose) describes a pre-DELIVER @xfail state no longer true (all 5 slices greened). **Folds into the TD-22 stale-RED-narrative class** (delete the inert hook OR compress to "all slices shipped"). Owner gated crafter; bound zero-behavior-change.

**ToC placement:** both R8 (pure test-corpus quality, mask/block no higher root — verdicts env-honest). No correctness lever. Fold TD-30→TD-23-class, TD-31→TD-22-class at near-zero marginal cost. **Highest-leverage takeaway is the NEGATIVE finding** (AD-56 exemplar). Static-ceiling caveat: dim1 flippability established by reading asserted observables against confirmed production token-emission sites (auditor-2 ran the suite green; no mutation run).

### Fire-11 2026-06-04 — `acceptance/atdd_pure_common_audit_log_ssot` (R2-adjacent audit-log SSOT, 5 slices / ~44 tests)

Dual-auditor, HEAD `a54775c47`, `10 passed / 4 xfailed / 30 XPASSED` (16.8s serial, parallel-SAFE-but-slow). **Verdict: slice-01 (writer SSOT + reader-filter + tamper-diagnostic + arch-gate) + slice-02d-N0 are genuinely high-quality; BUT the caller-migration slices (02 / 02c-A) carry a systemic stub-theater class that mis-scopes the migration ATs.** The common audit-log writer SSOT (the AtCompletionLedger per-feature→singleton/common-log migration).

**FINDINGS:**
- **TD-32 [SEV hi · dim1+dim2 · R2-adjacent · STUB-THEATER / mis-scoped-AT]** the slice-02 (11 caller rows) + slice-02c-A (6 callsite rows) ATs claim "the migrated PRODUCTION caller writes only to the common log" but their drivers spawn a hand-authored `python -c` stub that imports `AtCompletionLedger` and calls `.append_*` directly (`composition.py:1585-1766,1860-1978`) — they NEVER import/invoke the named production caller. **Verified-from-source (orchestrator-confirmed): the migration is NOT shipped** — the module documents two shapes (legacy per-feature `AtCompletionLedger(feature_id, repo)`→`.nwave/telemetry/atdd-pure/{feature_id}.jsonl`; migrated singleton `AtCompletionLedger(repo)`→`.nwave/audit/atdd-pure-events.jsonl` common log, `at_completion_ledger.py:10-23,228-260`), and EVERY production caller still uses the LEGACY per-feature form: `carpaccio_intercept.py:285,380,618` + `reverify_slice_commit.py:190,443`. So the ATs test a re-implementation, not the SUT → they cannot serve as the migration's completion oracle whether or not it ships. Mitigation present: the 3 slices ARE honestly `xfail`-marked pending (`conftest.py:_RED_SCAFFOLD_SLICES={slice-02,slice-02b,slice-02c-A}`) — NOT hidden — but `xfail(strict=False)` makes their organic stub-pass a SILENT XPASS (30 rows), so the scaffold gives no real signal. This MASKS an R2 spine surface (the audit-log SSOT migration is a spine done-gate substrate). Subsumes the stale-xfail (was TD-B), the arch-test skip-read-as-PASS (slice-02 AT-2 `test_no_per_feature_atdd_ledger_writes.py` self-skips in default mode → exit-0 read as migration-complete, `composition.py:139-141`), and the promised-but-absent `test_fixture_migration_cascade_detector.py`. Owner: gated DISTILL+crafter (the migration is a DELIVER concern; per `feedback_failing_ats_are_feature_debt_not_gate_problem` these ATs are feature-debt for an unbuilt migration). Bound: when the caller migration is built, re-scope each AT to the real driving port (CLI subprocess / hook-JSON-stdin for the 2 hook callers) + flip to `xfail(strict=True)` so XPASS fails-loud. Residue `F-TESTDEBT-COMMON-AUDIT-MIGRATION-STUB-THEATER`. Connects `feedback_at_as_specification_quality_gate` (wrong AT level → wrong software).
- **TD-33 [SEV med · dim7 · R8 · Mandate-13 in-process-adapter]** 3 step services construct `AtCompletionLedger` IN-PROCESS + invoke directly at the step boundary (`composition.py:666-674,752-768,1090-1105`) — `des.adapters.*` is the same forbidden driving class as domain (M15/M16). Regression-pin/seed paths (lower stakes than TD-32). Bound: route through a subprocess stub (the M32 pattern already used 18× in-file) OR consume the JSONL substrate as a file observable. Residue `F-TESTDEBT-COMMON-AUDIT-IN-PROCESS-ADAPTER-INVOKE`.
- **TD-34 [SEV med · dim8 · R8]** mkdtemp leak — `composition.py:184,234` zero teardown, empirically **397+14 `/tmp` dirs/run** (worse than fire-10's TD-30 — the audit-log writer leaves `.nwave/audit/*.jsonl` + temp src trees). Folds into the TD-23/TD-30 mkdtemp-class (route through `tmp_path`/session-template-repo). Owner gated test-optimizer; bound tests-only.
- **TD-35 [SEV low · dim4 · R8]** stub-builder DRY — 6 `_build_slice_02c_a_stub_*` (`composition.py:1860-1978`) collapse to 2 templates (~80 LOC; differ only by `event=` literal). **AD-56 cross-check: NO git-init clone → do NOT increment AD-56 15/24 git-count; BUT this IS a member of the broader CLI-subprocess-harness class** (6th inline-`python -c`-stub file, 7th `des`-console-script file) that `F-TESTDEBT-CLI-SUBPROCESS-HARNESS-DEDUP` supersedes. Fold into the AD-56 file-level APPLY.

**env-mask INERT per-SUT** (fire-8/9/10 rule): no-`env=` inheritor of `.env NWAVE_FRESHNESS=skip`, but both SUTs (`at_completion_ledger.py`, `verify_deliver_integrity.py`) have ZERO freshness coupling → no probe to mask. NOT a TD-19 fire; not added to AD-42 inventory.

**ToC placement:** **TD-32 → R2** (mis-scoped migration ATs MASK the audit-log-SSOT spine surface — per the test-debt ToC rule a green-but-bypassing AT hiding a spine surface ranks UP, not R8). It is the keystone fire-11 finding. TD-33/34/35 → R8 (Mandate-13 coupling + mkdtemp-class + harness-DRY-class, all gated-APPLY tests-only). Tests + conftest only — zero production code touched. TD-32's fix requires the slice-02 production migration to actually land (DELIVER), which is itself the R2-adjacent feature-debt the masked ATs hide.

### Fire-12 2026-06-04 — `acceptance/oss_feature_end_emit_cli` (R2 `des feature-end` feature, 4 slices / 23 AT + 6 integration)

Dual-auditor, HEAD `a18acd0de`, **29/29 GREEN** (sequential + `-n auto --dist worksteal`, parallel-SAFE; ~57s seq — real wheel-build legs). **Verdict: HIGH-INTEGRITY — the R2 feature-end feature audited as genuine anti-theater.** Audited my own R2 work (emit/sign/run/coverage-map). dims 1/2/5/6/7 ALL PASS; dims 3/4/8 PASS with 1 new LOW residue + already-ledgered class-extensions.

**Why clean (source-confirmed positives):**
- **dim1 BEHAVIOUR**: genuine divergence-pairs — `feature_end_cycle_service.py:139,258-266` RUNS the real ported `verify_coverage_map` then appends the 2 records ONLY on pass / mints nothing on `CoverageMapRefused`; a stub-always-emit fails the 5 unsigned/defect refusal rows (`test_feature_end_coverage_map.py:152` strict `== frozenset()`), a stub-never-emit fails the signed row. RM-1 (heartbeat ⟺ verify ran+passed) mechanically pinned. The verdict-laundering close-out is genuine (the `--walking-skeleton-outcome` injection seam was DROPPED; AT-3 fail-closes off a real broken feature). The port-parity integration test (`test_coverage_map_verify_core_port_parity.py:182,189,209,274`) is a genuine byte-identical drift-guard (ported≡upstream digest over a PBT body domain + canonicalization-invariance + anti-laundering law), legitimately importing the ported module (documented Mandate-13 contract-test carve-out).
- **dim2/7**: Layer-3 subprocess only (`-m des.cli.__main__`); the only `from des.*` is the sanctioned read-back substrate (`AtCompletionLedger` reader, documented carve-out). **slice-03/04 amendment is COHERENT** — the reviewer's earlier "scenario-4 conceptually-degraded" concern is RESOLVED (scenario-1 stages a genuinely-signed map → 6-record success; scenario-4 stages unsigned + asserts integrity-substrate honesty; distinct observables, no redundancy). `signed_coverage_map.py` shared builder is GOOD DRY (one §5.3 reproduction, slice-03+04 delegate, honest-by-construction, does NOT import the SUT).
- **AD-54 note**: NO test in this slice should have caught the yaml-bundle violation — `import yaml` in `coverage_map_verify_service.py:37` is a DES-bundle stdlib-only ARCHITECTURE invariant, caught by `test_des_bundle_steps.py:141` (arch/build-tier, correctly outside the acceptance slice). AD-54 is correctly an ARCH item (not test-debt); the genericità guard fired exactly where it should.

**FINDING (1 new, LOW):**
- **TD-36 ✅ DRAINED (2026-06-07, test-consolidation loop)** — re-pointed slice-04's inline §5.3 `_compute_canonical_digest`+3 helpers (`_select_signed_sections`/`_sort_feature_surface_lines`/`_collapse_blank_runs`) to import from sibling `signed_coverage_map.py` (was byte-identical copy, confirmed `diff` IDENTICAL) + deleted dead `_DIGEST_LINE_PATTERN`/`import re`/`_MANDATORY_SECTIONS_IN_ORDER`; `composition_slice_04.py` net −89 LOC. HARD GATE verified-from-source: 23 passed before==after (zero scenario delta), src/des coverage byte-identical (937 covered), tests-only diff (zero src/), ruff clean, order-independent. `F-TESTDEBT-SLICE-04-CANONICAL-DIGEST-INLINE-COPY` CLOSED. (AD-59 production-side `verify_coverage_map.py`↔`coverage_map_verify_service.py` §5.3 fold is DISTINCT, remains OPEN.)

**Confirm-and-skip (already-ledgered, class-extensions):** **AD-56** — this feature is the named **15th** (`ARCH_TECH_DEBT.md:444`) AND an exemplar-done-RIGHT (reuses `seed_dev_checkout_marker`, NO git-clone) → count STAYS 15/24, do NOT re-increment. **TD-15+TD-27** — the 9 heavy cycle-run scenarios (5.6-6.2s) fire a real `pip wheel`/PEP-517 build (the cycle COMPOSES the walking-skeleton + env-e2e gates) → inherited network-flake/wall-clock, extended-by-class not a new locus. **TD-19** — env-mask provably INERT per-SUT (4 `dict(os.environ)` sites seed `seed_dev_checkout_marker` → freshness autoskips via `.git`-adjacency; SUT zero freshness coupling; verified 29/29 GREEN under `NWAVE_FRESHNESS=""`); ADD the 4 sites to AD-42 `F-CLI-SUBPROCESS-ENVPARITY-AST-GUARD` inventory (→12) as seeded/defensible.

**ToC placement:** TD-36 → R8 (pure harness-DRY, masks/blocks no higher root — the port-parity PBT already protects production §5.3 drift; this is harness-internal lock-step). No correctness defect. The suite is high-integrity; the only residue is harness DRY folding into AD-56.

### Fire-13 2026-06-04 — `acceptance/walking_skeleton_feature_end_wiring` (R2-adjacent U4 hook→feature-end wiring, 1 slice / 4 AT)

Dual-auditor, HEAD `8ebae986b`, **4/4 GREEN** (sub-100ms, no build legs, `-n auto` SAFE, env-honest, deterministic). **Verdict: dims 3/4/5/6/8 CLEAN; dim2+dim7 FAIL — a CONFIRM-AND-EXTEND of the already-ledgered M13 hook-boundary-untested class (NO new TD-row, per texas-sharpshooter).** The U4 SubagentStop hook → feature-end done-gate wiring.

**The finding (orchestrator-verified-from-source, folds into existing rows):**
- **AT-1 drives the PRIVATE enforcer in-process, NOT the real hook boundary** — `composition.py:162-168` imports `_missing_feature_end_cycle_records` (a `_`-private inner fn, `subagent_stop_handler.py:870`) and calls it directly at the When-step; the real Claude-Code SubagentStop entry `handle_subagent_stop()` (`:1321`) + its block-path (`_handle_feature_end_gate` → `planned.issubset(shipped)` → `_emit_atdd_pure_block` `:969-980`) is NEVER exercised. A TBU regression in the hook's block-decision marshalling / `_resolve_shipped_slice_set` precondition / stdin-JSON protocol is invisible. Only AT-2 (`@real-io` `des verify-integrity` CLI subprocess, `composition.py:185-195`) is a genuine driving-adapter divergence-pair (drop the CLI-mirror frozenset entry `verify_deliver_integrity.py:463` → RED). AT-1-absent + AT-3 are non-divergent guards (authors concede observational degeneracy, `conftest.py:43-51`).
- **This is the 5th fire of the M13 private/direct-domain coupling class** (fire-2 TD-5 / fire-4 TD-11 / fire-9 TD-28 / + this) → **fold into TD-28 `F-TESTDEBT-ENVE2E-M13-DRIVING-PORT-COUPLING`** (extend fire-count to 5, cite this slice) AND structurally convergent with **TD-13 `F-TESTDEBT-FEATURE-END-GATE-DRIVING-PORT`** (the SubagentStop feature-end hook driving port never exercised live, R2). The systemic 5-fire recurrence STRENGTHENS the pending **Mandate-9(layer-2-in-memory-exempt)-vs-Mandate-13(hard-direct-domain-ban) ADR-call** (TD-28). Bound: add ≥1 `@driving_adapter` scenario invoking `handle_subagent_stop` via hook-JSON-stdin for the heartbeat-missing block path (mirror AT-2's CLI parity at the hook seam).
- **mkdtemp leak (SEV very-low)** — `composition.py:123` 4 dirs/run, absorbed for free by the AD-56 `_harness/cli_subprocess.py` session-template-repo fixture; folds into the TD-30/34 mkdtemp-class. NOT a standalone APPLY.

**Confirm-and-skip:** **AD-56** — NO git-clone (7th `des`-console-script file) → count STAYS 15/24, fold into the existing `F-TESTDEBT-CLI-SUBPROCESS-HARNESS-DEDUP`. **TD-19** — INERT (zero `dict(os.environ)`, no env-mask; SUT no freshness coupling). Nuance (not a TD): unlike the fire-10/12 exemplars this slice does NOT reuse `seed_dev_checkout_marker` (thinner ambient-inherit pattern; works only because `verify-integrity` on a config-only tmp repo has no freshness probe) — when `_harness/cli_subprocess.py` lands it should adopt the fresh-dict env-parity-by-construction path.

**ToC placement:** the M13 hook-boundary gap ranks **R2-adjacent** (the U4 feature-end done-gate is the R2 done-gate substrate — a hook-marshalling bug would let an incomplete feature ship despite correct enforcer logic, masking the R2 wiring). Folds into TD-28+TD-13, no new row. dims 3/4/5/6/8 clean; the mkdtemp is the only R8 residue (absorbed by AD-56).

---

## 🏛️ AUDIT PANEL — run-7 (2026-06-04, HEAD `369638f9f`, autonomous 3-architect)

Lenses: `nw-solution-architect-reviewer` (vs brief+ADR), `nw-system-designer-reviewer` (SSOT+duplication), `nw-ddd-architect-reviewer` (DRY+coupling). Opus, READ-ONLY, verify-from-source. Texas-sharpshooter: AD-01..AD-53 confirmed-and-skipped where unchanged. Disprove-before-flagging applied — the SSOT lens DISPROVED 5/6 control-plane suspects at HEAD (mode-resolution CONSOLIDATED `workflow_mode.py:92`, gate-composition LIVE-WIRED `carpaccio_intercept.py:522`, deploy-copy DISSOLVED `distribution.py:32`, digest-timing SINGLE-PORT `committed_scope_port.py:53`, code-is-SSOT SeamRot DISPROVED — surviving `docs/feature/**/*.feature` have zero executable twin). Production gate-CLI code verified genuinely DRY (`feature_at_files.feature_tag_files:38` SSOT, `_feature_scope_malformed` single-site, slice-01 `--run` non-extraction JUSTIFIED). New ids AD-54..AD-57.

| ID | Finding | Type | Severity | Lens | Evidence | ToC-root / backlog |
|----|---------|------|----------|------|----------|--------------------|
| **AD-54** | **Bundled `src/des` stdlib-only invariant is CI-enforced but has ZERO brief/ADR encoding (missing-guard) — and is VIOLATED.** `coverage_map_verify_service.py:37 import yaml` is the ONLY external import in all `src/des/**` (exhaustive scan {yaml,pydantic,requests,toml,pyyaml,httpx,click,typer} → 1 hit). Enforced ONLY by `test_des_bundle_steps.py:141-159` (`des_no_external_deps`, auto-marked acceptance → CI). `ADR-PLAT-001:57-59` permits requests/pyyaml but is INSTALLER-scoped, NOT runtime-bundle — the two layers have OPPOSITE dep contracts and no ADR draws the boundary, so the slice-04 relocation (`aa17d106`) reasonably-but-wrongly read PLAT-001 as runtime permission. | missing-guard / genericità-violation | **CRITICAL** | solution | `coverage_map_verify_service.py:37` · `test_des_bundle_steps.py:141` · `ADR-PLAT-001:57-59` · HALT `docs/analysis/HALT-r2-yaml-bundle-violation-2026-06-04.md` | **R4 genericità, COUPLES R2/R3** — blocks the zero-failing tree → holds R3 slice-01 uncommitted. `F-DES-BUNDLE-STDLIB-ONLY-ADR` (author ADR-PLAT-007 scoping PLAT-001 to installer + declaring `src/des` stdlib-only, naming the test as its gate) + the yaml-removal residue (HALT options A/B). Re-mark R2 "CLOSED"→"mechanism-complete, 1 genericità-residue open". |
| **AD-55** | **DoR checklist item-count SSOT drift 8-vs-9 — live enforcement hole.** `nw-dor-validation/SKILL.md:10,49` enumerates **8 items** (the skill the reviewer is instructed to LOAD); `nw-product-owner.md:174-184` (+ `:89`, `nw-discuss/SKILL.md:248,276`, `tasks/nw/discuss.md:148`) enumerate **9** (the canonical list adds Item 9 "Outcome KPIs defined with measurable targets"). A reviewer following the enumerated skill validates 8 and **silently skips the Outcome-KPIs hard-gate item**. No `dor-items.yaml` SSOT exists (glob empty). Canonical = 9; the skill is a stale missing-Item-9 copy. **(Answers SF cross-tier Q-8: canonical is 9, the 8 is a stale copy not a deliberate count.)** | SSOT-violation / behavioral-gate-gap | HIGH | system | `nw-dor-validation/SKILL.md:10,49` vs `nw-product-owner.md:174-184` | **R6/methodology-SSOT** `F-DOR-ITEMS-SSOT` — (a) add Item 9 to the skill (~2 lines, closes the hole now) + (b) extract `nWave/data/dor-items.yaml` SSOT both render from. Owner product-owner/ddd. Subordinate to R1/R2 (no re-rank). **SF cross-tier flag (Q-14 reply, SF verify-from-source):** SF converged on canonical-9 (`docs/feature/prism/discuss/dor-checklist.md:17` enumerates Item 9 = Outcome-KPIs) and the correction unblocked SF's `F-SF-CLI-DOR-VALIDATE` (was BLOCKED on the 8-vs-9). SECOND axis to resolve in the `dor-items.yaml` extraction (beyond the count): `nw-discuss/SKILL.md:105,161` adds **job-traceability as a hard-blocking DoR check WITHOUT enumerating it as an item** — clarify whether it is an enumerated item or a separate hard-gate ABOVE the 9 (it is NOT the 9th — that is Outcome-KPIs). |
| **AD-56** | **Cross-feature subprocess-test-harness DRY — empirically 15 features / 24 files; the test-debt ledger declared 5 (TD-6/16/23/24/29) → 3× UNDERCOUNT** (M50 Streetlight / fixture-fanout-enumeration defect, friction #42). A near-byte-identical `_git()`+`_subprocess_env()`+git-init bootstrap copied across 24 composition files; ~600-900 LOC dup + a wall-clock tax (~5-12 git spawns/scenario INSIDE the every-commit contract gate → additive to every root's throughput). No `tests/des/acceptance/_harness/` exists; each new spine feature silently re-clones (`oss_feature_end_emit_cli` shipped this session = the 15th). 6/15 build env from `dict(os.environ)` = the TD-19 env-mask shape. | DRY / fixture-fanout-enumeration | **CRITICAL (D8)** | ddd | grep-verified 24 files / 15 features; `coverage-manifest.md:24` (the 5-feature declaration to correct) | **R8 test-debt, HIGH-LEVERAGE (touches throughput root).** `F-TESTDEBT-CLI-SUBPROCESS-HARNESS-DEDUP` — extract `_harness/cli_subprocess.py` (git_repo_init/run_git/subprocess_env/run_des_subprocess) + session-scoped template-repo fixture (enforces fresh-dict env-parity by construction → closes TD-19 vector) + a DESIGN-time fixture-fanout gate (else feature #16 repeats). Tests-only, coverage-preserving, gated-APPLY. |
| **AD-57** | **`des.runtime.interpreter.python_for` capability-probed interpreter boundary has NO permanent ADR.** Load-bearing normative ("MUST resolve through `python_for`, not `sys.executable`"), failure-mode-rich (InterpreterUnavailable, bounded-probe-timeout), born from the F-21 production incident — its only record is a feature-local delta. The canonical seam for the python-only/target-machine-agnostic mandate at the spawn layer has no SSOT ADR. | undocumented-decision | MEDIUM | solution | `interpreter.py:1-57` · `docs/feature/fix-des-runtime-interpreter-boundary/feature-delta.md` | **R4 doc-debt** — promote to permanent ADR in `docs/product/architecture/` (folds with `F-GATE-GIT-DEPENDENCY-REMOVAL` genericità surface). Subordinate. |

**Catalog gap (flag-only):** `nw-ddd-architect/SKILL.md` does not exist (`nWave/skills/` has strategic/tactical/eventsourcing/event-modeling only) — the ddd lens applied its mandates from the agent spec. Either create the file or drop the load-mandate to keep the skill-loading gate honest. → `F-CATALOG-DDD-ARCHITECT-SKILL-MISSING` (R8 low).

**ADR-index tooling gap (flag-only, consolidation run-10, 2026-06-04):** `scripts/dev/generate_adr_index.py` is ABSENT (verified) — the deterministic ADR-index regeneration step cannot run; the cron hand-curates `docs/adrs/INDEX.md` instead (created run-10, read-only inventory + collision/dup flags from a verify-from-source `find docs -name 'ADR-*.md'` + SHA scan). → `F-ADR-INDEX-GENERATOR-MISSING` (R8 low; folds with `F-ADR-SSOT-COHERENCE` C.1). The INDEX re-confirmed AD-46 (ADR-001/002 DIVERGENT collisions), AD-20/AD-47 (PLAT-001/002/003 same-id byte-identical dups + ADR-004/005/006≡PLAT-004/005/006 renumber-twins) — all flagged, NONE resolved (Ale id-authority).

**Confirm-and-skip (already catalogued, re-verified accurate):** AD-20/46/47 ADR-SSOT folder fragmentation STILL OPEN (permanent ADRs across 4 folders, id-collisions, byte-identical PLAT-004/005/006≡docs/adrs twins) · AD-12 brief 5-phase-TDD vs ADR-025 3-phase · AD-10 installer verify-probe yaml (distinct from AD-54: installer-layer not bundle-layer) · AD-43/44/45 stale brief markers · AD-21/22/28 git-welded-into-application · AD-05 CLI-as-library inversion · AD-26 feature_end frozenset fallback drift (`subagent_stop_handler.py:821,850` — low, folds R1).

**ToC synthesis (run-7):** No root re-rank — R3 remains ToC #1 (gate non-vacuity; R3 slice-01 keystone built, held). **The binding immediate blocker is AD-54** (the yaml bundle violation) — it gates the zero-failing tree → holds R3 slice-01's commit + invalidates R2's unqualified "CLOSED". It is HALT-for-Ale (cross-tier data artifact + correctness-risk; `docs/analysis/HALT-r2-yaml-bundle-violation-2026-06-04.md`). Highest-leverage NON-blocked work: **AD-56** (the 15-feature harness dedup — recurs every spine feature, taxes the every-commit gate). Cheapest correctness win: **AD-55** (add DoR Item 9, ~2 lines, closes a live reviewer under-validation). All gated/HALT; no production code touched this audit.

**Backlog linkage (`docs/backlog.md`):** AD-54 → `F-DES-BUNDLE-STDLIB-ONLY-ADR` (R4×R2/R3, HALT-gated) · AD-55 → `F-DOR-ITEMS-SSOT` (R6 methodology) · AD-56 → `F-TESTDEBT-CLI-SUBPROCESS-HARNESS-DEDUP` (R8, supersedes/unifies TD-6/16/23/24/29) · AD-57 → `F-GATE-GIT-DEPENDENCY-REMOVAL` (R4 doc) · catalog gap → `F-CATALOG-DDD-ARCHITECT-SKILL-MISSING` (R8). Subordinate to R1/R2/R3 per the UNIFIED ToC.

---

## 🏛️ AUDIT PANEL — run-8 (2026-06-04, HEAD `608d9d3d4`, autonomous 3-architect)

Lenses: `nw-solution-architect-reviewer` ×3 (vs brief+ADR / SSOT+duplication / genericità+coupling). Opus, READ-ONLY, verify-from-source. Scope: ONLY the new production code since `4875c6a10` — E yaml→json loaders + R3 collect-AND-run arch-invariant set (6 files / +247 LOC). Texas-sharpshooter: AD-01..AD-57 confirmed-and-skipped where unchanged. **Status deltas this run: AD-54 RESOLVED** (E shipped `1b1d8ec87` + ADR-PLAT-007 `29d9adc0c`); **R3 slice-01 keystone SHIPPED** (`e625b1e16`, Gate-Scope-verified, was HELD behind AD-54). New ids AD-58/59. **R3 collect/run + E behaviour verified CLEAN** (Lens-3: git-free, stdlib+pytest-only, single pytest-argv seam, fail-closed-loud; Lens-2 confirms run-7's "`--run` non-extraction JUSTIFIED" ruling — the `--run` branch is a parameterized extension of the one seam, not a second collection impl).

| AD | Finding | Class | Sev | Lens | Evidence | ToC / F-id |
|----|---------|-------|-----|------|----------|-----------|
| **AD-58** | **ADR-PLAT-007 (authored this session as the AD-54 fix) is itself UNDER-ENFORCED — same defect class as AD-54.** The ADR decision (`:16` "no other external dependency") is broader than its enforcement (`test_des_bundle_steps.py:148`, fixed 5-package blocklist {yaml,pyyaml,pydantic,requests,toml}). `_collect_scope_worker.py:48 import pytest` (non-stdlib) ships in the bundle (`des_plugin.py:454` copytree copies all of `src/des`) and walks straight through the blocklist. **NUANCE (lowers severity):** pytest is a TEST-RUNNER dependency the arch-invariant gate legitimately needs (unlike yaml=data), and the worker is only imported when spawned via `python_for("pytest")`, which degrades LOUD upstream if pytest is absent → a latent/contract violation, NOT a hot-path crash. | missing-guard / genericità (ADR self-incomplete) | HIGH | A1 | `_collect_scope_worker.py:48` · `test_des_bundle_steps.py:148` · `des_plugin.py:454` · `ADR-PLAT-007:16,23` | **R4** `F-DES-BUNDLE-STDLIB-AST-ALLOWLIST` — promote the ADR's own "forward residue" (`:23`) to a tracked row. Remediation is NOT merely "AST allowlist" (pytest IS needed): (a) lazy-import pytest inside `_run_scope` so module-import stays stdlib-clean + the existing `python_for` degrade-loud covers absence; OR (b) ADR carve-out for test-runner deps (pytest/xdist) distinct from data-deps (yaml). Subordinate (no re-rank). Honest meta: the ADR shipped with the exact hole its own residue-note names. |
| **AD-59** | **The E refactor swapped both `_load_omission_class_ids` to json but left the §5.3 core DUPLICATED across the bundle boundary — ~210 LOC, CONSOLIDATABLE, loader+attestation legs UNGUARDED.** `coverage_map_verify_service.py:215-247` (bundled SSOT) ≡ `verify_coverage_map.py:213-247` (scripts/ copy) — byte-identical loader + 8 digest/structural helpers + constants + `_extract_attested_class_ids`. NOT boundary-forced: `scripts/cli/verify_coverage_map.py` ALREADY imports `from des.*` (`:53,57,58`) → it can import the core from the bundled module + delete its copy (scripts→des legal; bundle stays clean per `test_des_no_dev_root_imports.py:27`). The port-parity test (`test_coverage_map_verify_core_port_parity.py:182,189,209`) pins ONLY the digest core — the loader + attestation legs are unguarded → a one-sided weakening of the fail-closed chain in the scripts/ copy would let the CLI gate accept a malformed SSOT the in-process leg refuses (correctness-critical attestation gate). Distinct from TD-36 (the test-HARNESS copy in `composition_slice_04.py`). | DRY / SSOT (unguarded-drift) | HIGH (within R8) | A2 | `coverage_map_verify_service.py:215-247` · `verify_coverage_map.py:213-247,53-58` · `test_coverage_map_verify_core_port_parity.py:182-209` | **R8-HIGH** `F-VERIFY-COVERAGE-MAP-CORE-SSOT-FOLD` — re-point scripts/ to import the §5.3 core from `des.application.coverage_map_verify_service` + delete the ~210-LOC copy + extend the port-parity test to pin `_load_omission_class_ids` + `_extract_attested_class_ids` (drift-proof the fold). Subordinate to spine roots; higher leverage than TD-36 (production dup + correctness-adjacent). |

**Confirm-and-skip (re-verified):** AD-54 REMEDIATED (E clean, json-only `coverage_map_verify_service.py:35`); R3 collect/run CLEAN (run-7 "`--run` JUSTIFIED" confirmed from source); AD-21/37/38 git-seams untouched by R3 (zero new git call sites); AD-43/44/45 brief-marker staleness pre-existing (the +139 R3 LOC shifted the lines the brief mis-cites but did NOT author the mis-cites).

**ToC synthesis (run-8):** No root re-rank. **AD-54 RESOLVED → R3 slice-01 keystone SHIPPED** (`e625b1e16`) — the binding blocker is cleared; R3 #1 now means slice-02 (LOUD-on-absent-arch-scope). AD-58 (R4, ADR self-incomplete) + AD-59 (R8, SSOT fold) both subordinate to the spine roots, both consequences of this session's E+R3 work. Highest-leverage of the two: **AD-59** (removes ~210 LOC production dup + closes an unguarded fail-closed-drift on the attestation gate). **AD-58** is cheap (lazy-import, ~2 lines) and closes the ADR's own acknowledged hole. **Drift finding (Ale Q): code→architecture drift did NOT increase** — E+R3 conform to their feature-delta/ADR; the accumulating drift is doc→code (stale brief markers), a brief-projection job, not crafter deviation.

**Backlog linkage (`docs/backlog.md`):** AD-58 → `F-DES-BUNDLE-STDLIB-AST-ALLOWLIST` (R4) · AD-59 → `F-VERIFY-COVERAGE-MAP-CORE-SSOT-FOLD` (R8). Subordinate to R1/R2/R3.

---

## 🏛️ AUDIT PANEL — run-9 (2026-06-04, HEAD `04096b4c1`, LIGHT/FOCUSED — src/des byte-unchanged)

**`src/des` BYTE-UNCHANGED since run-8** (`608d9d3d4`): verified `git diff 0fc5644a5..HEAD -- src/des` = empty. This session's production touched ONLY `scripts/cli/check_reuse_first_design.py` (F-REUSE-GATE detector) + `nWave/skills/nw-design/SKILL.md` (slice-05 guidance) — both already architecture-reviewed in the F-REUSE-GATE feature-end deep-review (APPROVED; residue logged to backlog: methodology-path-default-wiring, malformed-path, cardinality-edge-ATs). No re-audit of identical src/des code. ONE NEW cross-file drift the feature-internal review did not surface:

| id | finding | class | rank | lens | file-scope | bound (backlog) |
|----|---------|-------|------|------|------------|-----------------|
| **AD-60** | **Two independent Reuse-Analysis-section parsers with DIVERGENT heading-match semantics — SSOT-violation.** `check_reuse_first_design.py:71` `_REUSE_ANALYSIS_HEADING_RE = ^##\s+(?:.*\bReuse Analysis\b.*)` (LENIENT, accepts the carpaccio `## Wave: DESIGN / [REF] Reuse Analysis` variant) vs `validate_feature_delta.py:125` `_REUSE_ANALYSIS_HEADING_RE = ^##\s+Reuse\s+Analysis\s*$` (EXACT, rejects the variant). Each gate re-implements `_extract_reuse_analysis_sections`/`_existing_component_cells` independently. **Root-cause**: the DDD-intended slice-02 consolidation ("promote the LENIENT parser to the sibling's `REUSE_ANALYSIS_HEADING`/columns parser") was deferred and never done — the CLI kept its local parser. **Correctness-adjacent**: a feature-delta with the carpaccio heading passes the CLI gate but FAILS the validator gate (empirically hit while authoring the reuse-gate's own delta) → the two reuse-first gates disagree on the canonical heading; a delta can be green on one and red on the other, masking. | DRY / SSOT (divergent-semantics) | R8 (correctness-adjacent on the divergence) | A2 | `check_reuse_first_design.py:71-118` · `validate_feature_delta.py:65,125,244` | `F-REUSE-GATE-PARSER-SSOT-FOLD` — re-point `check_reuse_first_design.py` to import the sibling's Reuse-Analysis parser + reconcile to ONE accepted heading form + delete the local parser (the CLI already imports `from des.*`, so importing `scripts/validation` or a shared `des.*` parser is legal). Folds the backlog F-REUSE-GATE dogfood finding #2 (divergent heading forms). |

**ToC synthesis (run-9):** No root re-rank (src/des untouched → spine roots R1/R2/R3 unchanged). AD-60 is R8, subordinate, and is the cross-file completion of the same reuse-gate residue the deep-review logged feature-internally — the refactor cycle should fold the two parsers when it consumes the reuse-gate residue. **Drift finding: code→architecture drift did NOT increase** — F-REUSE-GATE's scripts/cli + skill conform to their feature-delta/DDD (deep-review APPROVED); AD-60 is a DDD-acknowledged DEFERRED consolidation (slice-02 "promote to sibling" never done), not a crafter deviation.

**Backlog linkage:** AD-60 → `F-REUSE-GATE-PARSER-SSOT-FOLD` (R8; folds backlog F-REUSE-GATE dogfood finding #2).

---

## 🏛️ AUDIT PANEL — run-10 (2026-06-04, HEAD `560ac930`, LIGHT/FOCUSED — src/ byte-unchanged)

**`src/` BYTE-UNCHANGED since run-9** (`04096b4c1`): verified `git diff 04096b4c1..HEAD -- src/` = empty. This session's production touched ONLY `nWave/skills/nw-dor-validation/SKILL.md` (dor-items slice-03 GREEN) + `tests/` — no `src/` delta, so no re-audit of identical code. ONE NEW finding, dogfood-surfaced (NOT a new code-drift — a pre-existing SSOT-of-meaning violation hit empirically while driving the dor-items slice-03 carpaccio spine):

| id | finding | class | rank | lens | file-scope | bound (backlog) |
|----|---------|-------|------|------|------------|-----------------|
| **AD-61** | **`DES-PROJECT-ID` marker overloaded as the atdd_pure carpaccio feature-id — one marker, two semantics.** The atdd_pure PreToolUse carpaccio intercept derives its feature-id from the `DES-PROJECT-ID` marker: `pre_tool_use_handler.py:147` `feature_id = markers.project_id`, then `carpaccio_intercept.py:285` `AtCompletionLedger(feature_id, project_root)` resolves the per-feature ledger `.nwave/telemetry/atdd-pure/{feature_id}.jsonl`. But `DES-PROJECT-ID`'s classic semantics = the DES/CI **project** identity (e.g. `nwave-dev`), NOT the per-feature carpaccio id (e.g. `dor-items-ssot`). A dispatch carrying the classic-correct `DES-PROJECT-ID: nwave-dev` makes the order-check read a non-existent `nwave-dev.jsonl` → `verified_slices()` empty → false `CarpaccioSliceOutOfOrder` block of an IN-ORDER slice. **Empirically hit this session**: slice-03 A_GREEN dispatch with `DES-PROJECT-ID: nwave-dev` was falsely blocked; corrected only by setting `DES-PROJECT-ID: dor-items-ssot`. **Root-cause**: the carpaccio path reuses the classic project-id marker as the feature discriminator instead of a distinct feature-id marker → the same value-string carries two incompatible meanings; the nw-deliver atdd_pure dispatch template does not pin which. **Correctness-adjacent**: a false block halts the spine; conversely a real out-of-order could be masked if a project-id happened to name a real ledger. | SSOT-of-meaning / naming-is-design (overloaded marker) | R8 (correctness-adjacent; known workaround = set `DES-PROJECT-ID`=feature-id) | A2 | `pre_tool_use_handler.py:147` · `carpaccio_intercept.py:262-305,512` · `des_marker_parser.py:150,167` | `F-DES-FEATURE-ID-MARKER-DISTINCT` — introduce a distinct `DES-FEATURE-ID` marker the atdd_pure carpaccio path reads (leaving `DES-PROJECT-ID` to its classic project-identity meaning), OR formalize+document the overload and pin `DES-PROJECT-ID: <feature-id>` in the nw-deliver atdd_pure dispatch template. |

**ToC synthesis (run-10):** No root re-rank (src/ untouched → spine roots R1/R2/R3 unchanged). AD-61 is R8, subordinate; it is a marker-contract footgun on the atdd_pure dispatch surface, not a spine-blocking root (the workaround is mechanical + known). **Drift finding: code→architecture drift did NOT increase** (no src/ delta this session). AD-61 is a pre-existing overload the dogfood surfaced, not a crafter deviation.

**Backlog linkage:** AD-61 → `F-DES-FEATURE-ID-MARKER-DISTINCT` (R8; the refactor cycle should consume it alongside the dispatch-template clarification).

---

## 🏛️ AUDIT PANEL — run-11 (2026-06-05, HEAD `bcd469d90`, FOCUSED — src/des CHANGED: oss-spine-watchdog shipped)

**`src/des` CHANGED since run-10** (`560ac930`): the `oss-spine-watchdog` feature (RCA #68, friction #69, R1-adjacent) shipped 6 slices + the R-69-E refactor, ALL touching `src/des/adapters/drivers/hooks/subagent_stop_handler.py` (slice-04 terminal-coherence `d4d92f8f` · slice-05 collection-precheck `2c508ec6` · R-69-E vocab-SSOT refactor `762afee7` · slice-06 timeout-countability `330ca90e`). **Code→architecture drift did NOT increase**: every slice passed per-slice C_REVIEWER_AUDIT + the feature-end deep review (`a01511d9`, APPROVED-with-residues) verified cross-slice coherence (terminal SSOT complete, 3 fail-safe directions disjoint+non-interfering, hooks-only mandate honoured, 0 CREATE_NEW). No re-audit of the watchdog code (already deep-reviewed). TWO findings the cycle surfaced are ARCHITECTURAL (cross-feature), logged here:

| id | finding | class | rank | lens | file-scope | bound (backlog) |
|----|---------|-------|------|------|------------|-----------------|
| **AD-62** | **The feature-end WS-floor gate refuses a HOOK-ONLY src/des-bundled feature + its diagnostic GARBLES the cause — the `F-FEATURE-END-WS-GATE-APPLICABILITY` root is BROADER than "non-installer-shipped scripts/cli".** `des feature-end run --feature-id oss-spine-watchdog --verdict APPROVED` REFUSED: `{"event":"FeatureEndCycleRefused","error":"the walking-skeleton gate failed; …refuses to certify (anti-theater): {…freshness.autoskipped…}"}`. (a) **Applicability**: this is the **3rd confirmed WS-floor refusal** but the FIRST whose feature is `src/des`-BUNDLED (the watchdog is 3 conditionals + helpers INSIDE the shipped `subagent_stop_handler.py`, not a `scripts/cli` gate) — so the existing diagnosis (WS entry-point "absent from the installed set because non-installer-shipped", backlog F-FEATURE-END-WS-GATE-APPLICABILITY) does NOT explain this instance → the root is broader: the WS-floor's walking-skeleton-entry-point assertion does not fit a feature whose "entry point" is a hook code-path (no distinct installer-shipped artifact), regardless of bundling. (b) **Diagnostic-garbling** (NEW sub-finding): the refusal message INGESTED the unrelated `des.runtime.freshness.autoskipped` stdout JSON as the WS-failure "reason" → the gate parses a benign env-event line as the failure cause → the actual WS-failure reason is MASKED. A verify-the-instrument trap (env-mask class, same family as TD-19): anyone diagnosing the refusal is handed a non-cause. | gate-applicability / drift-vs-ADR (anti-theater floor over-broad) + diagnostic-reliability | **R8 → R2-adjacent** (blocks the feature-end DONE-GATE auto-certification of a substantively-complete feature; 3rd instance → recurrence-confirmed, not one-off) | A1/A2 | `walking_skeleton_feature_end_gate.py` (`_entry_points_absent_from` / facet-1) · the feature-end-cycle stdout-parsing that ingests the autoskip line | extend `F-FEATURE-END-WS-GATE-APPLICABILITY` — WS-floor must return NOT_APPLICABLE (not FAIL) for features with no installer-shipped walking-skeleton artifact (scripts/cli gates AND hook-only src/des code-paths), AND the feature-end cycle must not parse the freshness-autoskip line as a failure cause (fix the diagnostic). 3rd instance (watchdog) elevates priority. |

**ToC synthesis (run-11):** No spine-root re-rank, but a **severity ELEVATION on the deploy-copy gap (backlog #58 `F-DES-HOOK-SUBPROCESS-INSTALLED-COPY-STALENESS` + #67 facet-3)**: the watchdog feature-end deep review proved EMPIRICALLY that the gap is not merely dev-friction but a **CORRECTNESS gap for hook-delivered features** — the watchdog's own dogfood ledger showed a 14-deep re-fire loop with NO terminal because the hook fires the INSTALLED lib (`~/.claude/lib/.../subagent_stop_handler.py`, dated Jun 1) which PREDATES the entire watchdog (slices Jun 4-5). I.e. **a hook-code feature is INERT on the live spine until the installed lib is redeployed** — "shipped repo-src ≠ protects the live spine". This is the dominant operability finding of the watchdog cycle and should raise #58 toward R1-adjacency (it nullifies the live value of every hook-delivered fix, including this very watchdog, until an editable-install / auto-redeploy path lands). Connects `[[feedback_verify_plan_claims_empirically_shipped_neq_elevated]]`. **Cross-tier corroboration**: SF Q-58 independently confirmed the reuse-gate methodology-blindness (already `F-REUSE-GATE-COVER-METHODOLOGY-COMPONENTS`) with an orthogonal-vacuity taxonomy (OSS component-aware-methodology-blind vs SF presence-only; convergent cure = path-kind-aware detection + coverage-verification). **New untracked backlog id** (methodology-doc, R6): `F-OSS-DISCUSS-DOR-LOCATION-RECONCILE` (SF Q-46, confirmed-from-source) — `nw-discuss/SKILL.md:255` + `nw/discuss.md:128/198` stale table-rows route DoR to a separate file, contradicting the ADR-022 single-narrative already declared at `SKILL.md:323` (same stale-copy class as AD-55 DoR 8-vs-9). **Named residue** R-69-G (cross-invocation collection-terminal AT gap; membership correct by-construction, regression-unguarded).

**Backlog linkage:** AD-62 → extend `F-FEATURE-END-WS-GATE-APPLICABILITY` (3rd instance + diagnostic-garbling sub-finding); severity-elevation on `#58 F-DES-HOOK-SUBPROCESS-INSTALLED-COPY-STALENESS` (dev-friction → hook-feature correctness gap); new `F-OSS-DISCUSS-DOR-LOCATION-RECONCILE` (R6 methodology-doc); R-69-G named residue (watchdog feature-delta).

**AD-62 CORRECTION (verify-from-source, 2026-06-05, HEAD `4a8bdd278` — ran the WS-gate directly, unmasked):** the run-11 row's "broader-than-non-installer-shipped" was directionally right but imprecise. REAL reason, verified by running `des walking-skeleton-gate --feature-dir docs/feature/oss-spine-watchdog`: `{"event":"WalkingSkeletonGateUsageError","reason":"no walking-skeleton.json manifest under docs/feature/oss-spine-watchdog"}`, **exit 2 (USAGE error, not a FAIL verdict)** — the atdd_pure spine never emits a `walking-skeleton.json` manifest, so the gate cannot even reach an evaluation. **The garbling mechanism is precise**: `feature_end_cycle_service.py:292 _gate_diagnostic` returns `completed.stderr.strip() or completed.stdout.strip()` — the `des.runtime.freshness.autoskipped` line (emitted to STDERR by every dev-checkout dispatch) shadows the real `WalkingSkeletonGateUsageError` (in STDOUT). **Meta-root unified**: the WS-floor is modelled for INSTALLABLE-PACKAGE features (build wheel → install → assert WS entry-point present); it is structurally NOT_APPLICABLE to non-installable-package features — BOTH the scripts/cli gate-CLI instances (1&2, manifest present but entry-point not installer-shipped → exit 1 FAIL) AND hook-only src/des code-path features (3=watchdog, no manifest → exit 2 USAGE). **`WalkingSkeletonFeatureEndGate:71` ALREADY honours `GateVerdict.NOT_APPLICABLE`** (`proceeds = verdict in (PASS, NOT_APPLICABLE)`) — the scaffolding exists; the gap is that nothing returns NOT_APPLICABLE for a non-installable feature. **Anti-theater caveat (DESIGN-stakes, needs Ale ratification)**: NOT_APPLICABLE must be a POSITIVE, EXPLICIT, JUSTIFIED feature declaration (e.g. `walking_skeleton_applicable: false` with rationale), NOT a gameable absent-manifest→skip (else any feature dodges the anti-theater floor by omitting the manifest). The diagnostic-garbling fix (`_gate_diagnostic` must filter `des.runtime.*` event lines) is the clean no-fork companion.

---

## 🗄️ ARCHIVE — completed & historical (SKIP unless explicitly asked)

> **Empty by design (2026-06-01).** This file is **append-only**: AD-rows are amended in place (status delta), never deleted, and the dated audit panels above are living historical records that feed the UNIFIED ToC. No AD-row currently carries an explicit terminal-close marker (GREEN/CLOSED/✅) — findings advanced by the night refactor (AD-22 committed-scope-port, AD-05 CLI-library inversion, AD-02 ledger-port DIP, AD-16 domain singletons, plus phase-identity/VALIDATION_COMMANDS consolidation) are tracked as *amended-in-place* status deltas within their audit panels, not as moved entries. **AD-04 is DISPROVED** (PhaseEvent VOs are load-bearing) — it awaits a row revision, not archival. When an AD-row reaches a verified terminal close, relocate it here verbatim (content preserved, never summarized).
