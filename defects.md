- [ ] spine-wave-floor-not-feature-scoped: paradigm=object-oriented defect="`.nwave/wave-active/active.json` is a single global `{wave, provenance, armed_at}` triple per repo, not keyed by feature-id — two genuinely independent, non-conflicting wave dispatches on different features trip WAVE_MARKER_BYPASS on each other purely because the floor can't represent two features' waves at once (6 occurrences logged: dual-ownership close bug, provenance:inferred floors with no expiry, a 3-week-stale global-config floor, and cross-feature scoping blocking legitimate parallelism the swarm-parallel-delivery epic depends on)" proposed_solution="scope the wave-floor marker by feature-id (a per-feature file, or a feature-id key in the existing JSON) so the guard only fires on a genuine same-feature collision; separately, fix `_maybe_close_owner_floor` to handle dual-ownership {design, devops} and arm only on dispatch success, not on a blocked dispatch"
- [ ] workflow-mode-not-shareable-gitignored: paradigm=object-oriented defect="workflow.mode lives in gitignored .nwave/config.yaml; a fresh clone or sister instance silently defaults to classic, hiding the whole atdd_pure spine" proposed_solution="declare workflow.mode in the tracked local-config.json (or infer it from atdd_pure artifacts) so the mode travels with the repo"
- [ ] dispatch-guard-how-doesnt-route-to-des-dispatch: paradigm=object-oriented defect="the crafter dispatch-guard rejection tells the operator to HAND-ADD the missing marker/section instead of naming des dispatch, the tool that generates a compliant envelope" proposed_solution="the guard HOW routes to 'des dispatch ...' to regenerate the envelope, never hand-add the section/marker"
- [ ] rust-regression-at-kind-semi-wired: paradigm=object-oriented defect="rust-regression at-kind is accepted by record-at-review-verdict + verify-slice-commit but rejected by carpaccio-slice-gate and the dispatch-guard hook" proposed_solution="propagate rust-regression to carpaccio_slice_gate and des_crafter_dispatch_guard so it is valid across the whole cycle"
- [ ] reuse-first-gate-branch-topology-false-positive: paradigm=object-oriented defect="check_reuse_first_design diffs unscoped master...HEAD; on a long-lived branch thousands of added files count against one feature's Reuse table, false-FAILing the architect" proposed_solution="default --base-branch to None; resolve the feature-delta's own git-genesis parent as base, falling back to master"
- [ ] gcommit-contract-suite-multi-run: paradigm=object-oriented defect="G_COMMIT E2 --verify-gate-scope re-collects and re-runs the whole-tree suite atop the terminating run + pre-commit, so the suite runs several times per slice commit" proposed_solution="cache/reuse the collect digest across the terminating run and the exit gate (reuse-first, no new artifact)"
- [ ] verify-slice-commit-e2-wrapper-divergence: paradigm=object-oriented defect="verify-slice-commit's composed E2 subprocess returns exit 1 while the identical hand-run returns exit 0; likely capture_output pipe-buffer-fill under a large suite" proposed_solution="stream the child to a tempfile instead of an in-memory pipe; add an AT asserting wrapper-verdict equals standalone-verdict"
- [ ] phase-c-f-verdict-ledger-record: paradigm=object-oriented defect="no des recorder for the C_REVIEWER_AUDIT/F_FINAL_REVIEW verdict, so the slice commit Reviewed-by trailer cites the AT-review instead of the code-review" proposed_solution="add des record-reviewer-verdict --phase C|F, reusing the same ledger-writer and record-shape"
- [ ] commit-slice-amend-hazards: paradigm=object-oriented defect="des commit-slice amends a foreign HEAD ignoring --message; the backfill amend re-runs the full suite (flock-deadlock); a narrow --path can drop the guard-test; --no-verify silently bypasses" proposed_solution="pin/assert the placeholder SHA or refuse a foreign HEAD; backfill amend passes --no-verify; guard crafter --no-verify"
- [ ] axis-b-levers-hardcode-nwave-paths: paradigm=object-oriented defect="axis_b_levers.py hardcodes tests/, src/des/ and rglob('test_*.py'); on a different target layout it scans the wrong/empty dir, giving false-PASS or a crash" proposed_solution="discover source+test paths from the target config (pyproject testpaths/.nwave/flags), degrade-LOUD when undeterminable"
- [ ] concurrent-git-writers-and-gate-timeout-orphan: paradigm=object-oriented defect="killing verify-slice-commit/feature-end mid-E2 orphans its pytest-child; --expected-head passed to an amending gate false-races; a git-writer concurrent with a resumed crafter corrupts HEAD" proposed_solution="the gate SIGTERM-kills its children and pins the POST-amend SHA for its race-check; never pass --expected-head to an amending gate"
- [ ] test-git-pollution-resets-real-head-under-precommit-fullsuite: paradigm=object-oriented defect="conftest._REAL_REPO_CWD_RE matches only cwd=_REPO_ROOT|Path.cwd(), not local cwd=repo, so pre-commit full-suite git-ops concurrently reset real HEAD (orphaned a slice commit) plus flaky guard ERRORs" proposed_solution="widen the regex and pin those suites to real_repo_scan, or use hermetic tmp repos"
- [ ] refactor-entry-gate-missing-from-drain-batch: paradigm=object-oriented defect="des refactor's entry-gate check is wired into drain_one but not _drain_concurrently, so an unclassified/Mikado item is refused at --max-parallel 1 yet merges silently at 2+" proposed_solution="wire the entry-gate refusal (same cleanup guarantee) into _drain_concurrently; add a regression test proving a batch drain currently merges an unclassified item"
- [ ] serial-gate-fallback-vs-run-timeout-ceiling-uncoupled: paradigm=object-oriented defect="NWAVE_GATE_JOBS=serial (the sanctioned false-red fallback) slows the whole-tree run but run_timeout_seconds (2700s, tuned for -n auto) is uncoupled, so serial mode false-times-out on a large/contended suite" proposed_solution="when jobs=serial is detected, auto-scale the run-timeout ceiling (e.g. x2) instead of requiring a second manual env var"
- [ ] dispatch-hook-not-worktree-aware: paradigm=object-oriented defect="readiness-pre-dispatch/PreToolUse dispatch-guard defaults repo-root to the main repo, not the caller's worktree; the DES-PROJECT-ROOT escape is opt-in, risking a silent cross-worktree production-code write" proposed_solution="auto-detect that cwd is inside a worktree and self-derive the repo-root marker; resolve from the caller's cwd, never a fixed main-repo path"
- [ ] feature-end-real-io-no-external-dep-escape: paradigm=object-oriented defect="des feature-end run's walking-skeleton @real-io leg waits the full 600s timeout on an exhausted-quota API error, with no progress output and no skip flag, halting feature-end tree-wide" proposed_solution="add a documented --skip-step/@real-io exemption with required justification, and print elapsed/remaining progress near timeout"
- [ ] blast-radius-cannot-answer-where-it-matters-most: paradigm=object-oriented defect="des blast-radius CLI times out silently (exit 143) on high-consumer files; re-parses whole tree once per symbol, scanning .venv/third-party" proposed_solution="derive exclusions from target own .gitignore + cache parsed trees per process; apply in both AST and grep floor tiers"
- [ ] prepush-hook-cannot-import-des: paradigm=object-oriented defect="pre-push hook runs validate_language_adapter_catalog.py under an interpreter lacking the editable des install, so ModuleNotFoundError aborts every verified push" proposed_solution="hook invokes the script with the project venv / PYTHONPATH=src, resolving the interpreter that has des rather than a bare python"
- [ ] wave-marker-blocks-non-atdd-pure-child: paradigm=object-oriented defect="PreToolUse S2 denies an in-wave non-atdd_pure child carrying DES-WAVE but no DES-VALIDATION marker that no producer generates, making concurrent in-wave dispatch impossible" proposed_solution="S2 allows an in-wave child whose DES-WAVE matches the active wave; deny only mismatched/markerless; fix the recovery message"
- [ ] feature-end-refused-reason-not-surfaced: paradigm=object-oriented defect="FeatureEndCycleRefused prints a bare event with zero cause though the real fail reason exists internally at feature_end_cycle_service:1585" proposed_solution="surface which leg went indeterminate and why into the CLI output"
- [ ] des-doctor-vs-carpaccio-gate-disagree: paradigm=object-oriented defect="feature-delta-doctor reports gap_count:0 while carpaccio-slice-gate rejects the same file as malformed; the doctor advertised as authoritative gives a false all-clear" proposed_solution="doctor includes the carpaccio-gate predicate, or stops declaring itself the backstop"
- [ ] des-dispatch-repo-root-two-roots: paradigm=object-oriented defect="one --repo-root must serve both the SSOT-in-install and feature-delta-in-project roots; either setting breaks the other, unsatisfiable" proposed_solution="split into two params (install-root vs project-root); also let feature-delta-doctor accept --feature-id"
- [ ] reverify-slice-commit-no-at-kind: paradigm=object-oriented defect="des reverify-slice-commit lacks --at-kind so a Python slice routes to cargo and hangs silently (empty output at 900s)" proposed_solution="add --at-kind (or deduce it as commit-slice does) and make the cargo-routing announce what/why"
- [ ] commit-slice-cargo-enumerate-vs-build-race: paradigm=object-oriented defect="commit-slice runs whole-tree cargo nextest list concurrent with its own E2 build on the same target/, so under contention nextest exits 0 and the gate false-records SliceCommitIndeterminate on Rust" proposed_solution="serialize enumerate after the build completes, or resilient-retry distinguishing 0-during-build from 0-genuine"
- [ ] e2e-install-flaky-under-parallel-fullsuite: paradigm=object-oriented defect="test_idempotent_install.py intermittently fails under feature-ends -n auto loadgroup (OOM/race), refusing every seal though it is green in isolation" proposed_solution="mark it serial / own xdist group, or run e2e in a bounded serial pass in the feature-end leg"
- [ ] execution-reach-hardcoded-src-dir-not-agnostic: paradigm=object-oriented defect="_EXECUTION_REACH_SRC_DIR=src is hardcoded so non-standard layouts exit INDETERMINATE and cannot be enforced" proposed_solution="detect the source dir from the targets own manifest/convention, or make it a per-language adapter concern"
- [ ] deliver-hook-misses-committed-pyc: paradigm=object-oriented defect="the DELIVER pre-commit gate did not catch 17 tracked .pyc committed across 9 commits though .gitignore forbids them" proposed_solution="pre-commit hook rejects tracked bytecode / .gitignore-forbidden artifacts"
- [ ] carpaccio-general-pytest-at-blind: paradigm=object-oriented defect="carpaccio-slice-gate rejects a normal-pytest-AT slice with no-scenarios-for-slice; check_carpaccios pytest arm only handles at_kind pytest-regression" proposed_solution="extend the no-scenarios branch to count pytest ATs via the head-comment @feature/@slice discovery already used at seal-side"
- [ ] repo-root-silent-cwd-fallback: paradigm=object-oriented defect="resolve_repo_root silently falls back to cwd so 10+ write-commands write .nwave/ to the wrong tree while reporting a success identical to a real write" proposed_solution="resolve the real repo-root via .git-adjacency walk-up (reuse freshness.py) and degrade-LOUD when none found; unify the 4 divergent resolvers"
- [ ] armed-floor-section-validator-ignores-reviewer-exemption: paradigm=object-oriented defect="the atdd_pure section-validator in pre_tool_use_service blocks a read-only reviewer dispatch though verify-wave-dispatch exempts reviewers; the two enforcement loci disagree" proposed_solution="apply the same reviewer/non-owner exemption before invoking the section-validator; GC the stale wave-dispatch-prompt.txt"
- [ ] installer-pyc-race-transient: paradigm=object-oriented defect="DES plugin copytree races a concurrent bytecode-cache write and fails with FileNotFound on a .pyc tmp file" proposed_solution="exclude __pycache__ from the copy step or use an ignore-errors/retry pattern for .pyc.* tmp files"
- [ ] docgen-interpreter-split-venv-shadow: paradigm=object-oriented defect="the freshness hook runs system python3 while the venv carries a stale shadow of scripts/shared, so the two interpreters docgen disagree and fail docs-freshness" proposed_solution="pin the freshness hook and docgen to the same (venv) interpreter, or make uv sync part of install-hooks"
- [ ] pre-commit-stash-race-fourth-victim: paradigm=object-oriented defect="staged docgen pages are lost when a commit launches while a push chains stash-isolation cycle is still active (fourth victim of the class)" proposed_solution="a commit/push wrapper that takes a session-level lock so a second git-write queues instead of interleaving"
- [ ] bugfix-carpaccio-gate-expects-gherkin-scenarios-not-plain-pytest: paradigm=object-oriented defect="the at_kind fix extended the CLI entry points but not carpaccio_intercept.py, so a pytest-regression bugfix slice is rejected at dispatch though the CLI passes on the same slice" proposed_solution="extend the intercept runner with at_kind/regression_test_file via the ADD-not-mutate precedent already in that file"
- [ ] acceptance-designer-dispatch-injects-misleading-des-wave-marker: paradigm=object-oriented defect="subagent_stop_handler:521 gates on the raw substring DES-WAVE so any prose mention sets saw_marker with no wave, tripping a permanent WAVE_GATEOUT false-positive for the session" proposed_solution="tighten line 521 to a well-formed-marker regex, or strip inline-code/quoted mentions before the substring test"
- [ ] pre-push-suite-not-diff-aware: paradigm=object-oriented defect="the pre-push chain runs the full ~5500-test suite plus a whole-tree collect-only backstop even for docs-only deltas, burning the shared host and forcing --no-verify" proposed_solution="make pre-push and the backstop diff-aware, skipping pytest tiers with a LOUD note when the delta is docs-only"
- [ ] crafter-dispatch-guard-missing-feature-end-phases: paradigm=object-oriented defect="the personal des_crafter_dispatch_guard recognizes only per-slice phases, so a well-formed feature-end dispatch (E_BATCH_REFACTOR/F_FINAL_REVIEW) is wrongly blocked" proposed_solution="extend the guards recognized phase set to the union of CANONICAL_PHASES and FEATURE_END_PHASES"
- [ ] installer-windows-logo-cp1252: paradigm=object-oriented defect="print_logo emits emoji/box-glyphs causing UnicodeEncodeError on cp1252 Windows consoles, a hard install failure" proposed_solution="force UTF-8 stdout at entry, or use an ASCII-only / safe-box logo"
- [ ] preserve-user-skills-on-reinstall: paradigm=object-oriented defect="skills_plugin.py shutil.rmtrees ~/.claude/skills/nw/ before copying, wiping user custom skills and blocking customization" proposed_solution="preserve user-owned skill files on reinstall instead of rmtree"
- [ ] des-dispatch-missing-project-root-marker: paradigm=object-oriented defect="des dispatch emits no DES-PROJECT-ROOT marker so carpaccio_intercept resolves project_root from session cwd not the dispatched worktree, reading the wrong tree ledger under swarm-parallel" proposed_solution="des dispatch stamps a DES-PROJECT-ROOT worktree-path marker; the hook prefers it over cwd inference"
- [ ] worktree-cleanup-force-remove-no-dirty-guard: paradigm=object-oriented defect="remove_worktree calls git worktree remove --force with no dirty-tree guard and the head_sha==tip short-circuit marks zero-commit worktrees CLEANUP_DUE, destroying uncommitted work three times" proposed_solution="add a dirty-tree guard (has_uncommitted_changes via git status --porcelain) so a worktree with uncommitted changes is never removed"
- [ ] feature-end-postgres-leak-on-exit: paradigm=object-oriented defect="des feature-end runs whole-tree gate spawns postgres/pgserver for env-e2e but never tears it down, leaving orphaned clusters running 20+ min" proposed_solution="register a teardown (atexit/finally or a reaper) that tears down the spawned cluster regardless of exit path"
- [ ] commit-slice-shadow-tree-dep-closure: paradigm=object-oriented defect="commit-slice builds its shadow tree from only --path files, so a new pytest marker registered in pyproject not in --path yields a strict-markers collection error surfaced only as opaque CommitFailed exit 1" proposed_solution="compute the slices dependency-closure (or warn on unlisted dep) and propagate the failing pre-commit hooks stdout/stderr into the failure surface"
- [ ] runner-specific-test-env-gate-not-hardfail: paradigm=object-oriented defect="a cargo-runner acceptance test hard-fails on a box with no resolvable cargo/interpreter, asserting the happy path though the code correctly records Indeterminate" proposed_solution="skipif-with-reason when the runner env is absent; do not weaken the gate"
- [ ] commit-slice-missing-slice-commit-verified: paradigm=object-oriented defect="commit-slice records SliceCommitted not SliceCommitVerified so the successor carpaccio gate hard-blocks, and a broken/partial install crashes the gate on import instead of degrading" proposed_solution="commit-slice emits SliceCommitVerified itself (or the gate accepts SliceCommitted verified:true); a broken install degrades LOUD telling the user to reinstall"
- [ ] jira-mirror-id-normalization-duplicate-risk: paradigm=object-oriented defect="the mirror drops the leading f- so its dedup label mismatches manual items f--prefixed labels, and a re-run creates duplicate Stories/Subtasks" proposed_solution="find_by_label tries both the normalized and the f--prefixed label (or a one-time relabel)"
- [ ] atreview-cli-template-drift: paradigm=object-oriented defect="record-at-review-verdicts real flags diverge from the dispatch template (--repo-root, --slice-id, required --reviewer-agent-id, verdict set without REJECTED), so operators hit it by trial-and-error" proposed_solution="align the template text to the real CLI and have the gate rejection print the exact signature"
- [ ] reviewer-model-from-rigor-not-spec: paradigm=object-oriented defect="the reviewer model defaults to Haiku from the agent spec instead of resolving from the rigor profile, so a substance gate runs on weaker reasoning" proposed_solution="resolve the model from resolve_review_steps().model_for(step); populate the rigor profile and reconcile the spec Haiku line with rigor precedence"
- [ ] verify-slice-commit-needs-uv-run-interpreter: paradigm=object-oriented defect="bare des verify-slice-commit cannot resolve an interpreter for the E2 gate and records an honest Indeterminate that blocks the successor slice; only uv run works" proposed_solution="the gate resolves a usable interpreter itself (target-agnostic), or the skill documents the uv run prefix"
- [ ] fix-des-commit-verifier-case-insensitive: paradigm=object-oriented defect="git_commit_verifiers case-sensitive --grep finds only ~30% of intended commits on mixed-case Step-Id trailers" proposed_solution="add -i at git_commit_verifier.py:54 and relax the fixtures (~10 LOC)"
- [ ] scope-violation-xdist-pollution: paradigm=object-oriented defect="test_scenario_011_no_violations asserts delta==0 but a sibling xdist worker writes SCOPE_VIOLATION into a shared audit log concurrently" proposed_solution="per-worker scoped fixture (worker_id/tmp_path_factory) or an xdist_group"
- [ ] flaky-pbt-phase-warning: paradigm=object-oriented defect="test_any_non_schema_phase_produces_warning fails once under -n auto (shared .hypothesis cache and seed race) but passes single" proposed_solution="capture the falsifying input and use a per-worker .hypothesis directory"
- [ ] feature-end-ws-gate-applicability: paradigm=object-oriented defect="the WS-gate floor asserts the WS entry-point is in the installed set, so a scripts/cli dev/CI gate not shipped by the installer fails facet-1 and refuses feature-end though substantively complete" proposed_solution="WS-floor returns NOT_APPLICABLE not FAIL for non-installer-shipped entry-points"
- [ ] attest-bundled-slice-gate-too-lax: paradigm=object-oriented defect="attest_bundled_slice accepts --bundle-commit on any Slice-Id trailer presence not membership, so a doc-only anchor commit satisfies the gate" proposed_solution="require --bundle-commit be ancestor-of/equal-to the commit that introduced the slices .feature and emit at_source provenance"
- [ ] featureend-verdict-non-reverifiable: paradigm=object-oriented defect="FeatureEndReviewVerdict drops reviewer_agent_id from the emitted record so no auditor can recompute verdict_hash; verify-integrity accepts any 64-hex, making every DONE stamp non-re-verifiable" proposed_solution="store reviewer_agent_id and have verify-integrity recompute via sign_feature_end_review"
- [ ] ag-001-lifecycle-seam-untested: paradigm=object-oriented defect="apply_gate sys.exit(0)s an inactive project, so hook-gate tests that never activate the tmp project bypass the lifecycle silently, producing false-greens that masked real reds" proposed_solution="an AT pinning the gate-to-lifecycle composition"
- [ ] done-gate-environmental-e2e-overbroad: paradigm=object-oriented defect="feature-end requires EnvironmentalE2eVerified for every feature, so a pure-CLI feature returning misscoped from verify-environmental-e2e is blocked" proposed_solution="make the EnvironmentalE2eVerified requirement conditional on the feature-delta declaring a Environmental E2E block"
- [ ] verify-integrity-atdd-pure-mode-detection-miss: paradigm=object-oriented defect="des verify-integrity runs the classic roadmap path on a roadmap-free atdd_pure feature and errors roadmap.json not found" proposed_solution="read .nwave/config.yaml workflow.mode (or treat --feature-id plus absent roadmap as atdd_pure) and branch to ledger-verification before the roadmap check"
- [ ] doccoh-warned-detail-says-refused: paradigm=object-oriented defect="the advisory DocCoherenceWarned record reuses the gates exit-1 stdout as its detail, so a Warned record reads DocCoherenceRefused / REFUSED and self-contradicts" proposed_solution="relabel/strip the nested REFUSED framing to advisory wording in the warn path"
- [ ] shared-stash-pop-is-blind-fishing: paradigm=object-oriented defect="bare 'git stash pop' in a shared repo pops another instance's WIP into your tree as conflicts (hit twice)" proposed_solution="des verb stashing with an instance-scoped label that only pops by ref, plus a guard refusing a bare pop"
- [ ] subagent-stop-handler-15-mypy: paradigm=object-oriented defect="mypy reports 15 pre-existing type errors in subagent_stop_handler.py; the typecheck gate scope never reached the file" proposed_solution="fix the 15 errors; optionally widen the typecheck gate scope to cover the file"
- [ ] examine-degrades-to-a-test-rerun: paradigm=object-oriented defect="an unreachable examine surface mints a PASS (Vera re-ran pytest) instead of a loud INDETERMINATE, signing circular evidence" proposed_solution="des record-examine-verdict refuses a PASS whose observations name only test-suite invocations"
- [ ] nothing-stops-a-hand-made-slice-commit: paradigm=object-oriented defect="a plain git commit of slice paths is only caught reactively at verify-slice-commit after commit+push, leaving an unattested commit" proposed_solution="pre-commit hook refusing slice-path commits with no Slice-Id trailer, HOW naming des commit-slice"
- [ ] two-gates-disagree-on-the-same-at-attestation: paradigm=object-oriented defect="G-DISTILL-EXIT clears slice-01 while carpaccio-slice-gate rejects it — two gates give opposite verdicts on identical AT evidence" proposed_solution="one shared attestation predicate both gates consult"
- [ ] des-dispatch-cannot-produce-an-at-review-dispatch: paradigm=object-oriented defect="des dispatch --phase C_REVIEWER_AUDIT hardcodes nw-user-examiner with no source access, so the required independent AT-review dispatch cannot be generated" proposed_solution="add a --reviewer at-review path selecting nw-acceptance-designer-reviewer with source access"
- [ ] des-dispatch-charter-lane-drops-intent: paradigm=object-oriented defect="des dispatch --lane charter --intent silently discards the intent text, emitting a well-formed but instruction-less prompt (exit 0)" proposed_solution="render intent into the charter-lane template, or REFUSE --intent on a lane that cannot carry it"
- [ ] distill-exit-unsatisfiable-under-jit: paradigm=object-oriented defect="G-DISTILL-EXIT computes missing = all planned Slice-Plan rows minus verdict_signed, never scoped to slices authored this dispatch; unsatisfiable under per-slice JIT" proposed_solution="scope the denominator to slices authored in the current dispatch (via /nw-bugfix charter)"
- [ ] distill-exit-gate-crashes-on-bugfix-lane: paradigm=object-oriented defect="G-DISTILL-EXIT crashes (SlicePlanParseUnresolved) on any D_DISTILL return with no feature-delta.md, incl bugfix lanes by design; 47/167 fix-* dirs affected" proposed_solution="align _slice_plan_slice_ids with the FileNotFoundError fail-open its sibling _mechanical_seal_cleared_slices already implements"
- [ ] commit-slice-exits-zero-on-unverified-seal: paradigm=object-oriented defect="des commit-slice produces an unattested commit (Gate-Scope 64 zeros, SliceCommitUnverified) yet exits 0, so consumers read it as delivered" proposed_solution="exit non-zero on SliceCommitUnverified/GateScopeUnverified; first isolate whether empty per-slice arch scope is legitimate"
- [ ] archive-tag-discipline-was-claimed-not-done: paradigm=object-oriented defect="branch-hygiene 'tag before delete' has no mechanical check; tags were reported done but absent, branches survived only as dangling objects" proposed_solution="des verb that tags-and-deletes as one atomic operation"
- [ ] broken-tag-ref-blocks-gc: paradigm=object-oriented defect="refs/tags/v3.20.1 is a bad object; git gc fails ('failed to run repack'), auto-cleanup disabled, every git op warns" proposed_solution="repair/delete the broken tag ref, after the worktree-a5b7255 triage since gc will then collect dangling objects"
- [ ] declare-done-hook-false-positive-on-bugfix-dispatch-id: paradigm=object-oriented defect="des-declare-done-pre-push false-positives (FeatureUnreconciled) on fresh-worktree bugfix pushes, reading only the empty local gitignored ledger and treating trunk's un-recorded slices as skipped" proposed_solution="add the git-verifiable Slice-Id-trailer fallback already shipped for the carpaccio order-gate to this hook's check path"
- [ ] reverify-slice-commit-deleted-at-not-refused: paradigm=object-oriented defect="tests/des/acceptance/test_reverify_slice_commit.py Criterion 2 fails on the 2026-07-21 whole-tree CI run: an AT deleted post-commit is expected to emit SliceReverifyRefused but does not, on a clean GitHub-hosted runner (not a contention artifact)" proposed_solution="RCA needed -- reverify's post-commit AT-deletion detection path, compare against the test's exact expected event shape"
- [ ] gate-scope-digest-cargo-runner-derived-fail: paradigm=object-oriented defect="tests/build/gate_scope_digest_runner_agnostic/acceptance/test_gate_scope_digest_cargo.py fails: a cargo-target slice commit does not earn a RUNNER-derived Gate-Scope digest on the 2026-07-21 CI run -- likely related to the rust-at-kind AT-discovery facet-pair merged same session (fix-rust-regression-at-kind-wiring), needs confirmation via git-stash pre-merge baseline comparison" proposed_solution="RCA against commit_slice.py's native-regression digest routing added this session; confirm pre-existing vs introduced via baseline diff before fixing"
- [ ] commit-slice-cargo-digest-route-gherkin-scope-leak: paradigm=object-oriented defect="tests/bugs/des/test_commit_slice_pytest_regression_ignores_cargo_digest_route.py fails: a genuinely Rust gherkin-kind slice on a cargo-root repo does not clear des commit-slice -- the fix must stay scoped to --at-kind pytest-regression, never widen to every cargo-root commit. CONFIRMED pre-existing (reproduces identically on the pre-rust-at-kind-merge baseline via git-stash comparison, not a regression from tonight's merges)" proposed_solution="RCA the cargo-root-detection branch in commit_slice.py/verify_slice_commit_completeness.py -- likely the same digest-routing logic gate_scope_digest_cargo needs, fix both together"
- [ ] test-coupled-to-argparse-error-message-formatting: paradigm=object-oriented defect="CORREZIONE DI UNA MIA RIGA PRECEDENTE, che accusava la cosa sbagliata e avrebbe fatto cancellare un'asserzione CORRETTA. Avevo scritto che `tests/bugs/des/test_dispatch_generates_all_feature_end_cycle_phases.py` porta un'aspettativa stantia su E_BATCH_REFACTOR. FALSO: quel test SA gia' che la fase e' ritirata -- uno dei suoi test si chiama `test_e_batch_refactor_stays_refused_and_absent_from_choose_from` -- e asserisce correttamente che resti assente. LA CAUSA VERA, misurata 2026-07-22: il test e' ACCOPPIATO AL FORMATO DEL MESSAGGIO D'ERRORE DI ARGPARSE. `_CHOOSE_FROM_RE` (riga 157) e' `re.compile(r"\(choose from ((?:'[^']*'(?:, )?)+)\)")`, cioe' pretende le alternative FRA APICI. In locale su Python 3.12.3 argparse emette `choose from 'A_GREEN', 'C_REVIEWER_AUDIT', ...` e il file passa 9/9. Nello stderr catturato in CI le stesse alternative sono SENZA apici (`choose from A_GREEN, C_REVIEWER_AUDIT, ...`), la regex non matcha, e l'assert fallisce con `expected a '(choose from ...)' clause`. CPython ha cambiato quella formattazione in una release di patch: il test si rompe su un interprete diverso a COMPORTAMENTO DEL PRODOTTO IMMUTATO, e passa in locale mentre fallisce in CI per la sola differenza di patch. E' la classe piu' insidiosa di test fragile, perche' il verde locale convince che la CI sia rotta." proposed_solution="Disaccoppiare l'asserzione dalla formattazione della stdlib: estrarre l'insieme delle scelte con una regex tollerante agli apici (`'?([A-Z_]+)'?`) oppure, meglio, non parsare affatto il messaggio di argparse e asserire contro la SSOT delle fasi che il generatore stesso espone (il file ha gia' un test `test_generator_phase_choices_derive_live_from_feature_end_phases_ssot`, quindi la sorgente autorevole e' gia' raggiungibile senza passare dallo stderr). REGOLA GENERALE: un test che fa il parsing del messaggio d'errore di una libreria standard sta asserendo su un dettaglio che non e' contratto -- si rompera' a un upgrade di patch senza che nulla del prodotto sia cambiato. Vale la pena cercare altri parsing di messaggi stdlib nella suite. NOTA SEPARATA, ancora valida: E_BATCH_REFACTOR e' deprecata (decisione di Ale 2026-07-22, il suo lavoro lo fanno prefactoring e loop di consolidamento) e cinque file la nominano ancora come fase viva (nw-execute, nw-bugfix, atdd-pure-phase-sequence.schema.json, dispatch/atdd_pure.yaml, flavors/_schema.yaml). Quella pulizia resta da fare ma NON e' la causa di questo rosso CI."
- [ ] attest-bundled-slice-cascades-from-reverify-failure: paradigm=object-oriented defect="tests/des/acceptance/attest_bundled_slice/steps/test_slice_01_scaffold.py fails because its own assertion requires test_reverify_slice_commit.py's suite to re-run GREEN as a behaviour-preservation check -- this is a CASCADE from the reverify-slice-commit-deleted-at-not-refused defect above, not an independent bug" proposed_solution="fixing reverify-slice-commit-deleted-at-not-refused should resolve this one too; re-verify after that fix lands before treating this as separate"
- [ ] p4-tracked-before-fallback-orphan-recovery-fail: paradigm=object-oriented defect="tests/des/acceptance/reverify_p4_tracked_before_fallback/test_slice_01_p4_accepts_tracked_before_orphan.py fails: an orphan whose acceptance test was committed before its code is not correctly recovered" proposed_solution="RCA needed -- read the P4 tracked-before-fallback logic against this specific orphan scenario"
- [ ] prefactoring-lane-exemption-leak-to-plain-slice: paradigm=object-oriented defect="tests/des/cli/f_prefactoring_dispatch_clears_honestly/test_bugfix_exit_gate_honors_prefactoring_lane.py fails: an UN-annotated 0-AT slice-commit is not refused at E2 as expected -- possible sign the @prefactoring lane exemption leaks to a plain slice carrying no lane annotation, a real safety-gate leak if confirmed" proposed_solution="RCA the E2 exit-gate's prefactoring-lane detection -- confirm whether it's over-matching plain slices as exempt"
- [ ] feature-end-batch-refusal-reason-missing-feature-root: paradigm=object-oriented defect="tests/des/acceptance/fix_feature_end_ws_gate_applicability/steps/test_gate_refusal_real_reason.py fails: expected 'feature_root' to appear in the batch-eligibility precheck's refusal reason text, but it's absent from the actual message" proposed_solution="RCA the batch-eligibility precheck's refusal-reason string construction -- likely a simple message-formatting gap, WHAT/WHY/HOW discipline applies"
- [ ] python-contract-gate-scope-empty-on-foreign-repo: paradigm=object-oriented defect="the contract gate's Python scope selects tests via -m 'unit or integration or acceptance', a marker convention nWave-dev's own conftest auto-applies per directory -- a target repo without that conftest (measured: Tsunami, 78 tests, ALL deselected) has zero tests match, so its Python scope is empty by construction and every slice on that repo seals SliceCommitIndeterminate, always (found by sister alpha-challenge dogfooding the gate against a foreign repo). Secondary unconfirmed suspect logged alongside: the verdict names reason=gate_scope_interpreter_unavailable while the observed cause is a collection exit-code mismatch (her CLI probe exits 5/zero-collected, the gate's in-process collection reportedly exits 2/collection-error) -- two different probes, not yet reconciled" proposed_solution="derive the Python scope from the target's own test layout/config (pytest.ini markers, pyproject testpaths) instead of assuming nWave-dev's unit/integration/acceptance convention exists; degrade-LOUD (not silent-Indeterminate) when no marker convention is found so the operator knows scope is undetermined, not empty-and-passing-by-omission; separately reconcile the interpreter_unavailable vs collection-exit-code naming mismatch before trusting it as root cause"
- [ ] des-refactor-pile-no-dry-run-validate: paradigm=object-oriented defect="`des refactor --pile` has NO dry-run/validate mode: an operator cannot check a pile file's row grammar (item_id / paradigm closed-set / defect / proposed_solution) before committing to a real drain that creates worktrees, dispatches agents and moves rows. The grammar refusal only surfaces AT the drain wall (GDP-2: reactive-at-gate, no proactive-inline affordance). Found while fixing the paradigm-field mismatch that silently refused all 10 pile rows. EVIDENZA DI RICORRENZA 2026-07-22, e il costo che il --check-only avrebbe evitato: al pre-volo manuale prima del primo drain reale, 6 righe su 16 di techdebt.md sono risultate MALFORMATE -- `paradigm=object-oriented smell` invece di `paradigm=object-oriented`, con la parola della tassonomia degli smell colata nel campo del paradigma. E' la TERZA variante della stessa classe trovata in una giornata (le altre due: `object-oriented/DRY violation` e la mancanza totale del campo). Senza il pre-volo -- che ho dovuto scrivere a mano perche' il comando non lo offre -- il drain avrebbe rifiutato quelle 6 righe AL MURO, cioe' dopo aver gia' creato i worktree, provisionato gli ambienti e dispacciato gli agenti. Il pattern e' ormai dimostrato: il campo `paradigm` viene riempito da chi scrive la riga con la CLASSE del difetto invece che col paradigma del progetto, e nulla lo intercetta finche' non e' troppo tardi." proposed_solution="Add `des refactor --pile <f> --check-only` (or `--dry-run`) that parses every row through the same SSOT (`select_paradigm_lens`, row regex) and reports per-row accepted/refused with the what/why/how, exiting non-zero if ANY row is malformed -- zero worktrees, zero dispatch, zero mutation. Same shape as the other `--check-only` gates."
- [ ] lyra-relay-voice-ask-hangs-on-unregistered-peer: paradigm=object-oriented defect="`voice_ask` to a peer that is NOT in the relay registry BLOCKS until the harness times the tool out, instead of failing fast. Measured 2026-07-22: `voice_peers` listed exactly ONE session (my own, `connected=0s ago` -- the registry had just reset), yet `voice_ask --peer alpha-challenge` did not refuse; it hung and returned `The operation timed out`, burning a full tool-timeout window and giving zero diagnostic. The caller cannot distinguish 'peer offline' from 'peer busy' from 'relay broken' -- all three present as the same silent hang (GDP-3: no what/why/how; GDP-6: indeterminate dressed as a failure)." proposed_solution="Make voice_ask fail FAST and LOUD when the target peer is absent from the registry: check membership before enqueueing and return a structured refusal naming WHAT (peer <name> is not registered), WHY (it has not called voice_identify in this relay generation / the relay restarted), and HOW (list currently-registered peers, retry when it registers, or queue explicitly with an opt-in flag). Never let peer-absence present as a timeout."
- [ ] des-dispatch-advisory-fires-despite-own-lane-exemption: paradigm=object-oriented defect="`des dispatch --lane bugfix` prints an unconditional readiness advisory ('the feature-delta for X is not readiness-ready -- no feature-delta.md found; fix it before dispatching the crafter (the readiness gate will otherwise reject it)') on the SAME invocation whose own generated output already carries the `DES-LANE: bugfix` + `DES-LANE-JUSTIFICATION` marker pair that EXEMPTS a bugfix-lane dispatch from the feature-delta requirement. The tool warns about a rejection its own output prevents. REPRODUCED 2026-07-22 on a probe feature-id with no feature-delta: the advisory and the two exemption markers appear in the same stdout. Found by sister alpha-challenge on fix-corpus-reseed-gate; cost real hesitation (had to re-read the advisory against the generated markers to confirm the dispatch would work). Same class as tonight's paradigm-field mismatch: two surfaces of one tool contradicting each other." proposed_solution="Condition the readiness advisory on the exemption-granting flags of THIS invocation: if `--lane bugfix` (or any other flag whose generated markers grant the exemption) was passed, either suppress the advisory or reword it to state the exemption explicitly -- e.g. 'feature-delta absent; --lane bugfix exempts this dispatch from that requirement, no action needed'. Derive the condition from the SAME predicate the readiness gate uses to honour the exemption, so the advisory and the gate cannot drift apart (point at one predicate, never restate it)."
- [ ] standing-loops-die-silently-on-session-restart: paradigm=object-oriented defect="The nine standing loops are session-scoped by construction, so a restart silently disarms ALL of them and nothing mechanical signals the loss. Measured 2026-07-22 across three sibling instances after a restart: one substituted its own judgement for the mandated ask ('this is daemon triage, not nWave delivery, so the default is off') thirty seconds before starting a delivery; one never connected 'session restarted' to 'the cadence that fired all night just stopped' and resumed ad-hoc; only one performed the batched ask. The guard is prose, and the prose said 'do this ONCE per session' -- that 'once' is exactly what lets an instance file it as done and never re-verify." proposed_solution="FIXED IN PROSE (no new machinery -- Ale, 2026-07-22): the catalog now states that the ARRIVAL of the injected block IS the trigger to re-check, every time it arrives, because the block is injected on SessionStart AND UserPromptSubmit precisely because loops die silently; only a tool call in the CURRENT session satisfies the step. Plus two new red-flag rows carrying the two measured rationalisations verbatim (the offer-instead-of-ask, and the my-own-scheduling excuse). Persisting an armed-loop declaration was considered and REJECTED as machinery where prose suffices."
- [ ] nwave-install-rewrites-shared-runtime-in-place: paradigm=object-oriented defect="`nwave_ai.cli install` rewrites the SHARED runtime tree (~/.claude/lib/python, plus skills/agents) IN PLACE, so any concurrent `des` invocation during the write window observes a PARTIAL tree and fails with an import error it correctly self-diagnoses as a corrupted or interrupted installation. MEASURED 2026-07-22 on a box shared by three sibling instances: a sister's `des bugfix-pipeline-tick` died with 'cannot import name ClaudeCodeTaskAdapter from des.adapters.driven (unknown location)' during another instance's reinstall; a process already running survived because its modules were already imported, so the breakage hit only NEW invocations -- which is what made it look like a package regression rather than a race. This recurs on EVERY reinstall by ANY instance, and the shared runtime means the blast radius is every other instance on the box."  AGGRAVAMENTO MISURATO 2026-07-22 (seconda occorrenza, blast radius piu' grande di quello descritto sopra): un reinstall `--dev` lanciato con QUATTRO corsie di sotto-orchestratori vive ha fatto cadere dal registro della sessione TUTTI i 35 tipi di agente `nw-*` (acceptance-designer, user-examiner, software-crafter, product-owner, ogni reviewer). I file su disco erano e sono INTATTI -- 51 agenti in ~/.claude/agents/nw/, frontmatter valido, verificato subito dopo -- quindi non e' corruzione: e' lo scanner degli agenti che ha osservato l'albero A META' mentre l'installer lo riscriveva in-place. Quindi la finestra di incoerenza non colpisce solo le NUOVE invocazioni `des` (come descritto sopra): fa perdere CAPACITA' a una sessione gia' in corso, e la perdita non si auto-ripara perche' il registro dei tipi di agente e' per-sessione. Effetto a valle: ogni corsia che dispaccia un agente di wave (l'AT dall'acceptance-designer, l'examine di Vera, il charter dal PO fresco) fallisce, e il rischio vero non e' il fallimento -- e' che l'agente bloccato SURROGHI la separazione (scrivendosi l'AT da solo, auto-esaminandosi) per sbloccarsi, perdendo esattamente le indipendenze che rendono valido il risultato." proposed_solution="Make the install ATOMIC from a reader's point of view: stage the whole tree into a temporary sibling directory, then swap it in with a single atomic operation (rename, or repoint a symlink), so a concurrent reader sees either the OLD tree complete or the NEW tree complete, never a half-written one. That removes the incoherence window entirely and, with it, the need for instances to coordinate reinstalls at all. Until then the mitigation is social and therefore unreliable: announce before reinstalling, not only after."
- [ ] record-verdict-cli-writes-artifact-key-readers-expect-observations: paradigm=object-oriented defect="CORREZIONE di una riga precedente di questo stesso pile, che accusava la cosa sbagliata. Il difetto NON e' `des record-review-verdict` che accetta un REJECTED con observations vuoto: il reviewer ha chiarito (2026-07-22) che la CLI espone `--artifact` e NON `--observations`, quindi il testo completo dei findings ERA nel record, sotto la chiave JSON `artifact` di .nwave/telemetry/review/<feature>.jsonl. A leggere il campo sbagliato ero io. Il difetto reale e' quindi un MISMATCH DI SCHEMA fra produttore e consumatore: chi scrive usa `artifact`, chi legge cerca `observations`, e il risultato osservato e' indistinguibile da un dato mancante -- ho concluso `REJECTED senza ragioni` e ho fatto ri-trasmettere findings che erano gia' su disco. Costo misurato: un round-trip completo con l'agente di review. La sorella `des record-examine-verdict` usa invece `--observations`: due recorder gemelli con nomi di campo divergenti per la stessa identica cosa." proposed_solution="Unificare il nome del campo fra i recorder gemelli (`record-review-verdict` e `record-examine-verdict`) su UN solo termine, con l'altro accettato come alias deprecato per non rompere i record esistenti; e far leggere ai consumatori il nome canonico da un punto solo invece di indovinarlo. Fino ad allora, il lettore che non trova il campo atteso deve dire QUALI chiavi il record porta davvero (what/why/how) invece di riportare il campo come vuoto -- un `REJECTED senza ragioni` e' una diagnosi troppo grave per derivarla da una chiave assente."
- [x] refactor-drain-has-no-shipped-agent-cmd-executable: paradigm=object-oriented defect="`des refactor --pile` is a COMPLETE, green mechanism (RefactorDrainService + GitWorktreePort/AgentInvocationPort/EnvProvisionPort, 29/29 tests) whose last mile is missing: `--agent-cmd` is a shell-command TEMPLATE run against a REAL executable (ShellAgentInvocationAdapter.probe does shutil.which(tokens[0]); invoke substitutes {prompt}/{worktree} and subprocess-runs it) -- but NO such executable is shipped anywhere in the repo. The only concrete values that exist are `true` in the acceptance tests and placeholders in the docs (`'<cli>'`, `'<crafter dispatch command>'`). The standing drain loop instructs the orchestrator to invoke the command with an argument that has no real value, so the loop is UNEXECUTABLE by construction: the pile can never drain. MEASURED 2026-07-22: techdebt.md carries 10 pending rows, all now grammar-valid after the paradigm fix, and still nothing can drain them. This is the catalogued-not-wired class one level up -- the mechanism IS wired, the ACTUATOR is absent." proposed_solution="Ship the actuator and document it as THE value of --agent-cmd: a small executable wrapper that receives the rendered prompt path and the worktree path and drives a headless crafter in that worktree (`claude` is on PATH, so `claude -p` with the rendered prompt is the obvious substrate). Put it under scripts/ with the {prompt}/{worktree} contract stated at the top, reference it from the des-command-catalog entry and from the drain loop so the loop's instruction names a command that EXISTS, and cover it with an AT that drains one real item end to end (the current ATs prove the lifecycle with `true`, which by construction cannot catch an absent actuator). FIXED 2026-07-22: scripts/refactor_agent.py shipped as the actuator (probe-clean, cwd-aware, what/why/how on every failure branch, exit 2 malformed input / 3 missing CLI / fixer code propagated -- verified by injection). Documented as THE --agent-cmd value in des-command-catalog.md and in the drain loop, replacing the placeholders."
- [ ] feature-end-collect-error-signs-as-suite-not-applicable: paradigm=object-oriented defect="A FALSE-GREEN in the feature-end TERMINAL gate. `_repo_has_contract_suite` (src/des/application/feature_end_cycle_service.py) catches `(_CollectionError, OSError, InterpreterUnavailable)` and returns `False` = NOT_APPLICABLE for all three, so a repo whose pytest collection ERRORS is treated as a repo that HAS NO SUITE -- the cycle emits FullSuiteLegNotApplicable, PROCEEDS, and SIGNS a verdict with the full suite never run. Correct only for InterpreterUnavailable (ontological absence: no pytest at all); WRONG for _CollectionError (epistemic gap: pytest ran and failed). The same function ~10 lines below already returns FullSuiteLegIndeterminate for the marker-agnostic path with a DDD-CERT-3 comment stating epistemic absence must never be conflated with ontological absence -- the handler violates the rule the rest of the function states. MEASURED 2026-07-22: _repo_has_contract_suite(Path('.')) returned False on nWave-dev (8084 tests); `des feature-end run-batch` then exited 0 with leg_census {ran: 0, not_applicable: 4} and signed autonomous-consolidation-and-bugfix-loops. The worker ALREADY emits the diagnostic (NWAVE_COLLECT_SCOPE_ERROR with pytest_exit_code + crashing_module) and the handler throws it away, so the operator gets a silent pass with zero signal." proposed_solution="Split the handler: InterpreterUnavailable keeps returning False (NOT_APPLICABLE); _CollectionError/OSError return FullSuiteLegIndeterminate carrying pytest_exit_code + crashing_module from the worker's NWAVE_COLLECT_SCOPE_ERROR payload, with a GDP-3 what/why/how whose HOW names the command that reproduces the collect. Cover BOTH directions with ATs (collect-errors -> INDETERMINATE, no signed verdict; genuinely pytest-less repo -> NOT_APPLICABLE, cycle proceeds) -- one direction alone cannot catch this class."
- [ ] stale-build-dir-shadows-src-in-whole-tree-collect: paradigm=object-oriented defect="A stale generated `build/` directory at the repo root SHADOWS `src/` during the gate's whole-tree collection. MEASURED 2026-07-22 on nWave-dev: 92 collection errors, every one `ImportError: cannot import name 'AtDiscoveryResult' from 'des.ports.test_runner_port' (<repo>/build/lib/des/ports/test_runner_port.py)` -- an 11-day-old build artifact winning over the live source and hiding a symbol that exists in src/. Plain `pytest --collect-only` is UNAFFECTED (it honours pyproject testpaths), which is what makes this invisible until the gate runs: only the worker, which passes the repo ROOT as the collection path, descends into build/. Combined with the sibling defect above it produced a signed feature-end with zero legs run. The exclusion intent ALREADY EXISTS -- `build` is a member of _CONTRACT_SUITE_PRUNE_DIRS (feature_end_cycle_service.py:1205+) -- but that set is applied ONLY to the SECONDARY marker-agnostic collect; the PRIMARY collect passes the repo root unpruned." proposed_solution="Make the primary collect honour the same _CONTRACT_SUITE_PRUNE_DIRS the secondary already does, so a generated artifact can never shadow source inside a gate's own collection. Point both paths at ONE prune set rather than restating it. Immediate operator workaround (not the fix): rm -rf build/ -- it is gitignored and regenerable."
- [ ] examine-seal-algorithm-changed-twice-retroactively-no-migration: paradigm=object-oriented defect="`charter_seal` changed its definition TWICE, each change silently invalidating every seal recorded before it, with no migration and no distinguishing diagnostic. History: 2026-07-03 (8060eb091) seal = sha256 of the WHOLE file; 2026-07-09 (001a22310) excludes the append-only `## Session log` section (exact-string partition); 2026-07-20 (041172d80) tolerates heading variants -- which re-invalidates every charter whose heading was a variant. MEASURED 2026-07-22: 44 records across ~25 features carry a seal that no longer verifies; the gate reports 'the charter changed after its feature-end examination' for charters that have not changed by a single byte -- the ALGORITHM changed, not the artifact. Those features can never pass feature-end, a strong candidate explanation for a scorecard reading 0 DONE. The blocked-then-unblocked case is provable: a record whose seal equals sha256(WHOLE file) is byte-identical-since-recording, which is the tamper-evidence property INTACT, merely computed under an older rule." proposed_solution="Make the VERIFIER accept a recorded seal that matches ANY historical seal algorithm over the current bytes (whole-file, exact-heading partition, heading-variant regex): each match still proves the sealed region is byte-identical since recording, so this is backward compatibility, not a weakening -- a genuinely edited charter still matches none of them. Then make the refusal message DISTINGUISH the two cases: 'recorded under a superseded seal algorithm (re-record with des record-examine-verdict)' vs 'the charter genuinely changed'. Structural lesson: a tamper-evidence primitive is a WIRE FORMAT -- changing it needs a version tag in the record plus a migration, never a silent redefinition."
- [ ] stop-hook-never-reconciles-coveragemap-na-markers: paradigm=object-oriented defect="DERIVA FRA DUE IMPLEMENTAZIONI SPECULARI della stessa regola, che blocca il feature-end di OGNI feature atdd_pure in questo stato del repo. `_missing_feature_end_cycle_records` (src/des/adapters/drivers/hooks/subagent_stop_handler.py:1830-1882) riconcilia SOLO `FullSuiteLegNotApplicable` -> `FullSuiteLegRan` (righe 1873-1874). La CLI speculare `verify_deliver_integrity.py:723-724` riconcilia ANCHE `CoverageMapNotApplicableAtDistillExit` -> `...VerifiedAtDistillExit` e il gemello Deliver-exit. Con `coverage_map_adoption` inattivo -- lo stato REALE di questo repo e il percorso CORRETTO per progetto (feature_end_cycle_service.py:984-995) -- il sistema emette per costruzione i marker NotApplicable e non emettera' MAI i Verified: il hook quindi pretende record che non possono esistere. MISURATO 2026-07-22: un agente di review e' rimasto bloccato in un loop di Stop-hook con 7+ firing identici su FeatureEndCycleIncomplete, senza alcuna azione disponibile per uscirne, finche' non l'ho terminato a forza; nel frattempo il suo `des verify-integrity` tornava PULITO (FeatureReconciled) perche' la CLI la riconciliazione ce l'ha. Terzo esemplare della stessa deriva: il docstring di `AtCompletionLedger.coverage_map_touchpoint_events()` (at_completion_ledger.py:1338-1341) DICHIARA gia' che la riconciliazione accetta i marker NA al posto dei record verified -- la prosa promette la sostituzione, il codice che consuma quel set non la esegue mai. SECONDA RIPRODUZIONE CONFERMATA 2026-07-22, con costo misurato: un `nw-software-crafter-reviewer` in ritorno da F_FINAL_REVIEW e' rimasto intrappolato ripetendo il proprio verdetto SEI volte ("REJECTED -- final, unchanged, sixth identical repeat. Stopping here; no tool in this dispatch can satisfy this hook"). Ha ENUMERATO i record che il hook pretendeva -- FeatureEndReviewVerdict, EBatchRefactorCompleted, EnvironmentalE2eGateRan, FullSuiteLegRan, WalkingSkeletonGateRan e i DUE CoverageMapVerified* -- ed ha diagnosticato correttamente la propria trappola: ruolo read-only, Bash limitata a `tsunami ...`, nessuna CLI `des`, nessun tool di scrittura. Quei record sono OUTPUT DELL'ORCHESTRATORE, non del reviewer: il gate e' puntato sul destinatario sbagliato. Costo misurato: il PRIMO run dello stesso reviewer ha restituito un messaggio finale COMPLETAMENTE VUOTO dopo 33 chiamate a tool e 178k token, e il verdetto e' stato estratto solo riprendendo l'agente. Sospetto forte (non ancora provato) che il messaggio vuoto sia lo stesso hook che mangia il report -- il che spiegherebbe anche il difetto gia' registrato "un verdetto REJECTED si registra senza alcuna ragione", che risulta quindi RICORRENTE e non isolato. Aggiungere al fix: il gate di completezza del ledger deve essere puntato sul RUOLO che puo' soddisfarlo, e un ritorno di un agente read-only non deve mai essere trattenuto per record che quel ruolo e' strutturalmente incapace di scrivere." proposed_solution="NON aggiungere solo le due righe mancanti: il difetto di fondo e' che la riconciliazione vive in DUE posti e uno si e' dimenticato di crescere, quindi il prossimo marker NotApplicable ricrea il buco identico. Estrarre UNA mappa di riconciliazione condivisa, dichiarata una volta e letta sia dal hook sia dalla CLI, cosi' la deriva diventa strutturalmente impossibile invece che vietata a parole. AT: un repo con coverage_map_adoption inattivo il cui ledger porta i due marker NotApplicable deve superare il check; oracolo negativo, un ledger che non porta ne' i NotApplicable ne' i Verified deve continuare a essere segnalato incompleto (il fix non deve diventare un passa-tutto)."
- [ ] dirty-tree-worktree-guard-has-zero-real-git-coverage: paradigm=object-oriented defect="La guardia che impedisce a `des verify-worktree-cleanup` di cancellare un worktree con lavoro NON committato ha ZERO copertura contro git reale. `GitWorktreeAdapter.has_uncommitted_changes` (src/des/adapters/driven/refactor/git_worktree_adapter.py:195-202, avvolge `git status --porcelain`) e' esercitata solo da (a) la truth-table pura su `classify_worktree_cleanup_state` chiamata con booleani nudi e (b) un FakeGitWorktree in memoria (tests/des/unit/application/test_worktree_cleanup_dirty_guard.py): nessuno dei due guida il parsing reale di git status ne' la CLI vera contro un worktree davvero sporco. Il commit che introduce la guardia (e1e38fc7b) dice nel proprio messaggio che il bug chiuso era stato `observed three times... during development` -- cioe' un worktree con lavoro reale rimosso a forza, perdita di dati. E' il rischio di regressione piu' grave dell'intera feature ed e' l'unico non verificato contro la realta'. Asimmetria che lo prova: il bugfix GEMELLO (falso-positivo non-diverged-ancestor, 17cd2a60a) un test di regressione con git vero ce l'ha (tests/des/acceptance/parallel_work_cleans_up_after_merge_back/test_nondiverged_ancestor_regression.py). Trovato dalla deep review di feature-end del keystone parallel-work-cleans-up-after-merge-back; e' il blocker D1 del suo verdetto REJECTED." proposed_solution="Aggiungere un test di regressione con git REALE speculare a test_nondiverged_ancestor_regression.py: creare un worktree via GitWorktreeAdapter, scriverci dentro un file untracked/modificato, eseguire verify-worktree-cleanup in modalita' ACT, e asserire che il worktree SOPRAVVIVE intatto con verdict=HAS_UNCOMMITTED_CHANGES. Il test deve fallire se la guardia viene rimossa -- e' l'unico modo di sapere che protegge davvero invece di sembrare protettiva."
- [ ] check-port-realization-silently-ignores-positional-catalog-arg: paradigm=object-oriented defect="`validate_language_adapter_catalog --check-port-realization` ACCETTA un argomento posizionale `catalog.yaml` e lo IGNORA in silenzio: in quella modalita' il gate legge dal registry degli entry-point `nwave.lang.adapter`, non da un file. MISURATO 2026-07-22 dall'examine di Vera sul charter `a-maintainer-registers-a-language-and-failures-name-what-to-implement`: l'invocazione CON e SENZA il percorso produce output IDENTICO (exit 0, `4 plugin(s) probed, 0 gap(s)`), e passare un percorso INESISTENTE non produce alcun errore. Il gate funziona; il problema e' che l'operatore che passa un catalogo crede di aver ristretto il controllo a quel file e non lo ha fatto, senza alcun segnale. E' la forma piu' insidiosa di silent-wrong: non un risultato sbagliato, ma un INPUT accettato e buttato via, che lascia credere a un ambito che non esiste. Non e' un difetto di funzionamento della gamba -- e' un difetto della superficie." proposed_solution="Rifiutare LOUD l'argomento posizionale quando `--check-port-realization` e' attivo, con un what/why/how che dica COSA e' stato ignorato (il percorso), PERCHE' (in questa modalita' la sorgente di verita' sono gli entry-point registrati, non un file di catalogo) e COME ottenere l'ambito voluto (`--plugin module:Class` per sondare uno specifico plugin, anche non registrato). In alternativa renderlo mutuamente esclusivo a livello di argparse, cosi' l'errore arriva prima che il gate parta. Mai accettare in silenzio un input che non si usa."
- [ ] regression-gate-resolves-nwave-dev-interpreter-not-target: paradigm=object-oriented defect="VIOLAZIONE DEL MANDATO DI AGNOSTICISMO DALLA MACCHINA TARGET, riprodotta direttamente da sister alpha-challenge 2026-07-22. L'attestazione comportamentale E2 di `des commit-slice --at-kind pytest-regression` (verify_slice_commit_completeness.py::_run_regression_gate) invoca `des_spawn('pytest', 'pytest', str(test_path), '-q', cwd=repo, ...)`, ma `des_spawn` NON propaga `repo_root=repo` a `python_for(capability)`. Su un repo CONSUMER il cui `.venv` sta altrove rispetto al pacchetto `des` installato, `python_for('pytest')` senza `repo_root` scende la scala generica dei candidati e sceglie IL VENV DI NWAVE-DEV (`/home/alexd/Projects/nWave-dev/.venv/bin/python3`) invece di quello del repo target. Quell'interprete non ha le dipendenze del target (osservato: `ModuleNotFoundError: No module named 'pgserver'`), pytest esce 2 (collection error), e `des commit-slice` RIFIUTA la slice con un `E2 behavioral attestation failed` SPURIO mentre il test di regressione passa perfettamente sotto l'interprete giusto. Il contrasto che lo prova: `pytest_contract_gate_adapter.py::collect_scope`/`run_suite` chiamano correttamente `python_for(None, repo_root=repo)` -- `_run_regression_gate` e' l'UNICO call site rimasto senza quella sterzata. Impatto: su ogni repo target che non sia nWave-dev, la gamba E2 della corsia pytest-regression e' rotta per costruzione, e il rifiuto NON dice che ha usato l'interprete sbagliato -- l'operatore vede un test verde rifiutato e non sa perche' (aggirato da alpha con --skip-verify + verifica manuale, cioe' il gate e' stato bypassato per poter lavorare)." proposed_solution="Propagare `repo_root=repo` da `_run_regression_gate` fino a `python_for(capability)` attraverso `des_spawn`, allineandosi al call site gia' corretto in pytest_contract_gate_adapter. Poi: rendere STRUTTURALMENTE impossibile ripetere l'omissione -- se `des_spawn` puo' risolvere un interprete, `repo_root` dovrebbe essere un parametro OBBLIGATORIO (o derivato da `cwd`, che questo call site gia' passa), non opzionale-e-silenziosamente-generico. Aggiungere inoltre al messaggio di rifiuto E2 QUALE interprete e' stato usato e da dove e' stato risolto: un rifiuto che non nomina l'interprete rende questa classe invisibile. AT: un repo target con un venv proprio e una dipendenza assente da nWave-dev deve far passare la gamba E2 usando il PROPRIO interprete."
- [ ] commit-slice-post-commit-verify-hangs-with-no-child-work: paradigm=object-oriented defect="`des commit-slice` si appende nella fase di verify POST-commit. Osservato DUE volte identico da sister alpha-challenge 2026-07-22: il commit atterra correttamente (trailer `Slice-Id` e `Gate-Scope` presenti, confermato da git log), poi il passo di ri-verifica resta vivo 30+ minuti con ZERO lavoro figlio attivo -- nessun pytest, nessun processo di supporto, il processo in stato sleeping. Non e' lentezza sotto contesa (che avrebbe figli attivi che macinano): e' genuinamente fermo. Killato dopo 30+ minuti in entrambi i casi; il commit era gia' valido, ad appendersi era solo la ri-verifica ridondante. Causa non ancora trovata -- candidati non verificati: un lock non rilasciato, un subprocess zombie che non segnala la propria uscita, un wait su un pipe che nessuno chiude. Gravita': e' il gate che chiude OGNI slice-commit, quindi ogni slice paga il rischio di uno stallo da mezz'ora, e l'operatore non ha modo di distinguere 'sta lavorando' da 'e' morto' -- assenza totale di segnale di progresso." proposed_solution="Tracciare lo stallo (py-spy dump sul processo appeso, o strace, per vedere su cosa e' bloccato) e chiudere la causa. Indipendentemente dalla causa: dare alla fase di verify un TIMEOUT esplicito con degrade-LOUD (INDETERMINATE con what/why/how, mai attesa infinita) e un heartbeat di progresso, perche' un gate senza segnale di vita e' indistinguibile da uno morto e costringe l'operatore a scegliere fra aspettare per sempre e killare alla cieca. Il commit essendo gia' valido, il verify appeso non deve poter bloccare il lavoro."
- [ ] commit-slice-all-stages-out-of-scope-changes-without-warning: paradigm=object-oriented defect="`des commit-slice --all` fa `git add -A` e mette in staging QUALUNQUE cosa si trovi nel working tree, senza confrontarla con l'ambito dichiarato della slice. Near-miss reale 2026-07-22 (sister alpha-challenge): un sotto-orchestratore ha usato `git reset <main-sha>` invece di merge/rebase per prendere il trunk -- un reset misto sposta HEAD e index ma lascia il working tree STALE, quindi `git status` mostrava come cancellati file reali gia' mergiati di ALTRE DUE feature in volo (codice di produzione, charter, docs). `commit-slice --all` stava fedelmente ri-stagionando quello stato corrotto quando e' stato ucciso a mano. Nota di onesta' importante: la causa radice NON e' commit-slice -- e' stato alpha stessa a ritrattare la prima diagnosi dopo aver letto il reflog (garbage in, garbage out). Il difetto qui e' diverso e piu' modesto: il gate non ha alcun controllo di sanita' d'ambito, quindi non puo' distinguere 'la slice tocca questi file' da 'il working tree e' corrotto e sto per committare la cancellazione del lavoro altrui'. E' stato un umano ad accorgersene, notando che l'hash di HEAD sembrava sbagliato." proposed_solution="Prima dello staging, confrontare i file modificati/cancellati con l'ambito dichiarato della slice (i `files_to_modify`/`boundary_rules` del suo dispatch originale) e AVVISARE -- o rifiutare -- quando un file FUORI da quell'ambito e' in staging per cancellazione o modifica. Una cancellazione fuori-ambito e' il segnale piu' forte e il piu' economico da controllare. Cosi' questa classe di corruzione viene intercettata meccanicamente invece che dipendere da un umano che nota un hash strano. Attenzione a calibrare: l'ambito dichiarato non e' sempre completo, quindi la forma giusta e' probabilmente warn-loud + richiesta di conferma esplicita sulle cancellazioni fuori-ambito, non un rifiuto secco che renderebbe il gate inusabile."
- [ ] arch-invariant-tier-selected-by-directory-not-by-assertion: paradigm=object-oriented defect="IL VETO DISCRIMINA CONTRO IL LAVORO DI CONSEGNA: colpisce le slice di FEATURE e risparmia i bugfix, quindi e' massimamente dannoso dove si consegna valore -- e' questo, non la meccanica, a determinarne la priorita'. IL TIER DEGLI INVARIANTI ARCHITETTURALI E' SELEZIONATO PER POSIZIONE, NON PER CIO' CHE IL TEST ASSERISCE, e la conseguenza e' che l'AT correttamente-rossa di una feature NON CONSEGNATA nega l'attestazione a OGNI slice commit dell'albero. `run_contract_gate._arch_invariant_paths` (riga 593) restituisce la directory `tests/build` INTERA -- nessun filtro per marker, nessuno scoping per feature. Il suo stesso docstring dice che l'insieme era stato ratificato come 'the tests/build directory (the F-D-09 forbidden-roots gate + the inline-interpreter-spawn ban live there)', cioe' test di boundary architetturale. Ma `tests/build/` oggi ospita ~19 suite di ACCETTAZIONE DI FEATURE accanto ai ~20 file di invarianti veri (gate_scope_digest_runner_agnostic/, language_port_realization_gate/, f_nonbypassable_attestation/, commit_slice_auto_stages_charter/, e altre): le suite di feature hanno colonizzato il tier architetturale e il gate non sa distinguerle. MISURATO 2026-07-22 da sister multilang: con il solo `test_gate_scope_digest_cargo.py` deselezionato, `NWAVE_TEST_RESULT:{passed:881,failed:0,skipped:8,exit_status:0}` -- 881 su 881 invarianti veri verdi. L'unico rosso e' lo scaffold active-RED di `gate-scope-digest-runner-agnostic` slice-01, feature `pending`, che si comporta ESATTAMENTE come atdd_pure prescrive (active-RED, non @skip). Effetto: la gamba E2 di `des commit-slice` non distingue 'un invariante architetturale e' regredito' da 'l'AT active-RED di una feature non consegnata sta facendo il suo lavoro', quindi la feature X veta il commit della feature Y. Auto-referenziale per giunta: il test rosso riguarda il digest DI commit-slice, quindi commit-slice e' bloccato dal test fallito di commit-slice. AMBITO ESATTO DEL VETO, MISURATO NEL CODICE 2026-07-22 (prima lo avevamo descritto sbagliato in DUE modi diversi): il discriminante NON e' cosa tocca il commit, e non e' la feature -- e' l'`at_kind`. `verify_slice_commit_completeness.py:301-303` (help di --at-kind) e il commento di dispatch E2 a :1383-1390 lo dicono esplicitamente: `pytest-regression` SOSTITUISCE il gate feature-scoped con un'attestazione COMPORTAMENTALE che esegue il file di regressione e ne usa l'exit code come verdetto E2. Quindi: le slice GHERKIN (il default, cioe' ogni slice di FEATURE) passano da `_mode_feature_scoped` -> `_arch_invariant_paths(repo)` INTERO -> vetate da qualunque rosso sotto tests/build/, ANCHE se la slice e' docs-only e non tocca nulla; le slice PYTEST-REGRESSION (bugfix) sono ESENTI PER COSTRUZIONE perche' il gate non viene ristretto, viene RIMPIAZZATO. Prova incrociata su due casi reali: `d0aa4c9a6` (bugfix, AT sotto tests/bugs/) attesta ed espone la firma del meccanismo -- ZERO eventi BuildTier* nel ledger, perche' quel gate non e' proprio girato; `d344ec608` (feature gherkin, un solo file .md modificato) viene rifiutata citando tests/build/**. LA FRASE CORRETTA, da usare al posto delle due sbagliate: il rosso veta l'ATTESTAZIONE di ogni slice GHERKIN tree-wide indipendentemente da cosa tocca; le slice di bugfix pytest-regression non sono mai state bloccate. CONSEGUENZA che aggrava il difetto: il veto colpisce esattamente il lavoro di FEATURE -- quello che consegna valore -- e risparmia i bugfix, cioe' e' massimamente dannoso dove costa di piu'. E I GATE CHE BLOCCANO UNA SLICE DI FEATURE SONO DUE, IMPILATI (misurato 2026-07-22): sul percorso gherkin `verify-slice-commit` rifiuta gia' a **E1** con "owns no recognized AT candidates for slice-05 ... nothing was verified", perche' gli AT di quella slice sono plain-pytest senza file `.feature` -- quindi il tier architetturale non viene nemmeno raggiunto. Due gate indipendenti che defaultano entrambi CONTRO le slice di feature, uno dietro l'altro: chi ripara solo il tier trovera' ancora il muro di E1. DATO UTILE PER CHI LO RIPARA, misurato 2026-07-22 in un terzo albero con il box verificato pulito prima E dopo e swap identico (quindi ne contesa ne pressione di memoria): il file rosso test_gate_scope_digest_cargo.py da {passed:5,failed:1} -- solo UNO dei suoi sei AT e quello attivo-rosso. Cinque sesti dello scaffold sono gia verdi, quindi il lavoro residuo su quella slice e molto piu stretto di quanto la presenza del file nel tier suggerisca. Confermato inoltre che il rosso e STABILE e IDENTICO in tre alberi diversi: nessuna divergenza fra worktree, nessun artefatto d'ambiente." proposed_solution="Selezionare il tier architetturale per CIO' CHE IL TEST ASSERISCE, non per dove sta il file: un marker esplicito (es. `@pytest.mark.arch_invariant`) che i file di invariante portano e le suite di accettazione di feature no, con `_arch_invariant_paths` che filtra su quello invece di restituire una directory. In transizione, escludere dal tier le sottodirectory che sono chiaramente suite di feature (`tests/build/<feature>/acceptance/`). Corollario di igiene: le suite di accettazione di feature non dovrebbero vivere sotto `tests/build/` -- il posto e' un dettaglio che e' diventato semantica. Aggiungere inoltre al rifiuto E2 la distinzione esplicita fra 'invariante regredito' e 'AT active-RED di feature non consegnata', perche' oggi l'operatore vede solo `a run-time architecture invariant FAILED` e deve scavare per scoprire che il rosso non e' suo."
- [ ] dispatch-feature-end-examine-falls-through-to-crafter: paradigm=object-oriented defect="`des dispatch --phase FEATURE_END_EXAMINE` genera un envelope che nomina l'agente SBAGLIATO: `src/des/cli/dispatch.py:168` `_PHASE_AGENTS` mappa solo tre fasi (D_DISTILL -> nw-acceptance-designer, C_REVIEWER_AUDIT -> nw-user-examiner, FEATURE_END_RETURN_PHASE -> nw-software-crafter-reviewer). `FEATURE_END_EXAMINE` e' ASSENTE, quindi cade sul `_DEFAULT_AGENT` (riga 143) e l'envelope emette `Agent: nw-software-crafter`. Verificato generando l'envelope reale. Perche' non e' cosmetico: `des feature-end --help` documenta `DES-PHASE: FEATURE_END_EXAMINE` + `DES-SLICE: feature-end` come IL dispatch canonico dell'examine a scope-feature, e `feature_end_cycle_service._run_feature_end_examine_leg` fail-chiude l'intero ciclo senza un PASS fresco da quello. Quindi l'UNICO dispatch che produce la prova-di-done della feature affida il lavoro a un crafter che LEGGE IL CODICE invece che all'esaminatrice deliberatamente IGNORANTE. Distrugge esattamente la proprieta' per cui l'esaminatrice esiste: l'ignoranza e' cio' che cattura la finzione-d'ambiente, e un crafter che legge il sorgente puo' convincersi di un PASS. Stessa classe del bug 'Face B' gia' fixato e descritto nei commenti a dispatch.py:145-151 per C_REVIEWER_AUDIT -- il fix e' atterrato per l'examine per-slice e non e' mai stato esteso a quello a scope-feature." proposed_solution="Aggiungere `FEATURE_END_EXAMINE -> nw-user-examiner` a `_PHASE_AGENTS`. Poi rendere la classe impossibile invece che tappata: il `_DEFAULT_AGENT` come fallback silenzioso E' il difetto -- una fase NON mappata dovrebbe RIFIUTARE con un what/why/how ('la fase X non ha un agente dichiarato; dichiaralo in _PHASE_AGENTS') invece di emettere un envelope plausibile che nomina l'agente sbagliato. Un envelope che nomina l'agente sbagliato e' peggio di un errore, perche' viene eseguito."
- [ ] inferred-wave-floor-blocks-cross-wave-dispatch-with-no-honest-escape: paradigm=object-oriented defect="Un floor di wave INFERITO blocca un dispatch legittimamente CROSS-WAVE, e le vie d'uscita che il messaggio stesso propone sono due, di cui una e' una dichiarazione falsa. MISURATO 2026-07-22: `.nwave/wave-active/active.json` conteneva `{\"wave\": \"discuss\", \"provenance\": \"inferred\", \"armed_at\": 1784716268}` -- armato QUATTRO MINUTI PRIMA per inferenza da un dispatch non correlato (nessuna wave DISCUSS era stata invocata in quell'albero). Il dispatch bloccato era una scansione tech-debt READ-ONLY che porta `DES-MODE: find`, cioe' per costruzione un'attivita' cross-wave, non un figlio della wave discuss. Il rifiuto WAVE_MARKER_BYPASS offre tre recovery: (1) copiare i marker della wave genitrice -- non esiste una wave genitrice; (2) portare `<!-- DES-WAVE: discuss -->`, che sarebbe una RIVENDICAZIONE FALSA di appartenenza fatta apposta per superare il gate; (3) `des wave-clear` motivandolo come floor STALE -- ma il floor non era stale, era spurio-e-fresco, quindi anche la ragione onesta va scritta contro un'etichetta che non descrive il caso. Nessuna delle tre riconosce la categoria vera: 'questo dispatch e' cross-wave e non appartiene ad alcuna wave'. Il rischio pratico e' che l'operatore prenda la via (2), perche' e' quella che NON distrugge stato -- e cosi' il gate insegna a mentirgli. SECONDA OCCORRENZA IN UN'ORA, 2026-07-22: floor `{"wave": "deliver", "provenance": "inferred"}` armato 12.7 minuti prima, che ha bloccato ESATTAMENTE lo stesso tipo di dispatch (la scansione tech-debt read-only con `DES-MODE: find`). Nome di wave diverso, stessa provenienza inferita, stessa vittima. Due occorrenze in un'ora con due wave diverse fanno di questo un PATTERN e non un incidente: l'inferenza arma un floor a partire dal normale traffico di orchestrazione (messaggi ai sotto-orchestratori, dispatch di agenti non-di-wave), e da quel momento OGNI dispatch cross-wave resta bloccato finche' qualcuno non cancella il floor a mano. La frequenza cambia la gravita': non e' un caso limite da documentare, e' un attrito ricorrente su un percorso caldo, e ogni occorrenza mette l'operatore davanti alla stessa scelta fra cancellare stato altrui e dichiarare il falso." proposed_solution="Riconoscere il dispatch cross-wave come categoria di prima classe: un dispatch che porta un marker cross-wave dichiarato (`DES-MODE: find` ne e' gia' uno, come lo sono refactor e le attivita' di manutenzione) deve superare il floor SENZA rivendicare appartenenza a una wave -- esattamente come `DES-MODE: find` e' gia' esente dal completeness-check del dispatch TDD classico. Secondo: il messaggio di rifiuto deve DISTINGUERE un floor `provenance: inferred` da uno dichiarato, e per l'inferito proporre come prima opzione la sua rimozione (con l'età del floor mostrata: 'inferito 4 minuti fa da <cosa>'), non la rivendicazione di appartenenza. Terzo: `des wave-clear` dovrebbe accettare la ragione senza costringerla nell'etichetta 'stale' quando il caso e' 'spurio' -- un floor sbagliato appena nato e uno vecchio dimenticato sono due difetti diversi e vanno contati separatamente, altrimenti la telemetria dei clear non distingue mai l'inferenza che sbaglia dalla dimenticanza."
- [ ] consolidation-signal-tick-success-path-is-completely-silent: paradigm=object-oriented defect="`des consolidation-signal-tick` non emette NULLA sul percorso di successo: `src/des/cli/consolidation_signal_tick.py:128-135` stampa una riga con WHAT/WHY/HOW solo sul ramo `IntakeDecision.REJECTED` (uscendo 1) e poi fa `return 0` nudo. L'ironia e' nel commento subito sopra quel ramo, che cita un fix di EXAMINE (Vera FAIL): 'the CLI surface must say it too (WHAT/WHY/HOW), never exit 0 silent' -- la disciplina e' stata applicata al ramo di FALLIMENTO e non a quello di SUCCESSO, che resta esattamente cio' che il commento vieta. MISURATO 2026-07-22: due invocazioni consecutive escono 0 stampando solo la riga di freshness del runtime; i record ci sono davvero (`.nwave/telemetry/atdd-pure/trunk-health.jsonl` porta due `PipelineStageStarted` con defect_id derivato), ma dalla superficie NON si distingue 'registrato' da 'no-op'. Costo reale: ho speso un'indagine completa -- lettura del CLI, del dominio, del ledger -- per concludere (sbagliando) che il comando non registrasse nulla, e la conclusione sbagliata sarebbe finita in un report se non avessi controllato il file. Il loop di consolidamento istruisce l'orchestratore a chiamare proprio questo comando per ogni segnale di salute del trunk osservato, quindi la cecita' e' su un percorso caldo, non su un angolo." proposed_solution="Emettere sul successo una riga che nomini COSA e' stato registrato e DOVE: l'evento, il defect_id derivato e il path del ledger -- per esempio `CONSOLIDATION_SIGNAL_RECORDED: <defect_id> -> .nwave/telemetry/atdd-pure/<feature-id>.jsonl`. Simmetria col ramo REJECTED che gia' parla, e costo nullo. Vale la pena estendere il controllo agli altri comandi `des` che registrano su ledger: la stessa asimmetria (il fallimento parla, il successo tace) e' probabilmente replicata, e ogni occorrenza costa a chi la incontra la stessa indagine che e' costata a me. Un comando che REGISTRA deve dire cosa ha registrato, altrimenti l'unico modo di verificarlo e' leggere il ledger a mano -- che e' precisamente il lavoro che il comando esiste per evitare. IL MODELLO ESISTE GIA' NEL REPO, non va inventato: `des validate-feature-delta` sul percorso di successo stampa `Feature delta is valid. 11 wave sections checked` -- nomina l'esito E il conteggio di cio' che ha verificato, cosi' chi legge sa non solo che e' passato ma SU QUANTO. Confronto misurato 2026-07-24: lo stesso giorno `validate_component_manifest` e' uscito 0 in silenzio assoluto e ho dovuto iniettare un fallimento (uno YAML valido ma non-manifest) per convincermi che stesse davvero validando invece di non fare nulla. Due gate della stessa famiglia, nello stesso repo, con comportamenti opposti sul ramo di successo: copiare la forma di quello che parla e' un fix a costo quasi nullo."
- [ ] feature-end-success-report-is-opaque-counts-not-legs: paradigm=object-oriented defect="Il report di SUCCESSO del ciclo feature-end e' opaco: riporta il CONTEGGIO delle gambe e mai QUALI, e butta via il numero di test eseguiti che gia' possiede. TROVATO 2026-07-22 da Vera durante l'examine del fix al false-green, come flag sul percorso di successo (non una regressione del fix). Osservazioni misurate su tre progetti costruiti apposta: (1) un progetto con 48 test reali produce un report che mostra solo `leg_census ran:2` -- il conteggio 48 ESISTE, e' scritto in .nwave/telemetry/feature-end/<feature>-suite.junit.xml, ma non compare mai nel report visibile: l'utente non puo' vedere che i suoi test sono girati; (2) un progetto Rust-only produce solo `not_applicable:4`, senza alcuna etichetta che nomini QUALE gamba e' stata saltata ne' perche'; (3) di conseguenza due progetti in stati radicalmente diversi (48 test eseguiti vs zero test python) producono report che l'esaminatrice non riesce a distinguere leggendoli. E' il gemello-di-successo del false-green appena riparato: li' il ciclo firmava senza osservare, qui osserva ma non lo dice -- in entrambi i casi il report non porta l'informazione che ne giustifica la fiducia." proposed_solution="Nel report di successo NOMINARE le gambe invece di contarle (quale ha girato, quale e' N/A e per quale ragione dichiarata) e SUPERFICIARE il conteggio dei test che il junit.xml gia' contiene -- il dato e' gia' su disco, va solo mostrato. ATTENZIONE ALLA BLAST RADIUS, misurata dalla corsia che l'ha trovato: rimodellare `leg_census` tocca 9 file di test che vi asseriscono sopra, quindi e' un difetto con il proprio ciclo, NON un'aggiunta a bolt-on su un altro fix. Progettarlo in modo che l'arricchimento del report sia additivo (nuovi campi accanto ai conteggi) prima di considerare un cambio di forma che rompa quei 9 file."
- [ ] port-realization-gate-launderable-by-the-idiomatic-stub-shape: paradigm=object-oriented defect="IL GATE CHE DEVE RENDERE IMPOSSIBILE UN SUPPORTO-LINGUAGGIO PARZIALE SI LASCIA AGGIRARE DALLA FORMA DI STUB PIU' IDIOMATICA DI PYTHON. `_is_pure_notimplementederror_stub` (src/des/testarch/port_realization_discovery.py:138-181) classifica un metodo come stub SOLO se il corpo e' ESATTAMENTE un `raise NotImplementedError`, dopo aver rimosso al massimo un docstring iniziale. Due forme passano quindi indisturbate: (A) qualunque istruzione PRIMA del raise -- un `...`, un `pass`, una riga di log; (B) un no-op che finge successo e non solleva mai (es. `return None`), che non viene nemmeno considerato, perche' l'unico segnale del classificatore e' 'e' un raise nudo'. NON TRACCIATO MA ESEGUITO 2026-07-22: il reviewer lo aveva dedotto senza poterlo eseguire (Bash limitata a `tsunami ...` nel suo ruolo, e lo ha dichiarato onestamente invece di asserire un risultato runtime); il sotto-orchestratore lo ha ESEGUITO con tre plugin sintetici, ciascuno dichiarante `port_coverage[verify_environmental_e2e] = True`. CONTROLLO (raise nudo) -> exit 1, correttamente segnalato con 2 gap nominati. EVASIONE A (`...` poi raise) -> exit 0, `conformant -- 1 plugin(s) probed, 0 gap(s) found`. EVASIONE B (no-op che finge successo) -> exit 0, identico. Il gate funziona perfettamente sulla forma per cui e' stato costruito e fallisce su quella che un manutentore vero scrivera' con maggiore probabilita': `...` e' il marcatore idiomatico di non-implementato in Python, piu' comune di un raise nudo nel codice di stub e di Protocol. Entrambi i charter della feature hanno l'oracolo negativo violato ALLA LETTERA ('the command must NOT exit 0 while any registered plugin declares a capability whose backing adapter is an unimplemented stub'). E poiche' l'hook pre-push E il job CI si basano entrambi su quell'exit code, entrambi farebbero passare queste due forme. Confermato non testato: 28 membri di test nel file del probe-contract, nessuno copre alcuna delle due forme." proposed_solution="Invertire il criterio: oggi il classificatore cerca la forma NOTA dello stub e tutto il resto e' 'reale'. Deve invece cercare la prova che il metodo FA qualcosa di reale, e trattare come stub tutto cio' che non la porta -- cosi' una forma nuova di finto-lavoro non e' un buco ma un default sicuro. Come minimo: ignorare `...`/`pass`/no-op statements nel corpo prima di decidere, e trattare un metodo che NON solleva e NON produce effetti osservabili come stub, non come reale. AT obbligatori sulle due forme eseguite qui (sono gia' riproducibili) piu' un oracolo che verifichi che il CONTROLLO continua a fallire, cosi' il fix non diventa un passa-tutto al contrario."
- [ ] dispatch-charter-lane-silently-drops-intent: paradigm=object-oriented defect="`des dispatch --lane charter` ACCETTA `--intent` e lo BUTTA VIA in silenzio, exit 0. MISURATO 2026-07-22 dispacciando un product-owner per un charter: l'envelope generato non contiene alcuna traccia del testo passato in `--intent`. Il set di sezioni della corsia charter e' DES_METADATA / AGENT_IDENTITY / SKILL_LOADING / TASK_CONTEXT / DESIGN_CONTEXT / BOUNDARY_RULES / TIMEOUT_INSTRUCTION -- nessuna di queste trasporta l'intent (la corsia deliver lo mette in ATDD_PURE_PHASES). Quindi l'intero brief del compito svanisce. Questo rompe frontalmente il contratto operativo dichiarato -- 'l'output di des dispatch si passa VERBATIM, le specifiche vanno in --intent' -- perche' obbedire a entrambe le regole insieme produce un PO a cui e' stato detto soltanto 'Slice slice-01 of feature X' e nulla di piu'. Stessa forma del difetto FEATURE_END_EXAMINE: un envelope plausibile ma sbagliato, che poi viene ESEGUITO." proposed_solution="Una corsia che non puo' trasportare `--intent` deve RIFIUTARE il flag con un what/why/how, non ingoiarlo -- lo stesso rimedio gia' scritto per la fase non mappata: mai emettere un envelope plausibile quando l'input non e' onorabile. In alternativa (preferibile) dare alla corsia charter una sezione che trasporti l'intent, visto che un charter ha bisogno dell'osservabile e del contesto di valore piu' di quasi ogni altra corsia. Regola generale: ogni flag accettato dal parser deve avere un consumatore in OGNI corsia che lo accetta, oppure essere rifiutato in quelle che non ce l'hanno -- un argomento accettato-e-scartato e' silent-wrong per costruzione."
- [ ] spine-at-discovery-blind-to-plain-pytest-ats-routes-to-duplication: paradigm=object-oriented defect="La scoperta degli AT dello spine vede solo i file `.feature` e dichiara 'acceptance tests are not yet authored' per una slice i cui AT esistono, passano e sono plain-pytest. RIPRODUZIONE ESATTA 2026-07-22: `des next --feature-id language-port-realization-gate --repo .` risponde 'slice slice-05 acceptance tests are not yet authored' e instrada a `/nw-distill`. L'affermazione e' FALSA: `tests/build/language_port_realization_gate/acceptance/test_pre_push_and_ci_blocking_wiring.py` porta cinque AT, uno per deliverable della slice-05 (righe 180/226/276/335/379), e l'intero pacchetto passa 58/58. Condizione scatenante: `find ... -name '*.feature'` restituisce VUOTO, quindi la scoperta conclude 'non scritti' invece di 'scritti in una forma diversa e legittima'. Fattore contribuente: il feature-delta non dichiara alcun `at_kind` (grep per `at_kind|at-kind|pytest-regression` restituisce nulla), quindi non c'e' dichiarazione su cui ripiegare. GRAVITA' OLTRE IL MESSAGGIO FALSO, ed e' il punto: il HOW instrada a `/nw-distill`, che farebbe scrivere a un acceptance-designer dei `.feature` RIDONDANTI sopra AT che esistono gia' e passano. Seguire il consiglio del gate DANNEGGIA attivamente l'albero -- e' una violazione GDP-4 in senso stretto: il HOW deve instradare al tool che produce l'artefatto, e qui instrada alla duplicazione. AGGRAVAMENTO MISURATO 2026-07-22, e la riga qui sopra SOTTO-VALUTAVA il difetto: la stessa cecita' non si limita a sbagliare il messaggio di `des next` e a instradare male -- **RIFIUTA L'ATTESTAZIONE**. Osservato eseguendo `des verify-slice-commit --repo . --commit e4d875ccf --feature-id language-port-realization-gate` in modalita' default: `{"event":"SliceCommitRefused","refused_half":"E1","error":"feature 'language-port-realization-gate' owns no recognized AT candidates for slice(s) ['slice-05'] -- nothing was verified, this is not a pass"}`, exit 1. Gli AT di quella slice sono CINQUE, esistono e PASSANO (58/58 sul pacchetto). Quindi la conseguenza vera non e' un consiglio sbagliato: **qualunque slice i cui AT siano plain-pytest e' NON ATTESTABILE in modalita' default**, e il gate dichiara "nothing was verified" di fronte a cinque test verdi. E' la stessa classe del false-green ma specchiata -- li' il gate firmava senza osservare, qui rifiuta di riconoscere cio' che ha osservato -- e la vittima e' il lavoro gia' fatto e verde. RIMEDIO PRATICO CONFERMATO nella stessa sessione: passare `--at-kind pytest-regression --regression-test-file <f>` supera E1, perche' quella corsia sostituisce la scoperta AT con l'attestazione comportamentale. Ma e' un rimedio che l'operatore deve CONOSCERE: il rifiuto non lo nomina." proposed_solution="La scoperta deve riconoscere la corsia pytest-regression come forma di AT di prima classe: cercare gli AT dichiarati (`at_kind`) e, in assenza di dichiarazione, riconoscere i file di test convenzionali della slice prima di concludere 'non scritti'. Il messaggio deve distinguere 'nessun AT trovato in ALCUNA forma' da 'nessun .feature, ma N test presenti' -- e nel secondo caso il HOW deve instradare alla coppia meccanica (`des verify-red-green --record-red --test-file <f>` + `des verify-negative-at --test-file <f> --all-critical`), MAI a /nw-distill. Separatamente: rendere `at_kind` dichiarabile e verificato nel feature-delta, cosi' la scoperta ha una dichiarazione invece di un'inferenza da estensione di file."
- [ ] port-realization-docstring-describes-superseded-all-semantics: paradigm=object-oriented defect="Deriva fra prosa e codice sul contratto stesso: il docstring di modulo di `src/des/testarch/port_realization_discovery.py:19-23` descrive ancora la semantica pre-slice-06 ('a port is stub-backed iff EVERY method is a pure stub', cioe' `all`) dopo che la slice-06 ha spostato il codice a `any` -- fix che esisteva proprio per chiudere un falso-negativo (un facet con un metodo reale e il resto stub riusciva a passare). Quindi il documento che dichiara il contratto afferma oggi la regola che il codice ha deliberatamente abbandonato. Chi legge il docstring per capire cosa il gate garantisce riceve la garanzia sbagliata, e in direzione permissiva." proposed_solution="Allineare il docstring al comportamento `any` e, gia' che si tocca, citare la slice/decisione che ha causato il cambio, cosi' la prossima inversione di semantica lascia una traccia invece di una contraddizione silenziosa. Vale la pena cercare altre occorrenze della vecchia formulazione nella prosa del progetto -- una regola di contratto restata in due posti diverge esattamente cosi'."
- [ ] prepush-hook-hardcodes-venv-python-breaking-agnosticism: paradigm=object-oriented defect="`.pre-commit-config.yaml:191-197` cabla `entry: .venv/bin/python -m scripts.cli.validate_language_adapter_catalog --check-port-realization`, mentre ogni hook fratello nello stesso file usa `uv run` o `python3`. E' una deviazione dal mandato di agnosticismo dalla macchina target: assume che esista una `.venv` locale in quella posizione esatta, quindi si rompe in un worktree o su un clone che non ce l'ha -- e si rompe nel momento peggiore, in pre-push, dove l'operatore la legge come 'il mio push e' bloccato' invece che 'il tuo hook assume un percorso che qui non esiste'." proposed_solution="Allineare l'entry alla convenzione degli hook fratelli nello stesso file (`uv run` o `python3`), cosi' la risoluzione dell'interprete segue la stessa strada di tutti gli altri. Piu' in generale: un hook che risolve un interprete non deve mai cablare un percorso -- e' la stessa classe del difetto dell'interprete nel regression-gate, un livello piu' su."
- [ ] examine-discipline-lives-only-in-prose-with-no-mechanical-guard: paradigm=object-oriented defect="La disciplina dell'esaminatrice vive SOLO nella prosa e viene violata anche quando la prosa e' presente, esplicita e propagata -- servirebbe una guardia meccanica nel recorder e non c'e'. MISURATO 2026-07-22, SECONDA occorrenza in una notte: una Vera ha registrato PASS con osservazioni che citano `Direct database inspection confirms ...` e `Verified via acceptance test (re-running pytest)` invece della superficie browser reale contro cui era stata dispacciata. Verifica fatta escludendo l'ipotesi comoda: la regola E' nel sorgente `nWave/agents/nw-user-examiner.md`, E' nell'installato `~/.claude/agents/nw/nw-user-examiner.md` riga 77, e `diff -q` fra i due dice IDENTICI -- quindi NON e' una mancata propagazione. Il testo vieta per NOME esattamente cio' che e' stato fatto ('opening a direct database client (`psql`, a driver REPL, a raw query), calling an internal function... a SIMULATION of the outcome, not an observation of it'). E `des record-examine-verdict` non ha ALCUN controllo: nessuna verifica sulla storia delle chiamate a tool, nessun rifiuto di un PASS le cui osservazioni portino la firma di un accesso interno. Il recorder accetta qualunque cosa gli venga passata. LA RAGIONE PER CUI LA PROSA NON BASTA E' STRUTTURALE, non di formulazione: un esaminatore davanti a una superficie lenta o che non carica ha davanti due strade, una che PRODUCE UNA RISPOSTA (il back door) e una che produce un INDETERMINATE. La prosa dichiara che l'INDETERMINATE e' la risposta di valore, ma la strada che produce un risultato resta piu' attraente -- e una regola che vieta cio' che e' piu' facile, senza conseguenza meccanica, viene violata. Segnalato da sister alpha-challenge, che aveva proposto ENTRAMBE le meta' (regola enumerata + guardia meccanica); e' stata implementata solo la prima e trattata la seconda come opzionale." proposed_solution="Costruire la guardia nel recorder: `des record-examine-verdict` RIFIUTA un PASS le cui osservazioni portino la firma di un accesso interno -- un client DB nominato, un percorso di modulo interno, formule come 'direct database inspection' o 'verified via <il test suite> ' usate come FONTE DI VERITA' al posto della superficie. Il rifiuto deve INSEGNARE, non solo bloccare: what/why/how che dica che la strada scelta non e' percorribile da un utente pagante e che l'INDETERMINATE e' il verdetto corretto in quel caso, cosi' l'esaminatrice sa QUALE osservazione le manca. Se e' disponibile la storia delle chiamate a tool del dispatch, controllare quella e' piu' robusto del testo delle osservazioni (che l'esaminatrice controlla). REGOLA GENERALE da applicare oltre questo caso: ogni volta che una regola di disciplina vive solo nella prosa E la strada vietata e' quella che produce un risultato, quella regola e' un desiderio e non un controllo -- va cercata la conseguenza meccanica o va accettato che verra' violata."
- [ ] blast-radius-counts-intra-file-helpers-as-consumers: paradigm=object-oriented defect="`des blast-radius` conta i consumatori di un helper LOCALE AL FILE come raggio d'azione, e cosi' classifica L una modifica a un singolo file di test. MISURATO 2026-07-22: `des blast-radius --repo . --paths tests/bugs/des/test_dispatch_generates_all_feature_end_cycle_phases.py` restituisce `tier=L (files=1, lines_changed=0)` con ragione `consumer_counts[..._run_dispatch]=38 >= large_min_consumers=10`. Ma `_run_dispatch` e' una funzione privata DENTRO quel file, chiamata dai 38 test dello stesso file: non esce dal file, non ha un solo consumatore esterno. Il raggio reale e' un file. Perche' conta: il tier decide la CORSIA, e una modifica banale spedita nella corsia lenta e' un costo puro -- il piano velocity lo dice esplicitamente ('un tier troppo prudente e' un costo, non una sicurezza'). Peggio: erode la fiducia nel misuratore, perche' l'operatore vede L su un file di test isolato e impara a non credergli, che e' esattamente il modo in cui un classificatore smette di essere usato." proposed_solution="Escludere dal conteggio dei consumatori i riferimenti INTERNI al file (o all'unita') che si sta misurando: un helper privato chiamato dai test del proprio modulo non e' un consumatore, e' struttura interna. Se serve una misura della complessita' interna, e' una metrica DIVERSA e va riportata a parte, non sommata nel tier. Verificare anche il caso simmetrico: un modulo con molti riferimenti interni e pochi esterni non deve risultare L, e uno con pochi riferimenti interni e molti esterni non deve risultare S."
- [ ] dispatched-agent-dies-silently-orchestrator-waits-forever: paradigm=object-oriented defect="Un agente dispacciato puo' MORIRE SENZA NOTIFICARE, e l'orchestratore che lo ha dispacciato resta in attesa di un evento che non arrivera' mai. MISURATO 2026-07-22 sul PERCORSO CRITICO: un `nw-troubleshooter` dispacciato per una RCA e' morto e il suo transcript non veniva piu' scritto da 3122 secondi (~52 minuti); nessuna notifica di completamento e nessuna notifica di errore sono mai arrivate. Il sotto-orchestratore lo ha scoperto solo perche' io gli ho chiesto lo stato dall'esterno, notando che non c'erano suoi processi vivi -- altrimenti avrebbe atteso indefinitamente. Costo: ~52 minuti sulla corsia da cui dipendevano l'attestazione di altre due corsie, piu' il rischio (evitato per un soffio) che io registrassi come FATTA una RCA che non era mai avvenuta -- il sotto-orchestratore ha dovuto correggermi esplicitamente ('se te l'eri segnata come fatta, cancellala'). La gravita' e' nella FORMA del fallimento: non e' un errore che si vede, e' un'ATTESA che non finisce, e un'attesa non si distingue da un lavoro lento finche' qualcuno non guarda da fuori." proposed_solution="Due livelli. (1) MITIGAZIONE IMMEDIATA, gia' adottata dalla corsia e da propagare come disciplina: non fidarsi del meccanismo di notifica come unico segnale -- controllare la FRESCHEZZA DEL TRANSCRIPT dell'agente (mtime del suo file) e trattare un silenzio oltre una soglia come morte presunta, non come lavoro in corso. Un orchestratore che aspetta deve avere un modo di distinguere 'sta pensando' da 'e' morto', e oggi non ce l'ha. (2) FIX STRUTTURALE: fare in modo che ogni agente dispacciato produca un battito o che la sua morte produca una notifica -- un'attesa senza scadenza e' un deadlock con un nome gentile. Corollario di progettazione emerso dallo stesso incidente: istruire OGNI agente dispacciato a PERSISTERE il proprio esito su disco man mano, cosi' la morte costa il tempo ma non il lavoro (stessa mitigazione gia' adottata per i reviewer che restituiscono messaggi finali vuoti)."
- [ ] verify-test-runner-defaults-to-pytest-and-abstains-on-real-tokens: paradigm=object-oriented defect="`des verify-test-runner` e' un verbo USER-FACING -- quello che un contributore trova leggendo `des --help` per capire come far girare i propri test -- e su un progetto Rust o vitest riporta in silenzio `runner: pytest, collected: 0, exit 5`. Non finge un verde (`0 test raccolti` non e' un falso PASS), ma il contributore lo legge come 'il mio progetto non ha test', non come 'hai usato il verbo sbagliato'. DUE SOTTO-DIFETTI IN UNO, misurati 2026-07-22: (a) il verbo DEFAULTA a pytest invece di risolvere il target come fa `des run-contract-gate` -- due verbi della STESSA CLI in disaccordo su come si sceglie un runner, e quello user-facing e' il piu' ingenuo; (b) accetta `--runner cargo-test` e `--runner vitest`, cioe' ESATTAMENTE i token che il registry usa, e su entrambi risponde `{\"status\":\"ABSTAIN\",\"reason\":\"runner-absent\"}` mentre `cargo 1.95.0` e' installato e sul PATH. Quindi un utente che INDOVINA il token giusto viene informato che il runner e' assente. Stessa famiglia del difetto `--intent` ingoiato dalla corsia charter: un flag accettato dal parser senza un consumatore dietro. Trovato perche' una Vera ha esaminato questo verbo credendolo la superficie della feature, ne ha tratto un FAIL e perfino una 'regressione Rust' -- entrambi confutati con sonde dirette (i sette token risolvono tutti, e il dispatch di produzione instrada correttamente cargo-test e vitest su fixture reali). Il verbo sbagliato ha prodotto una diagnosi sbagliata su un prodotto sano: e' un difetto che genera falsi allarmi, non solo confusione." proposed_solution="Far risolvere a `verify-test-runner` il runner dal TARGET esattamente come fa `run-contract-gate` -- una sola strada di risoluzione per tutta la CLI, dichiarata in un posto solo, non due verbi con opinioni diverse. Se il default a pytest deve restare per compatibilita', deve almeno DIRE che sta assumendo pytest su un progetto che non sembra Python, con il what/why/how e il verbo giusto da usare. E `--runner <token>` deve onorare i token del registry o rifiutarli nominando quelli validi: abstain 'runner-absent' su un token registrato con il tool installato e' silent-wrong."
- [ ] examine-ledger-latest-record-wins-erases-unresolved-reservations: paradigm=object-oriented defect="Il gate del feature-end legge SOLO L'ULTIMO record per (charter, slice), quindi una riserva registrata puo' essere CANCELLATA da un'osservazione successiva, piu' stretta e perfettamente onesta -- e il risultato e' un false-done costruito da due record entrambi veri. MISURATO/INTERCETTATO 2026-07-22: una corsia aveva registrato correttamente un INDETERMINATE su meta' di una feature (toolchain assente, riga d'oracolo infalsificabile su quella macchina); Vera stava per esaminare l'ALTRA meta', funzionante, e registrare un PASS sullo stesso charter e slice. Quel PASS sarebbe diventato l'ultimo e avrebbe reso la riserva INVISIBILE al gate -- non contraddetta, superata per ordinamento. Intercettato prima che accadesse, ma per giudizio umano, non per meccanismo. La formulazione piu' precisa e' del sotto-orchestratore che l'ha subito: 'un PASS non avrebbe CONTRADDETTO il mio INDETERMINATE, l'avrebbe reso INVISIBILE, che e' peggio, perche' una contraddizione si nota e un'invisibilita' no'. La radice: la riserva e l'osservazione riguardano righe DIVERSE dell'oracolo, ma il ledger ha spazio per UN SOLO verdetto per charter, quindi competono per uno slot che non dovrebbero condividere. Non serve alcun attore disonesto. PRECISAZIONE DELL'AMBITO, misurata 2026-07-22, che RESTRINGE questo difetto: la supersession vale DENTRO uno scope, NON fra scope diversi. Verificato sui record reali: PASS|slice=feature-end|10:01 -> FAIL|slice=feature-end|10:29 (superseduto, stessa chiave) mentre PASS|slice=slice-05|12:06 convive senza interferire, perche la chiave di lookup include lo slice_id. Quindi un verdetto per-slice NON puo oscurare per ordinamento un verdetto a scope-feature: la separazione strutturale e gia imposta dal modello dati, non solo dalla prosa. Il difetto resta REALE ma piu stretto di come lo avevo scritto -- il rischio e fra due osservazioni sulla STESSA chiave (due examine a scope-feature sullo stesso charter, una che riserva su una riga d'oracolo e una che osserva una riga diversa), non fra granularita diverse. Conseguenza pratica: il rimedio 'registrare per riga d'oracolo' resta quello giusto, ma la sua urgenza cala, perche il caso piu comune (per-slice contro feature) e gia protetto per costruzione." proposed_solution="Due direzioni, la seconda piu' onerosa e piu' onesta. (1) Il gate RIFIUTA finche' esiste un INDETERMINATE IRRISOLTO per quel charter, invece di leggere solo il piu' recente -- la riserva vive finche' qualcuno la risolve esplicitamente, non finche' qualcun altro scrive dopo di lei. (2) Registrare i verdetti PER RIGA D'ORACOLO, cosi' una riserva sul Go e un'osservazione su vitest non possono sovrascriversi: sono fatti diversi e meritano slot diversi. Regola di disciplina nel frattempo, da mettere in ogni dispatch di examine a scope-feature: il verdetto complessivo eredita la riga PIU' DEBOLE, non la piu' forte -- non puo' essere PASS finche' una riga e' infalsificabile su quella macchina, e va registrato come INDETERMINATE che CONTIENE le osservazioni positive per esteso. Ma va detto chiaramente che questa e' una disciplina da RICORDARE, e le discipline da ricordare sono quelle che falliscono: il fix vero e' meccanico."
- [ ] runner-unavailable-remediation-names-the-wrong-ecosystem: paradigm=object-oriented defect="Il messaggio di rimedio per un runner assente nomina l'ECOSISTEMA SBAGLIATO, quindi il suo HOW manda l'utente su una strada inesistente. OSSERVATO 2026-07-22 da Vera durante l'examine, rimuovendo `node_modules` da una fixture vitest per provare l'oracolo negativo: il gate fallisce rumorosamente -- che e' il comportamento corretto e desiderato -- ma il testo recita `vitest not found on PATH... install it (e.g. via rustup or 'cargo install vitest') and retry`. **vitest si installa con npm, non con rustup ne' con cargo**: `cargo install vitest` non esiste. E' il testo di rimedio del percorso Rust copiato nel percorso vitest. La meta' WHAT/WHY del messaggio e' corretta (dice cosa manca e perche' si ferma), la meta' HOW e' attivamente dannosa: un contributore che la segue esegue un comando che fallisce, e ne conclude che il problema e' il proprio ambiente invece che il consiglio. Un HOW sbagliato e' peggio di un HOW assente, perche' consuma fiducia oltre che tempo LOCALIZZATO NEL SORGENTE 2026-07-22 (verificato leggendo il codice, non sulla parola dell'esaminatrice): `src/des/adapters/driven/runner/tool_discovery.py:97` -> `f"install it (e.g. via rustup or 'cargo install {name}') and retry"`. E' un rimedio RUST CABLATO per QUALUNQUE tool mancante, nell'helper di discovery CONDIVISO. Quindi non colpisce solo vitest: colpisce OGNI runner non-Rust -- go, vitest, gradle-test, dotnet-test, maven-test. A un contributore Go viene detto `cargo install go`; a uno JS `cargo install vitest`. Nessuno dei due pacchetti esiste, ed entrambi mandano l'utente nel package manager dell'ecosistema sbagliato. Il messaggio e' CONFIDENTEMENTE E SPECIFICAMENTE SBAGLIATO, che e' peggio di un fallimento nudo perche' un utente ci agisce sopra. Scritto quando cargo era l'unico runner non-pytest e mai generalizzato: stesso schema "il cablaggio e' corso avanti alla propria generalizzazione" dei token del registry." proposed_solution="Legare il testo di rimedio al RUNNER RISOLTO invece che a un default: ogni adapter di runner dovrebbe portare il proprio comando d'installazione (npm/npx per vitest, cargo/rustup per cargo-test, go per go-test, pip per pytest), e il messaggio comporlo da li'. Cosi' aggiungere un linguaggio nuovo porta con se' il proprio rimedio e non eredita quello del vicino. Verificare nello stesso giro gli altri percorsi runner-assente: se questo era copiato, probabilmente non e' l'unico"
- [ ] retired-phase-still-normative-in-schema-while-cli-refuses-it: paradigm=object-oriented defect="La fase `E_BATCH_REFACTOR` e' DEPRECATA (decisione di Ale 2026-07-22: il suo lavoro lo fanno il prefactoring e il loop di consolidamento) ma sopravvive in CINQUE superfici, e in una di esse in modo NORMATIVO -- creando una contraddizione fra due autorita' dello stesso sistema. INVENTARIO MISURATO: (1) `nWave/schemas/atdd-pure-phase-sequence.schema.json:74,201` la elenca fra le FASI VALIDE dello schema, mentre `des dispatch --phase E_BATCH_REFACTOR` la RIFIUTA con `invalid choice` -- lo schema dice che e' legittima, la CLI dice di no, e un artefatto validato contro lo schema puo' quindi dichiarare una fase che il dispatch non eseguira' mai; (2) `nWave/skills/nw-execute/SKILL.md:283` la descrive come fase VIVA con istruzioni operative ('5. E_BATCH_REFACTOR -- separate crafter instance. L1-L6 batch refactor'), e alle righe 48/320 la colloca nel ciclo feature-end di /nw-deliver; (3) `nWave/skills/nw-bugfix/SKILL.md:296,306` idem; (4) `nWave/dispatch/atdd_pure.yaml:51` la nomina come fase esistente in un'istruzione di caricamento skill; (5) `nWave/flavors/_schema.yaml:131` la include in una sequenza di fasi data come esempio. Un agente che legge la skill trova istruzioni per eseguire una fase che il dispatch rifiutera'; un artefatto che passa la validazione dello schema puo' nominarla. NOTA: NON e' la causa del rosso CI che avevo erroneamente attribuito qui -- quel test fallisce per accoppiamento al formato di argparse, riga separata." proposed_solution="Rimuovere la fase dall'ENUM dello schema per primo, perche' e' l'unica superficie NORMATIVA delle cinque: finche' e' li', un artefatto puo' dichiararla legittimamente e il disaccordo con la CLI resta reale. Poi ripulire le quattro superfici di prosa, distinguendo dove va CANCELLATA (le istruzioni operative in nw-execute:283, che dicono a un agente come eseguirla) da dove va RIFORMULATA AL PASSATO (i riferimenti che spiegano cosa NON appartiene a una certa wave restano utili se dicono 'fase ritirata' invece di trattarla come viva). Verificare che i test che asseriscono sull'enum dello schema vengano aggiornati insieme -- e' un cambio normativo, non cosmetico, quindi va per la spina e non a mano. Regola generale: quando una fase/uno stato viene ritirato, l'enum normativo e la CLI vanno cambiati NELLO STESSO commit, o il sistema resta con due opinioni su cosa e' valido."
- [ ] dispatched-agent-final-message-lost-while-content-survives-in-agent: paradigm=object-oriented defect="Il MESSAGGIO FINALE di un agente dispacciato si perde, mentre il CONTENUTO esiste ancora dentro l'agente -- riprendendolo si recupera per intero. QUATTRO OCCORRENZE MISURATE 2026-07-22, su tipi di agente diversi: (1) un `nw-software-crafter-reviewer` in F_FINAL_REVIEW ha restituito un messaggio finale COMPLETAMENTE VUOTO dopo 33 chiamate a tool e 178k token; il verdetto (un REJECTED con un blocker preciso) e' stato recuperato SOLO riprendendo l'agente; (2) un agente di deep review ha restituito la sola parola `REJECTED` dopo 207k token e una chiamata a tool -- un messaggio quasi-vuoto, senza alcuna delle motivazioni che aveva prodotto; (3) un finder tech-debt e' andato idle senza consegnare nulla, e ha consegnato il report COMPLETO solo dopo che gliel'ho sollecitato esplicitamente; (4) un secondo finder e' andato idle senza messaggio finale, e il suo lavoro e' sopravvissuto SOLO perche' gli avevo ordinato di scrivere i reperti su file man mano. La forma e' costante: NON e' un fallimento di generazione (il contenuto c'e', ed e' buono), e' un fallimento di CONSEGNA. Il costo e' doppio: i token gia' spesi (178k e 207k nei due casi peggiori) piu' il round-trip per recuperare, e nel caso peggiore la conclusione sbagliata che l'agente non abbia trovato nulla -- che e' esattamente la deduzione che un orchestratore fa davanti a un silenzio." proposed_solution="MITIGAZIONE GIA' VALIDATA SUL CAMPO, da rendere standard in OGNI dispatch: istruire l'agente a PERSISTERE il proprio esito su file (fuori dal repo di lavoro) dopo OGNI reperto o decisione confermata, mai accumulandolo per la fine. Provata 2026-07-22: l'agente e' andato idle senza messaggio finale ESATTAMENTE come i precedenti, e il lavoro era integro sul disco -- stesso fallimento, costo zero. Costa una riga nel prompt. DISCIPLINA COMPLEMENTARE per l'orchestratore: un silenzio NON e' un 'niente trovato'. Prima di concludere che un agente non abbia prodotto nulla, RIPRENDILO e chiediglielo -- il contenuto e' quasi sempre li'. E la richiesta deve dire esplicitamente che 'non ho trovato nulla' e' una risposta accettabile, o l'agente sollecitato inventera' qualcosa per non deludere. FIX STRUTTURALE, se il canale e' sotto il nostro controllo: capire perche' il messaggio finale si perde -- la correlazione con l'alto numero di token spesi (178k, 207k) suggerisce un limite di dimensione o un troncamento silenzioso, che sarebbe a sua volta un silent-wrong da rendere loud."
- [ ] arch-tier-marker-migration-left-unfinished: paradigm=object-oriented defect="F-ARCH-TIER-MARKER-MIGRATION -- la migrazione a `@pytest.mark.arch_invariant` e' stata PROGETTATA e mai consegnata, lasciando in piedi la selezione del tier di architettura per REGOLA STRUTTURALE DI PERCORSO invece che per CIO' CHE IL TEST ASSERISCE. Il marcatore esiste gia' nel piano a `tests/des/acceptance/r3_gate_non_vacuity_build_tier/steps/domain_types_slice_01.py:35` e non e' mai arrivato al codice. NON e' debito da distrazione: e' una MIGRAZIONE INTERROTTA, che ha lasciato funzionante il meccanismo vecchio -- ed e' la forma di debito piu' difficile da vedere, perche' non fallisce, aspetta. La forma TRANSITORIA (escludere `tests/build/<feature>/acceptance/**`) e' quella consegnata da `fix-arch-tier-selected-by-assertion`, dichiarata come tale nel suo Definition of Done e non nascosta. La RCA di quella corsia (Q2b) conferma che `arch_invariant` e' registrabile nella sezione `markers` della ini a `pyproject.toml:232-248` senza ostacoli di `--strict-markers`, quindi non c'e' un impedimento tecnico noto. Evidenza completa: `docs/feature/fix-arch-tier-selected-by-assertion/deliver/rca.md` (321 righe)" proposed_solution="Completare la migrazione: marcare i ~55 file di invariante genuina con `@pytest.mark.arch_invariant`, registrare il marcatore nella ini, e far filtrare al gate su quello invece che sul percorso -- cosi' un file di invariante puo' vivere ovunque e una suite di accettazione di feature non entra nel tier per il solo fatto di stare in una certa directory. Rimuovere la regola strutturale transitoria solo DOPO che il marcatore e' in vigore, mai insieme. Nota di sequenza: questa e' la seconda meta' di un lavoro gia' iniziato, quindi il primo passo e' leggere il piano originale a domain_types_slice_01.py:35 e capire PERCHE' si e' fermato -- una migrazione si interrompe per una ragione, e se quella ragione e' ancora viva si interrompera' di nuovo"
- [ ] feature-id-permanently-locked-into-one-at-discovery-mode: paradigm=object-oriented defect="Una feature che ha gia' spedito una slice autorata in Gherkin NON PUO' aggiungere una slice successiva autorata come pytest-regression: `des carpaccio-slice-gate` rifiuta con `MalformedInput: at_kind='pytest-regression' but the feature also owns .feature scenarios; the two AT-discovery modes are mutually exclusive`. Il feature-id resta quindi LEGATO PER SEMPRE alla modalita' scelta dalla prima slice. Segnalato da sister alpha-challenge 2026-07-22 sotto scadenza demo-critica, con costo di tempo reale. NON e' solo un difetto di messaggio, e' un BUCO DI FUNZIONALITA': l'evoluzione naturale di una feature -- slice iniziali come scenari end-to-end in Gherkin, slice successive come regressioni puntuali in stile bugfix -- e' esattamente il percorso che il metodo incoraggia, e il gate lo vieta. Il WHAT e il WHY del rifiuto sono chiari; il HOW e' COMPLETAMENTE ASSENTE: non suggerisce nemmeno le due vie che un umano trova da solo (autorare la slice in Gherkin, o tracciarla sotto un feature-id separato). WORKAROUND ADOTTATO dalla sorella, che e' anche la misura del costo: ha creato un feature-id SEPARATO per la nuova slice, tracciata come propria slice-01, con la relazione col genitore documentata in prosa nel feature-delta. Funziona, ma FRAMMENTA L'AT-COMPLETION LEDGER su due feature-id per cio' che concettualmente e' l'evoluzione di una sola feature -- e un ledger frammentato e' precisamente cio' che rende non calcolabile la Definition of Done di quella feature." proposed_solution="Consentire la COESISTENZA delle due modalita' di scoperta all'interno di una feature, risolvendo l'at_kind PER SLICE invece che per feature: e' la slice a dichiarare come sono scritti i suoi AT, non la feature. Se l'esclusivita' esiste per una ragione tecnica reale (una collect ambigua quando i due tipi convivono), allora quella ragione va NOMINATA nel rifiuto invece di lasciarla implicita. E fino a quando la coesistenza non c'e', il HOW deve almeno esistere: nominare le due vie praticabili e dire QUALE conserva il ledger unito -- perche' la scelta fra loro ha una conseguenza a valle (la frammentazione) che oggi l'operatore scopre solo dopo averla presa."
- [ ] carpaccio-out-of-order-message-omits-the-two-real-remediations: paradigm=object-oriented defect="Il rifiuto per slice fuori ordine -- `carpaccio slice slice-02 entered out of order: its predecessor slice-01 has no SliceCommitVerified ledger record -- deliver carpaccio slices in order` -- ha WHAT e WHY presenti ma un HOW magro: dice cosa fare in astratto ('consegna le slice in ordine') senza nominare le DUE rimediazioni concrete che coprono i casi reali. Segnalato da sister alpha-challenge 2026-07-22. I due casi sono diversi e portano ad azioni opposte: (a) la slice precedente esiste ma non e' stata committata -> committala prima; (b) questa e' in realta' la PRIMA slice della feature e il numero e' sbagliato -> rinumerala a slice-01. Un operatore nel caso (b) che legge 'consegna le slice in ordine' va a cercare una slice-01 che non esiste, e perde tempo a capire che il problema e' il proprio numero" proposed_solution="Distinguere i due casi nel messaggio e dare a ciascuno il proprio HOW: se un record di slice-01 esiste ma non e' SliceCommitVerified, dire 'committa slice-01 prima' col comando; se NON esiste alcuna traccia di slice-01 nel feature-delta ne' nel ledger, dire 'se questa e' la prima slice della feature, rinumerala a slice-01'. Il gate ha gia' entrambe le informazioni in mano nel momento in cui rifiuta -- deve solo leggerle e ramificare"
- [ ] vitest-enumerate-facet-unwired-while-run-facet-ships: paradigm=object-oriented defect="Per il runner vitest e' registrato il facet RUN ma NON quello ENUMERATE: il gate emette `enumerate facet for vitest not wired` e non puo' completare la propria gamba di verifica su un target vitest. MISURATO 2026-07-22 durante l'examine: `des run-contract-gate` su una fixture vitest reale risolve il runner, ESEGUE i test e riporta conteggi che coincidono con `npx vitest run` -- ma poi la verifica non chiude, perche' manca il facet di enumerazione. E' coerente con l'ambito dichiarato della feature che ha consegnato i run-facet (quella si limitava a quelli), quindi NON e' un lavoro mal fatto: e' un PARZIALE ONESTO. Lo registro perche' un parziale onesto non registrato diventa, a distanza di settimane, indistinguibile da un lavoro completo -- e chiunque legga 'vitest supportato' concludera' che il gate ci gira sopra per intero, mentre puo' solo eseguire e non verificare" proposed_solution="Cablare il facet enumerate per vitest sullo stesso registry che gia' porta il run-facet, e verificare la stessa asimmetria sugli altri runner non-pytest (go-test, gradle-test, dotnet-test, maven-test): se il run-facet e' stato consegnato per tutti e l'enumerate per nessuno, e' una seconda meta' mancante uniforme e va trattata come un solo pezzo di lavoro, non come cinque. Fino ad allora, il messaggio `enumerate facet for X not wired` deve dire ESPLICITAMENTE che il target e' eseguibile ma non verificabile, cosi' l'operatore sa di essere in un parziale dichiarato e non in un guasto"
- [ ] refactor-actuator-shipped-nowhere-drain-is-noop-on-every-consumer: paradigm=object-oriented defect="`scripts/refactor_agent.py` -- l'attuatore che il catalogo delle affordance dichiara ESSERE quello spedito per `des refactor --pile --agent-cmd` -- NON viene distribuito ad alcun progetto consumer. Verificato: `UTILITY_SCRIPTS` in `scripts/build_dist.py:65` contiene esattamente DUE voci (`install_nwave_target_hooks.py`, `validate_step_file.py`) e refactor_agent non e' fra queste; zero occorrenze nel force-include del wheel (`scripts/release/patch_pyproject.py`); zero riferimenti nell'installer. La distribuzione degli script e' WHITELIST-ONLY per progetto, e la whitelist non e' stata aggiornata quando l'attuatore e' stato scritto. CONSEGUENZA MISURATA da sister alpha-challenge 2026-07-22 sul suo repo: `ls scripts/refactor_agent.py` -> file inesistente, e il drain rifiuta TUTTI E VENTI gli item pendenti con `--agent-cmd startup probe failed -- executable could not be resolved on PATH`. Quindi **l'intero loop di drenaggio del tech-debt e' un no-op garantito su ogni progetto consumer**, e il catalogo che istruisce a invocarlo nomina un file che li' non c'e'. QUESTO E' UN DIFETTO CHE HO INTRODOTTO IO STESSA lo stesso giorno, riparando il difetto precedente ('il drain non ha attuatore'): ho scritto l'attuatore, l'ho verificato funzionante SULLA MACCHINA DI SVILUPPO, e l'ho documentato come spedito senza aggiungerlo alla whitelist. E' esattamente la classe agnosticismo-dalla-macchina-target che il mandato vieta -- un fix verificato solo dove e' stato scritto -- applicata al fix di un difetto della stessa famiglia." proposed_solution="Aggiungere `refactor_agent.py` a `UTILITY_SCRIPTS` in build_dist.py e verificare che arrivi anche per le altre vie di distribuzione (force-include del wheel in patch_pyproject.py, e il plugin utilities dell'installer se e' quella la via per un repo consumer). Poi -- ed e' la parte che impedisce la ricorrenza -- un AT che verifichi che OGNI comando/affordance che nomina uno script per percorso trovi quello script in un target INSTALLATO, non nel repo di sviluppo. La regola generale: quando la prosa dice 'X e' lo strumento spedito', 'spedito' e' un'affermazione VERIFICABILE e va verificata su un albero installato; verificarla dove lo strumento e' stato scritto non prova nulla, perche' li' c'e' per costruzione."
- [ ] drain-actuators-dispatch-a-generic-agent-not-the-role-the-drain-needs: paradigm=object-oriented defect="I DUE DRAIN HANNO BISOGNO DI DUE RUOLI DIVERSI, e l'attuatore spedito non ne nomina nessuno. Vincolo di design ratificato da Ale 2026-07-22: (a) il drain dei BUGFIX deve invocare `/nw-bugfix` DENTRO un worktree -- cioe' serve un ORCHESTRATORE, perche' /nw-bugfix e' un flusso multi-fase (RCA, charter t=0 da un PO fresco, AT di regressione dall'acceptance-designer, crafter GREEN, examine di Vera, commit) e nessun singolo agente lo copre; (b) il drain dei REFACTOR deve dispacciare un CRAFTER in un worktree separato, ed e' il crafter perche' e' il ruolo che possiede le lenti di refactoring L1-L6 come conoscenza operativa -- con la condizione che non ci siano regressioni sui TEST TOCCATI DAL REFACTORING, non su una suite arbitraria. Cio' che ho spedito stamattina (`scripts/refactor_agent.py`) fa una cosa sola e generica: lancia un `claude -p` headless col prompt renderizzato. Gira nel worktree giusto (cwd corretta) ma NON nomina il ruolo, NON carica la spec del crafter ne' le sue skill, e NON distingue fra i due drain. Quindi il pile del tech-debt verrebbe drenato da un agente senza la tassonomia degli smell, e il pile dei difetti da un agente che non sa che deve guidare una spina a sei fasi -- entrambi produrrebbero lavoro plausibile e fuori disciplina. E' lo stesso errore che avevo gia' diagnosticato per il FINDER (un agente generico riporta cio' che e' VISIBILE invece di cio' che e' COSTOSO) applicato al lato FIX." proposed_solution="Due attuatori distinti, ciascuno che nomina il proprio ruolo. REFACTOR: dispacciare il crafter selezionato dal paradigma DICHIARATO del target (object-oriented -> nw-software-crafter, functional -> nw-functional-software-crafter), caricandone la spec verbatim come istruzioni quando il tipo di agente non e' invocabile nel contesto headless -- e' il pattern gia' validato oggi per copywriter e tutorialist. La verifica verde-a-verde deve essere ancorata ai test IMPATTATI dall'item, non a una suite scelta a caso: e' il drain a conoscere i path dell'item, quindi puo' derivarli. BUGFIX: dispacciare un sotto-orchestratore col mandato di guidare `/nw-bugfix` end-to-end nel worktree, con gli stessi vincoli di ogni altra corsia (niente --skip-verify, niente auto-esame, charter da autore non disqualificato). Ed entrambi devono ereditare i vincoli di sicurezza del worktree: cleanup obbligatorio su successo E su fallimento, mai `git reset` contro un trunk in movimento, `./.venv/bin/des` e mai il shim."
- [ ] utility-scripts-whitelist-restated-in-two-places: paradigm=object-oriented defect="La whitelist degli script distribuiti e' DICHIARATA DUE VOLTE, con lo stesso identico contenuto: `scripts/build_dist.py:65` (per la build del dist) e `scripts/install/plugins/utilities_plugin.py:41` (per l'installazione sul target). Nessuna delle due punta all'altra. Scoperto 2026-07-22 aggiungendo un attuatore mancante: ho dovuto modificare ENTRAMBE, e se ne avessi modificata una sola lo script sarebbe finito nel dist ma non sul target (o viceversa) -- un fallimento parziale silenzioso, perche' nessun controllo confronta le due liste. E' la stessa classe SSOT/DRY che il progetto sta inseguendo altrove, stavolta nel percorso di DISTRIBUZIONE, cioe' dove un disallineamento non si vede mai sulla macchina di sviluppo" proposed_solution="Dichiarare la lista UNA volta e farla leggere a entrambi i consumatori. Fino ad allora, un test che asserisca l'uguaglianza delle due liste -- e' un confronto di due costanti, costa nulla, e trasforma un fallimento parziale silenzioso in un rosso immediato"
- [ ] drain-agent-cmd-path-cannot-resolve-on-a-consumer-target: paradigm=object-oriented defect="Il comando che il catalogo documenta per il drain -- `--agent-cmd 'scripts/refactor_agent.py {prompt}'` -- e' un percorso RELATIVO, che si risolve contro la cwd del repo TARGET. Ma l'installer copia gli utility script in `~/.claude/scripts/` (`utilities_plugin.py`: destinazione `context.claude_dir / 'scripts'`), NON dentro il repo consumer. Quindi anche DOPO aver aggiunto l'attuatore alle whitelist, da un repo consumer `shutil.which('scripts/refactor_agent.py')` -- la sonda che il drain esegue sul primo token -- continuera' a non risolvere, perche' li' quella directory non contiene lo script. E il percorso corretto non e' scrivibile in modo portabile nella prosa: `~/.claude/...` non viene espanso da `shutil.which`, e nemmeno `$HOME/...`, perche' la SONDA non passa da una shell (l'espansione avviene solo nell'invocazione, che usa shell=True). Quindi documentare il percorso assoluto lo renderebbe specifico di un utente. MISURATO indirettamente: una sorella ha eseguito il drain sul proprio repo e ha ottenuto il rifiuto della sonda su tutti e 20 gli item" proposed_solution="Serve una decisione di design, non un'altra riga di prosa. Tre opzioni con conseguenze diverse: (a) installare l'attuatore in una directory che sta sul PATH, cosi' il primo token e' un nome nudo e la sonda risolve ovunque -- e' la piu' portabile e rende il comando documentabile una volta sola; (b) far risolvere al drain stesso il percorso dell'attuatore installato (il DES conosce gia' `claude_dir`), cosi' `--agent-cmd` diventa opzionale e il default e' corretto per costruzione -- sposta il costo sul sistema invece che sull'operatore; (c) installare anche dentro il repo target, che duplica file e crea un secondo posto da tenere aggiornato. La (b) e' la piu' coerente col principio che il costo sta sul sistema: un operatore non dovrebbe dover sapere DOVE e' installato l'attuatore per poterlo invocare"
- [ ] commit-slice-discards-the-hook-output-that-IS-the-why: paradigm=object-oriented defect="`des commit-slice` fallisce con un messaggio NUDO che butta via la spiegazione che ha gia' in mano: `{\"event\":\"CommitFailed\",\"error\":\"git commit failed: Command '['git','commit','--file','-']' returned non-zero exit status 1.\"}`. WHAT presente, WHY e HOW entrambi assenti. Ma il WHY NON MANCA: l'hook di pre-commit lo scrive esplicitamente sul proprio stdout/stderr -- `files were modified by this hook` e `Stashed changes conflicted with hook auto-fixes... Rolling back fixes...` -- e quel testo viene SCARTATO in favore del solo codice di uscita. Segnalato da sister alpha-challenge 2026-07-22, riprodotto altre due volte nella stessa serata e gia' osservato in una sessione precedente: e' RICORRENTE, non un caso. CAUSA RADICE che lei ha dovuto ricostruire a mano ogni volta: l'hook `ruff-format` mette in stash le modifiche non staged, riformatta il file STAGED, e nel ripristinare lo stash va in conflitto con l'auto-fix appena applicato -> ROLLBACK, che riporta in silenzio il file staged allo stato pre-formattazione; `git commit` fallisce sull'exit non-zero dell'hook. AGGRAVANTE che rende il difetto costoso invece che solo fastidioso: **ritentare `des commit-slice` immutato RIPRODUCE l'errore identico all'infinito**, perche' il conflitto di stash si ripresenta a ogni tentativo -- e senza il WHY l'operatore non ha modo di sapere che ritentare e' inutile. L'unico percorso di uscita e' deterministico e sempre lo stesso (eseguire il formatter direttamente sul file, aggirando la danza dello stash, e ri-stageare), quindi il HOW e' scrivibile UNA volta e vale sempre. TERZO esemplare oggi della stessa classe -- il dato che renderebbe utile il messaggio e' gia' nello scope di chi lo scrive -- e il piu' letterale dei tre: qui non va nemmeno derivato, va solo NON buttato." proposed_solution="Catturare e riportare stdout/stderr dell'hook di pre-commit invece di scartarli: quel testo E' il WHY, gia' pronto, e oggi viene sostituito da un codice di uscita che non dice nulla. Poi, poiche' il percorso di recupero e' deterministico, il HOW puo' essere meccanico: 'esegui <formatter> <file> direttamente, ri-stagea, ritenta'. E aggiungere l'informazione che salva piu' tempo di tutte: dire ESPLICITAMENTE che ritentare senza intervento riprodurra' lo stesso errore -- un operatore davanti a un fallimento nudo ritenta, ed e' la cosa peggiore che possa fare qui. Verificare nello stesso giro gli altri punti dove un subprocess fallisce e ne viene riportato solo il returncode: la cattura dell'output e' gia' possibile ovunque, e' solo questione di non scartarla"
- [ ] two-competing-install-root-idioms-one-silently-wrong-on-alt-profiles: paradigm=object-oriented defect="L'albero ha DUE idiomi per risolvere la radice d'installazione, e uno dei due e' DIMOSTRABILMENTE ROTTO per chi usa un profilo non-default. ROBUSTO: `src/des/runtime/interpreter.py:307-314` `_des_root()` risale con `parents[2]` dalla posizione del modulo -- non guarda MAI il NOME di una directory, quindi funziona identico su qualunque profilo, ed e' gia' load-bearing in produzione (`des_subprocess_env` lo usa per tenere `des` importabile in ogni figlio spawnato). FRAGILE: `src/des/adapters/.../tdd_template_loader.py:40-47` E `json_schema_loader.py:51-59` fanno entrambi `if parent.name == \".claude\"`, cioe' confrontano il NOME LETTERALE della directory. DIMOSTRAZIONE, e la fornisce questa sessione stessa: gira con `CLAUDE_CONFIG_DIR=/home/alexd/.claude-alt3`, e `/home/alexd/.claude-alt3/lib` ESISTE -- sotto quel profilo il confronto sul nome fallisce e il loader cade IN SILENZIO sul fallback di layout-sorgente, restituendo un percorso plausibile e sbagliato invece di rifiutare. Il progetto supporta ufficialmente i profili multipli (tre abbonamenti condividono l'installazione via CLAUDE_CONFIG_DIR) e l'INSTALLER li onora gia' (`PathUtils.get_claude_config_dir()`), quindi il runtime e l'installer sono in disaccordo su dove sia la radice. `json_schema_loader.py:11` si autodescrive come impalcatura che porta 'the candidate-path order and the parents[N]/marker logic', il che suggerisce che il pattern sia stato COPIATO e non derivato. Trovato da sister multilang durante la RCA sul percorso dell'attuatore, e riportato PRIMA di costruire, perche' copiare l'idioma fragile avrebbe spedito un resolver rotto per ogni utente multi-profilo." proposed_solution="Convertire i due loader fragili all'idioma robusto: risalire dalla posizione del modulo con `parents[N]` invece di confrontare il nome di una directory. Un nome di directory non e' un'identita' -- e' una convenzione che l'utente puo' cambiare, e qui la cambia per progetto. Aggiungere un AT che esegua i due loader sotto un `CLAUDE_CONFIG_DIR` NON-default e verifichi che risolvano la radice corretta invece di cadere sul fallback: e' il caso che oggi fallisce, ed e' banale da costruire in tmp. Poi valutare se estrarre l'idioma robusto in un solo posto letto da tutti e tre i chiamanti, cosi' non esistano piu' due opinioni su come si trova la radice. E' la stessa classe di `tool_discovery.py:97` (rimedio Rust cablato per ogni tool): un helper scritto quando esisteva UNA sola forma, mai generalizzato, ora silenziosamente sbagliato sulla seconda -- secondo esemplare noto oggi."
- [ ] refactor-drain-emits-no-progress-and-its-scope-may-exceed-the-design: paradigm=object-oriented defect="DUE OSSERVAZIONI DISTINTE dallo stesso run, e vanno tenute separate perche' hanno rimedi diversi. (1) OSSERVABILITA': `des refactor --pile` non emette NULLA per tutta la durata di un item -- 25 minuti di orologio, ZERO byte su stdout, nessun log di progresso da nessuna parte (il worktree contiene solo `.nwave/des/logs/audit-*.log`, che non riporta il drain). Per distinguere 'sta lavorando' da 'e' morto' ho dovuto fare TRE giri di diagnostica e finire per leggere `/proc/<pid>/stat` e il tempo CPU. Verdetto reale: stato R, 21.3% CPU, 5m23s di CPU su 25m14s di orologio -- lavora, affamato dalla contesa, ed esegue pytest IN-PROCESS (nessun figlio, il che rende inutile anche il controllo sui processi figli). Un operatore normale non ha modo di saperlo, e la conclusione naturale davanti a 25 minuti di silenzio e' 'e' bloccato, lo ammazzo' -- che distruggerebbe un lavoro in corso valido. (2) AMBITO, misurato ma non ancora spiegato: il loop di drenaggio specifica che per item devono girare SOLO i test veloci e impattati dal cambio di quell'item, MAI una suite intera, perche' altrimenti si uccide il parallelismo per cui il design esiste. Venticinque minuti sul primo item non e' 'veloce e impattato'. O l'ambito eseguito e' piu' largo del progettato, o l'impatto di un item god-class e' genuinamente enorme -- non l'ho determinato, e la differenza conta: nel primo caso e' un difetto, nel secondo e' una proprieta' dell'item. Con 19 righe in coda a questo ritmo il pile richiede ore, in serie." proposed_solution="(1) Emettere progresso: quale item e' in lavorazione, in quale fase (provisioning / verde-baseline / dispatch del fixer / verde-finale / merge / cleanup), e un battito periodico. Un processo che lavora 25 minuti in silenzio e' indistinguibile da uno morto, e la disciplina 'controlla la freschezza invece di fidarti della notifica' che abbiamo adottato per gli agenti vale identica qui -- ma richiede che ci SIA qualcosa da guardare. Minimo indispensabile: una riga per transizione di fase. (2) MISURARE l'ambito realmente eseguito dalla gamba verde-a-verde e confrontarlo con l'ambito impattato dall'item: se esegue piu' del necessario, restringerlo e' cio' che rende il drenaggio parallelizzabile come da design; se l'ambito e' corretto e l'item e' semplicemente grande, allora il rimedio e' diverso -- dichiararlo nel progresso, cosi' l'operatore sa che 25 minuti sono attesi e non un guasto."
- [ ] sourcing-loops-outpace-draining-loops-with-no-backpressure: paradigm=object-oriented defect="I loop che SORGENTANO (find tech-debt, source bugfix-queue) non hanno alcuna CONTROPRESSIONE dai loop che DRENANO, quindi girano alla loro cadenza indipendentemente dal fatto che la coda si stia svuotando o no. MISURATO 2026-07-22 nell'arco di una sola sessione: i pile sono cresciuti a 19 righe di tech-debt e 125 di difetti mentre il drain ne ha svuotate ZERO -- e quando il drain e' finalmente partito, ha impiegato 32 minuti di orologio per 5m51s di CPU sul PRIMO item, perche' affamato dalla contesa (18% su 4 core, con oltre un core occupato stabilmente da due sessioni claude, un server MCP e un demone prima che qualunque lavoro cominci). A quel ritmo la coda richiede una notte intera, in serie, mentre due loop continuano ad allungarla ogni 30 minuti. Ale l'ha nominato per primo: 'bisogna attivare i fixer, altrimenti nessuno sistema i difetti o fa i refactoring'. Il difetto non e' che i finder trovino troppo -- trovano bene, e i reperti sono verificati. E' che il sistema non ha modo di sapere che sta accumulando piu' velocemente di quanto smaltisce, quindi nessuno se ne accorge finche' qualcuno non guarda i numeri assoluti. Conseguenza qualitativa oltre a quella quantitativa: un pile che cresce e non cala smette di essere una coda di lavoro e diventa un archivio, e un archivio nessuno lo drena perche' non sembra piu' azionabile." proposed_solution="Dare ai loop di sourcing una contropressione misurata: prima di dispacciare un finder, confrontare la lunghezza del pile con il ritmo di drenaggio recente (item chiusi nelle ultime N ore) e SALTARE il giro quando il rapporto e' oltre soglia, dicendo perche'. Il segnale c'e' gia': i pile sono file, il drain lascia traccia. Serve solo che il sourcing lo legga. Regola di formulazione: il salto va DICHIARATO ('pile a 19, zero drenate nelle ultime 4 ore, salto il sourcing'), mai silenzioso -- un loop che tace non si distingue da un loop morto, che e' l'altro difetto di questa stessa famiglia. Piu' a monte, e' una questione di Theory of Constraints: aggiungere capacita' a MONTE del collo di bottiglia non produce throughput, produce coda -- e qui il collo di bottiglia non e' trovare, e' avere una macchina su cui riparare."
- [ ] refactor-drain-green-leg-deadlocks-on-nested-pytest: paradigm=object-oriented defect="LA GAMBA VERDE-A-VERDE DEL DRAIN VA IN DEADLOCK SU PYTEST ANNIDATO, ed e' la ragione per cui il drenaggio del tech-debt non ha mai funzionato. Catena misurata 2026-07-22 leggendo /proc, dopo 35 minuti di silenzio totale sul primo item: `des refactor` (stato S, wchan `do_sys_poll`) -> `des.cli.run_tests --target <worktree>` (S, `do_sys_poll`, tre fd pipe aperti) -> `_collect_scope_worker.py --run --repo /tmp/pytest-of-alexd/pytest-5747/test_run_contract_gate_reports1/target-repo --jobs auto --path .../tests/build` (S, wchan `pipe_read`) -> worker xdist `python3 -u -c import sys;exec(eval(sys.stdin.readline()))` (S, in attesa su stdin). TUTTI DORMIENTI. Velocita' istantanea misurata campionando /proc/<pid>/stat a 10 secondi di distanza: 5 tick su 1000 possibili, cioe' lo 0.5% di un core -- NON e' lentezza da contesa, e' un blocco. LA FORMA: la gamba verde-a-verde esegue la suite del repo; quella suite contiene test che invocano il contract gate su repo-fixture sintetici; il gate lancia a sua volta un collect worker con `--jobs auto` (xdist); e l'annidamento si blocca sul buffer delle pipe. E' pytest dentro pytest dentro pytest. NOTA STORICA che rende il difetto riconoscibile: ADR-ULAR-004 documenta gia' un fallimento '8-nested-pytest / 22-min-kill' e una guardia di rientranza introdotta per quel caso -- questo percorso non ce l'ha, quindi la guardia esiste ma non copre la gamba del drain. TRAPPOLA DIAGNOSTICA da cui io stessa sono passata: `ps` riporta il %CPU MEDIO SULLA VITA del processo, non quello attuale, quindi un processo bloccato adesso ma attivo nei primi minuti riporta ~18% e sembra 'lento per contesa'. Solo il campionamento istantaneo di /proc/<pid>/stat, o lo stato S contro R, distingue fermo da lento -- e la distinzione decide se aspettare o uccidere." proposed_solution="DECISIONE DI ALE 2026-07-22, e va PRIMA di ogni altro rimedio perche' attacca la causa e non il sintomo: **i test non devono invocare la suite di test VERA -- devono costruirne una FINTA**, cosi' l'annidamento ricorsivo non comincia proprio. Il principio generale: un test che esercita l'ESECUTORE DI TEST non deve usare come input l'albero dell'esecutore stesso; se lo fa, la ricorsione e' illimitata per costruzione e nessun timeout la rende corretta -- lo rende solo sopportabile. Concretamente: i test che invocano `run-contract-gate` (e ogni altro gate che a sua volta lancia pytest) devono puntare a un repo-fixture SINTETICO che contenga una suite minima e finta, costruita apposta, non alla suite reale del progetto ne' a un albero che la contenga. Un fixture sintetico e' anche un test MIGLIORE: e' deterministico, veloce, e verifica il comportamento del gate invece di verificare incidentalmente la suite del progetto. I rimedi che avevo scritto prima restano validi ma DIETRO questo, come difesa in profondita' e non come cura: (a) estendere la guardia di rientranza di ADR-ULAR-004 alla gamba verde-a-verde del drain: quando `run_tests` gira su un worktree del repo stesso, i test che invocano il gate devono riconoscere di essere gia' dentro un'esecuzione e rifiutare di annidare, o essere esclusi dall'ambito verde-a-verde. (b) dare alla gamba un TIMEOUT esplicito con degrade-LOUD (INDETERMINATE con what/why/how, mai attesa infinita) e un battito di progresso -- un blocco che si presenta come silenzio e' indistinguibile da lavoro lento, e senza timeout il drain resta appeso finche' un umano non legge /proc. (c) verificare se `--jobs auto` dentro un contesto gia' parallelo sia parte della causa; su un box a 4 core un xdist annidato dentro un xdist e' una ricetta per l'esaurimento delle pipe."
- [ ] standing-loops-armable-concurrently-across-sessions-with-no-coordination: paradigm=object-oriented defect="I nove standing loop possono essere armati INDIPENDENTEMENTE in piu' sessioni contemporaneamente, senza che nessuna veda le altre -- e tre di essi dispacciano agenti che SCRIVONO sugli alberi di lavoro. MISURATO 2026-07-22: l'orchestratore di sessione aveva i dieci loop armati; un SUBAGENTE a meta' missione, in un worktree, ha ricevuto la stessa iniezione del catalogo, ha eseguito `CronList` (che vede solo la PROPRIA sessione, quindi ne ha visti zero) e ha armato tutti e nove. Risultato: diciannove job schedulati su un box gia' saturo, di cui sei duplicati di quelli che dispacciano scrittori. Il danno effettivo e' stato nullo per tempismo -- solo il loop di ragionamento e' scattato, e i tre che dispacciano sono stati cancellati prima delle rispettive marche cron -- ma la nullita' e' stata FORTUNA, non una proprieta' del sistema. CAUSA IMMEDIATA (mia, e gia' corretta): avevo cambiato la policy da 'chiedi prima di armare' a 'arma senza chiedere' senza che il testo nominasse il proprio DESTINATARIO, quindi un subagente che leggeva 'tu' lo applicava a se'. Ho aggiunto una guardia di ruolo, e il subagente si e' disarmato appena l'ha letta. PROVA CHE L'AMBIGUITA' ERA REALE E NON UN ERRORE DI LETTURA: due subagenti hanno letto lo stesso file nello stesso pomeriggio e si sono comportati in modo OPPOSTO -- uno ha armato, l'altro ha declinato e ha segnalato. CAUSA STRUTTURALE che la guardia NON risolve: `CronList` e' per-sessione, quindi nessun attore puo' sapere se i loop sono gia' armati altrove. La guardia di ruolo blocca il caso 'subagente', ma due SESSIONI ORCHESTRATRICI legittime sullo stesso box (qui ce ne sono abitualmente due o tre, per progetto) armerebbero entrambe i propri nove, ciascuna correttamente e all'oscuro dell'altra." proposed_solution="Rendere lo stato di armamento visibile OLTRE la sessione: un marcatore su disco (per esempio sotto la directory di configurazione condivisa) che registri quale sessione ha armato cosa e quando, letto prima di armare. Chi trova un armamento vivo di un'altra sessione non arma i loop che dispacciano scrittori -- puo' armare quelli puramente di ragionamento, che non hanno effetti collaterali. Il marcatore va con scadenza (una sessione morta non deve bloccare per sempre) e la sua assenza deve essere fail-safe verso l'armare, non verso il bloccare. Corollario che vale a prescindere: distinguere nel catalogo i loop che DISPACCIANO da quelli che RAGIONANO, perche' i secondi sono duplicabili senza danno e i primi no -- oggi sono presentati come un blocco unico di nove, e questo rende ogni decisione tutto-o-niente."

- [ ] feature-end-examine-phase-dispatches-crafter-not-examiner: paradigm=object-oriented defect="`des dispatch --phase FEATURE_END_EXAMINE` emette `Agent: nw-software-crafter`, cioe' dispaccia un CRAFTER per la fase il cui unico scopo e' un'osservazione INDIPENDENTE del sistema in esecuzione. Misurato 2026-07-22 generando l'envelope reale per la feature `codefact-similar-responsibility`: oltre all'identita' sbagliata, il corpo generato porta `# QUALITY_GATES: All the slice's ATs pass before commit. No new tests authored by the crafter.` e chiude con `STOP after the ATs are green` -- istruzioni che per un esame non significano nulla. CAUSA ESATTA, `src/des/cli/dispatch.py:168-172`: la mappa `_PHASE_AGENTS` contiene `D_DISTILL -> nw-acceptance-designer`, `C_REVIEWER_AUDIT -> _EXAMINER_AGENT` e `FEATURE_END_RETURN_PHASE -> _FEATURE_END_REVIEWER_AGENT`, ma NON contiene `FEATURE_END_EXAMINE`, che quindi cade su `_DEFAULT_AGENT = nw-software-crafter` (:143). La costante `_EXAMINER_AGENT = nw-user-examiner` esiste gia' a :151 ed e' gia' usata per l'esame PER-SLICE: e' l'esame a scope FEATURE che non e' mai stato aggiunto. PERCHE' E' GRAVE E NON COSMETICO: Vera vale precisamente perche' non puo' leggere il sorgente -- il verdetto nasce da comportamento osservato, non da codice letto. Un crafter legge il sorgente per definizione, quindi passare l'envelope verbatim (che e' la disciplina) produce un 'esame' che e' l'esatto opposto di un esame, e lo produce in silenzio: nessun gate si accorge che a esaminare e' andato chi ha scritto. Stesso schema del difetto sorella riportato da alpha-challenge, dove Vera ispezionava il database invece della superficie -- un verdetto raggiunto per la via sbagliata non e' un PASS piu' debole, e' un'affermazione diversa che indossa la stessa parola. IMPATTO MISURATO: 41 feature armate (50 charter) sono in attesa dell'esame a scope feature-end come precondizione del feature-end massivo; nessuna di esse puo' essere dispacciata col tool produttore finche' la mappa non e' corretta." proposed_solution="Aggiungere `ATDDPurePhase.FEATURE_END_EXAMINE.value: _EXAMINER_AGENT` a `_PHASE_AGENTS`. Ma il fix minimo non basta: (a) l'AT deve essere una PARAMETRIZZAZIONE su TUTTE le fasi, che asserisca per ciascuna quale agente e' ammesso -- l'omissione e' potuta esistere perche' la mappa e' un'enumerazione parziale con un default silenzioso, quindi ogni fase futura nascera' con lo stesso difetto finche' l'esaustivita' non e' verificata meccanicamente; (b) il corpo dell'envelope va reso coerente con l'identita' risolta -- QUALITY_GATES e TIMEOUT_INSTRUCTION parlano di crafter e di ATs verdi anche quando l'agente e' l'esaminatrice, quindi la sezione va derivata dal ruolo e non incollata; (c) considerare se un default silenzioso sia la scelta giusta per questa mappa: una fase non mappata che degrada verso il crafter e' un degradare-verso-il-permissivo, mentre un rifiuto loud 'fase X non ha un agente dichiarato' sarebbe degradare-verso-il-cauto."

- [ ] catalog-names-actuator-by-relative-path-absent-on-consumer: paradigm=object-oriented defect="IL CATALOGO DELLE AFFORDANCE ISTRUISCE IL DRAIN CON UN PATH RELATIVO CHE SU UNA MACCHINA UTENTE NON ESISTE, ed e' la causa che colpisce davvero un tester del canale experimental -- diversa dalle altre due gia' filate. `nWave/data/orchestrator-affordance/00-standing-loops.md:244` nomina l'attuatore come `scripts/refactor_agent.py`, cioe' un path RELATIVO alla cwd; la sonda lo risolve con `shutil.which` (`agent_invocation_port.py:45-48`, il cui contratto e' 'un comando irrisolvibile DEVE rifiutare'). Nel progetto di un utente quel file non e' li' -- l'installer lo mette sotto la directory di configurazione, non dentro il repo dell'utente -- quindi la sonda fallisce e il drain rifiuta OGNI item. MISURATO da alpha-challenge: 20 item su 20 rifiutati, messaggio identico. PERCHE' E' UNA CAUSA DIVERSA E NON UNA RIFORMULAZIONE: erano gia' note (a) l'attuatore assente dal force-include della wheel e (b) il deadlock dello stdin. Ma (a) NON colpisce questo canale -- `publish_experimental.py` non esclude `scripts/` dal suo RSYNC_FILTER, quindi il clone PORTA l'attuatore e `utilities_plugin.py:76` lo installa; il buco della wheel morde `pipx install nwave-ai`, un percorso che l'annuncio della beta non offre nemmeno. Quindi chi togliesse l'avvertenza dal post dicendo 'il fix della wheel e' uscito' toglierebbe un avviso ANCORA VALIDO. TRAPPOLA DA CUI SONO PASSATA IO: tre difetti con lo stesso sintomo osservabile ('il drain non parte') e tre cause indipendenti, di cui due che si presentano identiche dall'esterno. Il sintomo condiviso mi ha fatto trattare (a) come LA causa e dichiarare la catena chiusa quando non lo era." proposed_solution="Il fix della corsia `drain-resolves-its-own-actuator` -- far risolvere al DES il proprio attuatore installato quando `--agent-cmd` non e' passato -- copre questo caso SOLO se l'operatore NON passa `--agent-cmd`. Ma il catalogo istruisce esplicitamente a passarlo, col path relativo. Quindi servono due cose: (1) il catalogo (e ogni prosa che documenta il drain) deve smettere di nominare un path relativo -- o omette del tutto `--agent-cmd` lasciando che il DES risolva, oppure nomina il tool produttore invece del percorso; (2) il rifiuto della sonda deve dire COME rimediare nominando l'auto-risoluzione ('ometti --agent-cmd e il DES risolve l'attuatore installato'), non solo che il comando non e' risolvibile. Verifica finale che nessuna delle due copre da sola: un repo che NON e' questo, con nWave installato, che lancia il comando esattamente come documentato."

- [ ] examiner-reports-absence-from-a-bounded-observation: paradigm=object-oriented defect="VERA DICHIARA ASSENTE CIO' CHE NON HA CERCATO ABBASTANZA, e lo dichiara con la stessa sicurezza di una presenza. MISURATO da alpha-challenge, 3 occorrenze indipendenti sullo STESSO elemento in una sola serata: un link 'View Say-Do Ledger' che sta in fondo a una card Dash molto alta (sotto il gauge, la spiegazione, il timestamp, una sezione WHY collassata e un blocco 'Drawn from'). Due volte durante esami diretti del fix di quel link, una terza volta nel walk della demo su ambiente deployato: Vera ha riportato il link ASSENTE dal sito vivo, mentre uno screenshot Playwright diretto contro la stessa URL lo mostra renderizzato esattamente dove atteso. Ipotesi di alpha, precisa e da verificare non da confermare: l'automazione browser non scorre abbastanza DENTRO il contenuto della card prima di concludere l'assenza -- distinto dallo scroll di pagina, e distinto dalla causa delle prime due occorrenze (collisione di porte tra sessioni dashboard concorrenti, gia' trovata e chiusa). LA FORMA, che e' la parte generalizzabile: l'ASSENZA NON E' FALSIFICABILE DA UN'OSSERVAZIONE LIMITATA. 'Lo vedo' e' una prova; 'non lo vedo' e' una prova solo se la ricerca era esaustiva, e niente nello strumento di Vera stabilisce l'esaustivita'. Quindi riporta un'assenza dedotta da una finestra limitata con la stessa confidenza di una presenza osservata. E' una proprieta' strutturale dello strumento, non una disattenzione della singola esecuzione -- ed e' per questo che si ripete identica tre volte. E' la STESSA conflazione epistemico/ontologico del false-green del feature-end ('la collection e' fallita' letto come 'questo repo non ha una suite'), con il segno opposto. NOTA DI DIREZIONE (Ale 2026-07-22): un FAIL inventato e' MEGLIO di un PASS immeritato -- il primo costa un'indagine, il secondo spedisce il difetto. Quindi il difetto NON e' che Vera sbagli in senso prudente; e' che dichiari con certezza cio' che non ha stabilito." proposed_solution="Un'osservazione NEGATIVA deve portare la propria prova di esaustivita', o degradare a INDETERMINATE. Concretamente: quando l'elemento atteso non viene trovato, l'esaminatrice deve dimostrare che la ricerca era esaustiva -- contenitore scorso fino alla propria fine, oppure elemento assente dallo snapshot di accessibilita' dell'INTERO sottoalbero e non del solo viewport -- prima che un FAIL sia ammissibile. Senza quella dimostrazione il verdetto onesto e' INDETERMINATE, che e' esattamente vero e non e' la stessa affermazione di 'non c'e''. Da mettere nella skill dell'esaminatrice come regola enumerata, e possibilmente da rendere meccanico in `des record-examine-verdict`: un FAIL motivato da assenza che non porta evidenza di ricerca esaustiva viene rifiutato o riclassificato. Credito ad alpha-challenge per il pattern e per la precisione dell'ipotesi (profondita' di scroll DENTRO il contenitore, non di pagina) -- 'Vera e' ballerina' non sarebbe stato filabile."

- [ ] precommit-stash-rollback-silently-reverts-concurrent-agent-edits: paradigm=object-oriented defect="LO STASH/RESTORE DEL PRE-COMMIT CANCELLA IN SILENZIO IL LAVORO NON-STAGED DI ALTRI AGENTI VIVI, senza emettere una parola su cosa ha cancellato. MISURATO 2026-07-22, 3 occorrenze in un'ora su `nWave/templates/experimental-readme.md`. Meccanica: `pre-commit` mette in stash OGNI modifica non-staged dell'albero prima di girare gli hook e la ripristina dopo; ma se un hook modifica dei file fa ROLLBACK, cioe' ripristina lo snapshot catturato al momento dello STASH -- quindi ogni scrittura fatta da chiunque altro tra lo stash e il rollback viene ANNULLATA. PROVA: nove patch sotto ~/.cache/pre-commit/ contengono il template, e il mtime del file (15:50:21) coincide al secondo con l'ultimo ripristino, che proveniva da un mio commit. Ho committato alle 15:26, 15:28, 15:38, 15:47, 15:50; i tre revert dell'agente combaciano. PERCHE' E' PARTICOLARMENTE INSIDIOSO: (a) NESSUN MESSAGGIO -- l'unico output e' `[INFO] Restored changes from ...`, che suona come un ripristino riuscito, non come una cancellazione; l'agente vittima non ha modo di sapere che e' successo. (b) Non e' un overwrite dell'intero file ma di REGIONI ALTERNATE, a seconda di dove cadono le scritture rispetto alla finestra dello stash -- due revert hanno preso meta' delle modifiche ciascuno e il terzo tutte, il che rende il pattern irriconoscibile e fa sembrare 'flaky' un comportamento deterministico. (c) La verifica dell'agente e' ONESTA nel momento in cui la fa e FALSA un attimo dopo: il grep torna pulito, poi il file cambia sotto. (d) Il harness riporta la modifica come 'intenzionale, attribuita all'utente', quindi l'agente vittima non puo' distinguere un revert umano deliberato da questa corsa -- e giustamente si ferma invece di riapplicare, perche' sovrascrivere una decisione umana sarebbe peggio. E' anche la radice del `des commit-slice` che fallisce con messaggio nudo segnalato da alpha-challenge: stesso stash/rollback, sintomo diverso. CONDIZIONE DI INNESCO: qualsiasi commit in un albero condiviso mentre un altro agente ci scrive -- cioe' la condizione NORMALE del lavoro in sciame." proposed_solution="Tre livelli. (1) MESSAGGIO, il minimo indispensabile: quando il rollback annulla modifiche non-staged, deve dire QUALI file ha riportato indietro e che il loro contenuto post-stash e' andato perso, con il path della patch da cui recuperarlo -- oggi dice `Restored changes`, che afferma il contrario di cio' che e' accaduto. (2) RILEVAMENTO: confrontare l'mtime/hash dei file non-staged prima dello stash e prima del restore; se sono cambiati nel frattempo, RIFIUTARE il rollback e degradare LOUD invece di sovrascrivere -- 'l'albero e' cambiato mentre giravano gli hook, non ripristino'. (3) DISCIPLINA, la parte nostra: il commit nell'albero condiviso e' un'operazione che SCRIVE sull'intero working tree, non solo sui file staged; va serializzata come tale. Nessun commit nel checkout principale mentre un agente vi sta scrivendo -- e il corollario forte e' che il lavoro degli agenti va in worktree isolati proprio anche per questo, non solo per i conflitti di merge."

- [ ] mid-work-message-can-end-a-dispatched-agents-turn: paradigm=object-oriented defect="UN MESSAGGIO INVIATO A UN AGENTE MENTRE LAVORA PUO' TERMINARNE IL TURNO SULL'ACKNOWLEDGEMENT, e dall'esterno il risultato e' indistinguibile da una morte. OSSERVATO 2026-07-22, 3 occorrenze in una sessione. Caso piu' netto: un crafter dispacciato per il fix della frontiera degli spawn ha speso 185k token e 34 minuti, poi ha emesso come messaggio finale UNA riga -- `Acknowledged -- dropping the wide sweep and running the scoped checks instead` -- che e' la risposta a un'istruzione ricevuta a meta' lavoro, non un rapporto di lavoro. Nessun risultato, nessun verdetto, nessuna indicazione se le verifiche scoped siano poi state eseguite. Secondo caso: un test-optimizer terminato su `Confirmed byte-identical bodies. Let me now scan for the remaining categories in parallel` dopo 162k token -- una dichiarazione di intento, non una conclusione. PERCHE' E' UN DIFETTO E NON UN INCIDENTE: la consegna di un messaggio in ingresso costituisce un turno; un agente che risponde e non ha altro da emettere in quel turno finisce, e il harness lo segnala come `completed`. Quindi CHI SUPERVISIONA PUO' UCCIDERE CIO' CHE SUPERVISIONA, e piu' spesso proprio quando e' preoccupato che sia fermo -- la sonda e' il colpo. IRONIA STRUTTURALE, ed e' la ragione per cui va filato: il rimedio istintivo al silenzio (chiedere lo stato) e' la causa della terminazione, quindi piu' un agente sembra bloccato piu' e' probabile che lo si termini per verificarlo. Stessa famiglia del drain che tace: uno stato ambiguo spinge l'osservatore verso l'azione che distrugge l'informazione che cercava." proposed_solution="Tre direzioni, la prima e' disciplina mia e vale da subito. (1) NON sondare un agente vivo per sapere se e' vivo: leggere invece l'albero e i ledger, che e' evidenza senza effetti collaterali. Mandare un messaggio solo quando porta una DECISIONE o un fatto che cambia il lavoro, mai per chiedere conferma di esistenza. (2) CONTRATTO DI DISPATCH: istruire ogni agente che una risposta a un messaggio in arrivo NON conclude il compito -- deve riprendere il lavoro nello stesso turno, e il suo messaggio finale deve sempre portare il risultato verbatim. Il contratto di recupero esiste gia' per i verdetti (`nw-execute` sezione Recovery Fallback) ma copre la morte, non la terminazione-per-acknowledgement. (3) MECCANICO: distinguere nel harness un turno concluso con un risultato da uno concluso con una acknowledgement -- oggi entrambi arrivano come `completed`, e chi orchestra non puo' sapere quale dei due ha in mano. Senza questo, ogni 'agente finito' richiede una verifica sull'albero per sapere se ha finito davvero."

- [ ] publish-drops-packages-whose-name-collides-with-a-gitignore-pattern: paradigm=object-oriented defect="LA PUBBLICAZIONE SU EXPERIMENTAL PERDE IN SILENZIO OGNI PACCHETTO IL CUI NOME COLLIDE CON UN PATTERN GENERICO DI .gitignore, e un utente beta e' bloccato adesso. SEGNALATO da Marcio Silva 2026-07-22: cinque sottocomandi `des` vanno in crash all'import su experimental -- `ModuleNotFoundError: No module named 'des.adapters.driven.output'` -- e con essi cadono validate-feature-delta (gate-OUT di DISCUSS/DESIGN), at-review-verdict (verdetto AT di DISTILL), commit-slice (il commit di slice di DELIVER), examine-fixture (la superficie di Vera) e next. Sono le porte di uscita di quattro wave su cinque. CATENA MISURATA: (1) il nostro .gitignore ha `build/` (:75) e `output/` (:84) come pattern generici, con negazioni esplicite `!src/des/adapters/driven/build/` (:77) e `!src/des/adapters/driven/output/` (:86) che li salvano; (2) `git archive` esporta correttamente entrambi i pacchetti -- verificato eseguendolo; (3) `RSYNC_FILTER` in `scripts/release/publish_experimental.py:66` contiene `--exclude=.git*`, che esclude anche `.gitignore` dalla sincronizzazione; (4) il repo di destinazione conserva quindi il PROPRIO .gitignore, che ha `build/` e `output/` MA NON le negazioni; (5) il `git add` finale nel target salta le due directory in silenzio. Verificato dall'esterno: `gh api .../contents/src/des/adapters/driven/output` -> 404, e il diff dell'albero pubblicato contro il nostro mostra ESATTAMENTE due directory mancanti, `build` e `output`. Il commit pubblicato `e03699797` conteneva entrambi i pacchetti, quindi il branch e' sano e il buco e' interamente nella pubblicazione -- Marcio aveva ragione a dire 'al ramo atdd non manca nulla'. PERCHE' LA CLASSE E' PIU' AMPIA DEL CASO: qualunque futuro pacchetto chiamato `build`, `output`, `dist`, `cache`, `reports` o simile sparira' allo stesso modo, senza errore ne' avviso, e il sintomo si manifestera' solo come ModuleNotFoundError a valle su una macchina utente. E' la stessa forma di ogni difetto di stanotte: una cosa che non arriva al consumatore e niente lo dice." proposed_solution="La radice e' che l'albero pubblicato eredita un .gitignore che non e' il nostro. Sincronizzare il .gitignore insieme al resto e' il fix diretto -- ma `--exclude=.git*` esiste per non spedire `.git`, quindi va reso preciso (`--exclude=.git/`) invece che a prefisso, verificando cos'altro quel glob stava escludendo. Difesa in profondita' che vale a prescindere: dopo il sync, VERIFICARE che l'albero committato nel target contenga ogni file che `git archive` ha esportato, e RIFIUTARE la pubblicazione con l'elenco dei mancanti se non e' cosi'. Oggi la pubblicazione non ha alcun controllo di completezza: puo' pubblicare un albero mutilato ed esce 0. Un `git status --ignored` nel target prima del commit avrebbe reso il difetto visibile in un secondo. Verifica finale che nessuna delle due copre da sola: clonare il repo pubblicato, installare, e importare i cinque sottocomandi che Marcio ha visto cadere."

- [ ] examiner-blindness-claimed-by-construction-is-only-by-instruction: paradigm=object-oriented defect="L'ENVELOPE DICHIARA UNA GARANZIA CHE IL RUOLO NON HA, e questo ha invalidato CINQUE esami in due progetti nella stessa giornata. `src/des/cli/dispatch.py:464` e `:501` affermano che l'esaminatrice non ha accesso a sorgente, design e test di accettazione *by construction*. `nWave/agents/nw-user-examiner.md:6` dichiara `tools: Read, Edit, Bash, mcp__playwright__*` -- Read e Bash senza alcuna restrizione di percorso. Nulla le impedisce di aprire `src/`. La garanzia e' PER ISTRUZIONE, dichiarata come se fosse PER COSTRUZIONE. CINQUE FALLIMENTI, UNA RADICE: (1) esaminatrice che ha esaminato il repo principale invece del worktree bersaglio; (2) esaminatrice che ha esaminato la superficie CLI sbagliata; (3) esaminatrice che ha letto `src/des/cli/refactor.py` quattro volte, il file di test tre volte, ha eseguito un grep sull'implementazione e ha riportato 'code inspection confirms...' come propria evidenza -- un code review travestito da esame; (4) e (5), riportati indipendentemente da alpha-challenge, un PASS con osservazioni 'Direct database inspection confirms...' contro un charter che le era stato assegnato da percorrere in browser, e una seconda occorrenza identica. Ognuno sembrava un errore diverso. Sono lo stesso difetto. PERCHE' E' PEGGIO DI UN VINCOLO SEMPLICEMENTE NON IMPOSTO: un vincolo non imposto e DICHIARATO tale lascia all'orchestratore l'onere del controllo, e lui lo esercita. Un vincolo non imposto ma dichiarato IMPOSTO gli dice di non controllare. La frase 'by construction' e' precisamente l'istruzione a fidarsi. DANNO CONCRETO OGGI: due corsie non possono committare, perche' l'unica strada per un PASS onesto passa da un ruolo che non puo' garantire la propria cecita', e un verdetto raggiunto leggendo l'implementazione non e' un PASS piu' debole -- e' un'affermazione DIVERSA che indossa la stessa parola. Un'esaminatrice ha anche tentato una mutazione dal vivo su un albero (rinominare l'attuatore per simulare l'assenza) -- istinto GIUSTO, cioe' costruire un negativo vero, eseguito su un albero che le era stato detto di non toccare, perche' non esiste una via sanzionata per farlo su un bersaglio usa-e-getta." proposed_solution="Il confronto che decide il progetto e' il ruolo REVIEWER, che e' genuinamente vincolato: il suo Bash e' ristretto a un solo strumento, ed e' per questo che in un'indagine recente ha potuto solo TRACCIARE e ha DOVUTO dirlo, invece di affermare di aver eseguito. Quello e' cio' che 'by construction' deve significare. Due strade, e la prima e' quella giusta: (a) RESTRINGERE il toolset perche' corrisponda all'affermazione -- niente Read su percorsi sorgente, Bash limitato alle superfici di prodotto, ed eliminare Edit se non serve; l'unica eccezione da progettare e' una via SANZIONATA per costruire un negativo su un bersaglio usa-e-getta, perche' altrimenti l'istinto giusto continuera' a manifestarsi come mutazione non autorizzata. (b) Se (a) e' costoso, smettere di dire 'by construction' e dire 'by instruction -- verifica la sua trascrizione', cosi' l'orchestratore sa che il controllo e' suo. La (b) e' onesta ma lascia il costo sull'operatore, quindi e' un ripiego, non la soluzione. Rimedio immediato gia' in uso stanotte, da mantenere finche' (a) non atterra: la clausola esplicita nel dispatch ('se apri src/ il tuo verdetto e' VOID e leggero' la tua trascrizione') piu' il controllo effettivo della trascrizione PRIMA di registrare -- rende la violazione rilevabile a posteriori, che e' meno di imporla ma piu' di niente."

- [ ] staleness-detector-compares-versions-not-content: paradigm=object-oriented defect="IL RILEVATORE DI SKEW DEL RUNTIME INSTALLATO CONFRONTA STRINGHE DI VERSIONE, NON CONTENUTO, quindi tace esattamente nel caso che si verifica sempre. `src/des/adapters/drivers/hooks/session_start_handler.py:209-227` prende `installed_hook_version` e `checkout_version` e li passa entrambi a `_parse_version_tuple`, confrontando i numeri. MISURATO 2026-07-22: albero installato e sorgente differiscono in SETTE file -- `dispatch.py`, `feature_end_cycle_service.py`, `at_completion_ledger.py`, `run_contract_gate.py`, `tree_hash.py`, `coverage_map_signoff_writer.py`, `port_realization_discovery.py` -- e il rilevatore non segnala nulla, perche' entrambe le parti dicono 4.0.0. PERCHE' TACE SEMPRE: la versione la muove il RILASCIO, non il merge. Ogni merge su `src/des` lascia l'installato indietro senza toccare il numero, quindi la condizione che il rilevatore cerca non si verifica MAI nel flusso di sviluppo normale -- il rilevatore e' verde per costruzione proprio quando la deriva e' massima. Chiede 'la versione installata e' piu' vecchia?' quando la domanda e' 'il codice installato e' diverso?'. Sono la stessa domanda solo se ogni cambiamento bumpa la versione. DANNO MISURATO OGGI, e ne sono io la prova: ho committato un fix a `dispatch.py`, l'ho verificato con `uv run des` (che risolve al SORGENTE del repo tramite il venv), ho riportato 'verificato sul tool vero', e ho dichiarato sbloccate 41 esaminazioni. Un altro agente ha poi eseguito `des` da PATH -- lo shim sul runtime INSTALLATO -- e ha ottenuto l'agente sbagliato. Due misure entrambe vere che rispondono a domande diverse, e nessun rilevatore ha protestato. Il danno maggiore e' potenziale e vale la pena nominarlo: `feature_end_cycle_service.py` e `at_completion_ledger.py` sono fra i sette file stale, cioe' la macchineria che FIRMA i verdetti di feature-end. Chiudere feature con quella stale significa firmare con codice vecchio, in silenzio. NOTA AGGIUNTIVA: l'albero installato non ha affatto un file VERSION, quindi `installed_hook_version` e' None e il ramo `:223` prende un percorso di fallback -- da verificare se quel fallback sia loud o silenzioso." proposed_solution="Confrontare il CONTENUTO, non la versione, e la macchineria esiste gia' in casa: `tree_hash.py` (ironicamente uno dei sette file stale) calcola gia' un hash canonico di albero. Il rilevatore dovrebbe confrontare l'hash dell'albero installato con quello del sorgente e segnalare quando divergono, indipendentemente dalle versioni -- che e' la stessa proprieta' che il gate di contratto usa gia' per il proprio digest. La versione resta utile come informazione secondaria nel messaggio, non come criterio. Il messaggio deve portare i tre pezzi: COSA (l'installato differisce dal sorgente in N file), PERCHE' (un merge su src/des non aggiorna l'installato), COME (rieseguire l'installer, nominando il comando esatto) -- e deve ELENCARE i file divergenti, perche' 'sette file' e 'sette file di cui due firmano i verdetti di feature-end' sono avvisi di gravita' diversa. Guardia aggiuntiva che vale piu' del rilevatore: i comandi che FIRMANO qualcosa (feature-end, commit-slice) dovrebbero rifiutare di firmare quando il proprio runtime diverge dal sorgente del repo su cui operano -- degradare LOUD invece di firmare con codice vecchio."

- [ ] validator-refuses-the-dispatch-its-own-generator-produces: paradigm=object-oriented defect="IL VALIDATORE RIFIUTA L'ENVELOPE CHE IL PROPRIO TOOL PRODUTTORE GENERA, e il rifiuto e' deterministico su TUTTE le 45 feature candidate. MISURATO 2026-07-22 da un pilota costruito apposta: `des dispatch --phase FEATURE_END_EXAMINE` genera correttamente un envelope per l'esaminatrice (`Agent: nw-user-examiner`), che porta `# DESIGN_CONTEXT: N/A -- this dispatch is non-code-facing; no source, design, or acceptance-test access by construction` -- privo di citazione PER COSTRUZIONE, che e' l'intero punto del ruolo. Il validatore PreToolUse lo rifiuta con `DESIGN_CONTEXT carries no architecture citation (empty, placeholder, or citation-free body)`. Generatore e validatore non concordano su cosa SIA un dispaccio di esame a scope feature. CAUSA: `src/des/application/atdd_pure_prompt_validator.py` non conosce affatto la fase -- `FEATURE_END_EXAMINE` non compare NEL FILE. Due conseguenze: (a) `_is_non_code_facing_dispatch` (:136) riconosce una sola faccia dell'esaminatrice, `phase_match.group(1) == 'C_REVIEWER_AUDIT'`, quindi applica il controllo di presenza del DESIGN_CONTEXT a un dispaccio che strutturalmente non puo' soddisfarlo; (b) `_REVIEW_DISPATCH_PHASES` (:72-73) non la contiene, quindi ricade sul profilo DELIVER a 12 sezioni invece di quello REVIEW a 7 -- oggi non morde solo perche' il generatore emette comunque tutte e dodici. NON E' STALENESS: `diff -q` dice che il validatore e' byte-identico fra repo e installato. Sono ugualmente sbagliati, e reinstallare non aiuterebbe. LA FORMA, che e' la parte generalizzabile: il commento SSOT del validatore afferma di consultare 'la STESSA SSOT che `des dispatch` legge, mai una seconda lista privata' -- e poi hardcoda la stringa di fase, quindi E' una seconda lista privata. E' esattamente per questo che i due file hanno potuto divergere quando il fix `ff337d851` ha aggiunto la fase alla mappa degli agenti senza estendere il predicato che esenta gli esaminatori. Quarta occorrenza in giornata della stessa famiglia -- un'affermazione di garanzia piu' forte di cio' che il meccanismo regge -- insieme a 'by construction' sull'esaminatrice, al rilevatore di staleness che confronta versioni invece di contenuto, e alla gamba di feature-end che leggeva una collection fallita come assenza di suite. IMPATTO: 45 feature armate attendono l'esame a scope feature-end come precondizione di chiusura; il rifiuto dipende solo dal marcatore DES-PHASE, identico per tutte, quindi un lotto da 42 avrebbe prodotto 42 rifiuti identici e zero verdetti." proposed_solution="Il fix minimo e' estendere `_is_non_code_facing_dispatch` per accettare FEATURE_END_EXAMINE accanto a C_REVIEWER_AUDIT -- l'envelope porta gia' tutte e 12 le sezioni, quindi solo il controllo di CONTENUTO del DESIGN_CONTEXT fallisce. Ma va deciso deliberatamente, non per default, anche se la fase debba entrare in `_REVIEW_DISPATCH_PHASES` per il profilo leggero a 7 sezioni: tenere un'esaminatrice non-code-facing sul profilo di implementazione a 12 sezioni e' lo STESSO errore di categoria, semplicemente non ancora mordente. Fixarli entrambi impedisce che il prossimo cambiamento del generatore lo riesponga. IL FIX STRUTTURALE, che vale piu' di entrambi: far DERIVARE il predicato dalla mappa degli agenti del dispatch invece di riaffermarla. Finche' il validatore ripete a mano cio' che il generatore decide, i due possono divergere di nuovo alla prossima fase aggiunta -- e il commento continuera' ad affermare che non possono."

- [ ] drain-batch-path-is-fail-open-and-it-is-the-documented-one: paradigm=object-oriented defect="IL PERCORSO BATCH DEL DRAIN FONDE OUTPUT D'AGENTE NON ATTESTATO, E L'INVOCAZIONE DOCUMENTATA E' PROPRIO QUELLA. `drain_batch` non e' un ciclo su `drain_one`: e' una COPIA di ~57 righe del suo ciclo di vita, e la copia non chiama mai il gate d'ingresso. Verificato con risoluzione dei binding: `_entry_gate_refusal` ha ESATTAMENTE UN chiamante, `drain_one:207`; `land_and_remove_integration` ha esattamente un chiamante, `drain_one:233`. Conseguenze asimmetriche e invertite rispetto all'intuizione: `--max-parallel 1` rifiuta ogni fix non attestato (fail-CLOSED, sicuro) mentre `--max-parallel N` FONDE modifiche generate da un agente, N alla volta, su un branch di integrazione che poi ABBANDONA senza rimuoverlo. Nessun verdetto di sicurezza viene consultato lungo quel percorso. PERCHE' E' LA COSA PIU' PERICOLOSA TROVATA OGGI: e' fail-OPEN, non una frizione. Il gate d'ingresso esiste per impedire che modifiche scritte da un'AI atterrino senza che qualcuno abbia giudicato se siano sicure; il percorso parallelo lo salta interamente. E la documentazione INDIRIZZA li': `scripts/refactor_agent.py:26`, il blocco USE dell'attuatore spedito, dice ai manutentori di usare `--max-parallel 2`, e il comando del loop di drenaggio nel catalogo delle affordance diceva `--max-parallel N` -- l'ho scritto io. Quindi chiunque seguisse le istruzioni prendeva il percorso senza gate, e chi sbagliava usando 1 era protetto. MITIGAZIONE GIA' APPLICATA nella prosa: il comando documentato dice ora `--max-parallel 1` con la ragione per esteso, cosi' nessuno ci finisce seguendo le istruzioni mentre il codice aspetta il fix." proposed_solution="GATE PRIMA, LANDING DOPO. Chi lo sistema deve cablare `_entry_gate_refusal` sul percorso batch prima di toccare qualsiasi altra cosa: fondere senza il gate spedisce merge non attestati sul branch dell'operatore, ed e' esattamente il danno. Ma il fix minimo non basta e la radice e' la duplicazione: un ciclo di vita copiato invece di riusato diverge in silenzio, e questa e' la seconda volta oggi che una coppia di funzioni sorelle diverge cosi' -- `_report` e `_report_batch` hanno lo stesso difetto sullo stesso file, dove la sorella batch e' quella CORRETTA e la singola quella rotta. Qui e' invertito: la singola e' corretta e la batch e' rotta. Due copie, due divergenze, direzioni opposte: e' la duplicazione stessa il difetto, non quale delle due sia sbagliata. Il fix strutturale e' far diventare `drain_batch` un ciclo su `drain_one` (o su un ciclo di vita condiviso), cosi' che il gate non POSSA essere saltato da un percorso. Chiudere anche il branch di integrazione abbandonato: `land_and_remove_integration` va chiamato su entrambi i percorsi, altrimenti ogni drain batch lascia dietro un branch."

- [ ] install-silently-disarms-every-running-session-on-the-box: paradigm=object-oriented defect="L'INSTALLER TOGLIE LA CAPACITA' DI DISPACCIARE AGENTI A OGNI SESSIONE VIVA SULLA MACCHINA, SENZA DIRLO A NESSUNO. MISURATO 2026-07-22: eseguito `python -m nwave_ai.cli install` con SEI corsie di consegna in volo. L'installazione e' riuscita e i fix sono live -- quella meta' ha funzionato. Ma Claude Code ha ricaricato il proprio registro degli agenti e TUTTI i tipi `nw-*` sono spariti dalla sessione in corso: crafter, acceptance-designer, product-owner, user-examiner, cinquanta in tutto. I 35 file di specifica sono INTATTI su disco, timestamp originali, nulla perso -- ma una sessione viva non puo' piu' DISPACCIARLI. Le sei corsie sono diventate mute nella stessa finestra di 25 minuti, ognuna a meta' missione con lavoro non committato. AGGRAVANTE, ed e' la parte che rende il difetto serio invece che seccante: l'installazione e' CONDIVISA fra profili -- `~/.claude-alt/lib` e `~/.claude-alt3/lib` sono symlink a `~/.claude/lib` -- quindi un'installazione lanciata da una sessione puo' disarmare le sessioni SORELLE che non ne sanno nulla, su una macchina il cui stato non puo' vedere. Verificato stanotte con una sorella che aveva una demo: la sua si e' salvata, ma per fortuna, non per progetto. FORMA DEL DIFETTO: non e' una perdita di dati ne' un crash. E' una perdita SILENZIOSA DI CAPACITA': il processo continua a girare, si dichiara sano, e il difetto si manifesta solo al successivo tentativo di usare la cosa che non c'e' piu'. Nessun avviso prima, nessun messaggio dopo, nessun modo di distinguere 'il registro e' stato ricaricato' da 'questo agente non e' mai esistito'. E' anche una recidiva: lo stesso incidente e' negli appunti di questa istanza da una sessione precedente." proposed_solution="Tre livelli, in ordine di valore. (1) AVVISARE PRIMA: l'installer deve rilevare le sessioni vive (i marcatori esistono gia' sotto la directory di configurazione) e dire, PRIMA di scrivere, che l'installazione ricarichera' il registro degli agenti e che le sessioni in corso perderanno la capacita' di dispacciarli fino al riavvio -- con l'elenco di cosa e' in volo se lo sa. Costo sul sistema, non sull'operatore che deve ricordarsene. (2) AVVISARE DOPO, a chi non ha lanciato: la perdita e' silenziosa proprio per chi non ha premuto il pulsante -- una sessione sorella scopre il danno provandoci. Un marcatore su disco con il timestamp dell'ultima installazione, letto dal hook di SessionStart e da un check periodico, renderebbe la causa leggibile invece che deducibile. (3) NON RICHIEDERE il riavvio: se il registro puo' essere ricaricato senza perdere la sessione, e' quella la cura vera -- oggi il recupero costa tutto il contesto della conversazione, che su una sessione lunga e' il bene piu' caro. OSSERVAZIONE DI PROGETTO emersa dallo stesso incidente e che vale oltre il difetto: i DRAIN sono rimasti immuni, perche' lanciano il proprio fixer come PROCESSO ESTERNO (`claude -p` via shell) e non come tipo di agente della sessione. Una capacita' risolta attraverso la sessione dura quanto la sessione; una risolta attraverso il filesystem e un confine di processo le sopravvive. Il meccanismo che sembrava piu' costoso e' l'unico che ha continuato a funzionare quando la sessione si e' degradata."

- [ ] probe-refusal-names-a-cause-it-never-established: paradigm=object-oriented defect="UN RIFIUTO CHE PORTA TUTTI E TRE I PEZZI E HA IL PERCHE' SBAGLIATO, il che e' PEGGIO di un messaggio muto perche' dirotta lo sforzo con sicurezza. `refactor_drain_service.py:368-374`: la sonda e' un BOOLEANO -- `if not self._git_worktree.probe(repo)` -- e il messaggio afferma `this repo is not a usable git repository (or git worktree is unavailable here)` con il rimedio `run des refactor from inside a real, initialised git repository`. La sonda sa soltanto 'false'; il messaggio dichiara una diagnosi. MISURATO 2026-07-22: rifiuto scattato su `/home/alexd/Projects/nWave-dev`, dove `git rev-parse --is-inside-work-tree` restituisce true e `git worktree list` funziona. Il repo E' usabile. CORREZIONE 2026-07-22 21:10 -- questa riga affermava che 'la causa vera era la contesa fra due drain'. NON era stabilito: era la mia ipotesi, scritta come fatto, dentro una riga che denuncia esattamente quell'errore. Ritirata. ECCO INVECE CIO' CHE HO MISURATO leggendo `git_worktree_adapter.py:137-147`: (a) la sonda NON e' un controllo leggero -- fa `git worktree add -b refactor-probe-health-check <repo>-refactor-probe HEAD`, cioe' CREA UN WORKTREE VERO con checkout completo (6832 file su questo repo, riprodotto) e poi lo rimuove, a ogni avvio di `des refactor`; (b) il nome del branch e il percorso sono COSTANTI, quindi la sonda non e' ne' concorrenza-sicura ne' crash-sicura: due drain simultanei collidono, e una sonda che muore a meta' lascia branch e directory sul posto, dopodiche' OGNI sonda successiva fallisce per sempre con lo stesso messaggio falso; (c) LA PERDITA D'INFORMAZIONE E' LETTERALE E LOCALIZZABILE -- `except subprocess.CalledProcessError: return False` cattura l'eccezione di git e la BUTTA VIA. Git aveva gia' prodotto la causa esatta, in chiaro, e il codice la scarta prima che il messaggio venga composto. Il mandato what/why/how non e' violato per mancanza d'informazione: e' violato perche' l'informazione esisteva ed e' stata gettata. (d) Riprodotta ora, la sonda RIESCE (exit 0) -- quindi il fallimento delle 18:48 era transitorio, e il messaggio non poteva dire quale dei tre meccanismi sopra fosse, perche' li aveva gia' persi tutti. PERCHE' E' PEGGIO DI UN MESSAGGIO MUTO: un `FAILED` nudo lascia l'operatore a indagare, e indaga. Un messaggio che nomina con sicurezza la causa sbagliata gli dice DOVE guardare, e quel posto e' gia' a posto -- quindi verifica che il repo sia un repo, lo trova sano, e conclude che lo strumento sia confuso invece di scoprire che ha un problema di concorrenza. Il HOW aggrava: manda a fare una cosa gia' vera. CONTRASTO DENTRO LA STESSA FUNZIONE, che dimostra che il progetto giusto era gia' in casa: il ramo immediatamente sopra (`:358-367`, worktree collegato) nomina la condizione ESATTA che ha verificato, spiega perche' importa -- muterebbe il `.git` comune condiviso e corromperebbe i ref degli altri worktree -- e da' due rimedi concreti. Quello e' un perche' STABILITO. Quello sotto e' un perche' INVENTATO. Stessa famiglia dei difetti trovati oggi: un'affermazione piu' forte di cio' che il meccanismo ha accertato -- l'esaminatrice 'cieca by construction' con Read e Bash liberi, il rilevatore di staleness che confronta versioni, la gamba di feature-end che leggeva una collection fallita come assenza di suite." proposed_solution="La sonda deve restituire una RAGIONE, non un booleano, e il messaggio deve riportare quella ragione invece di dedurne una. Se `git worktree list` fallisce, riportare l'errore di git verbatim -- git dice gia' cosa non va, e scartarlo per sostituirlo con una congettura e' la perdita di informazione che crea il difetto. Quando la ragione non e' determinabile, il messaggio deve DIRLO ('la sonda git non ha avuto successo e non so perche'; ecco l'errore grezzo') invece di sceglierne una plausibile: 'non riesco a stabilire la causa' e' onesto e verificabile, 'il tuo repo non e' un repo' e' falsificabile in un secondo e falso. AGGIUNGERE il caso che ha scatenato questo: nominare la concorrenza come causa possibile -- due drain sullo stesso `.git` non possono creare worktree simultaneamente, e il rimedio e' 'aspetta che l'altro drain finisca', non 'inizializza un repo'. TRE RIMEDI ULTERIORI dalla misura del 2026-07-22, tutti in `git_worktree_adapter.py:137-147`: (1) NON scartare l'eccezione -- `except subprocess.CalledProcessError as e: return ProbeFailure(stderr=e.stderr)` e riportare quello stderr verbatim; e' una riga di codice e da sola trasforma un why inventato in un why stabilito. (2) RENDERE UNIVOCI branch e percorso della sonda (suffisso pid+timestamp) e rimuoverli in un `finally`, cosi' una sonda morta a meta' non avvelena permanentemente ogni esecuzione futura -- oggi un crash singolo rende `des refactor` inutilizzabile per sempre, con un messaggio che accusa il repo dell'utente. (3) RIVEDERE IL COSTO: sondare la salute di git con un checkout completo di 6832 file a ogni avvio e' sproporzionato; `git rev-parse --git-dir` piu' un `git worktree list` provano quasi tutto cio' che serve a costo nullo, e se serve davvero provare la creazione, farla su un percorso vuoto e temporaneo con `--no-checkout`. REGOLA GENERALE che vale oltre questa funzione: un rimedio va emesso solo per una causa STABILITA. Un what/why/how con un why congetturale non soddisfa il mandato -- lo simula, ed e' piu' dannoso del silenzio perche' consuma la fiducia che il mandato esiste per costruire."

- [ ] assertion-says-what-it-expected-never-what-it-found: paradigm=object-oriented defect="UN'ASSERZIONE CHE DICHIARA CIO' CHE SI ASPETTAVA E MAI CIO' CHE HA TROVATO, su un test che e' rosso in CI e verde in locale -- cioe' esattamente il caso in cui il messaggio e' l'unica cosa che il lettore ha. `tests/bugs/des/test_dispatch_generates_all_feature_end_cycle_phases.py:157-163`: la regex `_CHOOSE_FROM_RE = re.compile(r\"\\(choose from ((?:'[^']*'(?:, )?)+)\\)\")` pretende alternative FRA APICI, e quando non combacia l'asserzione dice `expected a '(choose from ...)' clause in stderr={stderr!r}`. MISURATO 2026-07-22 su Python 3.12.3 locale: argparse produce `choose from 'A_GREEN', 'C_REVIEWER_AUDIT', ...` -- con apici, quindi la regex combacia e il test passa. In CI lo stesso test fallisce. La causa della divergenza NON e' stabilita e non la nomino: non ho verificato la patch di Python del runner ne' escluso altre differenze d'ambiente. IL DIFETTO DEL MESSAGGIO E' INDIPENDENTE DALLA CAUSA ed e' questo: il messaggio dichiara cosa si aspettava e stampa il blob grezzo, ma non dice mai cosa ha effettivamente TROVATO ne' contro cosa ha confrontato. Se la clausola e' presente in una forma diversa -- senza apici, con spaziatura diversa, con un separatore diverso -- il lettore vede `expected a '(choose from ...)' clause` e POI vede la clausola nel dump stampato subito accanto, e conclude che il test sia impazzito. E' la stessa forma del rifiuto della sonda git filato un'ora fa: un messaggio la cui affermazione e' contraddetta dall'evidenza che stampa lui stesso. Peggiora il fatto che il pattern atteso e' una REGEX: il lettore non puo' nemmeno sapere quale forma fosse richiesta senza aprire il sorgente del test." proposed_solution="L'asserzione deve riportare i tre pezzi. COSA: 'la clausola choose-from non e' stata trovata NELLA FORMA ATTESA'. PERCHE': stampare il pattern letterale contro cui ha confrontato, cosi' che il lettore veda 'volevo alternative fra apici' e possa confrontarlo con cio' che ha davanti. COME: nominare cosa fare -- se argparse ha cambiato formato, aggiornare la regex; se stderr e' vuoto, il comando non e' arrivato ad argparse ed e' un altro problema. Meglio ancora, estrarre le alternative con un parser tollerante alla forma (apici opzionali, separatori vari) e asserire sull'INSIEME, che e' cio' che al test interessa davvero -- il formato di presentazione di argparse non e' l'invariante sotto test, e legarcisi rende il test fragile a un dettaglio della libreria standard. REGOLA GENERALE, gia' filata per i rifiuti dei gate e valida identica per le asserzioni: un messaggio che dice solo cosa si aspettava costringe a un round-trip nel sorgente per sapere cosa ha trovato -- e su un rosso che riproduce solo in CI quel round-trip e' l'intera indagine."

- [ ] test-durations-keys-embed-foreign-worktree-abspath: paradigm=object-oriented defect="1469 of 6861 `.test_durations` keys (21% of entries but 46% of the measured wall-clock, 53.4 of 115.0 min) embed the absolute path `/home/alexd/Projects/wt/bugfix-ci-sharding` -- a worktree that is not the one being tested. These are the pytest-bdd scenario ids, whose nodeid is `<file>.py::::<ABSOLUTE feature path>: <scenario>`. pytest-split matches stored durations to collected tests by exact nodeid, so in any other checkout (a sibling worktree, a fresh clone, every CI runner) those 1469 nodeids match nothing and fall back to the default per-test estimate. The fallback lands on precisely the EXPENSIVE half of the suite, so the 4-way shard balance is computed almost entirely without the data that determines it. Failure is silent: sharding still 'works', it is just balanced on a fiction, and every shard timing derived from it is wrong. CAVEATS recorded by the finder rather than buried: the 46% is of measured TIME, by entry count it is 21% -- both figures are here so nobody re-derives the wrong one. The DEFECT is verified (the keys, the counts, the nodeid shape, that pytest-split matches by exact nodeid); the proposed solution is a DESIGN and has not been tested. SEVERITY RAISED 2026-07-22 -- this is not only a sharding-balance defect, it is an ANALYSIS-CORRUPTION vector, and it corrupts SILENTLY in the dangerous direction. A lane analysing which slow tests may be converted to in-memory counterparts ran two passes that reported ZERO `@walking_skeleton` scenarios in the heavy population; the true count is 24. Cause: every existence check against the absolute foreign feature path failed, and each scenario was then recorded as 'untagged' rather than as 'unresolvable'. `@walking_skeleton` is precisely the tag the project's own policy (`scripts/at_corpus_migration_scorecard.py`) uses to declare which subprocess-e2e tests must be KEPT -- so the corrupted analysis argued for converting the exact 24 tests that must not be converted. It was caught only because a stricter rewrite happened to CRASH instead of returning an empty tag set. Underlying shape, shared with several other defects filed today: a lookup that FAILS is not an observation that the thing is ABSENT -- an epistemic failure recorded as an ontological fact." proposed_solution="make the durations keys checkout-invariant -- have the generator rewrite pytest-bdd scenario ids to a repo-RELATIVE feature path before writing `.test_durations` (or post-process the file on write), regenerate it once from a clean run, and add a CI check that fails if any key in `.test_durations` contains an absolute path or a `/home/` prefix so the class cannot silently return"

- [ ] dispatch-advisory-warns-of-a-rejection-its-own-output-prevents: paradigm=object-oriented defect="`des dispatch --lane bugfix` STAMPA UN AVVISO CHE CONTRADDICE L'ENVELOPE CHE ESSO STESSO GENERA, e lo fa due sezioni piu' su. L'avviso dice `no feature-delta.md found ... fix it before dispatching the crafter (the readiness gate will otherwise reject it)`, ma il gate di prontezza sulla stessa invocazione restituisce `verdict: cleared` -- i tre invarianti meccanici sono soddisfatti e i sei di feature-readiness sono correttamente SALTATI per `--lane bugfix`. L'avviso non conosce l'esenzione della propria lane. Peggio: l'envelope generato contiene, due sezioni sotto l'avviso, un paragrafo DESIGN_CONTEXT che dice che la lane bugfix non porta delta di design PER PROGETTO. Il documento si contraddice da solo. TROVATO DUE VOLTE INDIPENDENTEMENTE: da alpha-challenge il 2026-07-22 mattina ('the advisory warns of a rejection its own output already prevents') e da una corsia interna la sera stessa, che l'ha DISPROVATO eseguendo il gate invece di obbedirgli. Che sia stato trovato due volte da due progetti diversi e' il segnale che conta: un avviso falso non e' rumore innocuo, e' un costo pagato da ogni operatore che lo prende sul serio -- e chi lo prende sul serio va a costruire un feature-delta che non serve, che e' precisamente il lavoro sprecato che la lane bugfix esiste per evitare. SECONDO DIFETTO NELLO STESSO ENVELOPE, minore ma della stessa famiglia: la sezione `ATDD_PURE_PHASES` dice `Execute the phase named in the DES-PHASE marker` su una lane PHASELESS, dove nessun marcatore DES-PHASE esiste. Istruzione che rimanda a qualcosa che non c'e'." proposed_solution="Condizionare l'avviso a cio' che l'invocazione ha DAVVERO chiesto: se `--lane` e' fra quelle esentate, l'avviso non va emesso. La proposta di alpha e' la piu' diretta -- condizionarlo su un flag di esenzione passato a QUESTA invocazione -- ma la forma piu' robusta e' derivarlo dal risultato del gate invece di predirlo: l'avviso prevede un rifiuto, e il gate sa se rifiuterebbe. Un avviso che PREDICE puo' sbagliare; uno che RIPORTA no. Stessa famiglia dei difetti trovati oggi: un'affermazione piu' forte di cio' che il meccanismo ha stabilito. Per la seconda: rendere la sezione ATDD_PURE_PHASES condizionale sulla presenza di una fase, o sostituirla su lane phaseless con l'istruzione che vale davvero li'."
- [ ] stash-create-recovery-snapshot-silently-omits-untracked: paradigm=object-oriented defect="`git stash create` is the obvious primitive for taking a non-destructive recovery snapshot of a dirty worktree -- it writes commit objects without touching the working tree or the index, so it is safe to run under a live agent. It also silently OMITS untracked files, and reports success either way. Measured 2026-07-22 across ten active worktrees: nine produced a snapshot sha and looked protected; the tenth (`feat-cpp-runner-adapter`) produced NOTHING and printed as 'nothing to snapshot' -- because 100% of its work (9 files, 1037 insertions: the whole AT directory, the feature dir, the expectation charters) was untracked. Worse than the outright miss is the partial one: the nine that DID produce a sha were each also carrying 1-4 untracked files, so every one of those snapshots protected less than the operator believed while presenting as a completed backup. A worktree removal at that moment destroys exactly the files the snapshot did not capture, and the recovery ref that exists is the reason nobody looks for them. Same defect class as the rest of today's pile: a mechanism whose success signal is stronger than what it actually established." proposed_solution="never use bare `git stash create` for recovery snapshots; snapshot through a TEMPORARY index instead, which captures tracked modifications and untracked files together and is equally non-destructive: `GIT_INDEX_FILE=<tmp> git read-tree HEAD; GIT_INDEX_FILE=<tmp> git add -A; tree=$(GIT_INDEX_FILE=<tmp> git write-tree); git commit-tree $tree -p HEAD -m snapshot` then `git update-ref refs/recover/<lane>-<ts> <commit>`. Wrap this as a `des snapshot-worktree` subcommand so the correct primitive is the reachable one (GDP-4/GDP-5: the HOW must invoke a producing tool, not a remembered four-line recipe), and have it print the untracked-file count it captured so the operator sees coverage rather than inferring it."
- [ ] inferred-wave-floor-blocks-non-wave-dispatch-with-only-dishonest-escapes: paradigm=object-oriented defect="`.nwave/wave-active/active.json` can be armed with `\"provenance\": \"inferred\"` -- nobody declared the wave, the hook pattern-matched one. Measured 2026-07-22 20:32: a floor for wave 'distill' armed itself in the trunk tree while the orchestrator was running no wave at all, then denied an unrelated READ-ONLY tech-debt sweep (a `nw-software-crafter-reviewer` dispatch carrying `DES-MODE: find`, no Write/Edit tools, no wave membership) with WAVE_MARKER_BYPASS. The denial is loud and well-formed -- it states what/why/how per GDP-3 -- but examine the three recovery paths it offers: (1) copy the parent wave's DES-VALIDATION markers onto the child, (2) declare `<!-- DES-WAVE: distill -->` on the child, (3) run `des wave-clear`. Options 1 and 2 both require the dispatch to CLAIM wave membership it does not have -- the compliant action is to misrepresent the dispatch. Option 3 requires DISARMING the floor for everything else in that tree. There is no fourth option meaning 'this dispatch is legitimately not wave work', which is the true and common case for read-only sweeps, finders, analysis, and orchestrator housekeeping. A gate whose only escapes are lie-about-yourself or turn-me-off trains operators to do one or the other by reflex, and the reflex it most rewards is the marker copy-paste -- which then makes every DES-WAVE marker in the corpus weaker evidence of anything. Compounding: because provenance is 'inferred', the floor that forces this choice was never a human decision in the first place. ESCALATED 2026-07-22 21:25 -- CLEARING IT IS NOT A FIX, IT IS A TREADMILL. I cleared the floor at ~20:52 with an honest reason; it RE-ARMED at 21:23:03, again with provenance=inferred, and blocked the next unrelated read-only dispatch identically. Mechanism verified in `pre_tool_use_handler.py:253-267`: ANY dispatch tool-use whose prompt carries a `<!-- DES-WAVE: <wave> -->` marker arms the floor via `arm_inferred`. The floor is ONE record per repo (`.nwave/wave-active/active.json`), so in a session running many parallel lanes, ONE lane legitimately entering a wave arms a floor that then blocks EVERY other lane's unrelated dispatch -- including read-only finders, analysis, and orchestrator housekeeping that are not wave work by anyone's definition. The blocked party cannot fix it by declaring the truth about itself; it can only clear a floor that belongs to someone else's legitimate work, or falsely claim membership. I also CONFIRMED there is no honest escape: I sent `<!-- DES-WAVE: none -->` and it is out-of-vocabulary, so per the handler docstring it is treated as ABSENT -- which correctly avoids ARMING a floor but does nothing to pass an EXISTING one. So the vocabulary has a value for 'I am not declaring a wave' and no value for 'I am declaring that I am not in one', and those are different claims." proposed_solution="add a first-class non-wave dispatch declaration (e.g. `<!-- DES-WAVE: none -->` or `<!-- DES-DISPATCH-KIND: read-only-finder -->`) that the guard ACCEPTS without either a false membership claim or a floor clear, and gate it on the dispatch genuinely being read-only (the agent's toolset carries no Write/Edit) so it cannot be used to smuggle write work past the floor. Separately, treat `provenance: inferred` as WEAKER than declared: an inferred floor should warn-and-allow a read-only dispatch rather than deny it, reserving hard denial for floors a human or a wave command actually declared -- an inference is not an authorization."
- [ ] voice-relay-times-out-intermittently-with-no-diagnosable-signal: paradigm=object-oriented defect="The cross-instance voice relay (`mcp__lyra__voice_*`) times out intermittently -- measured 2026-07-22, roughly one call in three across an evening of use, on `voice_inbox`, `voice_peers` and `voice_ask` alike. A retry has succeeded every time so far, including a retry issued seconds after a timeout under IDENTICAL machine conditions. The timeout carries no diagnostic: no distinction between 'the peer is unreachable', 'the peer is busy', 'the relay process is wedged' and 'the call was slow but would have completed', so the caller cannot tell whether the message was delivered-and-unacknowledged or never sent at all. That ambiguity matters for a coordination channel specifically: the natural response to a silent failure is to retry, and if the first call actually landed the peer receives the message twice. HONEST SCOPE, because I got this wrong once already and corrected it to the peer: I initially reported the timeouts as correlating exactly with high box load, and the very next call FALSIFIED that -- it succeeded at overcommit 84.5% / load 17.2, the same conditions under which the two preceding calls had just timed out. So load does NOT explain it. CAUSE NOW ESTABLISHED 2026-07-22 21:20, and it is worse than a timeout: `voice_ask` BLOCKS WAITING FOR THE PEER'S REPLY instead of returning once the query is QUEUED. The client-side timeout therefore fires on a query that was successfully delivered and is still alive -- so a 'timeout' is not a failure at all, and the natural response to it (retry) DOUBLE-DELIVERS. Diagnosed independently by peer lyra-evolution from its own send data, and CONFIRMED HERE from my own inbox: after `voice_ask` to alpha-challenge 'timed out' I retried it, and BOTH the timed-out send (Q-nwave-...-59) and the retry (Q-nwave-...-60) appear in my PENDING list -- the peer will receive the identical urgent message twice. Aggravating root, also from lyra-evolution: Claude Code does not re-send `initialize` after a daemon restart (upstream issues #9608/#30224), so every session silently LOSES its voice-relay identity until it calls `voice_identify` again -- meaning after any restart ALL peers look unregistered and EVERY `voice_ask` enters the hang path. The window is not rare; it opens at every restart. SECOND-ORDER DEFECT, and it is the one that cost most tonight: sixteen incoming queries from two peers sat in this inbox and EXPIRED unread, spanning 13.5h to 2.8h -- among them several precisely-characterised, directly-reproduced tool bugs. Nothing notifies the recipient that queries are waiting; the inbox must be POLLED, and a peer's carefully-written finding decays silently on a timer." proposed_solution="return immediately with query_id+PENDING and make waiting-for-a-reply an OPT-IN flag with an explicit timeout, so the common path cannot time out at all; make delivery idempotent on a client-supplied message id so the retry reflex cannot double-deliver; on a genuine peer-absent refusal LIST the registered peers, since with identity-loss an empty list IS the diagnosis (relay restarted, everyone must re-identify) rather than merely an error. And for the expiry problem: notify on inbound rather than requiring a poll, and do not expire an unread query silently -- an unanswered finding should surface louder over time, not quieter." proposed_solution="give the relay call a bounded timeout with a DISTINGUISHING failure result (unreachable / busy / relay-error / timed-out-may-have-delivered) so a caller can decide between retry and escalate instead of guessing, per GDP-3 and GDP-6 (degrade LOUD, never ambiguously). Make delivery idempotent -- a client-supplied message id the relay deduplicates on -- so the natural retry cannot double-deliver. Then instrument it: log call outcome with timestamp so the intermittency can be characterised against real data rather than against whichever hypothesis the operator happened to form."
- [ ] examine-gate-arms-on-feature-charter-as-if-slice-scoped: paradigm=object-oriented defect="the commit-time examine gate arms on charter PRESENCE anywhere under docs/product/expectations/{feature-id}/ and does not distinguish a FEATURE-scope charter from a SLICE-scope one. A feature whose charter names N slice outcomes therefore refuses slice-01 for failing slices 2..N -- the gate blocks correct work for the right reason at the wrong time. Observed live 2026-07-22: slice-01 of fix-nested-pytest-self-invocation-synthetic-fixture delivered its target and passed every claim in its own scope, and the examiner correctly returned FAIL because the only charter present also demanded a second target that slice-01 never claimed. The dangerous part is the escape hatch: the cheapest way out is to edit the charter until the slice passes, i.e. weaken the oracle that just did its job. A gate whose easiest exit is weakening itself will eventually be weakened; that this instance was escalated instead is a fact about the operator, not about the mechanism." proposed_solution="give charters an explicit scope (a slice-id field, or a per-slice subdirectory) and have the examine gate select ONLY charters matching the entering slice, degrading LOUD when a feature-scope charter is the only one present -- naming the missing slice charter and routing to `des charter-scaffold` to ADD it, never inviting an edit of the feature-scope one; the feature-scope charter stays the feature-end oracle"
- [ ] po-agent-cannot-run-its-own-producing-tool: paradigm=object-oriented defect="nw-product-owner is the natural owner of expectation-charter authoring, but its toolset is Read/Write/Edit/Glob/Grep/Task with NO Bash, so it structurally cannot run `des charter-scaffold` (the producing tool) nor `des verify-charter-filled` (its own gate). Dispatched to author a charter via those commands it correctly refused to fabricate their output and hand-wrote the file to the documented dialect instead, leaving the gate run to the dispatcher. That is the honest failure, but it means the GDP-5 'system produces the artifact' property is unavailable to the role that owns it, and charter authoring silently degrades to hand-assembly plus a hope that the dialect was matched by eye. Note the shape: an instruction that points at a capability the addressee does not have -- the same class as a dispatch advisory naming a command its own envelope cannot invoke." proposed_solution="either grant nw-product-owner a scoped Bash capability sufficient to run the des charter commands, or expose charter-scaffold/verify-charter-filled through a tool the agent already has -- so the authoring role can run its own producing tool and its own gate"
- [ ] examine-verdict-is-not-bound-to-the-content-it-attests: paradigm=object-oriented defect="The examine ledger records a verdict with a timestamp and NOTHING binds that verdict to the CONTENT it was rendered against. A recorded PASS therefore survives arbitrary later edits to the very code the examiner exercised -- it is a rubber stamp with a date on it. Observed live 2026-07-22 in worktree collect-error-indeterminate: ledger shows FAIL at 10:16:40 then PASS at 11:19:50 (the correct shape -- examined, failed, fixed, re-examined, passed), while the production files carry mtimes of 12:26:32 and 12:33:27, roughly 70 minutes AFTER the verdict. Whether the CONTENT changed is a separate question that mtime cannot answer, and that is exactly the point: neither the operator nor the gate can currently tell, because the only freshness signal available is a filesystem timestamp that moves for reasons unrelated to content (checkout, formatter, identical re-save). THE PATTERN ALREADY EXISTS IN THIS CODEBASE AND IS SIMPLY MISSING HERE: the RED seal IS bound to its test file's content and is VOIDED when the file changes -- `des verify-red-green --record-red` re-runs are required after any edit for precisely this reason. So the project already knows the correct design, applies it to RED, and does not apply it to EXAMINE. Sibling asymmetry again, the fourth instance found today: two mechanisms doing the same KIND of thing (attesting to a state of the code) where one is content-bound and the other is not, with nothing holding them to a shared standard. AGGRAVATING: the verdict is the LAST gate before commit, so a stale PASS converts directly into a false attestation on the commit record -- the failure lands in the audit trail, which is the one place it must not." proposed_solution="store a hash of the examined artifacts alongside the verdict (the same way RedObserved binds to its test file), and have `des commit-slice` recompute it and REFUSE with what/why/how when it differs -- naming which artifact changed and routing to a re-examine, so staleness is mechanical rather than a matter of somebody noticing that two clocks disagree. Emit the same check as an explicit ExamineVerdictStale outcome distinct from ExamineVerdictMissing, since 'never examined' and 'examined against different code' need different remediation. Do NOT use mtime for this -- it is not a content signal."
- [ ] drain-resolves-its-two-operator-inputs-from-different-bases: paradigm=object-oriented defect="`des refactor` takes two operator-supplied inputs and resolves them from DIFFERENT bases, in the same invocation, with nothing anywhere saying so. (a) `--agent-cmd` (the fixer) is EXECUTED INSIDE THE ISOLATED WORKTREE, which is built from the operator's LAST COMMIT -- so an uncommitted edit to the fixer script is invisible to the run. (b) The prompt template is read from the LIVE main checkout: `refactor_drain_service.py:508` -- `path = override if override is not None else repo / DEFAULT_TEMPLATE_PATH` -- so an uncommitted edit there takes effect immediately. One input honours the working tree, its sibling silently does not. MEASURED by the drain-silent-noop lane 2026-07-22, three cases: fixer edited but NOT committed -> refusal, exit 1; same edit committed -> `Drained 1 item`, exit 0; fixer at an absolute path OUTSIDE the repo, uncommitted -> `Drained 1 item`, exit 0. CONSEQUENCE, and it is how this surfaced: the entry-gate refusal's HOW says 'make your own --agent-cmd print exactly one of those tokens' and never mentions that the change must be COMMITTED. An examiner followed it verbatim, re-ran, and received the byte-identical refusal with no new information -- an infinite loop. A HOW that loops the operator is worse than no HOW, because it spends their trust on the way. It also lands on the WORST possible surface: the entry-gate refusal is the FIRST thing a from-scratch consumer meets, so this loop is the DEFAULT first-run experience. AGGRAVATING for the documentation: `.nwave/refactor-agent-prompt.md` cannot be committed at all -- `.nwave/.gitignore` is a bare `*` -- so 'commit your change' is correct advice for the fixer and IMPOSSIBLE advice for the template, and an operator who learns the first rule will try it on the second and fail. Fifth sibling-asymmetry of the day and the same family as `_report`/`_report_batch`, `drain_one`/`drain_batch`, `--agent-cmd`/`--pile`, and RED-seal-content-bound vs examine-verdict-not." proposed_solution="state the asymmetry IN the refusal, not only in docs -- name that the fixer is executed from your LAST COMMIT inside an isolated worktree while the template is read live, and give BOTH working routes (commit the fixer change, or point --agent-cmd at a path outside the repo). Better still, remove the asymmetry: resolve both inputs from the same base, or snapshot the live working tree into the isolated worktree so what the operator sees is what the drain runs. And VERIFY the HOW by executing it -- this one was present, well-formed, and wrong, which no amount of reading it would have caught."
- [ ] worktree-cleanliness-probes-are-blind-to-the-staging-area: paradigm=object-oriented defect="A worktree whose work is entirely STAGED reads as COMPLETELY CLEAN to the two probes everyone reaches for, and 'merged + clean' is the standard criterion for REMOVING a worktree. MEASURED 2026-07-22 21:35, one command away from destroying real work: `wt/spawn-stdin-deadlock` reported dirty=0 untracked=0 and HEAD-already-an-ancestor-of-trunk, so it qualified for removal on every check. Its actual state was 8 files, 2393 insertions, ALL STAGED and uncommitted -- an RCA, an expectation charter, a new `src/des/runtime/spawn.py`, and 1242 lines of tests. WHY THE PROBES MISS IT, and it is not a subtle bug but a column mistake anyone repeats: `git status --short | grep '^ M'` matches only column-2 modifications (worktree-modified-not-staged) and never column-1 (`M `, `A `), while `git ls-files --others --exclude-standard` excludes staged-new files because `git add` makes them TRACKED. Between them the two probes cover the unstaged world completely and the staging area not at all. So the more disciplined the operator -- staging work deliberately to hold it for a human decision, which is exactly why this worktree was staged and not committed -- the more likely their work is classified as absent. The failure is silent, it is confident, and its consequence is destructive rather than merely wrong. Same family as the rest of today's pile: a conclusion ('this worktree is clean') far stronger than what the mechanism established ('two specific probes returned empty'). Related and worth auditing for the identical blind spot: `des verify-worktree-cleanup`, whose ACT mode deletes worktrees and which is already known to have produced a false positive." proposed_solution="never infer worktree cleanliness from a filtered `git status` -- use `git status --porcelain` and treat ANY non-empty output as dirty, which covers staged, unstaged, and untracked in one check; equivalently assert all three of `git diff --quiet`, `git diff --cached --quiet`, and an empty `git ls-files --others --exclude-standard`. Audit `des verify-worktree-cleanup` for the same blind spot and make its ACT mode refuse on ANY porcelain output. And before any destructive worktree operation take a full recovery snapshot through a temporary index (which DOES capture staged+unstaged+untracked together) -- tonight's snapshot had already captured all 2393 lines, which is the only reason this was a near-miss rather than a loss."
- [ ] examiner-unscoped-collection-prohibited-in-writing-and-done-anyway-twice: paradigm=object-oriented defect="A prohibition that has now failed TWICE IN WRITING is not going to succeed on the third telling, and that is the finding -- not the individual disobedience. MEASURED 2026-07-22, two consecutive `nw-user-examiner` dispatches: examiner #1 ran a whole-tree `pytest --co -q` in the TRUNK checkout despite a dispatch naming only two commands (observed independently from outside her worktree, in a process survey: PID 95143 `/home/alexd/Projects/nWave-dev/.venv/bin/python3 -m pytest --co -q`, contributing to the box load being diagnosed at that moment). The dispatcher then tightened the wording exactly as asked -- 'FORBIDDEN, not merely unrequested', plus 'if you are about to invoke pytest without naming a specific file or directory, STOP'. Examiner #2 ran `uv run pytest tests/des/acceptance/ --co -q` anyway. Two for two against escalating prose. WHY IT COSTS: an unscoped collection on this repo walks 6800+ files, is one of the most fork-hungry things available on a shared box, and lands during exactly the window when a gate elsewhere is being judged -- so it manufactures the starvation that makes OTHER lanes' reds indeterminate. It also silently widens an examiner's exposure to the codebase she is supposed to be blind to. THE GENERAL SHAPE, and it is GDP-2 read in reverse: enforcement belongs at the mechanism, guidance belongs inline at the authoring surface, and PROSE IN A DISPATCH PROMPT IS NEITHER -- it is a request that a probabilistic agent is free to not honour, dressed as a control. Escalating the wording is the move that feels like progress and measurably is not." proposed_solution="stop rewriting the prohibition and BUILD the mechanism: wrap the examiner's test invocation in a runner that REFUSES an unscoped collection (no file or directory argument -> exit non-zero with what/why/how naming the scoped form), or add a PreToolUse hook that blocks a bare `pytest`/`pytest --co` from an examiner-role dispatch. Keep the prose as the inline affordance (GDP-2) so the block is rarely reached, but the prose must stop being the only thing standing between an examiner and a whole-tree walk. Pair with the related finding that SELF-REPORT of a blindness violation is necessary but NOT sufficient -- an examiner under-reported her own violations (confessed one `grep -r`, the transcript audit found a second, a `cat` of a marker file under tests/) -- so the transcript audit must be a PERMANENT step, not a temporary measure while the clause is new."
- [ ] mainline-first-parent-history-carries-no-attestation-at-all: paradigm=object-oriented defect="MEASURED 2026-07-22: of the 12 `fix`/`feat` commits on trunk's FIRST-PARENT mainline in the last 12 hours, ZERO carry a `Slice-Id:` trailer, zero carry `Gate-Scope:`, and most carry no `Co-Authored-By:` either. The attestation is not destroyed -- each attested commit IS an ancestor of trunk, verified -- but it lives on the merged-in SECOND parent, while the commit that sits on the mainline is a merge whose SUBJECT LINE DUPLICATES the attested fix verbatim. So `git log --oneline` shows every fix TWICE, and the copy on the mainline (the one a reader naturally inspects, the one `--first-parent` returns) is completely bare. Four pairs confirmed with identical file and line counts: f61bd0e1b/ff337d851, 8a69e6b24/da64aba54, 3219919e3/9e8d40035, ce8df9c0f/d0aa4c9a6. HONEST SCOPE, because I checked rather than assumed: I did NOT establish that any gate breaks because of this. The `--first-parent` traversal that exists in `git_subprocess.py:170-189` is used for ANCESTRY/reachability, not for trailer discovery, and the trailer readers (`attest_bundled_slice.py`, `domain/slice_id_trailer.py`) do not appear to walk first-parent. So this is filed as an AUDIT-TRAIL defect -- the mainline history reads as entirely unattested to a human or to any tool that walks it the obvious way -- and NOT as a broken gate. Whether some verifier does depend on it is unverified and is the first thing to check before designing a fix. WHY IT STILL MATTERS AT THIS SCOPE: the whole point of the trailers is that the audit trail is mechanically checkable after the fact, and the branch the trailers live on is deletable -- once a lane's worktree and branch are cleaned up, whether the second parent stays reachable is a garbage-collection question, not a guarantee." proposed_solution="FIRST establish whether any verifier walks first-parent for trailers -- if one does, this is urgent rather than cosmetic. Then either propagate the attestation onto the merge commit (a merge-time hook that lifts `Slice-Id:`/`Gate-Scope:` from the branch tip into the merge message), or make merge-back fast-forward/rebase so the attested commit IS the mainline commit and no duplicate subject exists. Do not solve it by teaching every reader to check second parents: the audit trail's value is that it is obvious, and an audit trail you need a technique to read is one nobody reads."
- [ ] examiner-routes-around-a-slow-surface-into-the-internals: paradigm=object-oriented defect="When the REAL surface an examiner was dispatched against is slow, unclear, or will not load, nothing in the `nw-user-examiner` skill or dispatch template tells her that this IS the finding. So she improvises her way inward. TWO INDEPENDENT INSTANCES reported by sister alpha-challenge 2026-07-22, both against a real product: (1) she guessed internal module paths (`src.alpha_challenge.adapters.real_postgres.RealPostgresAdapter` -- which does not exist) and attempted a direct `psql` connection when the dispatched surface seemed slow; caught before it reached a verdict. (2) she RECORDED A PASS whose observations read 'Direct database inspection confirms AAPL credibility cue stored with category=STRESS_TENSION' and 'Verified via acceptance test (re-running pytest)' -- neither of which is the browser/dashboard surface she was explicitly dispatched against with Playwright tools. The second one is the dangerous shape: a PASS attested through a channel the charter never named, indistinguishable in the ledger from a PASS earned on the real surface. WHY THIS DESTROYS THE ROLE'S ENTIRE VALUE: the examiner exists precisely because isolated-green and assembled-green diverge -- she proves the running SYSTEM through the surface a user touches, which is the one thing the unit tests cannot do. An examiner who reaches into the database or re-runs pytest has become a second, worse copy of the test suite, and her PASS now certifies the very thing it was supposed to independently corroborate. A slow or unloadable surface is EXACTLY the class of defect she is there to catch; routing around it converts the finding into a green. Same family as the rest of tonight's cluster -- an inability to observe, recorded as a successful observation." proposed_solution="add an ENUMERATED rule to the examiner skill: if the real surface will not load, is unreachable, or is too slow to drive, that IS the finding -- record INDETERMINATE with what was attempted and what happened, and NEVER route around it into the database, the module tree, or the test suite. Back the prose with a MECHANISM, because prose alone has now failed twice on a sibling rule (the unscoped-collection prohibition): have `des record-examine-verdict` inspect the tool-call history and REFUSE a PASS whose evidence shows a database connection, an internal-module import, or a pytest invocation -- degrade to INDETERMINATE with what/why/how naming the surface she was dispatched against. Pair with the transcript audit, which has proven necessary because examiner self-report is incomplete."
- [ ] examine-dispatch-does-not-name-which-surface-renders-the-charter-detail: paradigm=object-oriented defect="An examine dispatch names the charter and the product but does not consistently name WHICH shipped surface renders the detail that charter's oracle needs -- and it matters most exactly where it is least obvious: when the oracle is per-item or structural rather than a simple pass/fail. Reported by sister alpha-challenge 2026-07-22: ~10 minutes lost to blind CLI-versus-dashboard trial and error on one dispatch, because the same product exposes the same fact through several surfaces and only one of them renders it at the granularity the oracle asks for. The reporter framed it as their own dispatch discipline rather than a tool bug; it is BOTH, and the skill-level half is the one that generalises -- every orchestrator rediscovers it separately, and the cost is paid once per dispatcher instead of once per framework. It also compounds the sibling defect where an examiner routes around a surface she cannot make sense of: an examiner who is not told WHICH surface has more room to improvise into the internals." proposed_solution="make the surface a REQUIRED field of the examine dispatch template -- the concrete URL, CLI invocation, or screen that renders the oracle's detail -- and have `des dispatch` refuse to generate an examine envelope without it, per GDP-5 (the system produces the artifact, the operator does not hand-assemble it). Where the charter's oracle is per-item or structural, require the dispatcher to name the surface that renders it AT THAT GRANULARITY, since the coarse surface passing is not evidence for a per-item claim."
- [ ] charter-provenance-is-unrecorded-so-contamination-is-unfalsifiable: paradigm=object-oriented defect="The expectation-charter disqualification rule -- an author holding the design or implementation CANNOT produce an uncontaminated charter, which is why the bugfix flow authors it at t=0 before the fix exists -- is a discipline with NO mechanical trace whatsoever. Nothing records WHO authored a charter, in WHAT context, or at what point relative to the code it judges: the template carries no provenance field, no charter telemetry exists under `.nwave/telemetry/`, and no ledger records an authoring event. So a downstream reader cannot establish whether any given charter was authored clean, and the rule is unfalsifiable in exactly the direction that matters. OBSERVED 2026-07-22 in worktree collect-error-indeterminate: for `fix-na-marker-reconcile-drift` the file mtimes run test 11:52:25 -> implementation 12:05:40 -> CHARTER 12:13:26, all within one dead agent's session. The charter was written LAST, after both the test and the implementation existed. Its TEXT is clean -- no leaked function or module names -- and that is precisely the trap: textual cleanliness is not uncontamination, because the rule is about the author HOLDING THE DESIGN, not about vocabulary leaking onto the page. A charter written after the fact can steer its oracle toward scenarios the implementation already handles, in perfectly abstract language. WHAT MTIME CAN AND CANNOT ESTABLISH, stated because it is the whole difficulty: it establishes the file WRITE ORDER; it does NOT establish that the same context authored it, since a fresh PO dispatched afterwards would also write last. So contamination here is SUSPECTED and not PROVEN, and there is no artifact anywhere that could settle it. That is the defect. Compare the sibling mechanism that gets this right: `verify-red-green --record-red` binds the RED seal to the test file's CONTENT and voids it on edit. Charters -- which are the OTHER half of the same attestation chain -- carry no binding at all, to content or to author. Sixth sibling asymmetry of the day. MEASURED EVIDENCE OF WHAT CONTAMINATION ACTUALLY COSTS, 2026-07-22 -- a controlled comparison, same bug, two authors, one holding the implementation. The post-hoc charter for `fix-na-marker-reconcile-drift` (written AFTER its test and implementation) framed its negative oracle around "does the block explain itself" and split the epistemic-gap symptom TWO ways: "you have work left" vs "I cannot tell". A fresh author given ONLY the observable split the SAME symptom THREE ways -- "I could not check this" vs "checked, it is fine" vs "checked, it is not fine" -- naming a silent-false-fine AND a silent-false-broken as two DISTINCT failure modes. The fresh charter also demanded the full 2x2 matrix (tracking off/on x complete/incomplete) hold in all four cells, where the contaminated one asserted the coverage-ON half only abstractly. So contamination narrowed coverage in EXACTLY the predicted direction: the post-hoc author, anchored to the fix already built around a hardcoded 3-entry list, checked what that fix already covered and was vaguer about what it did not. The charter text was textually clean throughout -- no leaked names -- which is the point: contamination shows up as NARROWED SCOPE, not as leaked vocabulary, so a grep-for-implementation-nouns check cannot detect it. Note also that the fresh author independently reached the epistemic-vs-ontological three-way distinction, making it the THIRD clean-room author tonight to derive tonight's dominant defect class from the observable alone." proposed_solution="record charter provenance as a first-class artifact at authoring time: the authoring agent id, the dispatch id, and a hash of the inputs it was given (the observable statement) -- emitted by the producing tool `des charter-scaffold` so the operator never hand-asserts it (GDP-5). Then a downstream gate can REFUSE, or at minimum degrade LOUD, when a charter has no provenance record or when its provenance shows it was authored by a context that had already seen the implementation. Until that exists, adopt the cost-asymmetric default and write it into the skill: when charter provenance CANNOT be established, treat the charter as CONTAMINATED and re-author from the observable -- a fresh PO dispatch is cheap and cloud-bound, while a green earned against a contaminated oracle means nothing and is indistinguishable in the ledger from a real one."
- [ ] walking-skeleton-gate-refuses-an-operator-authored-file-with-no-how: paradigm=object-oriented defect="`walking_skeleton_gate.py` refuses on a file the OPERATOR authors by hand -- `walking-skeleton.json` -- and every one of its refusals gives WHAT without HOW: `unparseable walking-skeleton.json: {exc}` (:119), `walking-skeleton.json is not a JSON object` (:121), `walking-skeleton.json missing string 'feature_root'` (:171), `walking-skeleton.json missing list 'entry_points'` (:234). MEASURED: the file contains ZERO occurrences of `schema`, `scaffold`, `doctor` or `template` -- so a maintainer told their manifest is missing `entry_points` is given no schema to consult, no example to copy, and no command to generate it. They are left to reverse-engineer the expected shape from the gate's own source, which is exactly the investigation a self-explaining refusal exists to make unnecessary. THIS IS A GDP-4 VIOLATION SPECIFICALLY, not merely a terse message: the project already ships a family of producing tools for precisely this class of artifact -- `charter_scaffold.py`, `feature_delta_doctor.py`, `feature_end_preconditions_scaffold.py`, `flavor_scaffold.py` -- so the pattern of 'the system generates the checked artifact, the operator never hand-assembles it' is established, implemented four times over, and simply not applied here. Either no scaffold exists for this manifest (then the gate is the signal to build one, GDP-4's own remedy) or one exists and the refusal does not name it (then the fix is one line). SCOPE, stated honestly because the raw count overstates it: `src/des/cli/` + `src/des/application/` carry 44 raise/exit sites against only 24 `Fix:` markers, but MANY of those raises are INTERNAL invariant violations (e.g. `prompt_rendering_service.py`: 'Command cannot be None or empty') where the audience is a programmer and the remediation is 'fix the caller' -- applying the operator-facing what/why/how mandate to those would be over-applying it. The defect is the OPERATOR-FACING subset: a gate reading a hand-authored file and refusing it without naming how to produce a correct one. Confirmed operator-facing instances beyond the manifest: `verify_environmental_e2e.py:277` 'build produced no wheel under {dir}' and `:357` '--fixture-junit-xml resolved to no paths'. Counter-example proving the codebase knows better: `roadmap.py:30` refuses with the EXPECTED FORM inline -- `Invalid steps spec '{pair}', expected 'PHASE_ID:COUNT'`." proposed_solution="triage the 44 raise/exit sites into OPERATOR-FACING and INTERNAL-INVARIANT, and hold only the first class to what/why/how -- a blanket sweep would add noise to programmer-facing assertions where it does not belong. For each operator-facing refusal, route the HOW to a PRODUCING TOOL per GDP-4: add `des walking-skeleton-scaffold` (mirroring the four scaffolds already shipped) and have every manifest refusal name it, so the remedy is a command to run rather than a shape to guess. Where no tool exists yet, the gate is the signal to build one. Add a test asserting that operator-facing refusals contain a remediation marker, so the class cannot silently return -- and pick the marker over prose so the assertion is mechanical."
- [ ] fixer-prompt-template-lives-where-it-can-never-be-shipped: paradigm=object-oriented defect="`prompt_template.py:21` resolves the drain's fixer prompt from `DEFAULT_TEMPLATE_PATH = .nwave/refactor-agent-prompt.md`, i.e. from a PER-PROJECT RUNTIME directory -- and `.nwave/.gitignore` is a bare `*`, so that file is STRUCTURALLY UNCOMMITTABLE and therefore cannot enter any distribution. On a target machine it will never exist. The docstring claims 'the harness bootstraps a default template here on first run'; MEASURED 2026-07-22, nothing had ever created it on this repo -- it was hand-written tonight -- so either that bootstrap does not run or it never did, and nothing reported its absence. WHAT THE ABSENCE COSTS, and it is not a missing file: when the template is absent the code falls back to `DEFAULT_TEMPLATE_TEXT`, which passes the item's id/defect/solution/paradigm and NEVER ASKS FOR A VERDICT TOKEN. But the entry gate reads the fixer's STDOUT for exactly one of five verdict tokens before permitting a merge. So on every target machine the drain hands its fixer a prompt that does not mention the one output the gate requires, then refuses the result for not containing it. The drain is a guaranteed no-op on any consumer install, by construction -- which matches sister alpha-challenge's report that her whole tech-debt drain refused all 20 pending items. A silent FALLBACK that does not announce it is a fallback, whose degraded output then fails a gate that cannot say why. AGNOSTICISM IS THE REAL DEFECT (Ale 2026-07-22): the risk is not that the file is missing HERE, it is that a file the code DEPENDS ON lives outside the shipped surface, so similar things are scattered instead of kept together and consequently are not included in installs. `nWave/data/` is the established home for shipped framework data (`dor-items.yaml`, `omission-classes.json`, `language-adapter-ports.yaml`, `orchestrator-affordance/`, ...) and is read by the installed runtime -- `des_plugin.py:631` names it explicitly." proposed_solution="MOVE the canonical template to `nWave/data/refactor-agent-prompt.md` so it ships with the framework like every other data asset, and make resolution a two-step cascade: the shipped copy is the DEFAULT, and `.nwave/refactor-agent-prompt.md` becomes an OPTIONAL per-project override that wins when present. Verify it is inside the install/whitelist surface -- the point of the move is distribution, so a move that does not reach the target machine has fixed nothing. Then DELETE the hardcoded `DEFAULT_TEMPLATE_TEXT` fallback, or make it degrade LOUD: a fallback that silently omits the verdict-token instruction is worse than no template, because the failure surfaces later, elsewhere, as an unexplained gate refusal. Add a test asserting the shipped template contains all five verdict tokens, so the drain cannot ship with a prompt that omits what its own gate requires."
- [ ] background-agent-completion-notifications-are-dropped-silently: paradigm=object-oriented defect="Background sub-agent completion notifications go missing, and the loss is silent -- the orchestrator simply waits forever for a signal that will never arrive. OBSERVED 2026-07-22 by the drain-silent-noop lane: TWICE in one session (an RCA dispatch, then a crafter dispatch) the agent finished its work and no completion notification reached the dispatching orchestrator. It only recovered because it had, after the FIRST loss, armed its own backstop watcher on transcript idleness; without that improvised guard it would have blocked indefinitely on the second. WHY IT IS EXPENSIVE OUT OF PROPORTION TO ITS FREQUENCY: the whole parallel-lane design depends on dispatch-and-be-notified. A dropped notification does not present as an error -- it presents as an agent that is still working. So the orchestrator's correct behaviour (wait rather than poll, do not spam a running agent) is exactly what converts a lost message into an unbounded stall, and the failure is indistinguishable from slowness until someone gets suspicious. Same family as the rest of tonight's pile: an ABSENCE OF SIGNAL read as a state of the world (still running) rather than as an unknown. RELATED HAZARD reported in the same message, and it is a genuine race rather than a defect in itself: messages sent to a running agent are QUEUED and delivered at its NEXT tool round, so a RETRACTION of an instruction races the agent acting on the original. Tonight a wrong instruction was retracted in time by luck, not by design -- the agent happened not to have reached its next tool call. NOT VERIFIED by me directly; reported by the lane with its own timeline. Filed because it is the second independent instance and the recovery mechanism was improvised rather than provided." proposed_solution="give the orchestrator a POSITIVE liveness signal rather than relying on a terminal one -- a heartbeat, or a queryable agent state that distinguishes running / finished / died, so 'no notification' can be resolved into one of those rather than defaulting to 'still running'. Failing that, make the backstop first-class instead of leaving each lane to improvise a transcript-idleness watcher: an orchestrator-side timeout that escalates INDETERMINATE ('no completion signal in N minutes; the agent may have finished, stalled, or died') per GDP-6 degrade-LOUD. For the retraction race, expose message-delivery status to the sender so a time-critical correction can be known to have landed BEFORE the agent acts, rather than hoped to have."
- [ ] negative-at-seal-verifies-presence-not-scope: paradigm=object-oriented defect="`des verify-negative-at` passes on presence and naming convention (a `@pytest.mark.negative_at` marker or a `_not_` substring in a test name), never on the SPACE the negatives actually cover. CONFIRMED 2026-07-22 on a controlled pair: a charter-contaminated draft test authored post-hoc around a hardcoded 3-entry list PASSED with 2 negative ATs found, while a clean-room charter for the same bug demanded a strictly larger space (three-way distinguishability of 'could not check' from either verdict, plus a full 2x2 matrix in all four cells) that the draft never approaches. So the mechanical seal certifies a suite that checks only what its own implementation already handles. This is the SAME blind spot as the charter check one layer down: contamination manifests as NARROWED SCOPE, not as leaked vocabulary or missing markers, so neither a grep for implementation nouns nor a count of negative markers can detect it. Both seals answer 'is something there' when the question is 'is enough there'." proposed_solution="derive the required negative space from the CHARTER's oracles rather than from marker presence: map each charter negative bullet to at least one covering test and REFUSE (or degrade LOUD) when a bullet has no mapped test, naming which bullet is uncovered. Presence-of-marker stays as a cheap floor but must stop being the whole check. Until that exists, the fresh-charter-then-diff procedure is the only detector we have and its cost is one cloud PO dispatch."
- [ ] wave-floor-should-advise-not-hard-stop-between-waves: paradigm=object-oriented defect="The inter-wave floor (`.nwave/wave-active/active.json`, armed with `provenance: inferred` by the PreToolUse fallback in `pre_tool_use_handler.py`) HARD-STOPS sub-dispatches with `WAVE_MARKER_BYPASS` (exit 2, decision:block) whenever an in-wave child drops the DES markers -- e.g. a DESIGN sub-dispatch, or a read-only finder, denied because a stale/inferred `deliver` or `distill` floor is armed. MEASURED 2026-07-22..23: repeatedly blocked unrelated dispatches (read-only techdebt finders, a nw-solution-architect DESIGN dispatch), each requiring a manual `des wave-clear`. Ale 2026-07-23: the between-waves block has become an IMPEDIMENT and should be an ADVISORY, not a hard stop -- a suggestion to the user to read the docs DISCUSS produced (first) then DESIGN produced (after), not a denial. The hard stop is GDP-1-miscalibrated: it fires reactively at dispatch time on an INFERRED (not human-declared) floor, blocking legitimate work, and its only escapes are claim-false-membership or clear-the-floor (already filed: inferred-wave-floor-blocks-non-wave-dispatch-with-only-dishonest-escapes). Converting hard-stop→advisory removes the impediment while keeping the guidance." proposed_solution="downgrade the inferred-floor bypass from decision:block(exit 2) to an ADVISORY warning printed to stderr/context that POINTS the operator at the wave outputs to read in order (DISCUSS feature-delta / user stories, then DESIGN architecture + component boundaries) and proceeds. Keep a HARD block ONLY for a floor a human or a wave command actually declared (provenance != inferred). Route via the DES spine (production change to `wave_activation_service` / `pre_tool_use_handler` / `des_marker_parser`); add a test that an inferred floor warns-and-allows while a declared floor still blocks. Blast radius: measure via des blast-radius before lane selection."
- [ ] examiner-observations-not-handed-to-dispatching-suborch: paradigm=object-oriented defect="A dispatched nw-user-examiner returns its PASS/FAIL verdict + concrete observations in its completion notification (delivered to the top-level dispatcher), but the SUB-ORCHESTRATOR that dispatched it does not receive that report automatically — it must explicitly resume the examiner to read the observations text. record-examine-verdict --observations requires the examiner's real observation words (a disciplined sub-orch refuses to fabricate them), so the seal stalls until the report is manually retrieved. MEASURED 2026-07-23 on TWO lanes (lean-suite fix-gate-collect-spawn-plugin-overhead + validator fix-validator-refuses-own-generated-dispatch): both examiners finished PASS, both left .nwave/telemetry/examine/ empty, both sub-orchs sat ahead=0 waiting; unblocked only when the top-level orchestrator relayed the observations from the completion notification it alone had received." proposed_solution="Either (a) have the examine dispatch write the examiner's full verdict+observations to a known file/ledger path the dispatching sub-orch reads (not only the completion notification), or (b) document in the sub-orch dispatch pattern that after an examiner completes it must read the agent's completion output/transcript for the observations rather than waiting for a pushed message. Do NOT make the examiner self-seal record-examine-verdict — separation of examine from verdict-recording is intentional (prevents self-rubber-stamping)."
- [ ] examine-slice-key-feature-end-vs-commit-slice-slice-nn-mismatch: paradigm=object-oriented defect="`des dispatch --phase FEATURE_END_EXAMINE` auto-corrects --slice to the literal 'feature-end', so record-examine-verdict seals the verdict keyed 'feature-end'; but `des commit-slice` hard-requires a slice-NN identity and looks up the examine verdict under slice-NN, so it does not find the 'feature-end'-keyed verdict and the operator must MANUALLY re-record the same PASS under --slice slice-01 before commit-slice accepts it. MEASURED 2026-07-23 on collect-error lane, BOTH bugs (na-marker + primary-collect): same underlying PASS re-recorded twice purely to re-key the slice identity. Pure ceremony — the two subsystems disagree on the slice key for one examine." proposed_solution="Reconcile the slice-key contract between the FEATURE_END_EXAMINE dispatch/record path and commit-slice's lookup: either have commit-slice accept a 'feature-end'-keyed examine verdict for a slice-NN commit, or stop the dispatch auto-correcting --slice to 'feature-end' when a slice-NN was given. Add a test that a FEATURE_END_EXAMINE PASS recorded once is accepted by commit-slice without a manual re-record."
- [ ] drain-batch-merge-blocked-deletes-unmerged-work-asymmetry: paradigm=object-oriented defect="src/des/application/refactor_drain_service.py: on a FAILED (blocked) merge, drain_one (single-item, ~line 226-229) deliberately does NOT clean up — it preserves the unmerged branch+worktree for human recovery, enforced by the passing AT test_an_unmerged_branch_is_never_deleted_after_a_failed_merge (test_slice_01_merge_and_cleanup.py:121-144, docstring cites a spike incident where unconditional cleanup deleted an unmerged branch). BUT _drain_concurrently (batch, ~line 320-322) does the OPPOSITE: its merge-blocked branch runs remove_worktree+delete_branch inline, silently discarding the unmerged work the single-item path is contractually required to preserve. Batch and single-item modes disagree on merge-blocked semantics. Found during the orphan-cleanup RCA 2026-07-23." proposed_solution="Reconcile the two: batch merge-blocked should preserve the unmerged branch+worktree exactly as single-item does (do NOT delete on failed merge). Extend test_an_unmerged_branch_is_never_deleted_after_a_failed_merge to parametrize over both drain_one and drain_batch so the contract is enforced for both paths. Note: this is behavior-changing for batch, so route via /nw-bugfix with its own AT + Vera examine, not folded into the orphan-cleanup fix."
- [ ] commit-slice-path-stages-whole-file-cross-contaminates-shared-file: paradigm=object-oriented defect="`des commit-slice --path <file>` stages the ENTIRE file, not only the entering slice's hunks. When two logically-separate fixes coexist in one worktree and share a production file (e.g. two bugs both editing src/des/application/feature_end_cycle_service.py), committing the first with --path <shared-file> sweeps the SECOND fix's already-written hunks into the first fix's commit under the first fix's message — a mixed commit whose Gate-Scope/SliceCommitVerified then attests content that does not belong to that slice. MEASURED 2026-07-23 on collect-error lane: BUG A's commit-slice swept 100% of BUG B's FullSuiteLegRan changes (excluded_directories/exclusion_reason + _pruned_top_level_dirs helper) into BUG A's commit; caught only by manually diffing the committed hunks, fixed via git reset --soft HEAD~1 + unstage-the-shared-file + scoped recommit. Whole-file path granularity cannot separate interleaved changes." proposed_solution="commit-slice should build its commit from a temporary index seeded with HEAD plus only the slice's OWNED HUNKS (hunk-level), or at minimum detect that a --path file carries hunks outside the slice's declared change-set and REFUSE with a what/why/how (name the foreign hunks, point to `git add -p` / reverse-apply). Mirror the `des commit --owned-paths` correct-by-construction temp-index approach but at hunk granularity for the shared-file case. Add a test: two fixes sharing one file, commit-slice of the first must not include the second's hunks."
- [ ] freshness-gate-blocks-vera-real-cli-examine-in-dev-checkout: paradigm=object-oriented defect="The runtime freshness gate (which blocks executing a stale installed `des` in a developer checkout, detected via .git adjacency) ALSO blocks a surface-pinned Vera EXAMINE from invoking the real `des refactor` CLI when the worktree is a developer checkout. MEASURED 2026-07-23 (fix-drain-orphan-cleanup EXAMINE): Vera could not run the real CLI end-to-end — the freshness gate refused — so she fell back to the in-process 85-test suite. Verdict was still conclusive (PASS) but the examiner's core value (real-surface probing, which has caught defects every mechanical layer missed) was degraded to test-suite reading. The safety gate (prevent stale-runtime execution) and the examiner (must probe the REAL surface) are in tension: the gate fires on the examine context it should exempt." proposed_solution="Exempt a surface-pinned examine invocation from the freshness gate (e.g. an explicit NWAVE_EXAMINE_SURFACE=1 / --examine-surface flag that asserts the caller has pinned `uv run --project <worktree>` so the runtime is the editable source under test, not a stale installed copy), OR have the examine-fixture producing tool build a non-dev-checkout drivable repo so the real CLI runs without tripping the gate. Add a test that a surface-pinned examine can execute the real des CLI in a dev-checkout worktree."
- [ ] wave-guard-inspects-dispatch-prompt-content-and-self-blocks-suborch: paradigm=object-oriented defect="The WAVE_MARKER_BYPASS guard on an Agent/Task dispatch inspects the DISPATCH PROMPT TEXT for wave-context keywords (des dispatch / EXAMINE / commit-slice / feature-end / deliver / DES markers) and, if it detects partial wave-context WITHOUT a full DES-VALIDATION marker, classifies the dispatch as a markerless wave-owned child and DENIES it (exit 2) — even when the dispatch is an independent sub-orchestrator prompt that merely DESCRIBES the spine workflow it will drive. MEASURED 2026-07-23: an identical sub-orchestrator lane was denied 5 consecutive times with elaborate prompts mentioning those concepts, then dispatched successfully on the 6th attempt with a LEAN prompt that omitted the trigger keywords — with NO persistent background des process arming the floor between attempts (the floor read 'clear' immediately before each denied dispatch and 'clear' immediately after), isolating the arm to the dispatch's own PreToolUse hook path scanning the prompt. Combined with the inferred-provenance hard-stop, this makes resuming a parked lane a coin-flip on prompt wording." proposed_solution="The wave-guard must key membership off an explicit, structured marker (DES-WAVE / DES-VALIDATION) ONLY — never off free-text keyword scanning of the prompt body, which false-positives on any prompt that merely documents the spine vocabulary. A sub-orchestrator dispatch that carries no DES markers is NOT a wave-child and must pass regardless of what workflow concepts its prose mentions. Add a test: a dispatch prompt containing the strings 'des dispatch', 'EXAMINE', 'commit-slice', 'feature-end' but no DES-VALIDATION/DES-WAVE marker is NOT treated as a wave-bypass. Interim workaround: keep sub-orch dispatch prompts lean, free of wave keywords."
- [ ] examine-pass-does-not-attest-which-oracle-rows-were-exercised: paradigm=object-oriented defect="A recorded EXAMINE verdict (record-examine-verdict) captures PASS/FAIL + free-text observations but does NOT attest WHICH charter oracle rows the examiner actually exercised. An examiner that probes only a SUBSET of a feature-scoped charter's oracle (e.g. only the spawn-behavior rows, never the drain-report row) can still record a full PASS, and commit-slice then gates green on that latest-PASS — a false-green. MEASURED 2026-07-23 (fix-inherited-stdin-deadlocks-spawns): a late-completing examine PASSed on spawn/stdin/timeout/reaping behavior only, never exercising the #1 _report()/merge_blocked_reason path that the same feature-scoped charter requires (still unfixed); its ledger then carried FAIL followed by PASS for the same feature_id + charter_seal, and only a human diff of observations-vs-oracle caught that the PASS was scope-incomplete. commit-slice trusting the latest PASS would have shipped an unmet charter." proposed_solution="record-examine-verdict should require the examiner to enumerate which oracle rows it exercised (a checklist keyed to the charter's oracle), and mark the verdict INCOMPLETE (not PASS) if any oracle row was not exercised. commit-slice must reject a verdict that does not attest full oracle coverage for the charter's scope. Alternatively, bind the verdict to a charter-oracle-row hash so a PASS that omits rows is mechanically detectable. Add a test: an examine PASS that exercised only N-1 of N oracle rows is not accepted by commit-slice."
- [ ] feature-end-success-report-opaque-no-leg-names-no-test-count: paradigm=object-oriented defect="the FeatureEndCycleComplete payload reports only numeric leg COUNTS (leg_census ran/not_applicable/indeterminate) - it never names WHICH legs ran or were skipped, and it discards the executed-test count it already holds (the full-suite junit at .nwave/telemetry/feature-end/<feature>-suite.junit.xml records tests=48 on a fixture where the visible report says only ran:2). A user therefore cannot see from the report that their suite actually ran, nor which leg was skipped and why - the same produce-then-discard anti-pattern as the collect-error bug this was found alongside. Found by Vera examining charter a-passing-verdict-means-my-tests-actually-ran (2026-07-22); 3 flags, all on the SUCCESS path, none a regression from the collect-error fix" proposed_solution="surface the full-suite executed-test count into the success payload and name each leg in the census (additive keys keep the 9 test files asserting on the existing leg_census keys green); needs its own ATs, not a bolt-on"
- [ ] bugfix-lane-can-start-implementation-before-any-charter-exists: paradigm=object-oriented defect="on bugfix/collect-error-is-indeterminate-not-absent, after landing and committing its charter-governed slice (fix-collect-error-indeterminate, Vera PASS), the same lane started a SECOND bug (primary collect shadowed by a generated dir) implementation-first: a 260-line regression test plus a production diff to feature_end_cycle_service.py accumulated on disk with NO docs/product/expectations/ charter anywhere for it, and no des dispatch/RCA artifact naming it either. The agent died mid-flight and left it in that state. Nothing in the tooling refused this -- des dispatch, verify-red-green and the test-authoring path all accept a regression test with no charter behind it; the ONLY place the omission would have surfaced is commit-slice's examine gate, arms-on-charter, which the lane never reached. This is not a case of an agent skipping a known step -- it is that between one slice's COMMIT and the next slice's first write, nothing checks or reminds that Phase 0-charter (t=0, before any code) is owed again. MEASURED 2026-07-22: reconstructed from the dead agent's recovered working tree, not from any log naming the gap." proposed_solution="give the bugfix/atdd_pure per-slice entry a proactive-inline affordance (GDP-2) at the FIRST write after a prior slice's COMMIT: if a new tests/bugs/test_bug_*.py or equivalent regression-test path is about to be authored and no docs/product/expectations/fix-{slug}/ directory exists yet for that slug, warn before the write lands, naming the missing charter and routing to Phase 0-charter (fresh nw-product-owner, observable-only) as the HOW (GDP-4). Reactive backstop: des verify-red-green (or an equivalent early-RED check) could also refuse to record RedObserved for a test file whose feature-id has no charter directory, degrading loud rather than silently letting 260 lines accumulate uncatchable until commit-slice."
- [ ] dispatched-agents-inherit-orchestrator-session-cwd-and-misroute-to-wrong-worktree: paradigm=object-oriented defect="When the session-orchestrator's own cwd is one of the worktrees it dispatches into (here /home/alexd/Projects/wt/spawn-stdin-deadlock, the environment primary dir), EVERY dispatched sub-agent inherits that cwd and physically lands there instead of the worktree it was assigned — so relative-path reads/writes and identity inference (`git status` in the inherited cwd) resolve to the WRONG worktree. MEASURED 2026-07-23, bit TWICE: (1) a sub-orch assigned to feature A found itself in the session cwd, saw feature B's staged work, and began working B (caught only when it re-read the charter and noticed the mismatch); (2) a fresh bugfix lane spawned into the wrong worktree, mis-identified as a different lane, and correctly REFUSED to execute instructions for a worktree not its own. Damage: agents working/dirtying the wrong worktree, cross-contaminated .nwave ledgers/task-markers. Distinct from the DES-PROJECT-ROOT marker gap (that's des dispatch not emitting the marker; this is the harness cwd inheritance itself)." proposed_solution="Every dispatch prompt must force, as the agent's FIRST action: cd <assigned-worktree-absolute>, verify with `git -C <abs> rev-parse --abbrev-ref HEAD` matches the expected branch, ABORT if not; use absolute paths everywhere (relative resolves to the inherited cwd); inject the project-root marker; and after each sub-dispatch verify the other worktrees stayed untouched. Better: the dispatch producing-tool should bake the cd+verify preamble in by construction. Also instruct agents to REFUSE instructions for a worktree != their own (the discipline that prevented wrong-worktree writes tonight). Root harness fix: dispatched agents should default their cwd to the assigned worktree, not inherit the orchestrator's."
- [ ] examine-surface-pin-instruction-insufficient-needs-mechanical-binary-verification: paradigm=object-oriented defect="Instructing the examiner to drive `uv run --project <worktree> des ...` is NOT enough to guarantee it tests the code under test: the examiner can still (drop --project, run from a cwd uv doesn't resolve to the worktree project, or invoke bare des) fall back to the PATH and resolve to the GLOBAL INSTALLED shim (~/.claude-alt/bin/des -> ~/.claude/lib/python/des), a STALE pre-fix snapshot -> false-FAIL on code that is actually correct. MEASURED TWICE 2026-07-23 (drain-silent-noop, then spawn-stdin slice-02 signal-handler): both false-FAILed because the examiner probed the stale installed runtime; diagnosis proved the worktree code was correct (readlink -f showed the examiner hit ~/.claude/lib/python/.../refactor.py, 9449 bytes pre-fix, vs 25333 bytes in the worktree). The surface-pin as a prose instruction is not mechanically enforced." proposed_solution="The examine dispatch/producing-tool must MECHANICALLY verify, as a hard preamble the examiner runs before any probe, that `readlink -f \"$(uv run --project <worktree> which des)\"` resolves INTO the worktree (<worktree>/.venv/... or the editable src), and ABORT with INDETERMINATE(surface-unverified) if it resolves to the installed shim. Better: the examine-fixture producing tool emits the exact verified invocation + the readlink assertion by construction, so the examiner cannot silently test the wrong binary. Relates to the already-filed examine-surface-staleness + freshness-gate rows — this is the enforcement-not-instruction escalation."
- [ ] drain-real-ctrl-c-sigint-pgroup-still-orphans-subtree-uv-wrapper-race: paradigm=object-oriented defect="The slice-02 signal-cleanup fix reaps correctly on SIGTERM-to-the-top-PID (verified PASS) but NOT on a real operator Ctrl-C (SIGINT to the foreground process GROUP). MEASURED 2026-07-23 by a binary-verified nw-user-examiner (readlink confirmed the worktree's des, not the stale shim): `kill -INT -<pgid>` -> exit 130, empty message, grandchild sleep 600 alive, 5 orphan procs (uv, des, sh, stub, sleep), leftover branch+worktree refactor-TD-4b. Violates the charter negative 'no leftover process once my prompt is back (normally, early, or after Ctrl-C)'. Likely root: SIGINT to the pgroup hits both the uv wrapper AND des main simultaneously; uv dies via default disposition and kills its child des main before `_abort_on_signal`'s reap completes -- whereas SIGTERM-to-the-single-top-PID lets uv forward to des which handles it cleanly. Deeper tension: `start_new_session=True` on the agent subtree (needed so the timeout-reap's killpg targets only that child's group) also means a pgroup Ctrl-C never reaches the subtree directly, so ALL reaping depends on des main's handler surviving long enough -- which it does not under pgroup-SIGINT-with-a-dying-uv-parent. A prior diagnosis claimed pgroup-SIGINT reaped; the demanding examiner refutes it. NOTE: unresolved whether the INSTALLED des shim invocation (no uv wrapper) behaves differently from `uv run des` on Ctrl-C -- needs checking." proposed_solution="DESIGN-territory (process-supervision architecture), not a trivial patch: reconcile the timeout-reap's need for a separate child session with the Ctrl-C-reap's need to reach the subtree. Options to weigh in design: (a) des main installs the SIGINT handler to reap FAST + re-raise, and the invocation avoids the uv-wrapper death race (e.g. the shim exec-replaces so there is no wrapper parent in prod; verify the installed-shim Ctrl-C path); (b) a lightweight supervisor/pidfile that reaps even if des main is killed mid-handler; (c) block/mask the signal in des main until the reap completes. Route through /nw-design -- flagged for human decision, not forced. Interim: the SIGTERM path works; verify-worktree-cleanup remains the backstop for leftover git state."
- [ ] repo-root-resolution-lacks-single-composition-root-prefactoring-not-marker-patch: paradigm=object-oriented defect="ROOT of the cwd-contamination class (reclassifies the DES-PROJECT-ROOT / dispatched-agents-inherit-session-cwd rows). nWave HAS a canonical resolver (src/des/domain/repo_path_resolver.py:resolve_repo_root: --repo-root -> NWAVE_REPO_ROOT env -> Path.cwd() fallback) but centralization is PARTIAL: (a) it FALLS BACK to Path.cwd() when no flag/env is passed -- which is exactly where a dispatched agent, inheriting the orchestrator session's cwd, resolves to the wrong repo; (b) ~74 files touch repo-root and some resolve Path.cwd() AD-HOC, bypassing the resolver entirely (e.g. housekeeping_service.py:56 `Path.cwd() / '.nwave'`, log_persistence.py:105). So writer and reader of the same logical root resolve from DIFFERENT sources -> silent disagreement (the cwd-contamination that derailed lanes all night). Cross-diagnosed 2026-07-23 with Lyra-evolution: same common root as her backup-dir bug (LYRA_DATA_DIR unified only the read-view; write paths from independent env-vars) -- NO single composition root for a logical resource -> scattered N-site resolution -> silent consumer disagreement. A 'unify by setting one env/param' only covers a subset of sites." proposed_solution="This is a @PREFACTORING, not a bugfix of the gate (per the standing rule: scattered-resolution fix needs the resolver extracted BEFORE the behavioral fix -- patching a single site hits the trap). Shape: (1) route ALL repo-root/.nwave resolution through resolve_repo_root (eliminate the ad-hoc Path.cwd() sites); (2) have `des dispatch` set NWAVE_REPO_ROOT=<target-worktree> in the generated envelope so a dispatched agent's resolver picks the worktree, not the inherited-cwd fallback; (3) thread the resolved root as an injected capability, never re-resolve ad-hoc in consumers. Route through /nw-design (prefactoring) -- the DES-PROJECT-ROOT marker injection is only an interim mitigation, not the fix."
- [ ] consolidation-signal-tick-cannot-record-observed-healthy-trunk: paradigm=object-oriented defect="`des consolidation-signal-tick` NON puo' registrare un segnale trunk-health OSSERVATO-verde, mentre il loop di consolidamento (Loop 3/9 never-idle) istruisce l'orchestratore a `Record an OBSERVED trunk-health signal ... via des consolidation-signal-tick` a ogni fire. MISURATO 2026-07-24: la whitelist di `signal_type` nel dominio accetta SOLO segnali negativi -- `['drift', 'failing-gate', 'stale-branch', 'unmerged-work']` -- e rifiuta qualunque cosa altro con `CONSOLIDATION_SIGNAL_INTAKE_REJECTED: unsupported signal_type 'trunk-health'`. Non esiste un signal_type per 'trunk pulito / nessun segnale negativo osservato', quindi l'istruzione del loop e' letteralmente insoddisfacibile sul percorso felice: quando il trunk E' sano (il caso piu' frequente in steady-state) non c'e' NULLA da registrare e ogni fire diventa un no-op forzato. Contraddizione loop<->contratto-tool. Friction secondaria stesso comando: `--signal` e' un prefisso AMBIGUO fra `--signal-type` e `--signal-key` (argparse abbreviation), quindi un'invocazione naturale `--signal trunk-health` esce 2 con `ambiguous option`. Distinta dalla riga 101 (quella copre il SILENZIO sul success-path di un intake ACCETTATO; questa e' l'IMPOSSIBILITA' di accettare un intake positivo)." proposed_solution="Due opzioni. (A) Se il tick e' progettato SOLO per segnali negativi (una coda di consolidamento raccoglie problemi, non salute), allora il loop e' scritto male: Loop 3 non dovrebbe chiedere di registrare la SALUTE ma solo di registrare un segnale NEGATIVO quando osservato, altrimenti no-op onesto -- e il messaggio di rifiuto dovrebbe dirlo ('trunk-health non e' un segnale di consolidamento; registra solo drift/failing-gate/stale-branch/unmerged-work quando OSSERVATI, altrimenti nessuna azione e' corretta'). (B) Se si vuole davvero un heartbeat positivo, aggiungere un signal_type `healthy`/`trunk-green` che il dominio accetti e che serva da battito osservato. In ogni caso: allineare la prosa del loop e la whitelist del tool -- oggi si contraddicono, e l'operatore che segue il loop alla lettera colpisce un rifiuto a ogni fire su trunk pulito. Minore ma gratis: dare a `--signal-type`/`--signal-key` alias non-ambigui o rifiutare `--signal` con un hint esplicito."
- [ ] refactor-actuator-spawns-headless-claude-with-inherited-stdin: paradigm=object-oriented defect="`scripts/refactor_agent.py:219` spawna il fixer headless con `subprocess.run([cli, '-p', prompt_text, '--model', model, ...], cwd=worktree, check=False)` -- SENZA `stdin=subprocess.DEVNULL`. Lo spawn quindi EREDITA lo stdin del processo padre. Il meccanismo oggi non deadlocka solo perche' il chiamante canonico (Loop 7 standing) impone `des refactor ... < /dev/null`, cioe' la sicurezza e' delegata alla DISCIPLINA DEL CHIAMANTE, non garantita dall'attuator. Chiunque lanci `des refactor --agent-cmd 'scripts/refactor_agent.py {prompt}'` da un contesto con tty o pipe (una shell interattiva, un altro orchestratore che non conosce il redirect) fa ereditare allo spawn `claude -p` uno stdin vivo: se il fixer headless legge stdin in attesa d'input mentre nessuno scrive, e' esattamente il deadlock inherited-stdin della classe che da' il nome a questo worktree (bugfix/inherited-stdin-deadlocks-spawns). MISURATO 2026-07-24 durante l'indagine sul perche' i drain non fossero girati: `claude` e' su PATH (/home/alexd/.local/bin/claude), lo script e' eseguibile, la riga subprocess.run non contiene ne' `stdin=` ne' `env=`. Distinto dalle 6 righe refactor_agent gia' filate (path-relativo dell'attuator nel catalogo, ecc.): nessuna copre lo stdin dello spawn. E' l'attuator del DRAIN che riproduce il bug che il drain stesso dovrebbe aiutare a chiudere." proposed_solution="L'attuator deve passare `stdin=subprocess.DEVNULL` esplicitamente nella subprocess.run a riga 219 -- la sicurezza-da-deadlock non va delegata al redirect del chiamante, va garantita nel punto di spawn (stesso principio del fix slice-01 su src/des/runtime/spawn.py: DEVNULL di default al sito di spawn, non a monte). Additivo: passare `env` esplicito con possibilita' di isolare `CLAUDE_CONFIG_DIR` cosi' il fixer headless non contende quota/credenziali con la sessione orchestratrice (oggi eredita il profilo attivo del chiamante). Regola generale gia' stabilita in questo worktree: ogni spawn di subprocess che non consuma stdin deve chiuderlo a DEVNULL al sito, mai affidarsi a un redirect esterno."
- [ ] blast-radius-counts-consumers-by-bare-symbol-name-not-qualified-module: paradigm=object-oriented defect="`des blast-radius` conta i consumatori per NOME-SIMBOLO NUDO invece che per simbolo QUALIFICATO-DAL-MODULO, quindi ogni funzione che condivide un nome comune eredita il conteggio aggregato di TUTTE le omonime del repo. PROVA MISURATA 2026-07-24 (due sonde su moduli non-correlati): `des blast-radius --repo . --paths src/des/cli/consolidation_signal_tick.py` riporta `consumer_counts['consolidation_signal_tick.main'] = 333`; `--paths src/des/cli/dispatch.py` riporta `consumer_counts['dispatch.main'] = 333`. Numero IDENTICO su due moduli che non si chiamano fra loro -- non e' una coincidenza, e' il conteggio aggregato dei 213 `def main(` presenti in src/ + scripts/. Controprova sullo stesso comando: un simbolo con nome DISTINTIVO si comporta bene (`lane_profile.LaneProfile` = 4, plausibile), quindi il difetto morde selettivamente i nomi comuni (main, run, setup, __init__...) e NON e' visibile finche' non si misura un simbolo generico. CONSEGUENZA sul routing, che e' il punto: `large_min_consumers=10`, quindi QUALUNQUE modulo con una funzione `main()` viene classificato tier=L per costruzione -- tutti gli 87 CLI sotto src/des/cli/ inclusi. Il classificatore che deve decidere la CORSIA (S diretta / M one-shot / L a slice) manda in L-lane lavoro che e' S o M, cioe' impone DISCUSS/DESIGN a fix meccanici. Misurato in vivo: la riga defects 'consolidation-signal-tick-success-path' -- aggiungere UNA riga di output sul percorso di successo -- e' stata classificata tier=L con reason `consumer_counts[main]=333 >= 10`. E' il difetto peggiore possibile per un gate di misura: non e' silent-wrong sul permissivo (non lascia passare l'L come S), e' rumoroso sul restrittivo, quindi non si manifesta mai come incidente -- si manifesta come cerimonia che sembra dovuta, e nessuno la mette in discussione perche' il gate 'ha misurato'. AGGRAVANTE MISURATA 2026-07-24, e cambia la classe del difetto: il verdetto sbagliato NON resta nella cerimonia, ENTRA NEI RECORD DI VERITA'. Committando il bugfix `validate-manifest-missing-file-msg` (1 file di produzione, +27 righe, nessun boundary, consumatori reali <=1 -- tier S per qualunque lettura onesta) `des commit-slice --tier S` ha RIFIUTATO con `BlastRadiusTierExceeded`, reason `consumer_counts[validate_component_manifest.main]=333` E `consumer_counts[test_validator_missing_file_message.CliResult]=29` -- due conteggi per nome nudo, il secondo su un helper di test con un nome comune, che mostra che il difetto non tocca solo `main` ma qualunque identificatore condiviso. Le uniche uscite offerte erano dichiarare `--tier L` o spezzare la slice; ho dichiarato L, e ORA IL COMMIT PORTA IN PERMANENZA UNA CLASSIFICAZIONE FALSA prodotta dal difetto stesso. Un difetto di MISURA si propaga cosi' nel RECORD che quella misura doveva certificare -- il trailer del commit, il ledger, e a valle ogni analisi di tier-distribution che li legga. Non e' piu' 'un gate rumoroso': e' una sorgente di dati falsi nella storia del repo, e i dati falsi sopravvivono al fix del gate." proposed_solution="Qualificare il conteggio per modulo: il consumatore va risolto sul simbolo IMPORTATO-DA-QUEL-MODULO (`from des.cli.dispatch import main` / `dispatch.main(...)`), mai sul nome nudo `main` ovunque compaia. Dove l'analisi AST non riesce a risolvere l'origine di una chiamata, il conteggio deve degradare LOUD (INDETERMINATE per quel simbolo, mai un numero che sembra misurato -- GDP-6): un conteggio non-attribuibile presentato come conteggio e' esattamente il silent-wrong che il gate esiste per impedire. AT obbligatoria e gia' riproducibile: due moduli non-correlati che espongono entrambi `main` DEVONO produrre conteggi diversi, e un modulo il cui unico chiamante e' un test non deve ereditare il conteggio delle omonime. Aggiungere anche un oracolo negativo: il tier di un modulo NON deve cambiare rinominando una sua funzione da `helper_x` a `main` senza toccare nessun chiamante -- oggi cambia, e questa e' la formulazione piu' corta del difetto."
- [ ] validate-component-manifest-missing-file-emits-bare-stacktrace: paradigm=object-oriented defect="`scripts/cli/validate_component_manifest.py` reagisce a un path INESISTENTE con uno stack trace Python nudo -- `FileNotFoundError: [Errno 2] No such file or directory: '<path>'` risalendo da `pathlib.open` -- senza COSA/PERCHE'/COME. MISURATO 2026-07-24 con failure injection su tre input (exit misurati correttamente, senza pipe che mascheri il codice): manifest valido -> exit 0 silenzioso; YAML valido ma non-manifest -> exit 2 con messaggi ECCELLENTI e nominati ('manifest is malformed: schema-version is a required property', idem feature-id, unbounded-input-domains); file inesistente -> exit 1 con stack trace nudo. Il difetto e' quindi l'ASIMMETRIA fra due rami dello stesso comando: il ramo malformato e' esemplare, il ramo file-mancante e' grezzo. E il file-mancante e' il caso PIU' probabile in un dispatch cross-worktree (path relativo risolto contro il repo-root sbagliato -- vedi la riga composition-root gia' filata), quindi il ramo peggio-messo e' quello che l'operatore incontrera' per primo. Nota di misura, perche' e' la lezione: al primo tentativo avevo letto `exit=0` su tutti e tre i casi e stavo per filare 'il validator esce sempre 0, gate inefficace' -- era un artefatto della MIA pipe (`| tail; echo $?` cattura l'exit di tail, non del comando). Ri-misurato senza pipe, gli exit code sono corretti. Il difetto vero e' solo il messaggio." proposed_solution="Catturare il file-mancante prima della open e emettere lo stesso registro del ramo malformato: COSA ('component-manifest not found at <path>'), PERCHE' ('the design wave's manifest gate needs it to verify the unbounded-input-domain rows'), COME ('generate it with the design wave's producing tool, or check you are running from the intended repo root -- resolved root was <root>'). Stampare il repo-root RISOLTO nel messaggio e' il dettaglio che paga: in un dispatch cross-worktree la causa reale e' quasi sempre che il path e' stato risolto contro l'albero sbagliato, e nessun messaggio lo dice oggi. Estendere il controllo agli altri validator sotto scripts/cli/: l'asimmetria ramo-malformato-parla / ramo-IO-tace e' probabilmente replicata."
- [ ] wiring-slice-closes-feature-while-leaving-an-upstream-slice-unwired: paradigm=object-oriented defect="UNA SLICE DI WIRING CHIUDE LA FEATURE CABLANDO SOLO UN SOTTOINSIEME DELLE SLICE A MONTE, E NULLA LO RILEVA. Caso misurato 2026-07-24 su F-DESIGN-COMPONENT-MANIFEST (5 slice, tutte con test implementati, nessuna skipped): il simbolo `resolve_manifest_state` (prodotto da slice-03 'four-state-resolver') compare in slice-03 e in NESSUNA altra slice -- conteggio per-slice sui .feature: 01=0, 02=0, 03=3, 04=0, 05=0. Slice-05 si chiama 'wiring' e si dichiara 'the final slice wires the manifest into the design wave's exit review', ma cabla la VALIDAZIONE (slice-01/02, `validate_component_manifest` -- vivo, usato dal gate DESIGN in produzione) e il PRODUCER (slice-04), lasciando il RESOLVER scollegato. La prosa di slice-05 lo confessa nel descriversi -- 'Read in sequence after slice-04: slice-04 made the manifest produced; this slice makes its absence or wrongness block the design wave' -- nomina 04, nomina la validazione, e SALTA 03 senza dichiararlo. Conseguenza a valle, gia' materializzata: un detector never_wired ha segnalato il resolver come orfano, la riga di pile risultante e' stata instradata al drain, e il crafter ha CANCELLATO modulo + test + il .feature di slice-03 -- cioe' una specifica -- perche' dal singolo simbolo 'zero chiamanti di produzione' e' indistinguibile da 'codice morto'. La cancellazione e' stata fermata prima del commit solo perche' un umano ha guardato le slice SORELLE. LA CLASSE: il meccanismo di slicing traccia l'ORDINE (slice-05 dopo slice-04) ma non la COPERTURA (slice-05 cabla TUTTE le slice a monte?). Un fan-in che dichiara di raccogliere tutti i rami e ne raccoglie tre su quattro e' indistinguibile da uno completo. Stessa forma degli altri difetti di copertura-parziale-presentata-come-completa gia' filati (examine-PASS che non attesta quali oracle-row ha esercitato; report feature-end che conta le gambe invece di nominarle)." proposed_solution="Due livelli. (1) IL CASO: il difetto NON e' 'rimuovi resolve_manifest_state' -- e' 'slice-05 non cabla slice-03'. Decidere se il four-state resolver va cablato nel design-exit review accanto alla validazione, oppure se slice-03 e' stata superata dal design e va ritirata ESPLICITAMENTE (ritiro dichiarato, non cancellazione dedotta da un detector). (2) IL METODO, che vale piu' del caso: introdurre un gate di WIRING-COMPLETENESS. La slice di wiring deve ATTESTARE quali slice a monte cabla (elenco esplicito, non prosa), e il gate rifiuta se una slice a monte della stessa feature resta senza consumatore vivo dopo il wiring. Cosi' 'ramo non cablato' diventa un fatto dichiarato e verificabile invece di un'assenza silenziosa che solo un detector a valle notera' -- male, e a distanza di mesi. Corollario per la corsia di drain: una riga di pile prodotta da un detector che AVVERTE dei propri falsi positivi non deve essere drenabile in autonomia; il verdetto 'orphan/dead/unused' su un simbolo va sempre allargato alle sue SORELLE (le altre slice della stessa feature) prima di autorizzare una rimozione -- costa una `ls`, e qui avrebbe evitato la cancellazione di una specifica."
- [ ] pile-has-no-duplicate-or-composition-detection: paradigm=object-oriented defect="Il pile dei difetti non ha alcun rilevamento di RIGHE DUPLICATE ne' di COPPIE-CHE-SI-COMPONGONO, e nessuno dei due si vede leggendo le righe una per una -- che e' l'unico modo in cui le righe vengono lette. MISURATO 2026-07-24 raggruppando le 179 righe di defects.md per file citato (regex sui path src/|scripts/|nWave/|tests/, un comando): (a) DUPLICATO CONFERMATO -- `dispatch-feature-end-examine-falls-through-to-crafter` e `feature-end-examine-phase-dispatches-crafter-not-examiner` sono lo STESSO difetto, stessa causa (`src/des/cli/dispatch.py:168`, `_PHASE_AGENTS` non mappa FEATURE_END_EXAMINE) e stesso sintomo (l'envelope emette `Agent: nw-software-crafter`), filati in momenti diversi arrivandoci da due strade; (b) COMPOSIZIONE non descritta in nessuna scheda -- su `dispatch.py` + `nWave/agents/nw-user-examiner.md` convergono quattro righe (i due duplicati sopra + `examiner-blindness-claimed-by-construction-is-only-by-instruction` + `examine-discipline-lives-only-in-prose-with-no-mechanical-guard`) che insieme dicono una cosa che NESSUNA dice da sola: la prova-di-done di una feature e' affidata a un agente capace di leggere il sorgente, e la cecita' che lo impedirebbe e' garantita solo per istruzione; (c) seconda composizione -- su `scripts/build_dist.py` convergono `refactor-actuator-shipped-nowhere-drain-is-noop-on-every-consumer` e `catalog-names-actuator-by-relative-path-absent-on-consumer`, che insieme significano che la corsia di drain e' documentata e INERTE su ogni macchina che non sia quella di sviluppo. PERCHE' E' UN DIFETTO DEL MECCANISMO e non un caso singolo: le righe vengono classificate e prioritizzate INDIPENDENTEMENTE, quindi due 'minori' che condividono un meccanismo restano minori ciascuna a casa propria e nessuna viene mai promossa, benche' composte producano una modalita' di fallimento qualitativamente nuova; e un duplicato gonfia il denominatore e rende il difetto piu' facile da rimandare, perche' due righe minori pesano meno di una riga sola." proposed_solution="Un check meccanico sul PILE (non sul codice), eseguibile come loop ricorrente e non come analisi ad-hoc: raggruppare le righe per file/simbolo citato e segnalare (1) i gruppi con >=2 righe come CANDIDATI-COMPOSIZIONE da valutare insieme, (2) le righe che condividono lo stesso locus + lo stesso sintomo come CANDIDATI-DUPLICATO da fondere. Il raggruppamento e' una regex sui path piu' un defaultdict -- il costo e' un comando, e al primo giro su 179 righe ha prodotto un duplicato confermato e due composizioni. Aggiungere al momento del FILING un campo esplicito che nomini il meccanismo toccato (non solo il file), cosi' il raggruppamento non dipenda dal fatto che due autori diversi abbiano citato lo stesso path. Nota di processo: la fusione dei duplicati e la promozione delle composizioni CAMBIANO la priorita' del pile, quindi sono decisioni umane -- il check deve SEGNALARE le coppie, mai fonderle o ri-prioritizzarle da solo."
- [ ] pile-rows-are-not-freshness-checked-and-compose-into-phantom-blockers: paradigm=object-oriented defect="LE RIGHE DEL PILE NON HANNO ALCUNA VERIFICA DI FRESCHEZZA CONTRO IL CODICE, e un'analisi che le COMPONE amplifica quella staleness in un blocker che non esiste. MISURATO 2026-07-24: un composition-finder sul pile ha riportato come BLOCKER la coppia `dispatch-feature-end-examine-falls-through-to-crafter` + `validator-refuses-dispatch-its-own-generator-produces`, concludendo che 'la fase e' nella mappa del generatore ma non in quella del validator, quindi 41 feature armate per l'examine non possono essere dispacciate'. Ragionamento internamente CORRETTO e conclusione FALSA: verificato nel sorgente, `src/des/cli/dispatch.py:180` mappa gia' `ATDDPurePhase.FEATURE_END_EXAMINE` all'agente esaminatore, e `src/des/application/atdd_pure_prompt_validator.py:268-282` e' stato RIPROGETTATO per chiavare sull'AGENTE risolto invece che sulla FASE -- il suo docstring cita quel difetto AL PASSATO ('FEATURE_END_EXAMINE was refused by the very generator that produced it') come la ragione stessa del redesign. Entrambe le righe sono gia' fixate e ancora aperte `- [ ]`. LA CLASSE, che e' peggiore del singolo caso: un pile e' un registro di INTENZIONI, non uno specchio dello STATO. Una riga stale presa da sola e' solo rumore -- chi la lavora scopre subito che e' gia' fixata. Ma DUE righe stale COMPOSTE producono un blocker fantasma piu' convincente di ciascuna, perche' la composizione aggiunge un ragionamento nuovo e plausibile ('nessuna delle due blocca da sola, insieme garantiscono il fallimento') che nessuna delle due righe conteneva e che quindi nessuno riconosce come gia' risolto. Il metodo che abbiamo introdotto per trovare i rischi nascosti EREDITA e AMPLIFICA la staleness del suo input. Conseguenza secondaria sul denominatore: se due righe su una coppia campionata erano gia' chiuse, il pile a 180 righe sovrastima il lavoro residuo di una frazione ignota -- e ogni priorizzazione basata sul conteggio e' tarata su un numero gonfio." proposed_solution="Due livelli. (1) FRESHNESS-CHECK PRIMA DI COMPORRE, non dopo: il composition-finder deve, per ogni riga che entra in una composizione, RI-VERIFICARE nel sorgente che il difetto sia ancora presente, e marcare `STALE-CANDIDATE` (non `CONFIRMED`) qualunque riga il cui locus citato non mostri piu' il difetto. Una composizione che contiene anche UNA riga non ri-verificata va riportata come IPOTESI, mai come blocker. (2) FRESHNESS-SWEEP PERIODICO SUL PILE, indipendente dal finder: un passaggio read-only che per ogni riga aperta apre il file:riga citato e segnala i candidati-gia-fixati -- senza chiuderli da solo, perche' chiudere una riga e' un giudizio umano. La riga di pile dovrebbe portare, al filing, il commit-sha del momento in cui il difetto e' stato osservato, cosi' che 'quanto e' vecchia questa osservazione' sia un dato e non una congettura. Nota di metodo generale: vale per QUALUNQUE analisi derivata da un artefatto-registro (pile, backlog, catalogo, inventario) -- l'analisi eredita la freschezza dell'artefatto, mai quella del sistema, e va etichettata con la freschezza della sua FONTE."
- [ ] examine-attestation-is-more-ephemeral-than-the-commit-it-authorises: paradigm=object-oriented defect="LA PROVA SOPRAVVIVE MENO DELL'OGGETTO CHE PROVA. `.nwave/` e' GITIGNORED (verificato 2026-07-24: `git check-ignore -v .nwave/telemetry/examine/<feature>.jsonl` -> `.nwave/.gitignore:2:*`; `git ls-files .nwave/` restituisce solo `.gitkeep` e `local-config.json`, zero ledger). Quindi al termine di una corsia bugfix: il COMMIT viaggia sul trunk portando il suo `Gate-Scope:` sigillato e il `Slice-Id:`, mentre il VERDETTO EXAMINE che lo ha autorizzato -- il record `ExamineVerdictRecorded` con verdict, observations e charter_seal -- resta nel worktree e MUORE quando il worktree viene rimosso. MISURATO in vivo sulla chiusura di `validate-manifest-missing-file-msg`: il ledger e' stato scritto in `/home/alexd/Projects/wt/validator-missing-file-msg/.nwave/telemetry/examine/`, ed e' ASSENTE dal repo principale; il commit `a2f055f72` invece e' permanente. Fra una settimana, di quella catena (RED sigillato + GREEN + PASS su quattro oracle-row con output citato + charter_seal) sopravvivera' SOLO il commit. CONSEGUENZA: 'questa slice e' stata esaminata' torna a essere un'AFFERMAZIONE di chi c'era invece di un FATTO consultabile -- esattamente il punto di partenza da cui i ledger dovevano tirarci fuori, e la ragione per cui esiste il mandato earned-trust (evidenza meccanica, non verdetto asserito). SI COMPONE con due difetti gia' filati, e insieme danno TRE modi indipendenti di perdere la stessa prova, nessuno dei quali emette un segnale: (a) l'evento non viene mai scritto (`hook-diagnostic-logging-swallows-its-own-failure-without-a-trace`, techdebt); (b) viene scritto nell'albero sbagliato (`repo-root-resolution-lacks-single-composition-root`); (c) viene scritto correttamente e poi svanisce col worktree (questa riga). Tutti e tre a monte di una firma di gate terminale." proposed_solution="Il criterio generale: un artefatto di PROVA deve avere una durata MAGGIORE O UGUALE a quella dell'artefatto che certifica; se e' minore, la certificazione ha una data di scadenza che nessuno ha dichiarato. Opzioni, non esclusive: (1) al momento del commit-slice, incorporare l'ESSENZIALE dell'attestazione nel commit stesso -- il verdetto, il charter_seal e l'examiner id come trailer accanto a `Gate-Scope:` -- cosi' la prova viaggia con cio' che prova ed e' verificabile da chiunque legga la storia; (2) promuovere i ledger di attestazione (esaminate, verdetti, sigilli) da `.nwave/` gitignored a un percorso TRACCIATO, distinguendoli dalla telemetria volatile che giustamente resta ignorata -- oggi i due tipi di dato condividono lo stesso destino e hanno esigenze opposte; (3) come minimo immediato, un gate che al merge-back RIFIUTI se l'attestazione della slice non e' stata trasferita nel target, invece di lasciarla morire in silenzio nel worktree. Verificare anche il gemello: quali ALTRI record di verita' (AtCompletionLedger, verdetti di review, sigilli RED) vivono sotto `.nwave/` e condividono la stessa scadenza non dichiarata."
- [ ] missing-manifest-message-calls-the-cwd-a-repo-root-when-it-is-not-one: paradigm=object-oriented defect="Il messaggio di errore introdotto per il manifest mancante etichetta la directory risolta come 'repo root' anche quando NON e' la radice di un repository. OSSERVATO 2026-07-24 dall'esaminatrice durante l'examine di `validate-manifest-missing-file-msg` (verdetto complessivo PASS, questo rilievo esplicitamente NON bloccante): eseguendo il validator da `/tmp` con un path relativo, l'output dice `manifest not found: my-manifest.yaml -- resolved against repo root /tmp`. Ma `/tmp` non e' un repo root: e' semplicemente il current working directory. Il COMPORTAMENTO e' corretto -- l'esaminatrice ha verificato, creando un file in `/tmp` e ri-eseguendo, che la risoluzione avviene davvero contro quella directory (exit 2, file trovato) -- e' l'ETICHETTA a dichiarare piu' di quanto il dato sia. PERCHE' NON E' COSMETICO: il messaggio esiste proprio per dire all'operatore DOVE il tool ha cercato, nel caso in cui la causa reale sia una risoluzione contro l'albero sbagliato. Chiamare 'repo root' un cwd qualunque afferma che quella directory ha una proprieta' (essere la radice di un repository) che nessuno ha verificato, e un operatore che legge 'repo root /tmp' puo' concludere che il tool abbia mal-identificato il repository quando invece ha semplicemente usato il cwd. Il rimedio che il messaggio suggerisce ('controlla di essere nella repo root prevista') diventa fuorviante nel caso in cui non si stia affatto lavorando dentro una repo. Stessa classe della riga `blast-radius-counts-consumers-by-bare-symbol-name`: un dato corretto presentato sotto un'etichetta che afferma piu' di quanto il dato sostenga." proposed_solution="Nominare la directory per cio' che e' davvero: 'resolved relative to the current directory <path>' quando il path e' relativo, oppure 'looked in <path>'. Se si vuole conservare l'informazione utile 'e questa e' o non e' una repo', va VERIFICATA e detta separatamente ('<path> (not a git repository)') invece che asserita nell'etichetta. Regola generale applicabile a ogni messaggio diagnostico: un'etichetta deve descrivere cio' che il programma SA, non cio' che presume -- se il programma non ha verificato che quella directory sia una repo root, non puo' chiamarla cosi'. Il fix e' una stringa piu' una eventuale verifica; l'AT di regressione esistente (`test_missing_manifest_path_emits_what_why_how`) va estesa per asserire che l'etichetta non rivendichi 'repo root' per una directory che non lo e'."
- [ ] claude-home-assumed-canonical-instead-of-resolved-breaks-alt-profiles: paradigm=object-oriented defect="LA HOME DI CLAUDE E' ASSUNTA IN POSIZIONE CANONICA invece che RISOLTA dall'ambiente, e questo rompe per chiunque non usi il profilo di default. SEGNALATO da un utente (Attila, 2026-07-24: 'la home di claude deve essere recuperata e non assunta come se fosse in posizione canonica per tutti'). VERIFICATO nel sorgente, due forme distinte: (a) FALLBACK HARDCODED -- `src/des/domain/agent_capability.py:175` ritorna `Path.home() / \".claude\"`; (b) PEGGIO, MATCH PER STRINGA -- `src/des/application/tdd_template_loader.py:40` decide se e' un'installazione con `\".claude\" in module_str or \".claude\" in module_resolved_str`, e `:47` risale i parent cercando `parent.name == \".claude\"`. Cioe' l'identita' della directory di installazione e' dedotta dal suo NOME. CHI SI ROMPE: chiunque imposti `CLAUDE_CONFIG_DIR` a un percorso che non si chiama `.claude` -- e non e' un caso ipotetico, e' la NOSTRA configurazione documentata: CLAUDE.md descrive tre profili (`~/.claude`, `~/.claude-alt`, `~/.claude-alt3`). Per i due profili alternativi il match `parent.name == \".claude\"` FALLISCE e il loader non riconosce l'installazione; il fallback di (a) punta comunque al profilo di default anche quando l'utente ne ha configurato un altro, quindi legge la configurazione SBAGLIATA in silenzio invece di fallire. E' esattamente la classe che abbiamo inseguito tutto il giorno -- una proprieta' dedotta da un'ETICHETTA (il nome della directory) invece che da un'identita' RISOLTA (la variabile d'ambiente che la dichiara) -- qui applicata al percorso di installazione. Nota: `CLAUDE_CONFIG_DIR` E' gia' letto altrove (agent_capability.py stesso, install_utils.py, uninstall_nwave.py, rc_smoke), quindi il meccanismo corretto ESISTE e questi siti lo aggirano." proposed_solution="Un unico resolver della home di Claude, che legga `CLAUDE_CONFIG_DIR` e ricada su `~/.claude` SOLO in sua assenza, restituendo un percorso risolto; ogni consumatore lo riceve iniettato e nessuno lo ri-deduce. Eliminare in particolare il riconoscimento per NOME (`\".claude\" in path`, `parent.name == \".claude\"`): la domanda giusta non e' 'questa directory si chiama .claude?' ma 'questa directory E' la config-dir che l'ambiente dichiara?'. Dove il percorso non e' determinabile, degradare LOUD invece di ricadere in silenzio sul default -- leggere la configurazione del profilo sbagliato e' peggio che rifiutare. AT obbligatoria: con `CLAUDE_CONFIG_DIR` impostato a una directory che NON si chiama `.claude`, l'installazione deve essere riconosciuta e i template risolti; e il fallback non deve mai puntare al default quando la variabile e' impostata."
- [ ] installed-hook-invoked-as-module-assumes-dev-repo-on-pythonpath: paradigm=object-oriented defect="UN HOOK INSTALLATO VIENE INVOCATO COME MODULO PYTHON DEL REPO DI SVILUPPO, e sulla macchina di un utente il modulo non esiste. SEGNALATO da un utente (Marcio, 2026-07-24) come errore RICORRENTE: `PreToolUse:Bash hook error -- /usr/bin/python3: Error while finding module specification for 'scripts.hooks.no_verify_reminder' (ModuleNotFoundError: No module named 'scripts.hooks')`. L'hook e' quindi configurato come `python3 -m scripts.hooks.no_verify_reminder`, una forma che richiede che `scripts/hooks/` sia importabile -- cioe' che il CWD sia la radice del repo di sviluppo, o che il PYTHONPATH lo contenga. Su una macchina utente, dove gli script hook sono COPIATI altrove al momento dell'installazione (`scripts/install/plugins/des_plugin.py` li distribuisce sotto la config-dir), quel modulo non e' importabile e l'hook fallisce a ogni invocazione. E' una violazione del mandato di portabilita' gia' dichiarato in CLAUDE.md ('DES hook commands: usa la variabile $HOME, mai percorsi hardcoded o .venv') estesa alla forma di invocazione: `-m <modulo>` e' un percorso hardcoded travestito da import. AGGRAVANTE: l'errore e' non-bloccante, quindi l'hook fallisce SILENZIOSAMENTE a ogni comando -- l'utente vede un warning ricorrente e perde del tutto la funzione che l'hook doveva fornire, senza che nulla dichiari che quella protezione non e' attiva. Un hook che non gira e' indistinguibile da un hook che non ha obiezioni." proposed_solution="Invocare l'hook installato per PERCORSO ASSOLUTO risolto rispetto alla config-dir (che va a sua volta risolta dall'ambiente -- vedi la riga `claude-home-assumed-canonical-instead-of-resolved`), non come modulo: `python3 <config-dir>/hooks/no_verify_reminder.py`. Verificare TUTTI gli hook installati, non solo questo: la stessa forma `-m scripts.hooks.X` e' probabilmente replicata, e il difetto e' della CONFIGURAZIONE generata dall'installer, non del singolo hook. Aggiungere una verifica post-installazione che ESEGUA ogni hook configurato una volta e fallisca LOUD se non e' invocabile: oggi l'installazione riporta successo mentre configura hook che non partiranno mai, e il silenzio dell'errore non-bloccante fa il resto."
- [ ] examiner-dispatch-no-write-instruction-collides-with-her-own-session-log-step: paradigm=object-oriented defect="A dispatch that tells the examiner 'do NOT modify any file in the worktree' DIRECTLY CONTRADICTS her own spec's recording step, which appends a row to the charter's Session log. Measured 2026-07-22 on drain-resolves-its-own-actuator: the orchestrator wrote the no-write instruction (to protect uncommitted work), and the examiner resolved the conflict by following her spec and appending anyway. That is the CORRECT precedence -- but only by luck of which instruction she weighted. An examiner who instead obeyed the dispatch would SILENTLY LOSE the session-log record, and the loss is invisible: the ledger record would still exist while the charter -- the only part that travels with the branch, since .nwave/telemetry is gitignored -- would be missing its row. The two instructions are both reasonable in isolation; the defect is that the dispatch template lets them collide at all." proposed_solution="Carve the examiner's OWN ledger and session-log writes out of any no-write instruction, in the dispatch template itself rather than in each orchestrator's prose: the template should emit 'do not modify tracked files EXCEPT your own session-log row on the charter you are examining'. Same class as the --intent-swallowed-by-the-charter-lane defect: a template that permits a self-contradicting envelope. Cheap check that it worked: dispatch an examiner with a no-write instruction and confirm the session-log row still lands."
- [ ] blast-radius-staged-entrypoint-works-where-paths-entrypoint-hangs: paradigm=object-oriented defect="`des blast-radius --repo . --paths <files>` HANGS on high-consumer files -- measured 2026-07-22, killed at 180s on src/des/runtime/interpreter.py, and a sister lane measured the same non-completion at 280s on run_contract_gate.py across two attempts. That non-completion is a standing defect and it has been costing lanes their tier measurement, which then gets GUESSED. But the SAME analysis SUCCEEDS through a different entry point: the `--staged` scope used internally by `des commit-slice`'s tier check completed on the same worktree and returned a full consumer census (refactor.main=331, patch_pyproject.patch_pyproject=23, interpreter.des_spawn=23, interpreter.python_for=17), correctly refusing a declared --tier M as measured L. So the analysis engine is not what hangs; something on the --paths entry path is. WHY THIS MATTERS BEYOND ONE SLICE: the next operator to hit the --paths hang will conclude the TOOL is unusable and guess a tier, when a working measurement route exists two flags away. A guessed tier is not a cautious tier, it is an unmeasured one -- measured here: a deliberately conservative M was still wrong by a full tier." proposed_solution="Diagnose why --paths does not complete where --staged does on the same files (likely candidates: the path-set expansion or a consumer-walk that --staged bounds and --paths does not). Until fixed, make the failure self-explaining per GDP-3/GDP-4: --paths should time-box itself and refuse LOUDLY naming --staged as the working alternative, instead of hanging until an operator kills it. Cheap verification that it worked: run both entry points on src/des/runtime/interpreter.py and confirm either both complete, or --paths refuses within its box naming the alternative."
- [ ] examiner-role-claims-no-source-access-by-construction-but-nothing-enforces-it: paradigm=object-oriented defect="THE DISPATCH ENVELOPE ASSERTS A GUARANTEE THE TOOLSET DOES NOT PROVIDE. Every nw-user-examiner envelope emits 'no source, design, or acceptance-test access BY CONSTRUCTION' (DESIGN_CONTEXT + SKILL_LOADING sections). The role's actual toolset carries Read, Edit and Bash -- nothing prevents opening src/. The guarantee is BY INSTRUCTION, stated as if BY CONSTRUCTION, which is worse than an acknowledged-unenforced constraint because it tells the orchestrator not to check. FIVE MEASURED FAILURES, TWO PROJECTS, ONE ROOT, all 2026-07-22: (1) examiner walked the MAIN repo instead of the dispatch worktree and returned a confident PASS; (2) examiner probed the wrong CLI verb and returned FAIL plus a spurious 'Rust support is broken' claim, refuted by the orchestrator; (3) examiner read src/des/cli/refactor.py FOUR times plus the regression-test file, ran the AT suite, and reported 'Code inspection confirms...' as a PASS -- a code review wearing an examiner label; (4) sister project: examiner recorded PASS on 'Direct database inspection confirms...' against a charter she was dispatched to walk in a BROWSER; (5) sister project: a second instance of the same. Each looked like a different mistake. They are one defect. THE CONTRAST THAT SHOWS THE FIX: nw-software-crafter-reviewer's Bash is scoped to `tsunami ...` only -- which is exactly why, on the port-realization stub evasion, it could only TRACE the finding and had to SAY it had not executed it, instead of claiming a run. That scoping is what 'by construction' is supposed to mean." proposed_solution="Two options, both stated so whoever drains this does not have to re-derive the trade-off. (A) PREFERRED -- restrict the toolset to match the claim: deny Read on src/**, tests/**, and design docs; scope Bash to product surfaces. Then the envelope sentence becomes true. (B) HONEST FALLBACK if (A) is expensive -- stop saying 'by construction' and emit 'by instruction: the orchestrator MUST audit the examiner transcript for source reads before recording her verdict', so the check is explicitly relocated rather than silently assumed. Measured tonight: an explicit voiding clause plus a stated transcript audit ('if you open src/ your verdict is VOID and I will read your transcript') produced the first clean examination of the day -- zero source reads, verified. So (B) demonstrably works, it just puts the cost on every dispatcher forever. ALSO REQUIRED EITHER WAY: give the examiner a SANCTIONED way to construct a negative case. One examiner attempted to RENAME the actuator out of a live worktree to simulate an absent tool -- the right instinct, badly executed, because she had a prohibition and no alternative. She needs a sanctioned throwaway-target mutation path, not a rule she will work around."
- [ ] orchestrator-affordance-catalog-is-never-installed-only-its-refresh-hook-is: paradigm=object-oriented defect="IL CATALOGO CHE I LOOP LEGGONO NON VIENE MAI INSTALLATO -- viene installato solo lo SCRIPT che dovrebbe aggiornarlo. VERIFICATO 2026-07-24 dopo un `python -m nwave_ai.cli install` uscito 0: `~/.claude/scripts/orchestrator_affordance_refresh.py` E' PRESENTE (e' in `DESPlugin.DES_HOOKS`, riga 240 di `scripts/install/plugins/des_plugin.py`), ma la directory `orchestrator-affordance/` con i file `.md` del catalogo e' ASSENTE in TUTTI E TRE i profili (`~/.claude`, `~/.claude-alt`, `~/.claude-alt3`). Nessun plugin la installa: `grep -rn 'orchestrator-affordance' scripts/install/` trova solo il nome dello script hook, mai una copia della directory dati. CONSEGUENZA MISURATA: una correzione alla prosa dei loop committata nel repo NON diventa effettiva sull'ambiente. L'hook `~/.claude/hooks/load_project_context.py` dichiara nel proprio docstring che i mandati dei loop 'vivono nel catalogo INSTALLATO nWave/data/orchestrator-affordance/' -- un percorso che sulla macchina non esiste. Quindi o l'hook risolve il catalogo dal REPO (e allora funziona solo su una macchina di sviluppo, ed e' una violazione del mandato di target-machine-independence) oppure lo cerca dove non c'e' e l'iniezione dei loop e' silenziosamente vuota per chiunque non abbia il repo. In entrambi i casi il comportamento su una macchina utente diverge da quello su una macchina di sviluppo, e nulla lo dichiara. NOTA DI METODO su come e' stato trovato: cercato dopo che una modifica alla prosa dei loop, committata e installata, non risultava propagata. Il difetto NON e' che l'installer fallisce: e' che installa il consumatore senza il dato che consuma, e riporta successo. Stessa famiglia della riga `installer-hook-invoked-as-module-assumes-dev-repo-on-pythonpath` (l'hook e' configurato ma invocato in modo che sulla macchina utente non risolve): entrambe sono 'il pezzo c'e', il collegamento no'." proposed_solution="Installare la directory `nWave/data/orchestrator-affordance/` accanto allo script che la consuma, con lo stesso meccanismo a manifesto usato per gli altri asset -- e far FALLIRE l'installazione se il consumatore viene deployato senza il suo dato (un hook di refresh senza catalogo non ha nulla da aggiornare). Poi verificare la RISOLUZIONE: `load_project_context.py` deve leggere il catalogo dalla config-dir RISOLTA dall'ambiente (vedi la riga `claude-home-assumed-canonical-instead-of-resolved`), mai dal repo, altrimenti la funzione esiste solo per chi sviluppa nWave. Verifica post-installazione dovuta, e vale per ogni coppia consumatore/dato: dopo l'install, ogni asset che un hook installato dichiara di leggere DEVE esistere al percorso da cui l'hook lo cerchera' -- non basta che entrambi 'siano stati installati', devono essere installati NELLA RELAZIONE giusta."
- [ ] carpaccio-slice-table-parsed-by-column-POSITION-not-header-name: paradigm=object-oriented defect="Il parser della Slice Plan legge le celle per OFFSET POSIZIONALE FISSO invece che per nome di colonna, e su una tabella con un numero di colonne diverso attribuisce silenziosamente i valori sbagliati. SEGNALATO da un utente (Tsunami, 2026-07-24) con repro: `_check_value_annotation` (`src/des/cli/carpaccio_format.py:1268-1281`, chiamata da `_check_slice_size`) assume una tabella a 5 colonne e legge `slice_index+3` come annotation e `+4` come justification. Su una tabella a 4 colonne (Slice | Value statement | Annotation | Justification, senza Status) `row.annotation` contiene in realta' il testo della JUSTIFICATION e `row.justification` legge oltre la fine della riga (vuoto). AGGRAVANTE, ed e' la parte che costa: il messaggio risultante -- `CARPACCIO_SLICE_TOO_LARGE: ... carries annotation '<intera prosa della justification>' but records no justification` -- non da' ALCUNA indicazione che il problema vero sia una COLONNA MANCANTE. Si legge come un errore di contenuto dell'autore, che quindi va a riscrivere la prosa invece di aggiungere la colonna. L'utente l'ha riprodotto TRE volte prima di trovare la causa leggendo il sorgente del parser. Nota che rende il difetto sistemico e non accademico: questo repo ha gia' ALMENO DUE convenzioni diverse di numero-colonne in circolazione per la stessa tabella, quindi la forma a 5 colonne non e' garantita da nulla." proposed_solution="Leggere le colonne per NOME DI INTESTAZIONE (match case-insensitive) invece che per posizione: una tabella markdown porta la propria intestazione, e usarla elimina l'intera classe. In alternativa, come minimo immediato: validare ESPLICITAMENTE la forma dell'intestazione prima di leggere qualunque cella, e rifiutare nominando l'intestazione ATTESA e quella TROVATA -- cosi' l'operatore vede 'manca la colonna Status' invece di un errore sul contenuto. Vale la pena cercare gli altri parser di tabella nel repo: se questo legge per posizione, probabilmente non e' l'unico, e la doppia convenzione di colonne gia' esistente garantisce che il difetto si ripresentera'."
- [ ] wave-dispatch-guard-reports-an-infra-crash-as-a-policy-refusal: paradigm=object-oriented defect="Un CRASH del wrapper viene riportato con lo stesso messaggio di un RIFIUTO DI POLICY deliberato, quindi un problema di affidabilita' si nasconde dietro una motivazione plausibile. SEGNALATO da un utente (Tsunami, 2026-07-24): un dispatch di `nw-functional-software-crafter` -- un subagent_type che NON e' affatto nella mappa WAVE_OWNERS, quindi fuori dal vocabolario protetto di quel gate -- e' stato RIFIUTATO con `wave-dispatch guard rejected an off-spine wave-owner dispatch (exit 1): no gate output`. Un retry identico pochi secondi dopo e' passato pulito. DUE DIFETTI DISTINTI: (a) `_wave_owner_for()` dovrebbe restituire None per un subagent_type non-posseduto e ALLOW incondizionatamente senza fare lavoro reale -- quindi 'no gate output' non e' un blocco ponderato, e' un'invocazione crashata o vuota, e va classificata INDETERMINATE-con-retry, non BLOCK; (b) perche' il wrapper produce output ZERO in un caso che dovrebbe corto-circuitare ad ALLOW prima di qualsiasi lavoro? Il fatto che il retry funzioni prova che non era una decisione, era un guasto transiente. Stessa classe gia' filata per i gate uccisi dalle risorse: un fallimento INFRASTRUTTURALE riportato come verdetto di merito insegna all'operatore a dubitare della propria richiesta invece che dello strumento." proposed_solution="Distinguere i due esiti alla radice: un gate che non produce output NON e' un blocco -- e' INDETERMINATE, va detto come tale e va ritentato. Il messaggio deve differenziare 'la policy ti ha rifiutato per <ragione>' da 'il gate non ha potuto pronunciarsi (nessun output, exit <n>) -- ritenta; se persiste e' un difetto del gate'. Poi indagare il corto-circuito: per un subagent_type fuori dalla mappa la risposta ALLOW dovrebbe arrivare senza invocare il wrapper, il che elimina la finestra di crash invece di gestirla."
- [ ] dispatched-agent-cannot-be-woken-by-its-own-background-job: paradigm=object-oriented defect="Un agente dispacciato che lancia un comando in BACKGROUND e poi termina il proprio turno 'in attesa della notifica' resta bloccato per sempre: solo l'ORCHESTRATORE riceve le task-notification dei dispatch Agent/Task, mai un subagente per i propri job di shell in background. SEGNALATO da un utente (Tsunami, 2026-07-24) con caso reale: un agente DISTILL ha lanciato `des verify-red-green` come job backgrounded, ha chiuso il turno dicendo 'aspetto la notifica', e si e' fermato finche' l'orchestratore non se n'e' accorto con un pgrep e non l'ha ripreso a mano. Il costo e' subdolo perche' l'agente NON e' in errore: e' vivo, coerente, e sta aspettando un evento che per costruzione non arrivera'. Dall'esterno e' indistinguibile da un agente lento." proposed_solution="Documentare la convenzione dove vivono le regole di dispatch (la skill che descrive le convenzioni degli agenti dispacciati): un agente dispacciato che ha bisogno del risultato di un comando lungo deve eseguirlo in FOREGROUND dentro il proprio turno, oppure fare polling nello STESSO turno -- mai chiudere il turno 'in attesa di una notifica', che per i propri job in background non arrivera' mai. Il background e' uno strumento dell'ORCHESTRATORE, non del subagente. Vale la pena aggiungerlo anche al prompt di dispatch generato, non solo alla skill: e' esattamente il tipo di regola che si applica a chi non l'ha letta."
- [ ] reviewer-absence-claims-are-the-likeliest-false-negatives: paradigm=object-oriented defect="Le affermazioni di ASSENZA prodotte da un agente revisore ('X manca', 'Y non esiste') sono la classe di claim piu' esposta al falso negativo, e arrivano con l'autorevolezza di un verdetto. SEGNALATO da un utente (Tsunami, 2026-07-24), ricorso DUE volte nella stessa notte: un verdetto NEEDS_REVISION citava come mancanti una stanza `[[test]]` di Cargo.toml e una fixture di censimento -- entrambe verificate PRESENTI con grep/ls diretti. CONFERMA INDIPENDENTE nella nostra stessa sessione, stesso giorno, tre volte: un composition-finder ha dichiarato 'queste righe non compaiono nel pile' (c'erano, oltre il punto in cui aveva smesso di leggere) e 'questa riga e' marcata fixed' (non lo era); un freshness-sweep ha marcato STILL-PRESENT una riga scrivendo esso stesso 'occorrerebbe verificare'; e l'orchestratore ha letto sei campi vuoti di un `find` come sei assenze. LA RADICE COMUNE: una ricerca che si ferma presto -- per budget, per troncamento, per un file grande -- produce un'assenza INDISTINGUIBILE da una verificata, e nulla nel formato del verdetto costringe a dichiarare quale delle due sia." proposed_solution="Trattare una claim di ASSENZA come non-verificata finche' non porta la PROVA della ricerca: il comando eseguito e il suo scope. Nel formato dei verdetti di review, separare meccanicamente `ASSENTE-VERIFICATO` (ho cercato in <scope> con <comando>, non c'e') da `NON-TROVATO-NEL-MIO-SCOPE` (non ho potuto guardare ovunque) -- il secondo non e' un finding, e' un buco di copertura. Regola operativa gia' adottata per i finder e da estendere ai reviewer: dichiarare la copertura come FRAZIONE (esaminati N di M) e mai come aggettivo di fiducia. E per chi riceve un verdetto: una claim di assenza si ri-verifica prima di agirci, perche' costa un comando e ne evita molti."
