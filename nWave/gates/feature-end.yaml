# Gate-contract: feature-end (DDD-7 — consolidated feature-end namespace)
#
# Per `docs/analysis/d4-schema-spec-2026-05-26.md` § 3.1 GateContractFull.
# Schema: `nWave/gates/_schema.yaml#/$defs/GateContractFull`.
# Catalog entry: `nWave/gates/_catalog.yaml` gate_id=feature-end.

gate_id: feature-end
responsibility: |
  Consolidated feature-end command namespace (DDD-7, single entry point).
  Verb `sign` produces a keyless content-seal verdict_hash from a real reviewer
  deep-review verdict (agent + APPROVED/REJECTED + findings) via the
  at_review_signing SSOT -- the hex that feeds `des emit-feature-end
  --record FeatureEndReviewVerdict --verdict-hash`. A non-real verdict
  (no/empty agent, unknown/missing verdict) is refused (anti-theater); no hash
  is minted.
module: des.cli.feature_end
entry_function: main
language_neutral_contract: true

log_events:
  - FeatureEndReviewSigned
  - SignRefused

failure_modes:
  - error_class: SignRefused
    exit_code: 2
    remediation: "Supply a real deep-review verdict (non-empty reviewer agent + APPROVED|REJECTED verdict)."

idempotency: idempotent

host_visibility:
  - claude-code
  - codex
  - opencode
  - cli
