# Gate-contract: record-prose-delivered (D4 Phase 1 — control-plane SSOT mirror)
#
# Per `docs/analysis/d4-schema-spec-2026-05-26.md` § 3.1 GateContractFull.
# Schema: `nWave/gates/_schema.yaml#/$defs/GateContractFull`.
# Catalog entry: `nWave/gates/_catalog.yaml` gate_id=record-prose-delivered.

gate_id: record-prose-delivered
responsibility: |
  Record the completion of a PROSE slice -- one whose deliverable is documentation
  rather than code, and which therefore has no test suite to attest it. Without
  this producer a prose slice carries a Slice-Id commit but no SliceCommitVerified
  record, leaving feature integrity permanently unreconciled.
module: des.cli.record_prose_delivered
entry_function: main
language_neutral_contract: true

cli_args:
  - name: --feature-id
    required: true
    type: string
    help: "The kebab-case feature identifier the prose slice belongs to."
  - name: --slice-id
    required: true
    type: string
    help: "The slice identifier being attested (e.g. slice-05)."
  - name: --verdict
    required: true
    type: string
    help: "The doc-review decision. Only APPROVED is accepted."
  - name: --reviewer-agent-id
    required: true
    type: string
    help: "The reviewer agent that produced the doc-review verdict."
  - name: --doc-review-ref
    required: true
    type: string
    help: "Reference to the doc review backing this attestation."
  - name: --repo-root
    required: false
    type: path
    help: "Project root holding the .nwave/ ledger substrate. Defaults to the detected repo."

log_events:
  - SliceProseDelivered
  - MalformedInput

failure_modes:
  - error_class: MalformedInput
    exit_code: 2
    remediation: "Provide --feature-id, --slice-id, --reviewer-agent-id, --doc-review-ref and --verdict APPROVED; a verdict other than APPROVED is refused (anti-theater)."

idempotency: side-effects-explicit

host_visibility:
  - claude-code
  - codex
  - opencode
  - cli
