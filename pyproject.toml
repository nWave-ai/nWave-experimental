[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "test-project"
version = "0.1.0+atddpure.140da7ceb"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
