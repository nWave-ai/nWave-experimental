"""Phase 3 spike measurement harness — full-repo mutmut empirical timing.

Status: SPIKE (not production code path). Output: spike/full-repo-timing-empirical.md.
Owner: nw-platform-architect.
"""

import sys


def measure() -> dict[str, object]:
    """Run mutmut full-repo + capture wall-clock + result counts."""
    raise NotImplementedError("Phase 3 spike — implement when ready to measure")


if __name__ == "__main__":
    sys.exit(measure())
