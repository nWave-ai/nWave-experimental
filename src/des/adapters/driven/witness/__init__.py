"""Behavioral witness-check adapters (driven).

The Python realization of ``ClauseWitnessPort``: isolated-copy differential
perturbation with reason-discrimination (ADR-001). git-free, working-tree-safe,
the only language-bound steps (AST perturb + test run) live here behind the
port.
"""
