"""Install-time helpers for nWave (slice-04 of fix-oss-environmental-e2e-gate).

Pure-function modules exercised by the `nwave install` doctor + the build-tier
arch test. No I/O at import time; per-function callers pass paths in.
"""
