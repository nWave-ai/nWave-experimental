- [ ] desplugin-god-class: paradigm=object-oriented smell defect="DESPlugin (scripts/install/plugins/des_plugin.py, class at top) is 1792 lines / 37 methods / 16 fields — the highest-confidence god class (score 0.94, conf 0.95)" proposed_solution="Extract cohesive collaborators (hook-command generation, import-rewriting, manifest/verify) into separate classes behind DESPlugin's install/verify facade"
- [ ] subagent-stop-service-god-class: paradigm=object-oriented smell defect="SubagentStopService (src/des/application/subagent_stop_service.py) is 1001 lines / 21 methods, and its __init__ takes 12 constructor params (long_parameter_list score 1.0)" proposed_solution="Split the ATDD-pure vs classic validation responsibilities into collaborators and group the 12 injected dependencies into a cohesive context object"
- [ ] review-wave-triplicate-duplication: paradigm=object-oriented defect="The design/devops/discuss review machinery is copy-pasted 3x: ledger readers (design_review_ledger_reader.py:37, devops:37, discuss:43), verify CLIs (verify_design_review.py:46, verify_devops_review.py:47, verify_discuss_review.py:53), and verdict CLIs (design_review_verdict.py:44, devops:48, discuss)" proposed_solution="Introduce one parameterized ReviewWave abstraction (reader + verify + verdict) keyed by wave name, collapsing the three near-identical copies"
- [ ] coverage-map-verify-cli-runtime-dup: paradigm=object-oriented defect="scripts/cli/verify_coverage_map.py:63-69 duplicates a block from the runtime service src/des/application/coverage_map_verify_service.py:43-49 (dup score ~1.0) — logic forked across the script/runtime boundary" proposed_solution="Have the CLI script import and call coverage_map_verify_service instead of re-implementing the block"
- [ ] test-double-src-tests-duplication: paradigm=object-oriented defect="In-memory/mock adapters exist as byte-similar copies in both src/des and tests: in_memory_filesystem.py:8-14, mocked_time.py:8-14, mocked_hook.py:6-12, mocked_validator.py:6-12 each duplicated into tests/des/adapters/" proposed_solution="Keep one canonical copy of each fake (in src/des/adapters/driven) and import it from the tests, deleting the tests/des/adapters duplicates"
- [ ] at-completion-ledger-slice-ref-clump: paradigm=object-oriented smell defect="(feature_id, slice_id) travel together across 11 method signatures in src/des/adapters/driven/logging/at_completion_ledger.py (data_clump score 0.995); append_gate_event also takes 8 params" proposed_solution="Introduce a SliceRef value object bundling feature_id+slice_id and pass it through the ledger API"
- [ ] orchestrator-step-request-param-object: paradigm=object-oriented smell defect="DESOrchestrator (src/des/application/orchestrator.py) passes (agent, command, project_root, step_file, timeout_thresholds) as a recurring clump; execute_step/execute_step_with_stale_check take 7 params, and step_execution_service.execute_step takes 11" proposed_solution="Introduce a StepExecutionRequest parameter object carrying the step-execution fields and thread it through orchestrator + step_execution_service"
- [ ] prompt-dispatch-long-parameter-lists: paradigm=object-oriented smell defect="Prompt-building signatures are extreme: src/des/cli/dispatch.py::_build_prompt takes 15 params (score 1.0) and prompt_rendering_service.py::render_prompt takes 12" proposed_solution="Introduce a PromptSpec/dispatch-envelope dataclass to replace the flat argument lists"
- [ ] commit-slice-main-long-method: paradigm=object-oriented smell defect="src/des/cli/commit_slice.py::main is a ~491-line procedural function with 48% comment density (comments-as-deodorant score 0.48) — a giant CLI entrypoint" proposed_solution="Extract the argument-parse / precondition-check / gate-run / record phases into named helpers so main reads as a short sequence"
- [ ] git-string-constants-ssot: paradigm=object-oriented defect="Raw git/date literals are scattered across src/des git adapters: 'rev-parse' (19 sites), 'HEAD' (15), '+00:00' UTC-offset (11) all at matching call slots (duplicated_constant score 1.0/0.91)" proposed_solution="Centralize the git-subcommand and UTC-offset literals in a small constants module (or helper) imported by the git adapters"
