# NOTE (2026-07-27, drain owner): SEVEN pending rows below are invisible to the
# des.domain.refactor.pile parser -- `des refactor` never surfaces them as
# drainable, refused, or anything. The count was MEASURED, not eyeballed: run
# parse_pile_report over this file and subtract its item_ids from the `- [ ]`
# lines. Re-measure that way rather than trusting this list; the version of this
# note it replaces claimed 14 and was stale by half.
#
# READ THIS BEFORE REFORMATTING ANY OF THEM. Reformatting to the single-line
# grammar makes a row drainable, and for most of these that is the WRONG move:
# the drain dispatches a mechanical refactor agent, and five of the seven are
# design decisions with many consumers that a mechanical pass would take
# blindly. Verified 2026-07-27, each against the code:
#
# d7-application-imports-cli -- REAL and current (5 application files import
#   from des.cli). DESIGN: hexagonal inversion, same class as the CLASS B
#   residue in [[d1-app-imports-adapters]]. Do NOT feed to the drain.
# production-cli-shells-out-to-git-with-no-port-violating-the-agnosticism-mandate
#   -- REAL (3 direct `subprocess.run(["git", ...])` in commit_slice.py at
#   :1221/:1437/:1469). DESIGN already in flight: see
#   docs/feature/git-port-in-memory-execution + gate-trailer-read-git-port-extract.
# twelve-fixtures-fabricate-the-framework-source-tree-each-in-its-own-way -- root
#   debt REAL (no shared constructor exists), but the `data/` symptom it cites is
#   fixed. Overlaps the drainable [[six-fabricators-of-the-des-source-tree...]];
#   let that one drain first and re-scope this from the outcome.
# the-dune-runner-adapter-is-specified-and-unwritten-eighth-of-seven -- REAL
#   (7 runner adapters, no dune). Feature work, not a refactor; and the row's own
#   priority is making the ABSENCE loud, which is a different change.
# freshness-pulls-the-repo-probe-adapter-at-module-scope-... -- self-declared
#   DESIGN (relocating constants is a layering change with consumers).
# the-real-repo-xdist-group-pins-698-of-the-slowest-tests-to-one-worker -- test
#   -infrastructure performance work, verified by speedup, not a mechanical edit.
# arity-collapse-and-ssot-split-defect-families-must-route-to-DESIGN-... -- not a
#   defect at all: a routing decision prescribing TWO design items. BACKLOG.md has
#   no entry for either; it needs a human routing decision, not a drain.
#
# The other IDs the previous note listed are gone: two were closed to done.md
# 2026-07-27 as superseded by [[d1-app-imports-adapters]], the rest had already
# been reformatted or resolved.
- [ ] desplugin-god-class: paradigm=object-oriented defect="DESPlugin (scripts/install/plugins/des_plugin.py, class at top) is 1792 lines / 37 methods / 16 fields — the highest-confidence god class (score 0.94, conf 0.95)" proposed_solution="Extract cohesive collaborators (hook-command generation, import-rewriting, manifest/verify) into separate classes behind DESPlugin's install/verify facade"
- [ ] subagent-stop-service-god-class: paradigm=object-oriented defect="SubagentStopService (src/des/application/subagent_stop_service.py) is 1001 lines / 21 methods, and its __init__ takes 12 constructor params (long_parameter_list score 1.0)" proposed_solution="Split the ATDD-pure vs classic validation responsibilities into collaborators and group the 12 injected dependencies into a cohesive context object"
- [ ] review-wave-triplicate-duplication: paradigm=object-oriented defect="The design/devops/discuss review machinery is copy-pasted 3x: ledger readers (design_review_ledger_reader.py:37, devops:37, discuss:43), verify CLIs (verify_design_review.py:46, verify_devops_review.py:47, verify_discuss_review.py:53), and verdict CLIs (design_review_verdict.py:44, devops:48, discuss)" proposed_solution="Introduce one parameterized ReviewWave abstraction (reader + verify + verdict) keyed by wave name, collapsing the three near-identical copies"
- [ ] at-completion-ledger-slice-ref-clump: paradigm=object-oriented defect="(feature_id, slice_id) travel together across 11 method signatures in src/des/adapters/driven/logging/at_completion_ledger.py (data_clump score 0.995); append_gate_event also takes 8 params" proposed_solution="Introduce a SliceRef value object bundling feature_id+slice_id and pass it through the ledger API || INVESTIGATION (2026-07-27, shard techdebt-drain-s): re-measured -- 12 signatures carry both feature_id+slice_id (derive_correlation_id, append_gate_event, append_workflow_phase_completed_g_commit, append_review_verdict, append_prose_delivered, append_walking_skeleton_gate_ran, append_slice_commit_bypassed, append_attested_from_bundle, append_slice_commit_indeterminate, read_records, _apply_filters, count_slice_commit_blocked), close to the claimed 11. No existing SliceRef found (grepped repo-wide). Introduced frozen dataclass SliceRef(feature_id, slice_id) in src/des/domain/value_objects.py (alongside the file's existing StepId/FeatureName/AgentName/CommandName value objects), and threaded it through append_gate_event ONLY -- the method the row calls out by name (8 params) -- as a purely ADDITIVE ref=SliceRef(...) kwarg alternative to the existing slice_id positional + feature_id= kwarg (mixing both raises TypeError; every existing call site is untouched/byte-identical). Chose the conservative additive-only subset over a full replace because AtCompletionLedger is referenced by 189 files (33 src + 156 tests) and this file's OWN established convention for prior signature deltas (see append_gate_event docstring: gate/justification/attested_via/regression_test_file/predecessor) is exactly this additive, backward-compatible pattern -- a breaking replace across all 12 signatures is not a safe single-shard change. Remaining 11 signatures still take bare (feature_id, slice_id) pairs -- a follow-up shard can repeat the same additive-ref pattern on each, method by method, verifying callers after each one given the large blast radius. Tests: tests/des/unit/test_at_completion_ledger.py gained 5 new tests (ref= acceptance, byte-identical-to-positional-form, ref+slice_id mix rejection, neither-supplied rejection, SliceRef empty-feature_id validation) -- 20/20 passed in that file, 36/36 (+6 unrelated pre-existing xfail) across the ledger*/*ledger* unit sweep, 0 regressions; mypy src/des/ error count unchanged (164 before/after)."
- [ ] orchestrator-step-request-param-object: paradigm=object-oriented defect="DESOrchestrator (src/des/application/orchestrator.py) passes (agent, command, project_root, step_file, timeout_thresholds) as a recurring clump; execute_step/execute_step_with_stale_check take 7 params, and step_execution_service.execute_step takes 11" proposed_solution="Introduce a StepExecutionRequest parameter object carrying the step-execution fields and thread it through orchestrator + step_execution_service"
- [ ] prompt-dispatch-long-parameter-lists: paradigm=object-oriented defect="Prompt-building signatures are extreme: src/des/cli/dispatch.py::_build_prompt takes 15 params (score 1.0) and prompt_rendering_service.py::render_prompt takes 12" proposed_solution="Introduce a PromptSpec/dispatch-envelope dataclass to replace the flat argument lists"
- [ ] commit-slice-main-long-method: paradigm=object-oriented defect="src/des/cli/commit_slice.py::main is a ~491-line procedural function with 48% comment density (comments-as-deodorant score 0.48) — a giant CLI entrypoint" proposed_solution="Extract the argument-parse / precondition-check / gate-run / record phases into named helpers so main reads as a short sequence"
- [ ] resolve-manifest-state-never-wired: paradigm=object-oriented defect="scripts/cli/resolve_manifest_state.py:80 `resolve_manifest_state()` e' chiamata SOLO da codice di test (tests/scripts/cli/component_manifest/steps/composition.py:238), mai dalla produzione. Il commento alla riga 4 la lega a F-DESIGN-COMPONENT-MANIFEST slice-03: sembra scritta come parte di un compito di design e mai cablata in un gate o comando vivo. Trovata da una scansione read-only via detector never_wired e VERIFICATA per lettura diretta dei chiamanti (il detector ha falsi positivi noti su entry-point e classi registrate via registry, quindi la verifica non e' opzionale)" proposed_solution="O cablarla in un gate/CLI che ne ha bisogno, o rimuoverla se il compito di design e' stantio. Decidere quale: un modulo che esiste solo per i propri test e' codice che qualcuno manterra' credendolo vivo || INVESTIGATION 2026-07-27: re-verified, found NEW evidence sharpening the decision beyond wire-vs-remove. The module's own docstring names its two intended consumers -- fix-robustness-pbt-density-gate and fix-distill-human-signoff. grep confirms scripts/cli/check_robustness_density.py does NOT call resolve_manifest_state(); it reads component-manifest.yaml fields directly via its own module-level _EMPTY_RATIONALE_FIELD/_UNBOUNDED_DOMAINS_FIELD constants (lines 133-134), i.e. the gate independently re-implements a chunk of the same four-state classification this resolver exists to centralize. Deciding whether that inline logic is a faithful subset (safe to delegate) or has quietly diverged requires reading both classification paths side by side -- a design decision with real gate-behavior risk, not a mechanical rename/delete. Left PENDING for /nw-design: (a) wire check_robustness_density.py (and fix-distill-human-signoff, not yet checked) onto resolve_manifest_state and delete the duplicate inline logic, or (b) delete resolve_manifest_state.py as a design-time-only artifact superseded by each gate's own inline check before it shipped."
- [ ] write-marketplace-json-never-invoked-from-build-pipeline: paradigm=object-oriented defect="build_plugin.py exposes write_marketplace_json but its own build() pipeline docstring (line ~723) lists the full step sequence -- validate, read_version, copy_agents, copy_commands, copy_skills, copy_des_module, copy_templates, generate_hooks_json, generate_hook_wrapper, generate_metadata, write_metadata -- and write_marketplace_json is absent from it, confirming it is never invoked from the documented build path. Only reached by tests/build/acceptance/plugin/steps/test_release_pipeline_steps.py, which exercises it directly rather than via the real pipeline -- the same never-wired-from-production shape as the resolve-manifest-state row this was originally (mis)filed under." proposed_solution="Checked 2026-07-27 exactly per this row own proposed direction. grepped scripts/build_dist.py, docs/product/roadmap.md and BACKLOG.md for self-hosted/marketplace: zero hits in all three, no artifact in the repo references self-hosted distribution as planned or active work -- leans toward abandoned/safe-to-delete, but NOT deleted here: real deliberate test coverage exists (not incidental), so removing it is a product call (is self-hosted distribution truly off the roadmap, or just not yet scheduled) rather than a code-hygiene one to make unilaterally under a techdebt-drain mandate. || INVESTIGATION 2026-07-27: this note was previously misfiled as a second, unrelated investigation block under the resolve-manifest-state-never-wired row -- split out into its own row here so it is visible to the pile parser at all (the multi-line block format is invisible to des.domain.refactor.pile). Left PENDING: Ale/product decision needed on whether self-hosted distribution via marketplace.json ships; if not, delete write_marketplace_json and its dedicated test coverage."
- [ ] subagent-stop-service-high-dependency-orchestrator: paradigm=object-oriented defect="SubagentStopService(SubagentStopPort) in src/des/application/subagent_stop_service.py:250 orchestrates 11 injected ports/services across 21 methods (1,250 LOC). Primary method validate(context, hook_id) sequences ~15-20 gate validations (discuss → distill → wave-dispatch → classic-vs-atdd_pure → ...). High dependency count + sequential gate orchestration concentrated in one class. Likely acceptable per DDD if the SubagentStopContext → HookDecision transformation is genuinely a single use-case, but worth monitoring for continued growth and potential sub-aggregation as new gates are added" proposed_solution="Assess whether the validate() method can be decomposed into a pipeline or strategy pattern (each gate as a pluggable validator) without violating the single-entry-point contract for the SubagentStopPort driver port. If gates are independent, consider a ChainOfResponsibility or StrategyFactory pattern; if gates must run in sequence with shared mutable state, document the sequencing invariant and continue monitoring size growth at each iteration"
- [ ] refusal-messages-constant-while-context-is-in-hand-audit: paradigm=object-oriented defect="AUDIT SISTEMATICO, non un singolo difetto. Lo stesso schema e' emerso TRE volte in un giorno da fonti indipendenti: un punto di rifiuto emette un messaggio COSTANTE mentre il dato che lo renderebbe utile e' gia' nello scope della funzione che lo scrive. I tre confermati: (1) `_ArchVerdict` porta gia' `failed_node_ids` (run_contract_gate.py:585) e il rifiuto a :3389-3396 emette una stringa fissa senza usarli, quindi l'operatore legge 'un invariante architetturale e' FALLITO' e deve scavare per scoprire QUALE e che non e' suo; (2) il rifiuto carpaccio per slice fuori ordine non ramifica fra i due casi possibili -- predecessore non committato contro numerazione sbagliata -- pur avendo entrambe le informazioni in mano, e i due casi portano ad azioni OPPOSTE; (3) il rifiuto E2 dell'attestazione comportamentale non nomina l'interprete che ha usato ne' da quale gradino l'ha risolto, pur avendolo appena risolto, e senza quel nome l'intera classe di difetti interprete-sbagliato e' invisibile. MISURA della popolazione candidata (AST su src/des, chiavi error/reason/message/how/why assegnate a una stringa costante >40 char): **98 punti in 20 file**, con la densita' massima proprio nei due file che hanno prodotto tutti e tre i casi confermati -- 22 in `commit_slice.py` e 22 in `run_contract_gate.py`. ATTENZIONE, e va detto: 98 sono CANDIDATI, non difetti. Un messaggio costante e' legittimo quando non c'e' contesto da aggiungere. Il valore di questa riga e' che i tre confermati vengono dai due file piu' densi, il che suggerisce che siano campioni di una popolazione e non incidenti" proposed_solution="Passare in rassegna i 98 punti chiedendo a ciascuno UNA domanda: il contesto che renderebbe questo messaggio azionabile e' gia' nello scope di questa funzione? Se si', e' un difetto e va interpolato; se no, il messaggio costante e' corretto e va lasciato. Partire dai due file piu' densi, dove il rendimento e' massimo. Poi rendere la classe difficile da ricreare: un gate che possiede un oggetto-verdetto ricco (come `_ArchVerdict`) e ne emette una rappresentazione costante e' un odore riconoscibile a occhio in review, e vale la pena scriverlo nella checklist di review dei gate accanto ai GDP. Nota di metodo: NON aprire 98 bugfix -- e' un audit, il cui esito e' un elenco corto di punti veri piu' la conferma che gli altri stanno bene cosi'"
- [ ] sibling-asymmetry-drain-one-vs-drain-batch-paradigm-validation: paradigm=object-oriented defect="`drain_one` and `drain_batch` perform the same high-level task but enforce different validation standards: 1. **Paradigm validation**: Line 181-183, `drain_one` validates item paradigm acceptance via `select_paradigm_lens()` and refuses on rejection. Line 253-275, `drain_batch` has ZERO paradigm checks before processing items — it skips directly to probe failure check (line 277) and concurrent dispatch. Items with invalid paradigms pass through silently. 2. **Prompt template**: Line 164, `drain_one` signature accepts `prompt_template_path: Path | None`. Line 204 passes it to `_dispatch_agent()`. Line 259, `drain_batch` has NO template parameter. Line 304 hardcodes `None` when dispatching — custom templates are always ignored in batch mode. EVIDENCE: src/des/application/refactor_drain_service.py:158-284. CITED RULE: No shared standard for sibling code paths; paradigm validation and template handling diverge without a documented reason (Principle 1 — reviewer mindset, assume nothing, both paths should follow the same gating)." proposed_solution="Extract paradigm validation into a helper applied to ALL items before any processing. Add `prompt_template_path` parameter to `drain_batch` or document why batch mode cannot support custom templates."
- [ ] D1-sibling-des-plugin-duplication: paradigm=object-oriented defect="Codex and Copilot DES plugins (codex_des_plugin.py, copilot_des_plugin.py) duplicate hook-config building & manifest handling logic instead of extending a shared base. codex_des_plugin.py:55-80 (_build_hook_entry), copilot_des_plugin.py:67-78 (_build_hook_config) both implement per-CLI config-dir resolution + hook-entry building. Violates SSOT/DRY (CLAUDE.md Architectural Constraints, hexagonal abstraction). Siblings differ in matcher, event key, nested-payload shape, but all paths write manifest files and must maintain parity through future changes." proposed_solution="Extract shared hook-registry abstraction (base class or mixin) with per-CLI subclass overrides for event key + nested-payload shape only. Codex: flat {type, command, matcher}; Copilot: double-nested {hooks: [{type, bash, timeoutSec}]}. Config-dir resolution + manifest lifecycle centralized; CLI-specific shape encapsulated."
- [ ] D8-commands-plugin-size-asymmetry: paradigm=object-oriented defect="commands_plugin.py (130 lines) vs opencode_commands_plugin.py (305 lines) — 2.3x asymmetry. Similar to agents plugins (148 vs 526 vs 280). Suggests either opencode variant duplicates base logic, or base plugin is under-featured. SSOT/DRY principle violated: multiple paths to implement command registration, higher maintenance cost when the interface changes." proposed_solution="Audit both files to identify duplicated install/verify logic. Extract common behavior into base class; reserve subclass overrides for CLI-specific config paths (e.g., ~/.codex/commands.yaml vs ~/.claude/commands/). If base is intentionally minimal, document the design choice and enumerate expected subclass responsibilities in a docstring."
- [ ] d1-app-imports-adapters: paradigm=object-oriented defect="Application layer imports directly from adapters. RE-MEASURED 2026-07-27 (shard techdebt-drain-t): actual current count is 12 files / 22 instances (down from the originally filed 13 files/35+ -- prior shard techdebt-drain-r already removed orchestrator.py's AuditEvent instances, and this shard fixed 2 more). Violates hexagonal layering (CLAUDE.md Architecture section, GDP-1): application must import ONLY from domain + ports, not adapters." proposed_solution="Extract abstract types to new port interfaces or value objects in domain; invert deps via ports (driven_ports). GDP-5: cost on system, not operator. FIXED this shard (2 safest mechanical instances, AuditEvent value-object-move pattern): (1) moved ManifestAssetAbsent/ManifestAssetUndecodable exception classes from des.adapters.driven.skill_corpus_reader to des.domain.skill_normative_clause (pure value types, adapter re-exports for compat) -- skill_normative_gate_service.py's module-level adapter import is gone; (2) extended AtCompletionLedgerPort with append_walking_skeleton_gate_ran/append_walking_skeleton_tier_verified abstract methods mirroring the concrete adapter verbatim, then swapped walking_skeleton_feature_end_gate.py's TYPE_CHECKING-only AtCompletionLedger import for the port type -- file dropped out of the violator list. Verified: mypy clean, ruff clean, 22/22 skill-normative+walking-skeleton acceptance/bug tests pass, 293/293 tests/des/unit/application pass, 20/20 ledger unit tests pass. || INVESTIGATION 2026-07-27: remaining 12 files/22 instances split into two classes, neither mechanical. CLASS A -- legitimate composition-root factories (construct concrete adapters ONLY inside a factory method, arguably not a violation): orchestrator.py:97-102/191/200 (create_with_defaults), log_persistence.py:99 (config-driven adapter selection). CLASS B -- genuine behavioral coupling needing real port design (concrete adapter CONSTRUCTED or invoked at runtime, not just type-hinted): feature_end_cycle_service.py:70-71 (DESConfig + AtCompletionLedger() constructed at line 611), deliver_loop_projection.py:45 (AtCompletionLedger() constructed at line 188), blast_radius_measurement.py:35-37 (CodeFactChain/DESConfig/git_text), auto_marking_service.py:75 (DESConfig() constructed), update_check_service.py:29, pending_update_service.py:37 (DESConfig), audit_bridge.py:11/22 (JsonlAuditLogWriter/SystemTimeProvider), slice_at_completeness.py:51/294 (git_text/seed_runner_registry), refactor_drain_service.py:784 (ThreadingMergeLock), skill_normative_gate_service.py:40 (SkillCorpusReader TYPE_CHECKING -- no port exists yet for its read_manifest/read_asset surface; would need a new SkillCorpusReaderPort, left for a follow-up shard). Each CLASS B site needs its own reasoned port/factory-injection design, not a batch mechanical substitution -- follow-up shards should pick ONE file at a time, verifying callers after each."
- [ ] d3-feature-end-cycle-monolith: paradigm=object-oriented defect="feature_end_cycle_service.py is 2,067 lines and violates Single Responsibility. Combines orchestration (running gate CLIs), verification (coverage-map, signing), ledger emission, parsing (XML, JSON), and error handling in one class. One change reason would suffice; this has ≥5." proposed_solution="Extract responsibilities: (a) gate-invocation orchestration → GateRunnerService, (b) coverage-map verification → (already exists, but integration is mixed), (c) ledger emission → LedgerEmitService, (d) result parsing → ResultParserService. Split into 2-3 focused services, each ≤600 lines."
- [ ] d4-json-parsing-scattered: paradigm=object-oriented defect="JSON/XML parsing logic scattered across application services: feature_end_cycle_service.py (4 json.loads, 1 ElementTree.parse), subagent_stop_service.py (2), flavor_dispatcher.py, config_loader.py, coverage_map_verify_service.py, refactor_drain_service.py. No centralized parser service. Each caller handles errors locally (try-except → silent skip). Violates DRY (Parsing rules duplicated) and Error-surface standard (inconsistent error handling)." proposed_solution="Extract JSON/XML parser service(s) into domain or application layer. Centralize error handling with structured exceptions (e.g., JsonParseError, XmlParseError). Callers delegate, not parse inline. Reference: GDP-4 (HOW routes to producing system tool)."
- [ ] d5-missing-protocol-docs: paradigm=object-oriented defect="Several service classes lack explicit preconditions/postconditions or invariants. Example: HousekeepingService.run_housekeeping silently returns early if config.nwave_dir does not exist (line 81-82) — no exception, no log. Caller has no visibility into whether housekeeping actually ran. Contracts should be explicit in docstrings or Result types." proposed_solution="Add @precondition and @postcondition docstrings or use Result<T, E> pattern (already defined in domain/result.py). Fail-open services must document explicitly. Example: 'Returns None; runs with fail-isolated semantics — failures logged at DEBUG level' (currently undocumented)."
- [ ] d6-exception-over-result: paradigm=object-oriented defect="domain/result.py defines Result[T, E] type for explicit error handling (Success/Failure), but application layer NEVER uses it (0 Result[ occurrences in /src/des/application/). All services rely on exceptions instead. Result type is a wasted design artifact or a design intention not enforced. Violates explicit error handling pattern (domain intent)." proposed_solution="Either (a) enforce Result types in application layer by updating services to return Result, or (b) remove Result type if exception-based model is the chosen design. If (a): start with high-value services (orchestrator, feature_end_cycle, validator). If (b): document in CLAUDE.md/ADRs that application uses exceptions, not Result types."
- [ ] d7-application-imports-cli: paradigm=object-oriented defect="BACKWARDS dependency: application layer imports from CLI (driver adapters). Violates hexagonal inversion-of-control (CLAUDE.md Architecture: CLI should invoke application, never vice-versa). Examples: feature_end_cycle_service.py (5 imports from des.cli.*, including _CollectionError, examine_ledger_path, RECIPE_RELPATH, _undelivered_slice_plan_slices, _collect_node_ids), deliver_loop_projection.py (3: carpaccio_slice_gate, commit_slice, carpaccio_format), feature_end_batch_service.py, subagent_stop_service.py, atdd_pure_prompt_validator.py. This creates tight coupling and makes application untestable without CLI." proposed_solution="Invert the dependency: (a) Extract CLI-invoked logic to domain value-objects or application services, (b) Move CLI-specific constants (RECIPE_RELPATH, etc.) to ports or domain, (c) Re-route application calls via dependency injection (ports). feature_end_cycle_service should NEVER import from des.cli.*. GDP-6 (Reliability: NO silent-wrong): test should fail loudly if CLI import remains — add a static check to prevent." issued_rule="Hexagonal boundary invariant (CLAUDE.md). Critical blocking issue."
- [ ] d8-des-config-everywhere: paradigm=object-oriented defect="DESConfig imported in 7 application files (orchestrator, blast_radius, auto_marking, feature_end_cycle, pending_update, update_check, skill_tracking). Config object is tightly coupled to service logic. When config structure changes, multiple services break. Violates dependency injection principle — services should receive only needed config fields, not the entire DESConfig object." proposed_solution="Extract config-read logic to dedicated ConfigPort adapters. Services receive only required values (dependency injection), not DESConfig. Pattern: instead of 'service(config: DESConfig)', use 'service(audit_retention_days: int, signal_staleness: int)'."
- [ ] d9-application-adapters-coupling: paradigm=object-oriented defect="application services instantiate adapter classes directly (RealFileSystem, SystemTimeProvider, JsonlAuditLogWriter, DESConfig) in factory methods and __init__. This breaks testability: unit tests cannot inject test doubles without modifying service code. Example: orchestrator.py:89-99 (create_with_defaults factory creates RealFileSystem and SystemTimeProvider inline)." proposed_solution="Dependency injection: pass all adapters as constructor params. Move factory creation to a separate factory module or config. Services take abstract ports, never concrete adapters."
- [ ] d10-feature-end-cycle-complexity: paradigm=object-oriented defect="feature_end_cycle_service.py has 95 if/elif statements across 2067 lines (~1 per 22 lines). Cyclomatic complexity est. >20 (complex range). Service should have max CC ~10 per method. High complexity makes code hard to test, maintain, and reason about. Violates Object Calisthenics Rule 1 (methods ≤10 lines) and maintainability principle." proposed_solution="Extract conditional logic to domain decision objects or domain rule engines. Example: _parse_pytest_output contains multiple nested if/elif → extract to TestResultParser domain service. Gate coverage decisions → extract to CoverageGateDecision domain value-object."
- [ ] port-interface-imports-concrete-adapter-hexagonal-inversion: paradigm=object-oriented defect="`src/des/ports/test_runner_port.py` (il layer PORTS, cioe' le interfacce) importa da `des.adapters.driven.runner.runner_registry` -- righe 128-130, 156-158, 187-189 importano `GLOBAL_REGISTRY` e `seed_runner_registry` e li usano (es. riga 133 `GLOBAL_REGISTRY.lookup(self.name)`). E' un'inversione esagonale diretta: un port deve dipendere SOLO da domain/ports, mai da un adapter concreto -- la regola e' che gli adapter dipendono dai port, non viceversa (CLAUDE.md 'Architectural Constraints', layering domain<-application<-ports<-adapters). Gli import sono lazy (dentro i metodi, non a livello-modulo) il che oggi evita un ciclo d'import a runtime, ma la dipendenza-di-design resta: il port conosce e chiama un registry di adapter concreto. Distinto dalla riga 12 di questo pile (quella riguarda il NAMING `test_`-prefix del file, non la direzione della dipendenza). Confermato via grep 2026-07-24 dal finder domain/ports-purity." proposed_solution="Invertire la dipendenza: il port deve ricevere il run-facet / registry come CAPABILITY INIETTATA (parametro del costruttore o del metodo), risolta al composition-root dall'adapter, non importata ad-hoc dentro il port. In alternativa spostare la logica-che-consulta-il-registry nell'adapter che realizza il port, lasciando il port una pura interfaccia. Correlato al ceppo composition-root gia' filato in defects.md (repo-root-resolution-lacks-single-composition-root): stessa forma -- una risorsa (qui il registry) risolta ad-hoc nei consumatori invece che iniettata una volta."
- [ ] hook-diagnostic-logging-swallows-its-own-failure-without-a-trace: paradigm=object-oriented defect="I cinque siti di logging diagnostico degli hook ingoiano QUALSIASI fallimento di scrittura senza lasciare traccia rilevabile da nessuna parte: `src/des/adapters/drivers/hooks/hook_protocol.py:214-215` (`log_hook_invoked`) e `:271-272` (`log_hook_completed`), `pre_tool_use_handler.py:286-287` (`_log_wave_entry_clear_failed`) e `:341-342` (`emit_commit_attribution_mutation`), `subagent_stop_handler.py:143-144` (`_log_transcript_audit`) -- tutti `except Exception: pass` con il commento 'Diagnostic logging must never break the hook'. RUOLO STABILITO: produzione (verificato leggendo `hook_protocol.py:200-215`, la funzione e' invocata dal protocollo hook, non da un self-probe). IL FAIL-SAFE E' GIUSTO E NON VA RIMOSSO: un hook che si rompe perche' non riesce a scrivere una riga di log sarebbe molto peggio. IL DIFETTO E' CHE IL FALLIMENTO NON LASCIA TRACCIA: se `audit_writer.log_event` fallisce -- disco pieno, permessi, o (il caso realistico) il path del ledger risolto contro il repo-root SBAGLIATO -- l'evento non viene scritto e NESSUNO lo sa mai. Il ledger diventa silenziosamente incompleto, e un ledger con buchi e' INDISTINGUIBILE da un ledger completo. Questo conta piu' di un normale log perche' i ledger di questa spine NON sono diagnostica accessoria: sono la prova su cui si decide (attestazioni, scorecard, verdetti di gate). SI COMPONE con la risoluzione-sparsa del repo-root gia' filata in defects.md (`repo-root-resolution-lacks-single-composition-root`): se il root si risolve male, l'audit writer fallisce o scrive nell'albero sbagliato, e il fallimento viene ingoiato -- quindi la telemetria appare vuota nel posto giusto e nessun segnale distingue 'non e' successo nulla' da 'non siamo riusciti a registrarlo'." proposed_solution="NON rimuovere il fail-safe. Renderlo OSSERVABILE: al fallimento incrementare un contatore persistente o scrivere un marker minimale su un percorso indipendente da quello fallito (stderr strutturato, o un file sentinella accanto al ledger), cosi' che 'telemetria assente' sia distinguibile da 'nessun evento'. Il consumatore del ledger deve poter leggere quel segnale e degradare a INDETERMINATE invece di leggere l'assenza come assenza-di-fatti. Regola generale da applicare agli altri fail-safe della spine: un fail-safe puo' legittimamente non propagare l'errore, ma non puo' legittimamente non lasciarne traccia -- altrimenti non e' fail-safe, e' fail-invisible."
- [ ] failure-mode-coverage-decided-by-bare-substring-match-on-test-names: paradigm=object-oriented defect="`src/des/testarch/adapters/python_ast.py:288` decide la COPERTURA di un failure-mode con un substring match nudo sul nome del test: `if not any(mode_id in test_name for test_name in test_names)`. RUOLO STABILITO: produzione (`PythonAstAdapter` e' la realizzazione concreta della porta, consumata dalle regole testarch nei gate AT reali). La scelta e' DICHIARATA nel docstring (riga 279: 'a mode is covered iff its id is a substring of some test name ... no judgment'), ma dichiarare un'euristica non ne stabilisce l'AFFIDABILITA', e qui sbaglia in DUE direzioni opposte. (a) FALSO POSITIVO DI NON-COPERTURA, rumoroso ma innocuo: il match e' case-sensitive e senza normalizzazione, quindi un mode `NPE` risulta coperto da `test_NPEHandling` e NON coperto da `test_npe_handling` o `test_null_pointer_exception_handling` -- cioe' il verdetto dipende dalla convenzione di naming, non dalla copertura. (b) FALSO NEGATIVO SILENZIOSO, che e' il difetto vero: un mode-id CORTO matcha per pura coincidenza. Un id come `IO` e' 'coperto' da qualunque test il cui nome contenga quelle due lettere in sequenza (`test_VALIDATION`, `test_ACTION`, `test_RATIO`), e un id come `AT` da quasi ogni nome. Il gate riporta quindi COPERTO un failure-mode che nessun test esercita, ed e' indistinguibile da una copertura reale. Violazione GDP-6 (no silent-wrong: qui il silent-wrong e' sul ramo PERMISSIVO, quello che non produce mai un incidente che lo smaschera) e GDP-7 (execution-observing: la copertura e' asserita dal NOME, mai osservata dall'esecuzione). Stessa famiglia gia' catalogata del conteggio-per-nome-nudo del blast-radius: l'identita' di un'unita' non e' la sua etichetta, e una sottostringa lo e' ancora meno." proposed_solution="Non decidere la copertura su una sottostringa. In ordine di forza: (1) OSSERVARE l'esecuzione -- un mode e' coperto se un test che lo esercita e' effettivamente girato (il pavimento execution-observing di GDP-7), non se il suo id compare in un nome; (2) se resta una mappatura per-nome, renderla ESPLICITA e dichiarata dal test (un marker/decoratore che NOMINA il mode coperto, es. `@covers_failure_mode('NPE')`), cosi' il legame e' un fatto dichiarato invece che una coincidenza lessicale -- e come effetto collaterale sparisce la dipendenza dalla convenzione di naming; (3) come minimo immediato, richiedere un match su TOKEN delimitati e normalizzati (mai una sottostringa libera) e RIFIUTARE come INDETERMINATE i mode-id piu' corti di una soglia, perche' sotto quella lunghezza il match e' rumore. AT obbligatorie sulle due direzioni: un mode `IO` NON deve risultare coperto da `test_VALIDATION`, e un mode `NPE` non deve cambiare verdetto se il test si chiama `test_npe_handling` invece di `test_NPEHandling`."
- [ ] production-cli-shells-out-to-git-with-no-port-violating-the-agnosticism-mandate: paradigm=object-oriented defect="Il codice di PRODUZIONE invoca `git` come dipendenza DURA, senza porta, in violazione diretta del vincolo standing di CLAUDE.md ('Generality & target-machine agnosticism': l'unica dipendenza di runtime e' Python; `git` NON e' una dipendenza; tutto cio' che e' tool-bound deve stare dietro una porta driven che degrada LOUD, mai silent-pass). MISURATO 2026-07-24 su `src/des/cli/commit_slice.py`: 16 siti di spawn complessivi, di cui almeno tre `subprocess.run([..., \"git\", ...])` diretti alle righe 1200, 1416 e 1448, piu' invocazioni pytest intorno alle righe 269-273 e 999-1018. Non e' un adapter driven che il consumatore possa sostituire: e' una chiamata diretta dentro un modulo CLI del layer applicativo. CONSEGUENZA 1, conformita': su una macchina target senza `git` questi comandi non degradano INDETERMINATE, falliscono in modo non governato -- esattamente cio' che il mandato vieta. CONSEGUENZA 2, misurata e costosa: uno spawn git costa ~305 ms su questo box (10 `git status` consecutivi = 3055 ms). Un test che esercita una di queste CLI paga 15-30 spawn, cioe' 4.5-9 s, e infatti i test che le esercitano misurano 4-10 s ciascuno contro gli 0.07 s/test del tier in-process: un rapporto 60-140x. La violazione architetturale E il collo di bottiglia della suite sono LO STESSO difetto visto da due lati -- la conformita' al mandato e la velocita' non sono in tensione, sono la stessa cosa. NOTA DI STATO: una wave DESIGN e' in corso su questo (feature `git-port-in-memory-execution`) e deve stabilire se la soluzione e' un EXTEND dello strato adapter git gia' esistente (`src/des/adapters/driven/git/`: git_subprocess, git_history_probe, git_commit_verifier, git_mutate, git_track_probe) oppure una porta nuova. Questa riga resta aperta finche' il debito non e' effettivamente rimosso dal codice: un design non chiude un debito." proposed_solution="Instradare ogni accesso a git attraverso una porta driven con due realizzazioni -- un adapter subprocess reale e un doppio in-memory/sintetico per i test -- iniettata come capability, mai risolta ad-hoc nel consumatore (CLAUDE.md impone capability injection e vieta la risoluzione sparsa). Il primo lavoro NON e' scrivere la porta ma CENSIRE cosa le CLI chiedono davvero a git (status, rev-parse, log, diff, commit, operazioni su worktree): una porta che rispecchia l'intera CLI di git e' un wrapper, non un confine. Dichiarare esplicitamente cosa la porta NON copre -- i test la cui autenticita' dipende da git vero (race a livello di ref, ciclo di vita dei worktree, fixture che devono essere repository reali) restano su git reale, e il design deve nominarli invece di lasciarli scoprire a chi convertira'. Verificare infine il gemello: quali ALTRI moduli di produzione shellano git o altri CLI esterni senza porta -- questa riga nomina commit_slice.py perche' e' dove la misura e' stata fatta, non perche' sia l'unico."
- [ ] reviewer-agents-have-no-shared-locus-so-every-universal-rule-is-written-22-times: paradigm=object-oriented defect="I 22 agent revisori NON hanno un punto comune dove ancorare una regola che vale per tutti, quindi ogni regola universale va scritta 22 volte e da quel momento puo' divergere in 22 direzioni. MISURATO 2026-07-24, e la misura e' il reperto: esistono skill di review che SEMBRANO condivise -- `nw-review-workflow`, `nw-review-output-format` -- ma il conteggio per FILE (non per occorrenza, che e' come si sbaglia questa misura) dice che le caricano rispettivamente 2 e 1 reviewer su 22; almeno una delle due, 3 su 22. Il punto comune non e' assente per svista: e' APPARENTE, cioe' esiste con un nome che promette condivisione e una copertura del 14%, che e' peggio dell'assenza perche' invita a scriverci dentro credendo di coprire tutti. Nemmeno la struttura dei file e' uniforme: `## Critical Rules` c'e' in 16/22 e l'unica sezione presente in 22/22 e' `## Constraints`. CONSEGUENZA CONCRETA, appena pagata: aggiungere la regola sulle claim-di-assenza ha richiesto un'inserzione programmatica in 22 file, e la prossima regola universale paghera' lo stesso prezzo -- piu' il rischio che qualcuno la aggiunga a 18 file su 22 e dichiari la cosa fatta, che e' la copertura-parziale-presentata-come-completa contro cui esiste gia' una regola standing. Stessa famiglia dei doppioni adapter src/tests: contenuto identico duplicato senza un locus, con la deriva come esito garantito e solo il tempo come variabile." proposed_solution="Dare ai reviewer un locus condiviso vero e MISURARE l'adozione invece di dichiararla. Due strade, da valutare in DESIGN: (a) una skill di review comune che TUTTI i 22 caricano -- e allora `nw-review-workflow` va estesa e adottata, non lasciata al 9%; (b) una sezione strutturale obbligatoria negli agent revisori, garantita da un gate che verifica che ogni file `*reviewer*.md` la contenga. In entrambi i casi serve il controllo meccanico, altrimenti si torna al punto-comune-apparente: un gate che confronta l'elenco dei reviewer con l'elenco di chi ha il locus e RIFIUTA quando la frazione non e' 22/22 -- lo stesso principio della parita' catalogo/registry gia' adottata altrove. Prerequisito onesto: prima di scegliere, censire quali regole sono davvero universali e quali sono per-dominio, perche' un locus comune riempito di regole che valgono per meta' dei reviewer e' rumore che tutti imparano a saltare."
- [ ] twelve-fixtures-fabricate-the-framework-source-tree-each-in-its-own-way: paradigm=object-oriented defect="Almeno DODICI file di test costruiscono ciascuno per conto proprio un albero-sorgente nWave sintetico, e nessuno di essi ha un'idea condivisa di cosa quell'albero debba contenere -- quindi ogni requisito nuovo dell'installer va aggiunto dodici volte, e chi lo aggiunge non ha modo di sapere che i punti sono dodici. MISURATO 2026-07-24, e la misura nasce da un caso reale: un fix ha reso `nWave/data/` obbligatorio per l'installazione (contratto fail-loud: sorgente assente -> rifiuto esplicito). Sei AT sono diventati rossi perche' il loro albero sintetico non ha mai contenuto `data/`. Ma la ricerca `grep -rln 'framework_source' tests/ --include=*.py` (scope: tutto tests/) trova DODICI costruttori distinti, e il controllo `grep -q '\"data\"'` su ognuno di essi restituisce NO per tutti: gli altri sei non falliscono soltanto perche' non attraversano il percorso che consuma il nuovo requisito. Il debito e' latente su tutti e si manifestera' al prossimo requisito. FIRMA DEL PROBLEMA, visibile nel codice: il docstring di `tests/installer/acceptance/installer_orphan_sweep/steps/composition.py:250` dichiara 'Satisfies DESPlugin.validate_prerequisites (scripts + shims + templates)' -- un elenco che era vero quando e' stato scritto e che nessun meccanismo tiene allineato al contratto reale del plugin. Ogni costruttore ha la propria versione di quell'elenco, e ognuna invecchia per conto suo. E' la stessa classe dei test-double duplicati fra src/ e tests/ gia' catalogata e chiusa: contenuto identico replicato senza un locus, con la deriva come esito garantito e solo il tempo come variabile." proposed_solution="UN costruttore condiviso dell'albero-sorgente sintetico, che sappia cosa l'installer pretende e sia l'unico posto da aggiornare quando il contratto cambia -- i dodici chiamanti lo invocano invece di rifabbricare. Il rimedio SBAGLIATO e' evidente e allettante: aggiungere `data/` in dodici punti, che ripara i sei rossi di oggi e lascia intatta la ragione per cui domani saranno di nuovo dodici. Il costruttore condiviso dovrebbe derivare cio' che semina DALLE DICHIARAZIONI del plugin (`DES_SCRIPTS`, `DES_SHIMS`, `DES_TEMPLATES` sono gia' costanti lette dalle fixture) invece di ripeterle, cosi' un requisito aggiunto al plugin si propaga da solo. Verificare inoltre se esista gia' un embrione di questo costruttore -- con dodici copie e' probabile che almeno due siano stati copiati l'uno dall'altro, e la piu' completa e' il punto di partenza naturale."
- [ ] three-skills-give-opposite-orders-on-the-same-question-and-nothing-arbitrates: paradigm=object-oriented defect="Tre coppie di skill danno istruzioni INCOMPATIBILI sullo stesso argomento, e nessun documento arbitra: chi ne carica una agisce con sicurezza contro cio' che l'altra prescrive, e chi le carica entrambe segue la prima che parla. CENSITO 2026-07-24 su 50 skill lette integralmente di 279 (le restanti scansionate per grep). (a) BUDGET DEI TEST: `nw-test-optimizer` impone `2 x behavior_count` STRETTO, ogni test eccedente e' una violazione; `nw-at-completeness-check` accetta `2 x AT_count + varianti PBT/parametrize`, dove l'inflazione da varianti e' ATTESA. Dodici test su sei comportamenti: rifiutati dalla prima, accettati dalla seconda. (b) STRATEGIA DI CARICAMENTO: `nw-ab-create-agent` dice di caricare tutto all'avvio; `nw-skill-reviewer` dice di caricare on-demand perche' il preload spreca contesto -- sulle STESSE skill, strategie opposte. (c) POSTURA DI REVIEW, ed e' la contraddizione vera: `nw-adversarial-refutation` prescrive di assumere l'artefatto SBAGLIATO e pretendere che sopravviva all'attacco (default-to-refuted, onere sull'artefatto); `nw-tdd-review-enforcement` prescrive che se i gate passano il verdetto e' APPROVED (onere sul gate, il reviewer conferma). Caricate insieme in una review, una ordina di attaccare e l'altra di accettare. Le prime due sono trade-off non arbitrati -- entrambe difendibili, nessuna dichiara di vincere; la terza sono opposti diretti. NOTA DI COPERTURA ONESTA: il campione e' 50/279 scelte sul percorso critico DELIVER/DISTILL, quindi il conteggio e' un MINIMO, non un totale -- le contraddizioni nelle 229 non lette non sono state cercate." proposed_solution="Per ciascuna delle tre: nominare l'SSOT e far puntare l'altra skill a quella, invece di lasciare due valori in circolazione. Il budget dei test e la strategia di caricamento sono decisioni di prodotto che vanno prese una volta e dichiarate in UN posto; la postura di review e' la piu' urgente perche' cambia il verdetto e non solo il costo -- va deciso se una review si apre in postura avversariale e i gate sono un input, oppure se i gate sono il verdetto e la review conferma. Le due cose non possono coesistere senza un arbitro. E il rimedio strutturale che le previene tutte e tre: quando una regola vale per piu' agenti, deve vivere in UN locus proiettato (il meccanismo di regione generata da docgen esiste gia' in questo repo), non replicata in prosa dove ogni copia invecchia per conto suo."
- [ ] seven-skills-instruct-the-reader-to-use-a-helper-module-that-does-not-exist: paradigm=object-oriented defect="Sette skill istruiscono chi le legge a usare un modulo helper che NON ESISTE, con la formula imperativa 'usa <helper> -- NON scrivere direttamente'. Chi obbedisce ottiene un ModuleNotFoundError. CENSITO 2026-07-24: `scripts/shared/telemetry.py:write_density_event(...)` e' citato in `nw-deliver`, `nw-density-resolution-contract`, `nw-design`, `nw-devops`, `nw-discover`, `nw-discuss`, `nw-distill` -- 8 riferimenti in 7 skill -- e il file non esiste. Piu' un ottavo riferimento a `scripts/validation/validate_atdd_pure_telemetry.py`, anch'esso inesistente, trovato nel campione letto (la ricerca su questo secondo caso e' incompleta: potrebbero essercene altri nelle 229 skill non lette). CIO' CHE RENDE LA RIGA PIU' DI UNA SVISTA: la forma dell'istruzione e' PRESCRITTIVA e VIETA l'alternativa -- 'non scrivere direttamente il JSONL' -- quindi chi la segue non ha nemmeno il ripiego di fare la cosa a mano; e' mandato in un vicolo cieco con la sicurezza di un ordine. E' la stessa classe gia' catalogata oggi due volte: un'istruzione impossibile enunciata con l'autorevolezza di un verdetto, che attribuisce a chi la esegue il fallimento di una via che non esisteva." proposed_solution="Decidere quale delle due direzioni e' vera, e nessuna delle due e' ovvia: o l'helper doveva esistere e non e' mai stato scritto (allora si scrive, ed e' un modulo piccolo con sette consumatori gia' pronti), oppure e' stato rimosso e le sette skill sono rimaste indietro (allora si rettificano, e il fatto che siano SETTE dice che sono state scritte copiando l'una dall'altra). Il controllo che impedisce il ripetersi: un gate che verifichi che ogni percorso di file e ogni modulo citato nelle skill esista davvero -- e' meccanico, non richiede giudizio, e in questo repo esistono gia' gate di coerenza documentale su cui appoggiarlo. Verificare inoltre l'ampiezza reale: il campione era 50 skill su 279, quindi otto riferimenti morti sono un MINIMO misurato, non un totale."
- [ ] a-ratified-skill-ownership-transfer-was-declared-five-times-and-never-executed: paradigm=object-oriented defect="Un trasferimento di PROPRIETA' di dieci skill e' stato DECISO, scritto cinque volte nello spec dell'agente cedente, ESEGUITO sul lato che cede -- e MAI eseguito sul lato che riceve. Le dieci sono da allora irraggiungibili da chiunque, e il loro frontmatter dichiara ancora il proprietario PRECEDENTE. CENSITO 2026-07-24, con la prova in tre pezzi che si chiudono a vicenda. (1) Lo spec di `nw-functional-software-crafter` dichiara la cessione in CINQUE punti distinti (righe 3, 45, 51, 86, 234, 279), in forma esplicita: 'The crafter does NOT load PBT skills as a test author; the acceptance-designer has owned those skills since plan v3 §3.B'. Non e' un'omissione: e' una decisione ratificata e messa per iscritto. (2) `git log -S'nw-pbt-python' -- nWave/agents/nw-functional-software-crafter.md` restituisce il commit `397f5ee98` ('ship Phase 1 scaffolding + Phase 2 agent restructure'): le skill C'ERANO ed e' quella ristrutturazione ad averle tolte. (3) `git log -S'nw-pbt' -- nWave/agents/nw-acceptance-designer.md` restituisce ZERO commit: il destinatario dichiarato non le ha MAI elencate, in tutta la storia del file. Le dieci sono ferme allo stesso commit del 2026-03-19 -- create insieme, mai piu' toccate. QUATTRO MESI in cui la decisione era scritta, meta' eseguita, e nulla lo ha segnalato. CONSEGUENZA che rende la riga piu' di un inventario: il campo `agent:` nel frontmatter delle dieci non e' semplicemente NON LETTO -- e' ATTIVAMENTE SBAGLIATO, perche' nomina il proprietario da cui sono state tolte. Un campo dichiarativo che SOPRAVVIVE alla decisione che lo smentisce, per quattro mesi, senza che nulla se ne accorga, e' l'argomento piu' forte per RIMUOVERLO invece che per renderlo obbligatorio: reso obbligatorio, obbligherebbe a mantenere aggiornato un dato che nessun consumatore legge. E' 'catalogato non e' cablato' nella sua forma piu' pura -- non un modulo dimenticato, ma una DECISIONE eseguita a meta'." proposed_solution="Completare il trasferimento invece di deciderlo di nuovo: il proprietario corretto e' l'acceptance-designer -- l'agente della wave DISTILL, quindi indipendente dal paradigma del progetto, il che risolve da se' il caso del progetto non-funzionale senza bisogno di un asse nuovo. La regola di caricamento per LINGUAGGIO si applica li': il fondamentale piu' la sola variante del linguaggio del progetto (circa il 21% del peso della famiglia contro il caricamento in blocco). Attenzione a una distinzione che il disegno deve rispettare: `nw-pbt-stateful` non e' una variante di linguaggio ma una TECNICA, e condizionarla sul linguaggio ripeterebbe l'errore di usare un asse per un fatto che non gli appartiene -- va condizionata su un fatto osservabile del bisogno. E rimuovere il campo `agent:` dal frontmatter, o renderlo derivato: oggi esiste in 35 skill su 279, e in dieci di esse e' falso da quattro mesi. IL CONTROLLO che impedisce il ripetersi vale piu' del fix: una cessione di proprieta' dichiarata nello spec di un agente dovrebbe essere VERIFICABILE -- se lo spec di A dice 'queste ora sono di B', un gate deve poter confrontare quella dichiarazione con il load-set di B e rifiutare quando non corrispondono. Senza, la prossima migrazione si fermera' a meta' allo stesso modo, e nessuno lo sapra' per altri quattro mesi."
- [ ] the-dune-runner-adapter-is-specified-and-unwritten-eighth-of-seven: paradigm=object-oriented defect="Il registro dei runner ha SETTE adapter di linguaggio (cargo, csharp, go, java, kotlin, pytest, vitest) e nessuno per dune/OCaml, benche' l'architettura a plugin sia gia' cablata e i tre facet (run / at-discovery / enumerate) siano gia' definiti dal registro. La specifica del dune mancante e' stata MISURATA su un albero OCaml reale da un utente (lyra-evolution, 2026-07-24) e va registrata qui prima che si perda, perche' e' il lavoro piu' costoso dell'adapter. (1) RUN, suite intera: `dune exec test/unit/unit_main.exe` (o `dune test`), exit 0 = pass. Il parser deve leggere la riga RIASSUNTIVA finale -- `N failure(s)! in Xs. M tests run.` oppure `Test Successful` -- e NON i marker `[OK]/[FAIL]` in linea, che sono per-caso: alcotest scrive i dettagli di fallimento solo a fine corsa. (2) AT-DISCOVERY per-slice: l'unita' AT in alcotest e' la SUITE REGISTRATA (es. `Metacognition.Activation_atomic`), dichiarata nella lista dell'eseguibile (`test/unit/unit_main.ml`, forma `(\"Suite.Name\", suite_tests)`); la discovery per slice e' il match del nome-suite nel sorgente di registrazione. Non esiste alcun file `.feature` ne' node-id in stile pytest: **il nome della suite E' il node-id**. (3) ENUMERATE: `dune exec test/unit/unit_main.exe -- list` elenca suite e casi -- ATTENZIONE, il comando e' il SOTTOCOMANDO `list`, NON `test --list` che su alcotest reale risponde `unknown option --list`; il primo comando registrato qui era sbagliato, corretto dall'utente che l'ha verificato sul proprio albero. FORMATO DI OGNI RIGA, misurato con `cat -A`: il nome-suite e' AVVOLTO IN CODICI ANSI (cyan `\e[36m...\e[0m`), le colonne sono PADDED a larghezza VARIABILE (spazi, mai tab), e la prima riga e' rumore da scartare (`qcheck random seed: <n>`). Il parser corretto: scarta l'header, STRIPPA l'ANSI, split su sequenze di spazi -- token[0] = nome-suite, token[1] = indice numerico del caso, il resto e' descrizione. LA TRAPPOLA: un regex che assuma assenza di ANSI o colonne a larghezza fissa scarta righe IN SILENZIO invece di errorare, producendo un enumerate parzialmente vuoto indistinguibile da uno completo. DIFESA suggerita dall'utente e da adottare: asserire che il numero di voci di `list` (meno l'header) EGUAGLI il `M tests run` della riga riassuntiva di una corsa completa; se divergono, l'enumerate e' parziale e il parser deve ERRORARE, mai degradare; il filtro per-suite e' `-- test <SuiteName>` e il singolo caso `-- test <SuiteName> <case-index>`; il node-id e' quindi `<SuiteName>/<case-index>` oppure `<SuiteName>` a livello di suite. Quest'ultimo punto e' esattamente cio' che la E2 contract gate raccoglieva a ZERO -- cercava node-id pytest su un albero senza adapter." proposed_solution="Registrare il plugin dune accanto agli altri sette usando la specifica sopra: non e' lavoro di architettura, il registro espone gia' i tre facet e la discovery dei plugin e' automatica. L'utente che ha misurato la specifica si e' offerto di fornire comandi e parsing esatti su richiesta. MA LA PRIORITA' NON E' L'ADAPTER: e' rendere LOUD la sua assenza. Un albero con `dune-project` alla radice e nessun `pyproject.toml`/`Cargo.toml`/`pytest.ini` non deve risolversi a `runner=pytest` ma a `nessun adapter per dune; disponibili: [...]`. Con quella sola riga l'utente si sarebbe fermato al primo dei sette muri invece che all'ottavo -- e senza di essa il prossimo linguaggio ripaghera' lo stesso prezzo, adapter o non adapter. Nota di igiene sul percorso di fuga: l'utente ha risolto con l'esenzione sanzionata e ha messo il gap in cima al proprio backlog **proprio perche' l'esenzione non anestetizzi la scrittura dell'adapter** -- e' la disciplina giusta e va rispecchiata qui tenendo questa riga aperta e visibile."
- [ ] six-fabricators-of-the-des-source-tree-with-two-declared-copy-paste-chains: paradigm=object-oriented defect="Sei costruttori distinti fabbricano un albero-sorgente DES sintetico nei test, tre dei quali HARDCODANO nomi che `DESPlugin` gia' dichiara come costanti -- oggi identici byte-per-byte, stale per costruzione appena una delle due parti si muove. CENSITO 2026-07-24 su tutto `tests/`: 44 costruttori di albero sintetico in 36 file, ma la classe rilevante e' piu' piccola di quanto sembri e la correzione al conteggio e' parte del reperto -- circa 35 seminano `agents`/`skills`/`commands` per plugin diversi e sono legittimamente eterogenei, non incontrano mai questo contratto. I SEI della classe DES: `installer_orphan_sweep/steps/composition.py:249` (ricetta piena), `installer_orphan_sweep/steps/composition_families.py:374` (la stessa piu' utilities e asset dir), `test_des_shim_installation.py:147` `_make_context` (hardcoded), `test_des_shim_installation.py:110` `_make_context_from_dirpath` (copia verbatim del precedente, stesso file, identici a meno del nome di un parametro), `test_des_plugin_chaos.py:88` (copia della copia), `test_des_scripts_manifest.py:86` (parziale). DUE CATENE DI COPIA-INCOLLA, e la parte notevole e' che sono DICHIARATE NEI DOCSTRING: 'Extends the slice-01 recipe', 'Mirrors _make_context()', 'mirrors test_des_shim_installation.py'. Chi ha copiato lo ha scritto -- l'informazione c'era, mancava solo qualcuno che la leggesse come un debito invece che come una nota di cortesia. Il debito peggiore non e' la ripetizione della struttura: e' che tre di essi ripetono i NOMI (`DES_SCRIPTS`, `DES_SHIMS`, `DES_TEMPLATES`) che il plugin espone gia' come costanti, quindi al primo rinomino o aggiunta le fixture continueranno a fabbricare l'albero VECCHIO e i test resteranno verdi su una forma che il prodotto non usa piu'. DUE FALSI MEMBRI verificati e da non contare: `test_des_plugin_errors.py:75` rifiuta gia' in `validate_prerequisites` e non raggiunge mai il codice interessato (verde per la ragione giusta), e i quattro sotto `plugin-architecture/` puntano al repo reale invece di fabbricare." proposed_solution="Un costruttore condiviso per la sola classe DES -- sei chiamanti, non quarantaquattro -- che DERIVI cio' che semina dalle costanti gia' esposte dal plugin invece di ripeterle: cosi' un requisito aggiunto al prodotto si propaga da solo alle fixture e la deriva diventa impossibile invece che probabile. Le due catene di copia-incolla indicano da dove partire: la ricetta piu' completa e' la base naturale, e i docstring dichiarano gia' chi discende da chi. NON estendere il costruttore agli altri ~35: fabbricano alberi genuinamente diversi per plugin diversi, e unificarli sarebbe l'errore opposto -- un'astrazione che schiaccia una differenza reale. Nota di metodo dal censimento: la prima stima parlava di 'dodici costruttori di una classe' e la misura ne ha trovati 44 di cui 6 della classe -- il numero sbagliato veniva dall'aver cercato un nome di variabile invece della classe di comportamento, che e' lo stesso errore di unita' che questa sessione ha incontrato piu' volte."
- [ ] the-suite-verifies-the-globally-installed-package-instead-of-the-artifact-it-just-built: paradigm=object-oriented defect="Una sonda autouse della suite verifica che il pacchetto sia importabile lanciando un subprocess con `sys.executable` -- che eredita il site-packages del processo padre, dove vive l'installazione editable. NON verifica il wheel che la suite ha appena costruito. CENSITO 2026-07-24 su `tests/conftest.py:351-372` (`_verify_nwave_ai_subprocess_importable`, scope session, autouse): esegue `subprocess.run([sys.executable, '-c', 'import nwave_ai'], cwd='/tmp')`. Il `cwd` pulito suggerisce l'intenzione di isolare, ma l'interprete resta quello del padre e il suo `sys.path` contiene l'editable install. DIREZIONE DELLA MENZOGNA: falso VERDE. Se l'editable globale e' sano ma il wheel appena prodotto e' corrotto o incompleto, la sonda passa. Un operatore esegue la suite, la vede verde, poi installa quel wheel e fallisce FUORI dalla suite -- cioe' nell'unico posto dove il difetto conta. La sonda risponde a «il pacchetto e' importabile su questa macchina?» mentre la domanda che il suo nome promette e' «l'artefatto che ho appena costruito e' importabile?». LA STESSA FORMA, LO STESSO GIORNO, NEL PRODOTTO: l'installer del framework verifica che il modulo installato sia importabile eseguendo `from des.application import DESOrchestrator`, mentre lo shim che l'installer STESSO genera esegue `from des.cli.__main__ import main` -- la sonda passa, il comando muore. Due implementazioni indipendenti dello stesso errore, una nei test e una in produzione: **verificare un parente plausibile dell'oggetto invece dell'oggetto**. Il fatto che ricorra in due luoghi scritti da persone diverse indica che non e' una svista ma una tentazione strutturale — verificare cio' che e' facile raggiungere invece di cio' che verra' eseguito." proposed_solution="La sonda deve esercitare l'ARTEFATTO, non l'ambiente: estrarre il wheel condiviso in un venv temporaneo, installarlo li', e verificare l'import da quel venv -- oppure riusare il venv che la suite gia' costruisce per il wheel, che e' la strada piu' economica perche' l'infrastruttura esiste. In alternativa minima, azzerare `PYTHONPATH` e forzare l'interprete del venv del wheel, cosi' l'editable globale non possa rispondere al posto suo. E la regola che copre entrambe le istanze di oggi, quella nei test e quella nell'installer: **quando un sistema produce un artefatto o genera un consumatore, la verifica deve esercitare ESATTAMENTE cio' che verra' eseguito, mai un suo parente raggiungibile** -- e il modo di renderlo strutturale e' derivare la sonda dalla stessa fonte che genera il consumatore, cosi' non possano divergere. Nota di priorita': questa sonda e' AUTOUSE, quindi la sua rassicurazione arriva a ogni sessione di test, il che la rende piu' persuasiva di quanto meriti."
- [ ] the-cure-for-vacuous-pass-was-applied-to-one-gate-and-never-propagated-to-its-three-siblings: paradigm=object-oriented defect="La correzione che distingue «ho verificato e l'insieme era vuoto» da «non ho potuto verificare nulla» ESISTE gia' in questo repo, e' scritta bene, ed e' stata applicata a UN gate su quattro della stessa famiglia. Gli altri tre continuano a dichiarare successo su un insieme vuoto. CENSITO 2026-07-24: i gate il cui nome promette completezza o copertura sono quattro -- `check_slice_at_completeness`, `mode_registry_completeness`, `verify_slice_commit_completeness`, `verify_spec_coverage`. IL GATE CORRETTO: `slice_at_completeness.py:245-258` espone `AtCompletenessOutcome` con un campo `verifiable`, documentato cosi': «verifiable e' False SSE nessun file AT ha corrisposto... il consumatore DEVE trattare verifiable=False come *non posso verificare*, mai come [un successo]», e il messaggio all'operatore dice testualmente «nothing was verified, this is not a pass». E' esattamente il terzo stato che serve, scritto da qualcuno che aveva capito il problema. IL GATE NON CORRETTO, misurato: `mode_registry_completeness` risponde exit 0 con «the mode registry is complete» mentre SETTE agenti su CINQUANTUNO hanno una riga nel registro -- copertura 14%. Verifica che ogni agente CITATO esista (coerenza), mai che ogni agente ESISTENTE sia citato (completezza); su un registro VUOTO passerebbe vacuamente. STORIA CHE RENDE IL DIFETTO LEGGIBILE, e che va conosciuta prima di propagare la cura: quando `verifiable` fu introdotto, la sua onesta' rese ROSSI sei test che prima passavano -- non perche' il commit li avesse rotti, ma perche' prima 'zero candidati' era indistinguibile da 'tutti verificati'. Quei sei rossi erano difetti veri che il falso-verde nascondeva. **Propagare la cura agli altri tre gate produrra' altri rossi, e quelli saranno il valore della propagazione, non il suo costo.** E' la stessa forma gia' catalogata oggi per una migrazione di proprieta' di skill dichiarata cinque volte ed eseguita a meta': una decisione presa, applicata a un punto, mai portata alla classe." proposed_solution="Applicare agli altri tre gate il modello che `AtCompletenessOutcome` gia' realizza: un esito che distingua VERIFICATO-VUOTO da NON-VERIFICABILE, con il consumatore obbligato a trattare il secondo come indeterminato e mai come successo. Non serve inventare nulla -- serve copiare una forma che funziona, ed e' anche il modo di renderla una convenzione invece di un'eccezione. LA SONDA MECCANICA che trova il resto della classe, e va eseguita una volta su tutti i gate del repo: **si puo' soddisfare questo gate con l'insieme VUOTO?** Se si', verifica che nulla sia sbagliato e non che qualcosa sia completo, e il suo nome mente. E' un test che non richiede giudizio e si scrive una volta. ATTENZIONE alla sequenza: propagare la cura scoprira' altri falsi-verdi, quindi va fatto quando c'e' capacita' di ripararli -- ma NON va rimandato per evitarli, perche' ogni giorno in cui il gate mente e' un giorno in cui qualcuno prende una decisione su un verdetto vuoto."



- [ ] arity-collapse-and-ssot-split-defect-families-must-route-to-DESIGN-not-individual-bugfix-lanes: paradigm=object-oriented debt="DECISIONE DI INSTRADAMENTO (loop-10), non un difetto nuovo: ~9 righe gia' in defects.md sono facce di DUE problemi di design ACCOPPIATI con rimedi OPPOSTI, e instradarne una qualsiasi in corsia bugfix isolata applicherebbe il rimedio sbagliato o lascerebbe viva la classe. La distinzione e' la tassonomia costruita 2026-07-24 (radice: 'una rappresentazione che diverge silenziosamente da cio' che rappresenta'). FAMIGLIA A -- COLLASSO DI ARITA' (un terzo stato negativo/indeterminato non ha casa nel TIPO e collassa in positivo/vuoto): [[the-doctor-counts-an-undeterminable-check-as-passed-in-its-own-total]], [[the-at-review-ledger-persists-only-approvals-so-reviewed-and-rejected-collapses-into-never-reviewed]], [[the-hermeticity-gate-prescribes-as-the-fix-the-very-form-it-cannot-see]], [[examine-ledger-latest-record-wins-erases-unresolved-reservations]]. RIMEDIO: aggiungere il terzo esito AL TIPO e propagarlo fino all'AGGREGATO -- l'aggregato e' cio' che il lettore vede, se il terzo stato muore nella cella non arriva alla riga di sintesi. FAMIGLIA B -- SSOT-SPLIT / DISACCORDO (un confronto ESISTE fra due rappresentazioni della stessa regola e non viene fatto, o le due divergono): [[two-cli-commands-on-the-same-artifact-disagree-on-whether-it-may-exist-in-this-mode]], [[two-gates-disagree-on-the-same-at-attestation]], [[des-doctor-vs-carpaccio-gate-disagree]], [[two-distribution-channels-keep-independent-inclusion-lists-with-no-cross-check]]. RIMEDIO OPPOSTO: collassare la molteplicita' -- UNA definizione, le altre derivano; la divergenza diventa impossibile per costruzione invece che improbabile per disciplina. PERCHE' DESIGN E NON BUGFIX: (1) i rimedi sono opposti (aggiungi-uno-stato vs togli-una-copia), quindi la scelta del rimedio E' la decisione di design -- fast-lanare una singola istanza la prende alla cieca; (2) Famiglia A richiede un TIPO condiviso di esito a tre stati che TUTTI i consumatori (i 10 check del doctor, i ledger, i gate) adottano -- e' un cambio con molti consumatori, la definizione stessa di L-lane; (3) fixare `version_sync` da solo lascia l'aggregato del doctor incapace di esprimere l'indeterminato -- la cura applicata a un gate e mai propagata ai fratelli, gia' filata come pattern. CONTRO-ESEMPIO GIA' NEL CODEBASE, da copiare: `verify_red_green.py:469-485` gestisce la STESSA situazione (un artefatto che potrebbe essere stale) CORRETTAMENTE -- osserva il content_sha256 reale, degrada LOUD (REFUSED con COSA/PERCHE/COME), il COME invoca il tool-produttore. Il pattern giusto esiste; il design deve propagarlo alle due famiglie." proposed_solution="DUE item di DESIGN separati, non ~9 bugfix. DESIGN-1 (Famiglia A): un tipo di esito a tre stati (pass/fail/indeterminate) e la regola che l'aggregato DEVE esprimerlo -- riusare la forma gia' ratificata di `AtCompletionOutcome.verifiable` in `slice_at_completeness.py:245`, propagata. DESIGN-2 (Famiglia B): per ogni coppia di rappresentazioni della stessa regola, derivare-da-una o un test di uguaglianza fail-closed; l'axis-check (costruisci la coppia che il discriminante confonde) le trova. Nessuna delle due va in corsia bugfix. L'autorizzazione a saltare DESIGN, se mai, e' umana ed esplicita -- ma qui il caso per DESIGN e' forte: molti consumatori, rimedi opposti da scegliere, contro-esempio da propagare."

  - RESOLVED 2026-07-26 (techdebt drain shard B, commit 9e88fbd6e): `patch_pyproject.py` now
    imports `UTILITY_SCRIPTS` from `scripts/build_dist.py` and renders the force-include lines
    from it via a new `_utility_scripts_force_include_block()`, replacing the 3 hardcoded lines.
    The two representations can no longer disagree by construction. The invariant test
    (tests/release/test_wheel_utility_scripts_invariant) was KEPT as the second axis, per the
    row's own `how` -- it now passes trivially since both sides derive from one source.
    Verified: 917 passed / 3 failed on the targeted scope, all 3 failures pre-existing and
    unrelated (catalog gate registry, cargo digest gate-scope, install paths). Only
    patch_pyproject.py was touched.
  - paradigm: object-oriented
  - what: `UTILITY_SCRIPTS` (scripts/build_dist.py:65-69) names which utility scripts must ship;
    the wheel force-include map (scripts/release/patch_pyproject.py:186-196) repeats the same set
    as hardcoded strings. Two representations, no mechanism holding them equal -- only a test
    (tests/release/test_wheel_utility_scripts_invariant) notices after the fact.
  - why: the class BIT today, not hypothetically. refactor_agent.py was in UTILITY_SCRIPTS and
    absent from the map, so every pipx-installed nwave shipped `des refactor` WITHOUT the script
    it spawns -- a drain that cannot run, failing only at drain time far from the omission. The
    instance is fixed (commit 9abbc8259); the STRUCTURE that produced it is not: the next script
    added to one list and not the other reproduces it exactly.
  - how: make UTILITY_SCRIPTS the single source and DERIVE the force-include lines from it
    (import the list into patch_pyproject and render the map), so the two cannot disagree by
    construction. Keep the invariant test as the second axis, not as the only one.
  - found: 2026-07-25, read-only structural finder (crafter-reviewer), confidence HIGH

  - RESOLVED 2026-07-26 (techdebt drain shard B, commit ee411cb8e):
    `read_declared_parallel_slice_ids` now returns None for "no Slice Plan heading at all" and
    () for "section present, declares zero parallel slices" (GDP-8 arity corollary -- the third
    state reaches the caller). `des parallel-safety-report` rejects the omission with its own
    reason and routes the HOW to `des feature-delta-doctor` (GDP-4), verified to actually report
    the missing locked section. A present plan declaring zero parallel slices keeps its existing
    per-scope rejection -- a valid monolithic plan.
    RED verified EMPIRICALLY, not argued: the pre-fix module replayed from HEAD answered () for
    BOTH inputs, exactly as this row claimed.
    SCOPE CALL, deliberate: `validate_slice_plan_content` ALREADY distinguishes the two cases
    (missing-slice-plan vs malformed-slice-plan) -- but parallel_safety_report never CALLS it.
    The fix did NOT wire in the full validator, because that would also start rejecting
    malformed-but-parsable plans, widening behaviour past this defect.
    151 passed / 0 failed on the targeted scope, 5 tests new.
  - paradigm: object-oriented
  - what: `_plan_table_rows` (src/des/cli/validate_feature_delta.py:507-508) returns None when the
    Slice Plan HEADING is absent, and a list (possibly empty) when it is present. The caller
    `read_declared_parallel_slice_ids` (:742-744) does `if not rows: return ()`, so None and []
    become the same answer.
  - why: the consumer cannot tell "the author never wrote a Slice Plan" -- a structural omission
    that should block -- from "the Slice Plan is there and declares no parallel slices" -- which is
    valid and means the feature is monolithic. Same shape as the charter-mapping collapse fixed
    today (unadopted practice vs forgotten slice): whatever policy is chosen fits one case and
    mispunishes the other. Cited rule: GDP-8 arity corollary, nw-architecture-patterns SKILL.md
    § "Gate Design Principles" -- the third state must reach the caller, never collapse.
  - how: return the two facts distinctly (a sentinel/None for absent-heading vs an empty list for
    present-but-empty) and let the caller decide; block on the omission, accept the empty.
  - found: 2026-07-25, arity finder — VERIFIED BY ME at the cited lines (both paths read)

  - RESOLVED 2026-07-26 (techdebt drain shard B, commit a3a83fc80) -- fixed per the RECTIFIED
    reading below, not per this row as originally reported. See the RETTIFICA row for why that
    distinction mattered: filing this report verbatim would have aimed the fix at the wrong site.
  - paradigm: object-oriented
  - what: reported by the arity finder, NOT independently verified by me — cited lines
    src/des/adapters/drivers/hooks/subagent_stop_handler.py:178 (file absent -> None), :235-237
    (OSError/PermissionError -> None), :245-246 (readable, no DES markers -> None); and :623-624
    vs :638-639 returning [] for absent vs unreadable.
  - why (as reported): an operator debugging a missing transcript cannot tell "never existed"
    (user error) from "exists but unreadable" (permissions) from "readable but wrong format".
    A stale-agent watchdog reading an empty entry list may allow a return while the transcript is
    actually corrupt -- absence and incapacity wearing the same face.
  - how: distinguish the causes at the boundary and carry the reason to the audit surface; a
    could-not-read is INDETERMINATE, not an empty result.
  - confidence: finder says HIGH with line citations; treat as NEEDS-CHECK until someone reads
    both paths. Do NOT act on it as fact before that.
  - found: 2026-07-25, arity finder (coverage honestly declared 42% of that file)

  - RESOLVED 2026-07-26 (techdebt drain shard B, commit a3a83fc80). Fixed exactly as this
    rectified reading prescribed -- OBSERVABILITY, not a contract change: both functions keep
    their existing return types (None / []), and the incapacity-to-read is now distinguishable
    from having-read-nothing through the AUDIT trail.
    Three audit events added at the three previously-mute branches:
    * `HOOK_TRANSCRIPT_ABSENT` -- extract_des_context_from_transcript's file-absent branch
      (the one this rectification correctly identified as the ONLY mute branch on that side;
      its OSError and no-markers branches already emitted HOOK_TRANSCRIPT_ERROR /
      HOOK_TRANSCRIPT_NO_MARKERS and were left untouched).
    * `HOOK_TRANSCRIPT_ENTRIES_ABSENT` and `HOOK_TRANSCRIPT_ENTRIES_UNREADABLE` (the latter
      carrying the error string) -- _read_transcript_entries' two branches, the FULL collapse
      this rectification named as the real defect.
    5 new tests in tests/des/unit/adapters/drivers/hooks/test_transcript_read_audit_observability.py,
    one per branch -- including regression cover for the two already-correct branches, which
    had none. 41 passed / 0 failed on the targeted scope.
    METHOD NOTE worth keeping: this row exists because someone READ both branches instead of
    filing the finder's report. The reported row and the verified row were different objects,
    and only the verified one pointed at the right site.
  - paradigm: object-oriented
  - what: the finder reported both transcript sites as equally collapsed. Reading both paths shows
    they are NOT equal, and only one is a real defect:
    * `_extract_des_context`-side (subagent_stop_handler.py:177 / :235-236 / :245): all three return
      None, BUT two of them EMIT a distinct audit event first -- `HOOK_TRANSCRIPT_ERROR` on
      OSError/PermissionError, `HOOK_TRANSCRIPT_NO_MARKERS` on readable-but-marker-less. The return
      value collapses; the AUDIT does not. Only the file-absent branch (:177) is fully mute.
      -> partial: the fix is one missing audit event, not a redesign.
    * `_read_transcript_entries`-side (:623-624 absent -> [] and :638-639 OSError/PermissionError
      -> []): BOTH return the empty list and NEITHER logs anything. Full collapse, no signal.
      -> THIS is the defect: a caller (and the stale-agent watchdog reading the entry list) cannot
      tell "no transcript" from "transcript unreadable/corrupt", and nothing anywhere records which
      happened. An unreadable transcript reads exactly like an empty one.
  - how: emit a distinct audit event on the unreadable branch and return a value the caller can
    distinguish (or raise); add the missing absent-file audit event on the first site. The property
    to hold: an incapacity to read must never be indistinguishable from having read nothing.
  - why the rectification matters beyond this row: a reported finding and a verified one are
    different objects. Filing the report verbatim would have aimed the fix at the wrong site.
  - found: 2026-07-25, verified by reading both branches at the cited lines

- [ ] freshness-pulls-the-repo-probe-adapter-at-module-scope-because-its-constants-live-there
  - paradigm: object-oriented
  - what: src/des/runtime/freshness.py:33-37 imports RepoSourceProbe AND the constants
    NO_MANIFEST_REASON / NO_MANIFEST_REMEDIATION from src/des/adapters/driven/freshness/
    repo_source_probe.py. The class is only needed at :265 (`(probe or RepoSourceProbe()).probe()`),
    but the CONSTANTS are used at MODULE scope (:84, a remediation map), so the adapter module is
    loaded on import no matter what. src/des/cli/__init__.py:15 imports this module, so EVERY
    `des <cmd>` pays it.
  - why: it is why `import des.cli.__main__` still pulls 62 des.* modules after the root-package
    re-exports were made lazy (measured 2026-07-25: `import des` alone now loads 0). The declared
    layering (CLAUDE.md § Architecture: application on PORTS, adapters at the edge) is violated in
    the direction that costs startup time on every command, for the user as well as the tests.
  - how: NOT a lazy import -- moving the class import into the function leaves the constants
    behind, and they are the reason the module loads. The fix is to relocate the two constants to
    a light module the runtime may depend on (the freshness PORT or a domain-side text SSOT), then
    import the concrete probe at point of use. That is a layering change with consumers, so it goes
    through DESIGN, not a bugfix lane -- which is exactly why it is filed here rather than done.
  - verification for whoever takes it: `python -X importtime -c "import des.cli.__main__"` must stop
    listing des.adapters.driven.freshness.*; count the des.* modules before and after (a count
    survives contention, a duration does not).
  - found: 2026-07-25, after the root-package lazy-export fix exposed the next layer of the cost

- [ ] the-real-repo-xdist-group-pins-698-of-the-slowest-tests-to-one-worker
  - paradigm: object-oriented
  - what: tests/conftest.py:1757 marks every item whose module (or its composition/steps siblings)
    drives a `cwd=<real repo>` subprocess with `xdist_group("real_repo_scan")`. Under
    `--dist loadgroup` a group runs entirely on ONE worker. Measured 2026-07-25 on 8774 collected
    items: real_repo_scan 698 (8.0%), installer_walking_skeleton 370 (4.2%), e2e_docker_install
    185, others 68 -- 1321 items (15.1%) pinned to a single worker each.
  - why: those 698 are precisely the SLOWEST tests (they spawn processes: the serial census
    attributes 776s / 47% of the suite to nested pytest spawns). So one core grinds the heavy work
    in file order while the other two finish 7400 light tests and idle. Measured effect: serial
    1641s, `-n 3` 1024s -- a speedup of 1.6x where 3 workers should approach 3x. Roughly EIGHT
    MINUTES of the seventeen are workers waiting, not work.
  - how: the constraint is real but over-applied. "Two tests must not drive the SAME real repo
    concurrently" does not imply "all such tests run on one worker" -- give each its own isolated
    checkout/copy and the group dissolves. Where isolation is genuinely impossible, split the group
    by resource (one group per repo/fixture) instead of one group for the whole class, so the
    serialisation is as narrow as the conflict.
  - verification by property, not by hope: after the change, compare serial wall vs `-n W` wall and
    assert the speedup approaches W (today 1.6 on 3). A count of items per group is not the
    measure -- the measure is worker idle time.
  - found: 2026-07-25, explaining why two import fixes bought 50s while the suite stayed at 17min

  - RESOLVED 2026-07-27 (drain shard E, mirroring commit ee411cb8e as prescribed).
    `read_declared_parallel_feature_ids` (src/des/cli/validate_feature_delta.py) now
    returns None for "no Feature Plan heading at all" and () for "section present,
    declares zero parallel features" -- same GDP-8 split as its slice sibling.
    src/des/cli/parallel_safety_report.py's `--epic-delta` branch now rejects the
    None case with its own reason, routing the HOW to `des feature-delta-doctor`,
    mirroring the already-landed slice-side rejection verbatim.
    New tests/des/unit/cli/test_validate_feature_delta.py::TestDeclaredParallelFeatureIdsArity
    (3 tests) pins both states + the populated case; RED verified before the fix
    (assert ... is None failed with "() is None" on the pre-fix code).
    31/31 passed in tests/des/unit/cli/test_validate_feature_delta.py, plus the
    parallel_safety_report feature-plan-input AT suite (8/8) unaffected.
  - paradigm: object-oriented
  - what: `read_declared_parallel_feature_ids` (src/des/cli/validate_feature_delta.py:810-812)
    carries the IDENTICAL `if not rows: return ()` collapse that was just fixed in its sibling
    `read_declared_parallel_slice_ids`. Consumer: src/des/cli/parallel_safety_report.py:177, the
    `--epic-delta` branch.
  - why: same GDP-8 arity corollary breach as the sibling row already drained -- an epic-delta
    carrying NO Feature Plan section at all is reported identically to one whose Feature Plan is
    present and declares zero parallel features. The consumer therefore still misreports the
    structural omission on the epic-delta path, and the operator is sent hunting through --scope
    bindings instead of being told the section is missing.
  - how: mirror the fix already landed on the slice sibling (commit ee411cb8e) -- return None for
    the absent heading, () for present-but-empty, and have parallel_safety_report reject the
    omission with its own reason routing the HOW to `des feature-delta-doctor`. The pattern is
    established; this is applying it, not designing it.
  - found: 2026-07-26, by the crafter draining the slice-plan sibling row -- named explicitly as
    an unfixed twin rather than silently fixed out of scope.

  - RESOLVED 2026-07-27 (drain shard E). READ delivery_plan.py:56 first, as this row's
    own `how` instructed, before assuming the slice-plan sibling's policy transfers --
    it does NOT collapse identically. delivery_plan.py's `main()` already calls
    `validate_slice_plan_content(content)` and rejects (verdict="missing-slice-plan")
    BEFORE ever reaching `read_slice_plan_dependencies` when the Slice Plan heading is
    absent (confirmed empirically: validate_slice_plan_content on heading-less content
    returns verdict != VERDICT_ACCEPTED). So the arity collapse this row names is
    LATENT for delivery_plan.py's sole call site today, not a live silent-wrong.
    Fixed the accessor anyway for API-contract consistency with its two siblings
    (`read_declared_parallel_slice_ids`/`read_declared_parallel_feature_ids`): now
    returns None for absent-heading, () for present-but-empty. delivery_plan.py got
    an explicit (currently unreachable) `if graph is None: return _reject(...)` guard
    rather than silently trusting the upstream validator's invariant (GDP-6) and to
    satisfy the type checker on the new Optional return.
    New tests/des/unit/cli/test_validate_feature_delta.py::TestSlicePlanDependenciesArity
    (3 tests) pins both states + the populated dependency graph; RED verified before
    the fix. 34/34 passed in test_validate_feature_delta.py plus the delivery_plan
    acceptance suite (3/3) unaffected.
  - paradigm: object-oriented
  - what: `read_slice_plan_dependencies` (src/des/cli/validate_feature_delta.py:773-775) carries
    the same `if not rows: return ()` collapse. Consumer: src/des/cli/delivery_plan.py:56.
  - why: third instance of the same arity collapse in one module -- absent Slice Plan section vs
    present-but-declaring-no-dependencies reach the consumer as the same empty answer. The
    consumer here differs from the other two (delivery_plan, not parallel_safety_report), so the
    downstream consequence needs its own reading before the fix: what delivery_plan does with an
    empty dependency set determines whether the omission should block or merely be reported.
  - how: same shape as the sibling fixes, but READ delivery_plan.py:56's use of the result first
    -- do not assume the slice-plan row's policy transfers.
  - found: 2026-07-26, by the crafter draining the slice-plan sibling row.

- [ ] release-train-e2e-smoke-rebuilds-candidate-per-test-instead-of-once-per-session: paradigm=object-oriented defect="`tests/release/test_release_staging_ssot.py` (1875 righe, marcato in gran parte `@pytest.mark.e2e_smoke`) chiama `_build_des`/`_patch_stamp_and_build_des` almeno 8 volte -- una per test -- e ognuna di queste prima clona l'intero albero sorgente (`nWave`, `scripts`, `src`, `nwave_ai`, `schemas`, ~23MB via `shutil.copytree`) in un `tmp_path` fresco, poi invoca `scripts/build_dist.py` (0.75s da solo, misurato 2026-07-26) e/o installa un wheel reale via `NWaveInstaller.install_framework()`/pip. MISURATO 2026-07-26: l'intera suite di questi 2 file (`test_release_staging_ssot.py` + `test_wheel_ships_nwave_runtime_assets.py`, 28 test totali) ha impiegato **oltre 15 minuti** in un run, contro build_dist.py da solo (0.75s) e la copia (23MB, trascurabile su disco locale) -- quindi il vero collo di bottiglia non e' ancora isolato con certezza (probabilmente l'installazione/pip reale ripetuta per ogni scenario e2e_smoke, non ancora misurata isolatamente). Segnalato da Ale 2026-07-26 durante la verifica pre-push di `parity-final`: 'va costruito una volta al setup e riusato per gli altri test' -- prescrizione diretta, non solo osservazione." proposed_solution="Introdurre una fixture pytest a scope `session` (o `module`) che costruisce/installa il candidato UNA SOLA VOLTA e la condivide fra tutti gli 8+ test che oggi la ricostruiscono da zero -- pattern standard per suite e2e con un artefatto costoso e immutabile all'interno di una run. Prima di implementare: isolare con un profiler/timing mirato QUALE fase (copytree, build_dist.py, patch_pyproject.py, o l'installazione/pip reale) domina il tempo totale, per non ottimizzare la fase sbagliata -- la misura fatta finora esclude solo build_dist.py e la copia file come cause principali, non le conferma ne' le nega per le altre fasi."
- [ ] compiled-control-plane-branch-name-is-sf-vocabulary-in-the-oss-tree: paradigm=object-oriented defect="Il branch `refactor/compiled-control-plane` di QUESTO repo (nwave-dev, OSS) usa vocabolario architetturale che il LYRA-DEV MANDATE del CLAUDE.md di questo repo vieta esplicitamente: 'nwave-dev no sequencer no engine, SOLO hooks' -- 'compiled control plane' e' precisamente il pattern (state-machine YAML compilata interpretata da un sequencer a runtime) che il mandato marca come IP-blind import da SF. TROVATO 2026-07-26 dalla review avversariale di Fable (nw-solution-architect) sul Mikado operativo + verdetto Sol: punto 4 della review segnala esplicitamente che P4 'compiled GateBundle' e P6 'RoleCapsule' sono pattern SF gia' nel piano di destinazione del SSOT Mikado (nwave-oss-delivery-ssot.mikado.md riga 144), e che il vocabolario SF e' GIA' nell'albero OSS tramite il nome di questo branch -- non solo nel piano futuro. Verdetto completo con riferimenti file:riga: /tmp/nwave-performance-mikado-review/docs/analysis/2026-07-26-fable-mikado-review-verdict.md. NOTA ONESTA: non ho verificato SE il contenuto del branch (non solo il nome) importa gia' il pattern SF a runtime -- Fable riporta P9 (commit 3415442f4) come read-only/zero-dispatch e P8 (fe43d1620) come advisory exit-0, quindi hook-compatibili ALLO STATO ATTUALE; il rischio segnalato e' sul NOME e sulla DESTINAZIONE dichiarata (riga 144), non (ancora) sul comportamento a runtime." proposed_solution="Rinominare il branch a un nome che non evochi 'control plane compilato' (es. qualcosa che nomini la proprieta' misurata -- 'delivery-dag-parsed-once' o simile -- invece del pattern architetturale). Il mandate-check protocol del CLAUDE.md di questo repo si applica: prima di procedere sul contenuto di quel branch, chiedere 'OSS-scope? hook-compatible? importa il mental model SF?' Se P4/P6 (compiled GateBundle, RoleCapsule) restano nel piano di destinazione, servono autorizzazione umana esplicita di Ale prima che diventino codice, non solo un rename cosmetico. Non toccare il worktree/branch senza coordinamento: e' territorio attivo di un'altra istanza (Sol), quindi il rename va proposto e concordato, non eseguito unilateralmente da chi trova la riga."

- [ ] consolidation-queue-intake-toctou-race-on-already-queued-check: paradigm=object-oriented defect="src/des/domain/consolidation_queue_intake.py dichiara nel proprio docstring (righe 39-43) che un segnale e' 'gia' in coda' SOLO se il ledger porta un record PipelineStageStarted, e ri-rilevarlo NON DEVE aggiungerne un secondo. Ma intake_signal (righe 137-188) fa una ledger.read_records(...) NON LOCKATA (riga 175), decide via _already_queued(records, defect_id) (riga 176) in Python, e SOLO se non gia' in coda chiama evaluate_and_record(...) (riga 179), che alla fine chiama AtCompletionLedger.append_bugfix_pipeline_event (src/des/adapters/driven/logging/at_completion_ledger.py:1447). Quel metodo di append acquisisce un fcntl.flock SOLO per la scrittura stessa (docstring riga 491); read_records (riga 1610) e' una chiamata separata e non lockata. La sequenza leggi-decidi-scrivi in intake_signal non e' quindi atomica. Il design stesso della pipeline dichiara esplicitamente che gli stage cloud (RCA, dove questo intake entra) vengono lanciati 'senza tetto di concorrenza, sempre ammessi' (docstring del modulo, righe 12-14) -- cioe' chiamate intake concorrenti per lo stesso segnale sono il modo operativo ATTESO, non un caso limite. Due chiamate intake_signal concorrenti per lo stesso (signal_type, signal_key) possono entrambe leggere il ledger prima che una delle due abbia scritto, vedere entrambe 'non ancora in coda', e appendere entrambe un PipelineStageStarted(rca) per lo stesso defect_id -- violando la garanzia di idempotenza dichiarata e duplicando il lavoro RCA/bugfix-loop a valle per un solo segnale reale." proposed_solution="Rendere il controlla-e-appendi un'unica operazione atomica sotto LO STESSO lock -- es. aggiungere un metodo del ledger che tiene l'fcntl.flock per tutta la durata sia della lettura-per-controllo-idempotenza sia dell'append condizionale (una primitiva compare-and-append), invece di due chiamate indipendentemente lockate con un varco decisionale in mezzo."

- [ ] logging-port-and-both-adapters-dead-in-production-superseded-silently: paradigm=object-oriented defect="LoggingPort (src/des/ports/driven_ports/logging_port.py:12-52) e' elencato in CLAUDE.md 'Architecture: DES' come una delle porte driven vive, ma ha ZERO chiamanti di produzione. StructuredLogger (structured_logger.py:16, il proprio docstring dice letteralmente 'production logging implementation') e SilentLogger (silent_logger.py:13, 'test logging implementation') sono i suoi due adapter. mcp__tsunami__callers_of('StructuredLogger', transitive=true) ritorna esattamente 4 call site, TUTTI E 4 in tests/des/unit/ports/test_logging_port.py -- nessun sito di costruzione in produzione. mcp__tsunami__dead_module conferma indipendentemente silent_logger.py e logging_port.py come all_public_members_test_only_and_no_prod_import. Grep su src/des/application/ e src/des/adapters/drivers/ per LoggingPort/logging_port non trova nulla. Il wiring reale degli hook (hook_protocol.py:31-76) costruisce invece JsonlAuditLogWriter/NullAuditLogWriter (via create_audit_writer()) e JsonlNWaveLogWriter/NullNWaveLogWriter (via _get_nwave_log_writer()) -- due porte DIVERSE e piu' nuove che sembrano aver sostituito LoggingPort senza che venisse mai cancellata." proposed_solution="Confermare con la storia git/con Ale se LoggingPort e' stata effettivamente sostituita da AuditLogWriter+NWaveLogWriter; in caso affermativo cancellare logging_port.py, structured_logger.py, silent_logger.py, i loro re-export nei vari __init__.py, e tests/des/unit/ports/test_logging_port.py, oltre a togliere LoggingPort dall'elenco porte di CLAUDE.md. Se qualche consumatore esterno/plugin la usa davvero, cablarla in almeno un driver di produzione -- una porta non cablata e' altrimenti indistinguibile da codice morto."


- [ ] abandoned-worktrees-with-uncommitted-wip-need-owner-triage: paradigm=object-oriented defect="Loop-2 reconciliation (2026-07-27) trovato due worktree il cui branch e' gia' MERGED su origin/feature/atdd-pure-staging ma che portano lavoro sostanziale NON committato in piu (staged+unstaged), quindi non scartabili senza autorizzazione: (1) /home/alexd/Projects/wt/des-saturated-scheduler [feat/des-saturated-scheduler] -- feature-delta.md + DISTILL artifacts (red-classification.md, test-scenarios.md) + sorgenti src/des/cli/schedule.py + src/des/domain/scheduling_policy.py + AT acceptance/des_saturated_scheduler/* tutti staged-non-committati, oltre a modifiche a src/des/cli/__main__.py/bugfix_pipeline.py/patch_pyproject.py; (2) /home/alexd/Projects/wt/impacted-selection [feat/impacted-test-selection] -- src/des/domain/refactor/test_selection.py nuovo + test nuovi + modifiche a tests/des/refactor/{composition,doubles,test_slice_01_worktree_and_green_to_green}.py, tutti non committati. Altri 3 worktree simili (fix-eager-adapter-import/tmp-rung1 esplicitamente annotato 'ESPERIMENTO NON committare', hook-interpreter-durable e spawn-stdin-deadlock il cui unico contenuto non-tracciato erano execution-log.json generati) sono stati rimossi in sicurezza nella stessa passata." proposed_solution="Ale/owner-review: per ciascuno dei due worktree ancora vivi, decidere se il lavoro staged va (a) completato e committato come feature separata, (b) recuperato in una nuova lane dedicata, o (c) scartato esplicitamente con --force una volta confermato irrilevante. NON rimuovere questi due worktree senza quella decisione -- a differenza dei tre gia' ripuliti, qui la diff non e' ne' annotata come scratch ne' puramente generata."

- [ ] needs-design-d5-batch-precheck-vs-single-feature-feature-end: paradigm=object-oriented defect="e' una domanda di SCOPE su un invariante di dominio (D-5), non una patch, quindi va in /nw-design e NON va fast-lanata. EVIDENZA: `des feature-end run --repo <r> --feature-id demo --feature-dir <d>` -- cioe un run su UNA feature -- viene rifiutato dal precheck di eleggibilita BATCH (D-5) con 'batch-eligibility precheck (D-5) refuses demo: ... its manifest-declared deep-review verdict is ..., not APPROVED', PRIMA che venga raggiunto il controllo che l operatore stava esercitando (la feature root mancante). Il manifest messo in scena dichiara entry_points: [], eppure il precheck produce comunque un rifiuto sul verdetto: quindi un verdetto ASSENTE sembra trattato come non-APPROVED. Se e cosi il raggio non e un test solo -- QUALSIASI run su singola feature senza verdetto a manifest viene rifiutato a D-5 prima di ogni gate reale. DATAZIONE che stabilisce il meccanismo: il test che lo scopre e del 2026-06-05 (af1266f4c), il precheck del 2026-07-20 (8b0f28aa3), sei settimane dopo. Ironia da notare in design: il test esiste proprio per garantire che i rifiuti dicano la ragione VERA, e oggi fallisce perche una ragione diversa maschera quella vera. Riferimento: riga defects.md del test fix_feature_end_ws_gate_applicability." proposed_solution="Decidere in DESIGN se D-5 e per costruzione un invariante di BATCH (e allora un run singolo non deve attraversarlo, oppure lo attraversa come batch da un membro con regole esplicite per il verdetto assente) o un invariante di ELEGGIBILITA generale (e allora il verdetto assente va definito, non lasciato implicito). Qualunque sia la risposta, il rifiuto deve restare attribuibile: se due gate possono rifiutare lo stesso run, l ordine fra loro e una decisione, non un accidente di implementazione. NON aggiustare la fixture del test mettendo verdict=APPROVED prima che questa domanda abbia risposta: farebbe sparire il sintomo lasciando intatto lo scope indefinito."
- [ ] committed-scope-adapter-hardcodes-pytest-naming-in-polyglot-gate: paradigm=object-oriented defect="`_is_contract_suite_path` (src/des/adapters/driven/git/committed_scope_adapter.py:42-62) decides what belongs to the CONTRACT SUITE by matching Python-only filename conventions: `name.endswith('.py')` then `test_` prefix / `_test.py` suffix / `conftest.py`. On any non-Python target every committed path is rejected, `committed_contract_files` returns an EMPTY CommittedFileSet, and the Gate-Scope digest fingerprints nothing -- a vacuous pass, not an Indeterminate. This violates GDP-7 (language-agnostic: no language hard-dep baked into gate logic) and GDP-6 (no silent-wrong -- an empty file-set is indistinguishable from 'a repo with no committed tests'). It also contradicts the architecture the tree already has: a polyglot runner registry with SEVEN adapters (cargo, csharp, go, java, kotlin, pytest, vitest), each exposing an at-discovery facet, and `des.ports.test_runner_port.resolve()` which was already fixed to return `UnrecognizedRunner` (degrade-LOUD) rather than default to pytest -- see defects.md row a-missing-language-runner-degrades-to-pytest. That LOUD resolution never reaches here: this adapter never consults the registry at all. Distinct from the closed techdebt row unhandled-exception-oserror-from-subprocess-run-committed-scop (that one was about OSError on the git spawn, not about which paths count)." proposed_solution="Route the contract-suite predicate through the resolved runner's at-discovery facet instead of hardcoding pytest's patterns, so a Rust/Go/TS target gets ITS conventions; when no adapter matches the target, return Indeterminate (the port already models it) rather than an empty CommittedFileSet that reads as a successful listing."
- [ ] is-test-path-predicate-restated-six-times-with-divergent-sets: paradigm=object-oriented defect="The predicate 'is this path a test file' is independently re-derived in at least SIX places, and no two agree on the same set -- src/des/adapters/driven/git/git_test_loc_diff_adapter.py:30-40 `_is_test_path` (tests/ or test/ path segment, or test_* / *_test.py / *_test.ts); src/des/cli/carpaccio_slice_gate.py:583-589 `_is_test_path` (BYTE-IDENTICAL logic to the previous one, a verbatim twin, differing only in docstring); src/des/adapters/driven/git/committed_scope_adapter.py:61 (test_* / *_test.py / conftest.py -- has conftest, has NO .ts); src/des/application/slice_at_completeness.py:68 `_PYTEST_COLLECTIBLE_PATTERNS` (test_*.py / *_test.py only); src/des/cli/verify_spec_coverage.py:422+ (adds TS/JS *.test.ts / *.spec.ts); src/des/cli/verify_negative_at.py:417 (test_*.py / *_test.py / *.feature). Each gate therefore measures a DIFFERENT denominator for the same concept: a `foo.spec.ts` counts for verify-spec-coverage and for nothing else, a `conftest.py` counts only for the committed-scope digest, a `*.feature` only for verify-negative-at. Violates the SSOT/DRY constraint the project applies to its own control plane, and compounds the polyglot-agnosticism problem (GDP-7) because a new language must be taught to six separate places. Verified by grep 2026-07-27; distinct from the closed defects row utility-scripts-whitelist-restated-in-two-places (different constant, different files)." proposed_solution="Extract ONE test-path predicate (a domain-level value object or, better, a facet on the resolved language runner adapter so it is per-target rather than global) and have all six call sites read it; delete the byte-identical twin in carpaccio_slice_gate.py outright. Where a call site genuinely needs a NARROWER set (verify-negative-at also wants *.feature), express that as an explicit extension of the shared base, so the divergence is declared rather than accidental."
- [ ] feature-delta-composite-section-silently-unchecked-when-heading-substring-misses: paradigm=object-oriented defect="`_verify_composite` (src/des/cli/feature_delta_schema.py:326-334) gates the whole Composite-section validation behind `if entry.heading not in content: return None` -- a bare substring match over the entire document, and a `None` return that the aggregate reads as 'nothing to say'. Two defects in one line. (1) GDP-8 designation: the same class already PROVEN to bite in this tree and fixed 2026-07-27 in src/des/domain/feature_classifier.py (defects row feature-classifier-slice-plan-heading-substring-match-false-positive, where prose merely QUOTING the heading made classify() report a Slice Plan that was never written); the fix there -- require `line.strip() == HEADING` on a real heading line -- was not propagated to this sibling, nor to src/des/cli/verify_readiness_pre_dispatch.py:358 which still does `if _SLICE_PLAN_HEADING not in text` before parsing. (2) GDP-8 arity corollary: when the heading is absent or spelled differently, the section is not validated AT ALL and the function returns None, so 'could not verify' collapses into 'no finding' and never reaches the aggregate the reader sees -- a byte-locked Composite section can therefore go entirely unchecked while the gate reports a clean verdict." proposed_solution="Reuse the anchored heading matcher the same subsystem already owns (the regex-anchored `_SLICE_PLAN_HEADING_RE` style in validate_feature_delta.py) at all three sites rather than each re-deriving a weaker substring test, and make the absent-heading branch return an explicit could-not-verify verdict that propagates to the aggregate instead of None."
- [ ] des-task-signal-singleton-deleted-by-sibling-subagent-completion: paradigm=object-oriented defect="`remove_des_task_signal` (src/des/adapters/drivers/hooks/des_task_signal.py:110-129), called from subagent_stop_handler.py:2862 on every SubagentStop, unconditionally deletes the shared legacy singleton `DES_TASK_ACTIVE_FILE` (.nwave/des/des-task-active) whenever it exists, regardless of whether the finishing subagent's own project_id/step_id owns it -- because `create_des_task_signal` (same file, lines 45-80) ALSO unconditionally overwrites that same singleton on every concurrent task start, with no per-task ownership tag. `pre_write_handler.py:207-208` reads ONLY this singleton (`des_task_signal.DES_TASK_ACTIVE_FILE.exists()`), never the namespaced per-task signal file, to compute `des_task_active` for `SessionGuardPolicy.check` (src/des/domain/session_guard_policy.py:45-70), where `des_task_active=False` on a protected path (src/, tests/) with an active deliver session BLOCKS the write. REPRODUCED empirically: created two concurrent namespaced signals (slice-01, slice-02) via direct function calls, then called `remove_des_task_signal` for slice-01 only -- slice-02's own namespaced file survives correctly but the singleton is gone right after. Consequence: in any concurrent multi-subagent dispatch (parallel swarm / cross-worktree, an established pattern in this project per its own memory notes), whichever DES-validated subagent finishes SubagentStop first deletes the shared singleton, and a completely unrelated sibling subagent still mid-task has its own subsequent legitimate src/ or tests/ write BLOCKED by PreWrite -- a false positive triggered by a sibling's completion, not by the writer's own state. Hexagonal / shared-mutable-state violation: a hook (pre_write_handler) reads a global fallback signal instead of the namespaced signal that is the actual source of truth for its own task." proposed_solution="Make pre_write_handler.py check the namespaced signal for the CURRENT task's own project_id/step_id (parsed the same way pre_tool_use_handler already does via DesMarkerParser) instead of the global singleton; or, if the singleton must stay for legacy callers, make remove_des_task_signal only delete it when no OTHER namespaced signal file remains under session_dir (glob check before unlinking) so a sibling's still-active namespaced signal keeps the singleton alive."
- [ ] release-workflows-duplicate-ci-gate-job-verbatim: paradigm=object-oriented defect="The ci-gate job in .github/workflows/release-rc.yml (lines 165-222) and in .github/workflows/release-prod.yml (lines 131-188) are byte-identical except for one trailing comment ('next RC version' vs 'stable + nwave-ai versions') -- the entire roughly 55-line gh run list / jq polling script that queries CI Pipeline status for a commit is copy-pasted verbatim across the two workflows. release-dev.yml's own ci-gate (lines 54-116) is a near-duplicate too (only the commit-SHA source differs: git rev-parse HEAD vs needs.validate-source.outputs.commit_sha). Three release workflows (release-dev.yml, release-rc.yml, release-prod.yml -- 652/1622/1470 lines respectively) each re-embed this same polling logic rather than sharing it, so a bugfix or behavior change to the CI-gate polling (a retry/backoff tweak, or a change to which workflow name is queried) must be applied identically in three places by hand, and nothing enforces that the three copies stay in sync -- an SSOT/DRY violation across workflow files, the same mandate this repo already applies to its control-plane code." proposed_solution="Extract the ci-gate job into a GitHub Actions reusable workflow (workflow_call) taking the target commit SHA as an input, and have all three release workflows call it via uses: ./.github/workflows/_ci-gate.yml instead of re-embedding the script."
- [ ] json-schema-loader-domain-class-does-direct-file-io-bypassing-filesystem-port: paradigm=object-oriented defect="`JsonSchemaLoader` (src/des/domain/json_schema_loader.py, base class for `RoadmapSchemaLoader` in roadmap_schema.py:37 and `TDDSchemaLoader` in tdd_schema.py:126) lives in `src/des/domain/` -- the layer CLAUDE.md's hexagonal architecture declares must contain only pure business logic, with I/O routed through `src/des/ports/driven_ports/filesystem_port.py` and its adapters. Yet `_resolve_default_schema_path` (lines 37-77) walks `Path(__file__)`/`.resolve()`, calls `.exists()` repeatedly against the live filesystem to pick among three candidate deployment layouts, and `_read_schema_file` (lines 96-99) does a bare `with open(self._schema_path, ...) as f: json.load(f)` -- never through `FileSystemPort`/`SafeFileSystem`, which already exist in `src/des/ports/driven_ports/` and are used by adapters elsewhere in the tree for exactly this purpose. Both `load()` callers inherit this behaviour unconditionally: there is no injectable filesystem seam, only an optional `schema_path` constructor override that still resolves via direct `open()` when used. Distinct from the closed rows about feature_classifier.py (a probe classifier explicitly designed to re-read fresh, and never described as a hexagonal-boundary case) -- this is a base class literally named/placed as domain that performs unmediated disk I/O and path-existence probing, a plain domain-does-I/O boundary violation, not a substring/parsing bug." proposed_solution="Inject a `FileSystemPort` (already defined in src/des/ports/driven_ports/filesystem_port.py) into `JsonSchemaLoader.__init__` and route `_read_schema_file`/`_resolve_default_schema_path`'s existence checks through it, defaulting to the real adapter only at the composition root; alternatively, if the class must stay dependency-free for bootstrap reasons, move it out of `src/des/domain/` into `src/des/adapters/driven/` (or a dedicated `config/` package) so its location stops contradicting the layering the rest of the tree enforces. || INVESTIGATION 2026-07-27: confirmed both fix paths in proposed_solution have real, non-trivial blast radius, not a mechanical drain fix. Grepped construction sites: RoadmapSchemaLoader()/TDDSchemaLoader() are instantiated at 23 sites across src/des (domain, application/validator.py, cli/roadmap.py, cli/log_phase.py) and, critically, src/des/adapters/drivers/hooks/service_factory.py -- the hook-adapter COMPOSITION ROOT itself constructs these loaders directly with zero DI today. Injecting FileSystemPort per proposed_solution option 1 means deciding how the composition root default-wires a port for something that currently self-resolves its own bootstrap path across three deployment layouts (source/installed/plugin) via Path(__file__) probing -- before any DI wiring exists at hook-cold-start. That default-wiring decision (what the real adapter is, whether SafeFileSystem is safe to construct that early, whether the 3-context resolution logic itself moves behind the port or stays outside it) is a genuine architecture call, not a mechanical substitution. Option 2 (relocate the class out of domain/) is lower-risk but still touches all 23 import sites and needs a decision on where it lands (adapters/driven/ vs a new config/ package) plus whether Generic[SchemaT] template-method loaders belong in the adapter layer at all. Left PENDING for /nw-design -- needs a human call on bootstrap-time port wiring before either path is safe to execute."
- [ ] wave-dispatch-guard-policy-claims-pure-domain-but-does-direct-fs-and-clock-io: paradigm=object-oriented defect="`src/des/domain/wave_dispatch_guard_policy.py:2` declares itself 'A pure domain policy' and its own docstring states it 'runs as PRODUCTION RUNTIME enforcement in the DES runtime' (line 5-6, gating every Agent/Task dispatch). Despite the purity claim, `_feature_delta_witness_present` (lines 193-213) does `repo_root.rglob('feature-delta.md')` then `delta.read_text(encoding='utf-8')` over the entire docs/feature/ tree, `_valid_pre_grant_present` (lines 215-234) does `grant.is_file()` then `grant.read_text()` then `json.loads()` against `.nwave/des/wave-skip-grant-{session_id}.json`, and line 233 compares against raw `time.time()` -- none of it routed through the project's own `FileSystemPort` (src/des/ports/driven_ports/filesystem_port.py) or `TimeProviderPort` (src/des/ports/driven_ports/time_provider_port.py), both of which already exist and are the established seam for exactly this. A domain module that walks the filesystem and reads the system clock directly is neither pure nor deterministically testable without real files/real time, contradicting both its own docstring and the hexagonal layering CLAUDE.md's architecture section declares for src/des/domain/." proposed_solution="Inject FileSystemPort and TimeProviderPort into decide_dispatch's call chain (threading them through _feature_delta_witness_present / _valid_pre_grant_present as parameters, not module-level open()/time.time() calls) so the policy's decision logic can be unit-tested with fakes and the module's own purity claim becomes true instead of aspirational; alternatively, reword the docstring to stop claiming purity and document it as an impure policy-plus-adapter hybrid, though the former is preferable since the ports already exist and sibling domain code elsewhere in the tree already uses them."
- [ ] deliver-progress-tracker-module-docstring-claims-pure-domain-but-does-atomic-file-io: paradigm=object-oriented defect="`src/des/domain/deliver_progress_tracker.py:1-3` module docstring: 'Pure domain logic that cross-references roadmap steps against execution-log entries... Provides save/load for persisting progress state between handler invocations.' `track_progress()` (lines 58-96) reads via `roadmap_path.read_text()` and `execution_log_path.read_text()` directly; `save_progress()` (lines 121-142) performs a full atomic-write sequence -- `progress_dir.mkdir(parents=True, exist_ok=True)`, `tempfile.mkstemp`, `os.fdopen`, `Path(tmp_path).replace(progress_path)` -- with zero port indirection, all inside a module that names itself 'pure domain logic' in its own first line. Same class as the wave-dispatch-guard-policy row filed alongside this one: a src/des/domain/ module whose own docstring asserts purity while its functions perform unmediated filesystem I/O, contradicting the hexagonal boundary CLAUDE.md's architecture section declares (I/O routed through FileSystemPort and adapters, domain stays pure)." proposed_solution="Either inject FileSystemPort into track_progress/load_progress/save_progress (threading read/write/exists/mkdir/atomic-replace operations through the port, matching the pattern other adapters already use) so the module's own docstring claim becomes accurate, or relocate the module to src/des/application/ (which does orchestrate I/O in this codebase) and correct the docstring to stop calling it pure domain logic."
- [ ] git-adapters-subprocess-calls-have-no-timeout: paradigm=object-oriented defect="Every `subprocess.run(['git', ...])` call in `src/des/adapters/driven/git/` -- 12 call sites across 7 files (git_subprocess.py x6, committed_scope_adapter.py, git_commit_verifier.py, git_mutate.py, git_commit_diff_adapter.py, git_history_probe.py, git_track_probe.py) -- passes no `timeout=` argument (grepped for `timeout=` across the whole directory, zero hits). These are the canonical git-access seam this codebase's own memory/pile entries repeatedly flag as concurrency-hazardous (parallel worktrees, concurrent commits racing on refs, index.lock contention under multi-lane orchestration) yet a `git` process blocked on a lock file, a stuck credential-helper prompt (e.g. a misconfigured remote triggering an interactive HTTPS auth prompt), or a hung filesystem (network mount) will hang the calling Python process, and therefore the DES gate/service invoking it, FOREVER -- no bound, no timeout exception to catch and translate into a named INDETERMINATE per GDP-6 (no silent-wrong; degrade LOUD). Contrast with e.g. tests/opencode/test_shim_edge_cases.py (a sibling area drained earlier tonight) which explicitly asserts `subprocess.TimeoutExpired` handling matters enough to test -- the production git seam itself has no such bound at all." proposed_solution="Add a conservative `timeout=` (e.g. 30s for read-only git_text/is_ancestor-class calls, longer only where genuinely needed for merge/worktree-creation operations) to every subprocess.run call in this directory, and catch `subprocess.TimeoutExpired` at each call site's existing error-translation boundary (the same place that already turns `CalledProcessError` into a refusal/Indeterminate) so a hung git process degrades LOUD with a named timeout reason instead of hanging the caller indefinitely. || INVESTIGATION 2026-07-27 (shard techdebt-drain-q): re-verified the call-site count and error-handling landscape before touching code, and found it heterogeneous enough that a single mechanical timeout= sweep is unsafe across all 7 files. git_subprocess.py (the 6-site SSOT, git_text/is_ancestor/is_merged_contribution) has TWO distinct semantics in the same module: check=True call sites (git_text) propagate CalledProcessError today and have real except-CalledProcessError translation at SOME callers (e.g. git_test_loc_diff_adapter.py:56 -> GitDiffUnavailable) but NOT ALL -- git_worktree_adapter.py alone has 10 git_text call sites with zero try/except around them (lines 172, 200, 222, 282, 300, 309, 317, 333, 343, 351), relying on the exception propagating uncaught. Adding timeout= there is safe (bounds an infinite hang into a bounded crash, strictly better, per the row own GDP-6 framing) but does NOT yet give those 10 sites the NAMED INDETERMINATE the proposed_solution asks for -- that requires reading each caller chain to decide the right degrade point, which is a judgment call per file, not a mechanical substitution. The other 6 call sites (committed_scope_adapter.py, git_commit_verifier.py, git_mutate.py, git_commit_diff_adapter.py, git_history_probe.py, git_track_probe.py) were NOT yet re-read for their own error-translation shape. Left PENDING rather than closing on a partial fix: recommend whoever picks this up next start with git_subprocess.py alone (bound the SSOT first, it is the highest-reuse module) and treat each of the other 6 files as its own reasoned edit, mirroring how the polyglot-runner sibling row (five-of-seven-polyglot-runners-shell-out-with-no-timeout-bound, closed this same session) was scoped to files sharing ONE identical shape -- these 7 git files do not share one shape."
- [ ] feature-delta-md-filename-hardcoded-across-73-files-no-external-charter-seam: paradigm=object-oriented defect="The literal string `feature-delta.md` is hardcoded across at least 73 files under src/des/ (grepped `grep -rl feature-delta.md src/des/ | wc -l` = 73; spot-checked `at_review_verdict.py` lines 440/453/465 build the path via f-string interpolation of the same literal each time), with no shared constant/module owning the filename. Gates and CLIs that need feature-scoped state (review verdicts, charter scaffolding, wave-review dispatch) read ONLY this one file -- there is no seam for an external ADR or an alternate charter source, so every consumer independently re-derives `docs/feature/{feature_id}/feature-delta.md` instead of calling one resolver. Surfaced 2026-07-31 as a byproduct of the qw5-track-c spike's measurement log (`.trackc-log/measures.md`), not from a dedicated investigation -- re-verify the exact file list before scoping a fix, this entry only confirms the count and one call site, not the full call-site inventory." proposed_solution="Introduce a single `feature_delta_path(project_dir, feature_id)` (or similar) resolver in one module (mirroring the `_declared_slice_plan_slice_ids` precedent already used for the FeatureUnreconciled slice-attribution fix) and thread every current f-string/literal construction through it; only then consider whether an external-ADR/charter override seam is worth adding on top." || AWAITING-VERIFICATION 2026-07-31: peer-reported finding (relayed via a read-only worktree-assessment agent), spot-checked (73-file count, one call site) but the full call-site inventory and consumer-behavior claim are not independently re-verified -- treat the count as real, the fix shape as unconfirmed.
- [ ] only-two-of-fourteen-feature-id-cli-flags-use-the-meaningful-identity-normalizer: paradigm=object-oriented defect="`src/des/cli/_identity_args.py`'s `meaningful_identity` normalizer exists specifically because a present-but-blank --feature-id value used to walk past `is not None` guards and let a gate silently scope itself to a feature literally named the empty string (RCA `docs/feature/fix-at-review-verdict-surface/deliver/rca.md`, 2026-07-14); the module docstring says explicitly that anything identity-bearing (--feature-id, --slice-id, --repo) gets this normalizer. Grepped every add_argument('--feature-id') site across `src/des/cli/`: 14 files declare the flag, but only `at_review_verdict.py` and `commit_slice.py` actually pass `type=meaningful_identity`. The other twelve -- `bugfix_pipeline_tick.py`, `charter_scaffold.py`, `record_review_verdict.py`, `verify_environmental_e2e.py`, `work_exhausted_tick.py`, `_wave_review_cli.py`, `carpaccio_slice_gate.py`, `convert_to_atdd_pure.py`, `record_prose_delivered.py`, `carpaccio_precheck.py`, `consolidation_signal_tick.py`, `next_step.py` -- declare `parser.add_argument('--feature-id', required=True)` with a bare string type; required=True only enforces PRESENCE, not meaning, so a blank/whitespace-only value still passes argparse untouched. Confirmed the live vulnerability in one sibling by reading the call site: `record_review_verdict.py` takes `args.feature_id` straight into `review_ledger_path(repo, feature_id)`, which builds `.nwave/telemetry/review/{feature_id}.jsonl` -- a blank feature_id silently writes/reads a review-verdict ledger keyed on nothing, indistinguishable from a real feature's ledger to any downstream reader, reproducing the exact bug class the normalizer was built to close, just in a different CLI entrypoint than the one originally patched." proposed_solution="Add `type=meaningful_identity` (already importable from `des.cli._identity_args`) to every --feature-id/--slice-id/--repo-shaped add_argument call in the twelve listed files, matching the pattern already proven in `at_review_verdict.py` and `commit_slice.py`; consider a cheap self-enforcing gate (an AST or regex scan asserting every such add_argument call site in `src/des/cli/` also carries type=meaningful_identity) so a future new CLI file cannot reintroduce the same gap silently. || INVESTIGATION 2026-07-27 (shard techdebt-drain-p): re-verified before applying, two findings change the shape of the work. (1) count is stale -- record_examine_verdict.py:104 is a 13th unfixed site the row missed (13 files, not 12). (2) the mechanical fix alone is unsafe: meaningful_identity converts blank to None, but required=True only checks argparse RECEIVED a token, never what type= returned -- a bare type= add on these 13 sites turns a blank feature-id into a silent None with zero None-refusal downstream (record_review_verdict.py would build .../None.jsonl instead of fixing anything). at_review_verdict.py's own cited precedent does NOT do a bare type= swap either -- it wraps it in a local _meaningful_identity_arg with its own explicit refusal. Each of the 13 sites needs its own reasoned None-refusal, not a mechanical type= swap across all -- too much blast radius for a single drain session. Left PENDING with the corrected count; the self-enforcing AST gate should land ALONGSIDE the per-site refusal fixes, not before them."

- [ ] commit-slice-slice-plan-status-sync-is-a-post-commit-side-effect-not-atomic: paradigm=object-oriented discovered_by=lane-d03b defect="`des commit-slice`'s `_sync_slice_plan_status` flips a feature-delta.md Slice Plan row from `pending` to `shipped` ONLY when `feature-delta.md` is included in that specific commit's `--path` list. Observed 3 times in the same lane (D03b slices 03, 04, 05): each slice's commit-slice invocation scoped `--path` to the files that slice actually owns (never feature-delta.md itself, since the crafter didn't edit it), so the Slice Plan row stayed stale `pending` in the working tree despite the slice being genuinely committed+verified -- requiring a manual second commit each time just to sync the status cell." proposed_solution="IPOTESI. The status-cell sync should be part of commit-slice's own atomic write (it already computes the digest/trailer atomically) rather than a side-effect gated on whether feature-delta.md happens to be in --path. Either commit-slice always stages+includes the Slice Plan status flip for the entering slice regardless of the caller's --path list, or the sync step is decoupled entirely into its own idempotent CLI step run automatically post-verify. Needs its own RCA before implementation -- not attempted here, found live by a downstream lane, not chased to avoid scope creep on their actual slice work."

- [ ] commit-slice-declared-regression-file-suppresses-its-own-build-tier-content: paradigm=object-oriented discovered_by=lane-e2-defer-fix defect="MISURATO 2026-07-30 leggendo il call site durante la fix E2. In `src/des/cli/commit_slice.py:2462` la scelta e' binaria: `light_invariant_paths = [] if regression_test_file is not None else _slice_build_tier_paths(repo)`. Quando una slice dichiara `--regression-test-file`, lo scope del build-tier exit check diventa `[regression_test_file]` e il contenuto che QUELLA slice sta committando sotto `tests/build/` non viene eseguito affatto. Il commento in loco documenta la meta' del caso (la branch Gherkin che risolve `_slice_build_tier_paths`) ma non nomina questa perdita. Non e' il difetto E2 e non e' stato introdotto da quella fix: e' preesistente e indipendente." proposed_solution="IPOTESI, serve RCA propria. L'unione e' il candidato ovvio -- `[regression_test_file, *_slice_build_tier_paths(repo)]` invece dell'aut-aut -- ma va verificato che non riapra il veleno JIT che la fix E2 chiude (i path di `_slice_build_tier_paths` sono per costruzione SOLO quelli staged dalla slice entrante, quindi in teoria e' sicuro, ma va MISURATO non dedotto). Deferito esplicitamente da team-lead 2026-07-30 per disciplina chiudi-non-apri: non includerlo nella fix E2."

- [ ] build-tier-whole-tree-deferred-how-names-a-flag-that-does-not-exist: paradigm=object-oriented discovered_by=lane-e2-defer-fix defect="Il record LOUD `BuildTierWholeTreeDeferred` emesso da `_resolve_build_tier_run_paths` (`src/des/cli/run_contract_gate.py`) porta un campo `how` che recita `run_contract_gate --repo . --full`. Il flag `--full` NON esiste sulla CLI di `run-contract-gate`: `full` e' un parametro keyword-only Python di `build_tier_exit_verdict`, mai esposto come opzione da riga di comando. Un manutentore che segue il HOW alla lettera riceve un errore di argparse. Viola la regola standing 'ogni fallimento spiega cosa/perche/come, e il COME dev'essere stato eseguito almeno una volta' -- questo HOW non e' mai stato eseguito. Il floor whole-tree reale a feature-end e' la modalita' full-suite DI DEFAULT (`run-contract-gate --repo <root>`, nessun flag), via `feature_end_cycle_service._run_full_suite_leg`." proposed_solution="IPOTESI. Correggere il `how` perche' nomini l'invocazione che funziona davvero (la modalita' default senza flag, o direttamente la leg di feature-end), e verificarlo ESEGUENDOLO prima di ricommittare la stringa. Deferito da team-lead 2026-07-30: catalogare, non fixare dentro la fix E2."

- [ ] certification-legs-slice-05-docstrings-claim-active-red-but-pass: paradigm=object-oriented discovered_by=lane-e2-defer-fix defect="MISURATO 2026-07-30 con baseline in worktree effimero staccato su HEAD e927b0ac7. I 4 test in `tests/des/acceptance/certification_legs_observe_real_execution/test_slice_05_build_failure_distinguishable_from_test_failure.py` portano docstring che dichiarano 'active-RED today' (es. 'POSITIVE AT (active-RED today, C8)... Today `_feature_scope_malformed` carries NO `kind` field at all -- the final assertion below is what fails'), ma al baseline PASSANO tutti e 4: la feature e' stata consegnata e le docstring non sono state aggiornate. E' una trappola attiva per il triage: chi classifica i rossi leggendo le docstring li archivia come 'rosso noto e deliberato' e manca una regressione VERA. Ci sono cascato io stesso in questa stessa sessione, in direzione opposta, attribuendo alla mia fix rossi che erano preesistenti -- stessa classe di errore, misurare invece di leggere la designazione (GDP-8)." proposed_solution="IPOTESI. Aggiornare le 4 docstring perche' descrivano lo stato reale (consegnato e verde) invece dello stato di authoring. Piu' in generale il marcatore 'active-RED at HEAD' e' una DESIGNAZIONE che nessun gate verifica contro la PROPRIETA': un check economico che confronti i file che si auto-dichiarano active-RED con l'esito reale della loro esecuzione impedirebbe a questa classe di ri-accumularsi. Non attempted qui -- fuori scope della fix E2."

- [ ] carpaccio-ceiling-has-only-one-early-signal-at-count-not-responsibility-breadth: paradigm=object-oriented discovered_by=orchestrator defect="MISURATO 2026-08-02, ispirato da un post-mortem di un'istanza sorella (tier SF, closed-source) su una mega-slice (167 AT, 4 bounce di review) nata perche' lo slicing DESIGN aveva compresso troppe responsabilita' in una fetta -- la lezione generica (non SF-specifica, mandato Lyra-DEV OSS-non-SF rispettato): l'AT-designer deve avere l'OBBLIGO di rifiutare una fetta che supera il proprio 'envelope', usando piu' di un segnale. Diligence in questa sessione ha trovato che nWave GIA' HA questo veto (`_check_slice_size_count`, `src/des/cli/carpaccio_format.py:1807-1868`, ratificato `ADR-028 D2`, tetto default 7 AT, escape `@coupled` con giustificazione o rialzo esplicito) -- il differenziatore di causa-radice principale rispetto a SF e' quindi gia' coperto. Il gap reale e' che il tetto usa UN SOLO segnale (conteggio AT), mentre la sorella ne propone tre (densita', blast-radius, numero-di-responsabilita'). TENTATIVO DI COMPOSIZIONE SCARTATO PER MOTIVO STRUTTURALE, non per pigrizia: comporre `des blast-radius` (che misura il DIFF reale) nel gate carpaccio e' SBAGLIATO perche' `carpaccio-slice-gate` gira a tempo di DISTILL, PRIMA che il crafter implementi (`D_DISTILL is upstream of the per-slice cycle`, verificato in `src/des/domain/atdd_pure_phases.py`) -- a quel punto non esiste ancora un diff di produzione da misurare per la fetta in ingresso. Un segnale precoce disponibile a tempo di DISTILL esiste (il numero di file/componenti di produzione DISTINTI citati negli AT gia' autorati, es. citazioni `sut:`), ma definirlo bene per entrambe le modalita' (gherkin e pytest-regression) e tararne la soglia e' un lavoro di DISTILL vero, non un addendum di due righe -- per questo non implementato qui, solo catalogato." proposed_solution="IPOTESI, non tentata. Se autorizzato: (1) DISTILL scoperto per definire 'componente distinto' per entrambe le modalita' AT (probabile: file `sut:`-citati unici attraverso gli scenari della fetta), (2) `ADR-028` addendum D2-bis che aggiunge questo come SECONDO criterio di rifiuto indipendente dal conteggio AT, sotto lo stesso proprietario di decisione, (3) implementazione in `_check_slice_size_count`/`_check_slice_size` con lo stesso messaggio WHAT/WHY/HOW gia' presente (che gia' dice testualmente 'over-ceiling AND not cleanly re-sliceable warrants deep-review + refactor at FEATURE scope'). Verifica del fix: una fetta con AT-count sotto tetto ma che tocca N componenti sopra una soglia dedicata deve rifiutare con lo stesso stile di messaggio del rifiuto AT-count esistente."

- [ ] slice-plan-status-corpus-59-files-carry-non-canonical-status-tokens: paradigm=n/a discovered_by=lane-edc7-bugfix defect="MISURATO 2026-08-01/02, censimento al momento della fix `slice-plan-status-token-edc-7-unenforced-silently-suppresses-sync` (ora chiusa, `f2abe4501`). Censimento su 311 `docs/**/feature-delta.md`: 361 celle `pending`, 172 `shipped` (conformi); 59 file non-archiviati sotto `docs/feature/` portano almeno una Status non-canonica rispetto al vocabolario chiuso EDC-7 (`pending`/`in-flight`/`shipped`) -- `planned` x25, `designed` x7, `delivered`, `Not started` x3, `active`, `deferred`, piu' forme decorate come 'shipped \`4c455714e\`' e 'shipped (merged \`3cb9cd783\`, 2026-07-20; SliceCommitVerified)'. Con la fix ora in trunk, `des next` riportera' un gap strutturale (`non-canonical-slice-status`) su ognuno di questi 59 file la prossima volta che venga toccato -- comportamento VOLUTO (nominare la causa a tempo di authoring invece del sintomo tardi), ma e' un cambio di comportamento reale e istantaneo su documenti che appartengono a corsie diverse, alcune vive in parallelo stanotte. Il corpus porta anche stati che il vocabolario chiuso non sa rappresentare (`**PARTIALLY SHIPPED**`, `superseded — delivered by ...`) -- la colonna Annotation, non un quarto token di Status, e' probabilmente la risposta, ma e' una decisione dei manutentori, non della fix." proposed_solution="IPOTESI, non tentata -- deliberatamente fuori scope dalla fix stessa. Remediation e' uno sweep coordinato SEPARATO, non parte di questa fix: (1) per i 59 file, riscrivere la Status cell al token canonico piu' vicino (es. `planned`/`designed`/`Not started`/`active`/`deferred` -> `pending`; `delivered` -> `shipped`), verificando caso per caso che non sia in corso una corsia viva su quel documento prima di toccarlo; (2) per le forme decorate (`shipped \`sha\``, `shipped (merged ...)`), spostare il contenuto extra nella colonna Annotation, lasciando la Status cell nuda; (3) decidere se il vocabolario chiuso necessita un quarto stato (es. `partial`) per i due casi che oggi non sa rappresentare, o se restano casi Annotation-only -- decisione dei manutentori, non auto-decisa qui."
