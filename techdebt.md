- [ ] desplugin-god-class: paradigm=object-oriented defect="DESPlugin (scripts/install/plugins/des_plugin.py, class at top) is 1792 lines / 37 methods / 16 fields — the highest-confidence god class (score 0.94, conf 0.95)" proposed_solution="Extract cohesive collaborators (hook-command generation, import-rewriting, manifest/verify) into separate classes behind DESPlugin's install/verify facade"
- [ ] subagent-stop-service-god-class: paradigm=object-oriented defect="SubagentStopService (src/des/application/subagent_stop_service.py) is 1001 lines / 21 methods, and its __init__ takes 12 constructor params (long_parameter_list score 1.0)" proposed_solution="Split the ATDD-pure vs classic validation responsibilities into collaborators and group the 12 injected dependencies into a cohesive context object"
- [ ] review-wave-triplicate-duplication: paradigm=object-oriented defect="The design/devops/discuss review machinery is copy-pasted 3x: ledger readers (design_review_ledger_reader.py:37, devops:37, discuss:43), verify CLIs (verify_design_review.py:46, verify_devops_review.py:47, verify_discuss_review.py:53), and verdict CLIs (design_review_verdict.py:44, devops:48, discuss)" proposed_solution="Introduce one parameterized ReviewWave abstraction (reader + verify + verdict) keyed by wave name, collapsing the three near-identical copies"
- [ ] coverage-map-verify-cli-runtime-dup: paradigm=object-oriented defect="scripts/cli/verify_coverage_map.py:63-69 duplicates a block from the runtime service src/des/application/coverage_map_verify_service.py:43-49 (dup score ~1.0) — logic forked across the script/runtime boundary" proposed_solution="Have the CLI script import and call coverage_map_verify_service instead of re-implementing the block"
- [ ] test-double-src-tests-duplication: paradigm=object-oriented defect="In-memory/mock adapters exist as byte-similar copies in both src/des and tests: in_memory_filesystem.py:8-14, mocked_time.py:8-14, mocked_hook.py:6-12, mocked_validator.py:6-12 each duplicated into tests/des/adapters/" proposed_solution="Keep one canonical copy of each fake (in src/des/adapters/driven) and import it from the tests, deleting the tests/des/adapters duplicates"
- [ ] at-completion-ledger-slice-ref-clump: paradigm=object-oriented defect="(feature_id, slice_id) travel together across 11 method signatures in src/des/adapters/driven/logging/at_completion_ledger.py (data_clump score 0.995); append_gate_event also takes 8 params" proposed_solution="Introduce a SliceRef value object bundling feature_id+slice_id and pass it through the ledger API"
- [ ] orchestrator-step-request-param-object: paradigm=object-oriented defect="DESOrchestrator (src/des/application/orchestrator.py) passes (agent, command, project_root, step_file, timeout_thresholds) as a recurring clump; execute_step/execute_step_with_stale_check take 7 params, and step_execution_service.execute_step takes 11" proposed_solution="Introduce a StepExecutionRequest parameter object carrying the step-execution fields and thread it through orchestrator + step_execution_service"
- [ ] prompt-dispatch-long-parameter-lists: paradigm=object-oriented defect="Prompt-building signatures are extreme: src/des/cli/dispatch.py::_build_prompt takes 15 params (score 1.0) and prompt_rendering_service.py::render_prompt takes 12" proposed_solution="Introduce a PromptSpec/dispatch-envelope dataclass to replace the flat argument lists"
- [ ] commit-slice-main-long-method: paradigm=object-oriented defect="src/des/cli/commit_slice.py::main is a ~491-line procedural function with 48% comment density (comments-as-deodorant score 0.48) — a giant CLI entrypoint" proposed_solution="Extract the argument-parse / precondition-check / gate-run / record phases into named helpers so main reads as a short sequence"
- [ ] git-string-constants-ssot: paradigm=object-oriented defect="Raw git/date literals are scattered across src/des git adapters: 'rev-parse' (19 sites), 'HEAD' (15), '+00:00' UTC-offset (11) all at matching call slots (duplicated_constant score 1.0/0.91)" proposed_solution="Centralize the git-subcommand and UTC-offset literals in a small constants module (or helper) imported by the git adapters. IN FLIGHT, PARKED (2026-07-22): a partial implementation already exists UNCOMMITTED in the worktree wt/techdebt-git-string-constants (branch refactor/git-string-constants-ssot, based on adfd5433c) -- a new src/des/adapters/driven/git/git_constants.py plus 7 insertions across git_history_probe.py, git_subprocess.py and git_test_loc_diff_adapter.py, untouched since ~02:00. Do NOT start this item from scratch and do NOT remove that worktree: resume it, rebase onto the current trunk, finish the remaining call-site sweep and commit through the spine. Parked rather than finished because the box carries four live delivery lanes and this is box-bound work."
- [ ] duplicate-logger-implementations-install-scripts: paradigm=object-oriented defect="scripts/install/install_utils.py:21 class Logger (789-line file, tsunami detect_god_class score 0.73: 13 methods/10 fields) duplicates the public API (info/warn/error/step/progress_spinner/table/panel/print_styled + timestamped _write_to_file) of scripts/install/rich_console.py:55 RichLogger / :273 PlainLogger (473-line file, god_class score 0.65) -- rich_console.py's own module docstring says it 'replaces the legacy Colors class ... while maintaining backward compatibility with existing Logger API', yet install_utils.Logger is still imported live at scripts/install/verify_nwave.py:45 and scripts/install/plugins/registry.py:25 -- two independently-maintained logger implementations answer the same calls, so verify_nwave.py never gets Rich styling while other install-flow scripts do" proposed_solution="retire install_utils.Logger; repoint verify_nwave.py and plugins/registry.py to rich_console.ConsoleFactory.create_logger() (already selects RichLogger/PlainLogger/SilentLogger by context), collapsing three call-compatible classes into one factory-selected family"
- [ ] src-test-prefixed-production-files-pytest-naming-collision: paradigm=object-oriented defect="two production port files under src/des/ports/ use the pytest test_*.py naming convention for domain reasons -- src/des/ports/driven_ports/test_loc_diff_port.py:26 class TestLocDiffPort(Protocol) ('Test' = test-LOC-delta domain term) and src/des/ports/test_runner_port.py (grep-confirmed Test*-prefixed class). pyproject.toml testpaths=['tests'] keeps a normal pytest run safe, but the stale-build-dir defect already documents the feature-end gate worker passing the repo ROOT unpruned into collection -- exactly the condition under which this naming would surface as a pytest collection TypeError (typing.Protocol classes refuse direct instantiation), compounding that already-open defect rather than being independent noise" proposed_solution="rename the colliding identifiers (TestLocDiffPort -> LocDiffPort, file to loc_diff_port.py; same pattern for test_runner_port.py) to remove the naming collision at the source, independent of whether the collection-scoping fix lands"
- [ ] testing-theater-shim-edge-case-tests: paradigm=object-oriented defect="tests/opencode/test_shim_edge_cases.py:132 test_nonexistent_python_raises_file_not_found and :159 test_timeout_raises_timeout_expired (tsunami detect_testing_theater, score 0.58/conf 0.7, rationale 'no production code path invoked') each call bare subprocess.run(...) directly and assert on stdlib exceptions (FileNotFoundError for a nonexistent interpreter path; subprocess.TimeoutExpired for a sleep(60) vs timeout=1) -- neither test invokes the shim/adapter module under test (ADAPTER_MODULE appears only as an argv token in the first test, not at all in the second); both pass by construction regardless of whether the shim's own fail-open/timeout-handling code is correct" proposed_solution="rewrite both to invoke the actual shim entrypoint and assert ITS handling (exit code / fail-open behaviour) instead of re-proving Python's subprocess module works"
- [ ] resolve-manifest-state-never-wired: paradigm=object-oriented defect="scripts/cli/resolve_manifest_state.py:80 `resolve_manifest_state()` e' chiamata SOLO da codice di test (tests/scripts/cli/component_manifest/steps/composition.py:238), mai dalla produzione. Il commento alla riga 4 la lega a F-DESIGN-COMPONENT-MANIFEST slice-03: sembra scritta come parte di un compito di design e mai cablata in un gate o comando vivo. Trovata da una scansione read-only via detector never_wired e VERIFICATA per lettura diretta dei chiamanti (il detector ha falsi positivi noti su entry-point e classi registrate via registry, quindi la verifica non e' opzionale)" proposed_solution="O cablarla in un gate/CLI che ne ha bisogno, o rimuoverla se il compito di design e' stantio. Decidere quale: un modulo che esiste solo per i propri test e' codice che qualcuno manterra' credendolo vivo"
- [ ] write-marketplace-json-never-wired: paradigm=object-oriented defect="scripts/build_plugin.py:681 `write_marketplace_json()` e' chiamata SOLO dai test (tests/build/acceptance/plugin/steps/test_release_pipeline_steps.py righe 339 e 349), mai dal codice di build di produzione. Implementa la generazione del catalogo marketplace per la 'self-hosted distribution', ma nessuno step vivo della pipeline di build la invoca. Verificata per lettura diretta dei chiamanti dopo il detector" proposed_solution="Integrarla nella pipeline di build se la funzionalita' deve spedire, o rimuoverla se la self-hosted distribution e' stata differita o abbandonata. Controllare build_dist.py per capire quale delle due: se e' una feature pianificata, il difetto e' il cablaggio mancante e non il codice morto"
- [ ] test-update-check-service-real-fsync-fixture: paradigm=object-oriented defect="tests/des/unit/application/test_update_check_service_frequency.py usa 6+ istanze di `TemporaryDirectory`, ognuna delle quali crea file temporanei REALI in un test UNIT. Ogni attraversamento del filesystem aggiunge latenza misurabile alla suite unit, e l'obiettivo dichiarato e' la full suite sotto i 5 minuti (CLAUDE.md). Su WSL2 la classe e' aggravata: una sorella ha misurato stalli `jbd2_log_wait_commit` da fixture che fanno fsync reale, con singole suite bloccate 17-20 minuti" proposed_solution="Fattorizzare in una fixture a scope di classe (una creazione invece di sei) oppure sostituire il filesystem reale con l'adapter in-memory che il progetto GIA' possiede: `src/des/adapters/driven/filesystem/in_memory_filesystem.py`. La seconda e' preferibile in un test unit -- un unit test che tocca il disco sta testando anche il disco"
- [ ] event-name-constants-split-port-adapter: paradigm=object-oriented defect="Feature-end event-name constants are defined in src/des/ports/driven_ports/at_completion_ledger_port.py:38-39 (ENVIRONMENTAL_E2E_GATE_RAN, ENVIRONMENTAL_E2E_VERIFIED) and manually re-exported by src/des/adapters/driven/logging/at_completion_ledger.py:63,105-109, creating a ripple: new event names must be defined in the port first, then manually re-imported/re-exported by the adapter. If the adapter re-import is forgotten, tests reading from the adapter will break. The pattern is documented in a comment (line 105-109, DIP fix for AD-02) but not mechanically enforced. Readers verified at 8 sites via Tsunami (domain:2, adapter:3, tests:3)" proposed_solution="Consolidate all feature-end event-name constants into a SINGLE location (recommend src/des/domain/feature_end_events.py as the canonical SSOT, with domain logic as the reader and adapters as re-exporters), add an import-linter or AST gate verifying re-exports stay in sync with the source-of-truth definition, or auto-generate the adapter re-exports from the port definitions at build time"
- [ ] subagent-stop-service-high-dependency-orchestrator: paradigm=object-oriented defect="SubagentStopService(SubagentStopPort) in src/des/application/subagent_stop_service.py:250 orchestrates 11 injected ports/services across 21 methods (1,250 LOC). Primary method validate(context, hook_id) sequences ~15-20 gate validations (discuss → distill → wave-dispatch → classic-vs-atdd_pure → ...). High dependency count + sequential gate orchestration concentrated in one class. Likely acceptable per DDD if the SubagentStopContext → HookDecision transformation is genuinely a single use-case, but worth monitoring for continued growth and potential sub-aggregation as new gates are added" proposed_solution="Assess whether the validate() method can be decomposed into a pipeline or strategy pattern (each gate as a pluggable validator) without violating the single-entry-point contract for the SubagentStopPort driver port. If gates are independent, consider a ChainOfResponsibility or StrategyFactory pattern; if gates must run in sequence with shared mutable state, document the sequencing invariant and continue monitoring size growth at each iteration"
- [ ] refusal-messages-constant-while-context-is-in-hand-audit: paradigm=object-oriented defect="AUDIT SISTEMATICO, non un singolo difetto. Lo stesso schema e' emerso TRE volte in un giorno da fonti indipendenti: un punto di rifiuto emette un messaggio COSTANTE mentre il dato che lo renderebbe utile e' gia' nello scope della funzione che lo scrive. I tre confermati: (1) `_ArchVerdict` porta gia' `failed_node_ids` (run_contract_gate.py:585) e il rifiuto a :3389-3396 emette una stringa fissa senza usarli, quindi l'operatore legge 'un invariante architetturale e' FALLITO' e deve scavare per scoprire QUALE e che non e' suo; (2) il rifiuto carpaccio per slice fuori ordine non ramifica fra i due casi possibili -- predecessore non committato contro numerazione sbagliata -- pur avendo entrambe le informazioni in mano, e i due casi portano ad azioni OPPOSTE; (3) il rifiuto E2 dell'attestazione comportamentale non nomina l'interprete che ha usato ne' da quale gradino l'ha risolto, pur avendolo appena risolto, e senza quel nome l'intera classe di difetti interprete-sbagliato e' invisibile. MISURA della popolazione candidata (AST su src/des, chiavi error/reason/message/how/why assegnate a una stringa costante >40 char): **98 punti in 20 file**, con la densita' massima proprio nei due file che hanno prodotto tutti e tre i casi confermati -- 22 in `commit_slice.py` e 22 in `run_contract_gate.py`. ATTENZIONE, e va detto: 98 sono CANDIDATI, non difetti. Un messaggio costante e' legittimo quando non c'e' contesto da aggiungere. Il valore di questa riga e' che i tre confermati vengono dai due file piu' densi, il che suggerisce che siano campioni di una popolazione e non incidenti" proposed_solution="Passare in rassegna i 98 punti chiedendo a ciascuno UNA domanda: il contesto che renderebbe questo messaggio azionabile e' gia' nello scope di questa funzione? Se si', e' un difetto e va interpolato; se no, il messaggio costante e' corretto e va lasciato. Partire dai due file piu' densi, dove il rendimento e' massimo. Poi rendere la classe difficile da ricreare: un gate che possiede un oggetto-verdetto ricco (come `_ArchVerdict`) e ne emette una rappresentazione costante e' un odore riconoscibile a occhio in review, e vale la pena scriverlo nella checklist di review dei gate accanto ai GDP. Nota di metodo: NON aprire 98 bugfix -- e' un audit, il cui esito e' un elenco corto di punti veri piu' la conferma che gli altri stanno bene cosi'"
