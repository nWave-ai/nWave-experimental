"""GOLDEN FIXTURE leaf — a second fully-wired subcommand module (clean corpus).

A second clean row so the clean fixture's registry has more than one row — the
gate clears EVERY row, not just the first. Imports cleanly, exposes a callable
``main``.
"""

from __future__ import annotations


def main(argv: list[str] | None = None) -> int:
    return 0
