# Golden-fixture corpora for the M8 universe-bound-assertion gate (slice-03).
# These files are scanned-as-data by the gate; they are NOT collected as tests
# themselves (filenames are violation_*/clean_*, not test_*.py).
